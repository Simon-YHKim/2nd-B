# 2nd-Brain shared QA / AI test account.
# INTENTIONALLY committed (public repo) so every AI agent (local, cloud, or
# headless) can sign in and test the app freely. Authorized by Simon (2026-06-28).
#
# This is NOT a real secret:
#   - Disposable @example.com user, free tier, judge_mode=false.
#   - RLS-isolated: reads/writes ONLY its own rows. No privilege, no PII, no service_role.
#   - The Supabase anon key it uses is already public (see eas.json / CI workflows).
#   - Revoke anytime by deleting this user in Supabase Auth; recreate via the app sign-up.
# Real secrets (service_role, API keys, .env) still NEVER go in git.
#
# Sign in with email/password via the normal app auth (src/lib/supabase/auth.ts).
QA_TEST_EMAIL=qa.ai.b18807@example.com
QA_TEST_PASSWORD=Qa9RgfEtJTvY13H9oeh0bOGYT
QA_TEST_USER_ID=fcd4dec5-4fce-4e41-af40-ebff2640d163
QA_TEST_BIRTH_DATE=1995-06-15
