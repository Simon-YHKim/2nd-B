# DECISIONS

**append-only.** 요약·재작성·삭제 금지. 새 줄은 맨 아래에 붙인다.
한 줄 = `YY.MM.DD HH:MM · 결정 · 이유 · 뒤집는 조건`

이 파일이 하는 일: 결정이 난 그 턴에 남긴다. 대화는 버퍼고, 파일에 없으면 없는 것이다.
상태(지금 어디까지 / 다음 하나 / 막힌 것)는 여기 쓰지 않는다 — `STATE.md` 가 소유한다.
100KB 에 닿으면 요약하지 말고 기간으로 쪼갠다(`DECISIONS-2026H2.md`).

---

## 2026-09 · 레거시 은퇴 → 되살리기

`26.09.13 09:20 · Q1 /import 의 AI 분류를 되살린다(배송 화면에 import_ingest 경로 복원) · 붙여넣기→분류 경로가 레거시 렌더러에만 있었고 은퇴와 함께 소비자가 0이 됐다 · 뒤집는 조건: 붙여넣은 자료도 소스가 되어 Phase 1 이 그대로 받는다면 별도 분류 호출은 불필요해진다(Q11)`

`26.09.13 09:20 · Q2 화면을 잃은 번역 파일 둘은 남긴다 · 보관함 화면을 읽을 때의 문맥이다 · 뒤집는 조건: 보관함 화면 자체가 없어지면`

`26.09.13 09:20 · Q3 다음 은퇴 묶음 = capture 단독(4,636줄) · 큰 것부터 · ⚠ 26.09.13 09:20 실측으로 전제 반증됨 — capture 는 배송된다(capture-full.tsx:6,14,18 이 두 트랙 모두에서 CaptureLegacy 를 그리고, Web Share Target 이 /capture 로 들어온다). 대체안은 Q10 으로 다시 묻는다`

`26.09.13 09:20 · Q4 /ops 는 인용 갱신 후 확인 없이 은퇴를 이어간다 · 인용이 낡았을 뿐 주장은 참이므로 · 뒤집는 조건: 갱신 과정에서 배송 화면에 대응물 없는 계약이 나오면 멈추고 보고`

`26.09.13 09:20 · Q5 /inbox 의 옛 기능 셋을 되살린다(미리보기 · 요약+질문 넷 · 위키 페이지 생성) · 배송 신호 허브에 대응물이 0건이라 · 뒤집는 조건: 없음. 단 배치는 "허브 안"이 아니라 별도 소스 목록 화면 — 허브에 목록을 넣으면 정보 밀도 규칙(화면 하나에 메시지 하나)과 O-7 을 깬다`

`26.09.13 09:20 · Q6 기록 상세가 잃은 넷을 전부 되살린다(세컨비에게 묻기 · 원본 열기 · 후속 메모 · 홈에서 강조) · 홈 강조는 받는 쪽 결함부터 · highlightRecordId 를 보내는 곳은 둘(capture.tsx:2254, record/[id].tsx:224)인데 읽는 곳이 아카이브된 홈뿐이라 순서가 맞다`

`26.09.13 09:20 · Q7 /wiki 는 넷을 되살린다(한 장 지우기 먼저 · 내보내기 거처 결정 · 검색 · 지표 상세) · 한 장 지우기가 먼저인 이유는 자동 저장의 되돌리기 전제가 거기 걸려 있어서 · 내보내기 거처는 /data 와 같은 묶음으로 간다(Q8 과 같은 화면)`

`26.09.13 09:20 · Q8 /data 의 "이 기기에서" 기기 전용 저장 고지를 되살린다 · 권리가 아니라 투명성 문구이고 번역은 번들에 그대로 있다 · 뒤집는 조건: 없음`

`26.09.13 09:20 · Q9 프라이버시 화면의 신뢰 문구는 법률 자문에 올린다(D3 과 함께) · 표시 의무 여부를 먼저 확인 · 그때까지 "번들에 있고 화면에 없다"는 상태를 지키는 검사(#1791)를 건드리지 않는다 · 뒤집는 조건: 자문 회신`

`26.09.13 09:20 · 은퇴 후보를 줄 수로 고르지 않는다 · capture(4,636줄)가 가장 컸고 가장 살아 있었다. 이름의 Legacy 는 트랙 이름이지 상태가 아니다(formats.tsx 에서 한 번, capture.tsx 에서 또 한 번) · 뒤집는 조건: 없음. 앞으로 후보는 "배송되는가"를 읽어 확인한 뒤에만 세운다`
