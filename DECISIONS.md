# DECISIONS

**append-only.** 요약·재작성·삭제 금지. 새 줄은 맨 아래에 붙인다.
한 줄 = `YY.MM.DD HH:MM · 결정 · 이유 · 뒤집는 조건`

이 파일이 하는 일: 결정이 난 그 턴에 남긴다. 대화는 버퍼고, 파일에 없으면 없는 것이다.
상태(지금 어디까지 / 다음 하나 / 막힌 것)는 여기 쓰지 않는다 — `STATE.md` 가 소유한다.
100KB 에 닿으면 요약하지 말고 기간으로 쪼갠다(`DECISIONS-2026H2.md`).

기간 보관(닫힌 파일 — 줄을 그대로 옮김 · 2026-09-19 09:54 KST 분할 · 분할 전 sha256 `ace3826318ba`):
- `DECISIONS-2026-09-13.md` — 26.09.13 ~ 26.09.13
- `DECISIONS-2026-09-14.md` — 26.09.14 ~ 26.09.14
- `DECISIONS-2026-09-15_16.md` — 26.09.15 ~ 26.09.16
- `DECISIONS-2026-09-17_18.md` — 26.09.17 ~ 26.09.18 (2026-09-20 01:52 KST 분할 · 분할 전 sha256 `1bb273c78fab`)

---

26.09.19 08:49 · **[Orca 에뮬레이터 정상화 + QA 발견 — 재부팅 뒤 앱이 Loading 에서 멈춤]** (Simon 요청 "다음부터 orca에서 정상 실행되도록") 원인(확인): Orca 는 `emulator.exe -avd Pixel_9_Pro_XL -no-window` 만 넘기고, GPU auto 가 창 없는 실행에서 lavapipe 를 골라 부팅이 멈췄다(`hardware-qemu.ini`). 처음 댄 '잘린 스냅샷' 원인은 틀렸다(스냅샷 삭제·콜드 부팅에도 재발). 조치: AVD config `hw.gpu.mode=host` · `forceColdBoot=yes` · `forceFastBoot=no` · `hw.ramSize=4096` · `saveOnExit=false`(백업 스크래치패드 avd-backup-260919) → Orca attach 로 27초 부팅, 앱 lowmemorykiller 종료 0. **QA 발견(심각도 높음, 원인 미확정)**: QA 빌드 `84d6800c` 에서 앱 데이터 비움 → 실행(로그인 화면 정상) → **기기 재부팅 → 실행 → `[auth] recovery proof hydration failed; phase=bootstrap-marker-read` → `fail-closed-entry` 저장 실패 → `fail-closed-signout` 실패 → Loading 영구 정지**. 3회 재현(LMK 종료 뒤 1 · 에뮬 재시작 뒤 1 · `adb reboot` 뒤 1). 앱 데이터를 지우면 풀린다. 실기기에서도 재부팅마다 앱이 멈춘다는 뜻일 수 있다. main(#1807 wave4 auth) 원인인지 #1828 · #1831(같은 recovery-proof-store) 원인인지 미확정 — main 단독 네이티브 빌드는 #1819 없이 부팅 자체가 안 돼 'main + #1819' 빌드로 가려야 한다. 증거 `E:/Coding Infra/reports/qa-260919/`(logcat 3개 · 화면 3장). 게이트(19:47 뒤) 전에 원인을 가려 #1828 · #1831 점검에 넣는 것을 제안.
26.09.19 09:16 · **[에뮬레이터 재발 방지]** (Simon "재발 방지 했어?") Simon 이 Orca 에서 고른 AVD 는 `2ndB_Codex_API36_260727`(gpu enabled=no · auto · 1536M)이라 같은 멈춤 → AVD 6개 전부 `hw.gpu.enabled=yes · hw.gpu.mode=host · forceColdBoot=yes · forceFastBoot=no · hw.ramSize=4096` 로 맞춤(원본 백업) · Codex AVD 도 Orca attach 31초 부팅 확인. 재발 방지 장치: `E:\Coding Infra\tools\avd-guard\avd-guard.ps1`(값이 다를 때만 교정 · 백업 · RAM 올리기만 · 죽은 pid 표지 정리) + 시작프로그램 `avd-guard.lnk` 로 로그인마다 실행 — 가짜 새 AVD 로 교정 5 · 재실행 0 · BOM 없음 확인. 남는 틈: 로그인 뒤 새로 만든 AVD 는 다음 로그인까지(수동 실행 가능) · Orca 가 -gpu 를 안 넘기는 근본 원인은 Orca 쪽 · 앱의 '재부팅 뒤 Loading 멈춤'은 별개로 미해결. 뒤집는 조건: GPU host 로 부팅이 깨지는 PC(원격·GPU 없음)에서는 swiftshader_indirect 로.
26.09.19 09:29 · **[에뮬레이터 설정 완결]** (Simon "그냥 너가 완전하게 설정해") avd-guard 를 감시 모드(`-Watch`)로 바꿔 AVD 폴더 변경 1~3초 안에 교정 · 10분마다 전체 점검 · 뮤텍스로 1개만 · 시작프로그램 `avd-guard.lnk` 가 로그인마다 띄움 · 지금 떠 있음(86 MB). 시험: 가짜 새 AVD 1초 교정 · auto 되돌림 2초 · 두 번째 감시자 거부 · 실제 AVD(2ndB_QA_009) auto 되돌림 3초 뒤 host 복귀 · BOM 없음. Orca 1.4.200 에는 GPU 옵션 설정이 없어(app.asar 확인) AVD 쪽에서 막는 것이 유일한 경로. 시험 중 잡은 결함 2(공백 경로 따옴표 · 함수 로그가 반환값 오염)는 고친 뒤 재시험 통과.
26.09.19 09:55 · **[DECISIONS.md 기간 분할 — §0-1 "100KB 에 닿으면 요약하지 말고 기간으로 쪼갠다"]** 분할 전 175,752 B(LF 정규형 · sha256 `ace3826318ba` · 날짜 줄 177). 닫힌 보관 파일 3개 `DECISIONS-2026-09-13.md`(26,310 B · 49줄) · `DECISIONS-2026-09-14.md`(73,730 B · 70줄) · `DECISIONS-2026-09-15_16.md`(36,444 B · 28줄) + 활성 `DECISIONS.md`(09-17~ · 30줄). 검증: 나눈 본문을 다시 이으면 원본과 바이트 동일(쓰기 전 · 쓴 뒤 두 번) · 날짜 줄 49+70+28+30=177 · 시간순 정렬 확인. 발견: 작업본에 CRLF 191 · LF 59 줄바꿈이 섞여 있었다(09-14 08:34~09-16 구간을 LF 로 붙였던 것 — git blob 은 전부 LF 라 내용 손실 없음) → 작업본을 CRLF 로 통일, 붙이기는 `dec_append.py` 로만. 뒤집는 조건: 없음(줄을 옮겼을 뿐 — 되돌리려면 보관 파일 본문을 순서대로 이으면 된다).
26.09.19 09:55 · **[머지 전 확인 3건 — 코디네이터 실측, 운영은 읽기만]** ① `supabase secrets list -o json`(CLI 2.117.0) = `{name, updated_at, value}` 배열 40개 · value 는 소문자 64자리 hex = **값의 평문 SHA-256**(값이 알려진 플래그 5개로 대조: ENABLE_NAVER_OAUTH=true · ENABLE_XAI_PROXY=true · PADDLE_SELF_SERVICE_DRYRUN=0 · PADDLE_SELF_SERVICE_ENABLED=1 · PADDLE_WEBHOOK_ENABLED=1) → #1825 의 사후 검증 로직과 맞는다(워크플로 핀은 2.116.0 — 그 판 출력은 미확인). digest · 비밀 이름 전체는 출력하지 않았다. 부수 확인: 짧은 값은 digest 로 복원된다는 U5 의 위험이 실제로 성립. ② 운영 `PADDLE_API_BASE` 는 **미설정** → #1827 은 기본값 `https://api.paddle.com` 을 쓴다(misconfigured 로 닫히지 않음). `PADDLE_CHECKOUT_BINDING_SECRET` 도 미설정(D1 기존 판정 그대로). ③ Paddle API 응답 `Content-Type: application/json`(두 호스트 실측, 무인증 403) → #1827 정규식 통과. Paddle → 우리 웹훅 요청의 Content-Type 은 공식 문서에 없다 — 공식 Node SDK README :2154 가 `express.raw({ type: 'application/json' })` 로 본문을 받으므로 `application/json` 이 사실상 확정(간접 근거), 직접 확인은 샌드박스 시뮬레이터 1회(Paddle 로그인 = Simon). 뒤집는 조건: 시뮬레이터에서 다른 Content-Type 이 오면 #1829 의 415 정규식을 넓힌다.
26.09.19 09:55 · **[/vibe 13회차 발주 — Simon 지시 "/vibe 남은 문제들을 해결해"]** run `run_e3a3e38558ab`. 쿼터(09:41): claude 82%(>80 강등 · 리셋 오늘 13:00) · codex 99%(리셋 19:47) · grok 100% · gemini 워커 불가 · fable 0% → 코딩 2순위 `claude-fable-5-1` @max 로 워커 1개: r13-boot-hang(`ctx_63b463a1d113`, 워크트리 `fix-auth-boot-hang-260919` = origin/main `a690b742`) — "기기 재부팅 뒤 Loading 영구 정지" 원인 규명 + 수정 PR. 1차 분류(반증 대상): 실패 경로는 main `AuthContext.tsx:949 → failClosedRecovery → if (!closed) return` + #1815 로더, 머지 대기 PR 10개는 그 경로를 안 건드림(src/lib/auth 에서 recovery-proof-store.ts 23줄뿐). 빌드는 `E:/Coding Infra/tools/qa_apk_build.py`(QA 워크트리 로컬 시험 브랜치) · 에뮬레이터는 워커 전용 Pixel_9_Pro_XL(Simon 의 emulator-5554 는 건드리지 않음). 탐색 슬롯 없음(코딩은 제외 공정). 게이트 두 레인은 19:47 뒤 — codex CLI 가 최신보다 뒤처져 있어(check_tooling) 게이트 전에 올린다(G11).
26.09.19 10:08 · **[기록 PR #1832 머지 `d0929429` + 남은 워크트리 정리]** 기록 50커밋(09-16~09-19 결정 · 기간 분할 · STATE)이 main 에 올랐다 — CI 3 pass, 머지 뒤 기록 브랜치와 main 의 diff 0 확인 → 새 기록 브랜치 `claude/vibe-r260919b`(main 위). ⚠ 코디네이터 실수 1건: 첫 `gh pr merge` 가 거부된 것(BEHIND)을 확인하지 않고 워크트리를 main 기준 새 브랜치로 먼저 옮겼다 — 기록 브랜치 커밋은 그대로여서 되돌렸고 손실 0. 이후 "머지 상태 확인 → 그다음 브랜치 이동" 순서로 한다. **알게 된 것: main 보호 규칙 = 필수 체크 `verify` + strict(브랜치가 main 최신이어야 머지) · auto-merge 허용 · 관리자 우회 가능(쓰지 않는다)** → 오늘 밤 PR 11개는 하나 머지할 때마다 나머지를 main 에 맞춰 올리고 CI(약 3분)를 다시 기다려야 한다. 남은 워크트리: 머지 끝난 7개 폴더 제거(`fix-auth-loading-hold` #1815 · `fix-p1-followup` #1813 · `fix-probe-retry` #1811 · `p1-highlight` #1812 · `handoff-split` #1804 · detached 2 — 미커밋 0 · 로컬 팁이 머지된 PR head 의 조상 또는 동일 · 정션 해제 · force 없음 · node_modules 747 · 브랜치 유지). PR 없는 미푸시 브랜치 둘(`fix/legacy-archive-integrity-pin-260913` 102커밋 · `codex/prod-workflow-ref-gates-260913` 7커밋)은 주인이 달라 그대로 두고 번들만 떴다(`E:/Coding Infra/_rescue/unpushed-prless-2-260919.bundle`, 763,900 B). Hyper-V vmmem 은 PC 재부팅으로 사라짐(09:41 실측 0). 게이트 라운드는 이 세션에 19:53 KST 자기 예약(CronCreate 5ab452f3, 세션이 살아 있어야 함). 뒤집는 조건: 없음.
26.09.19 10:24 · **[치명 결함 원인 확정 — main 의 Android 빌드는 두 번째 실행부터 전부 Loading 에 갇힌다 · 코디네이터 판단 D]** 워커 r13 중간 보고 + 코디네이터 소스 대조. 재부팅은 필요 없다: `am force-stop` 뒤 재실행만으로 100% 재현 · main + #1819 계측 빌드에서도 재현(머지 대기 PR 탓 아님 — 1차 분류의 "PR 탓 아님"은 맞았고 "재부팅이 조건"은 틀렸다). 진짜 오류는 `secure_storage_decrypt_failed`(마스터 키 · 센티넬은 정상). 원인: main `src/lib/storage/encrypted-native-storage.ts:905` 가 `AESSealedData.fromCombined(<base64 문자열>)` 을 부르는데 expo-crypto 56 의 Android 네이티브는 `fromCombined(combined: ByteArray, …)` 바이트 전용(upstream `expo/expo` sdk-56 `AesCryptoModule.kt:80` 코디네이터 확인) · iOS 는 `BinaryInput`(`AesCryptoModule.swift:78`)이라 문자열을 받는다. 첫 실행은 쓰기만 해서 멀쩡하고, 그 뒤 모든 실행이 복호화 실패 → fail-closed → 로컬 로그아웃도 같은 저장소라 실패 → `if (!closed) return` → #1815 로더가 영영 안 풀림. 기존 테스트 mock 이 "문자열이어야 한다"를 강제해 가려져 있었다. **영향: #1807(09-13) 이후 main 의 모든 Android 빌드(실기기 포함, 확신도 높음) · iOS 와 v0.8.0 설치본(#1807 이전)은 무관 · 공개 QA 릴리스 `qa-260918-84d6800c` APK 도 해당** → 릴리스 안내문 맨 위에 경고를 넣었다(10:2x). 워커의 (가) 수정(바이트로 디코드해 전달 · 빨강→초록 · 변이 2)은 끝났고 검증 빌드 중. **코디네이터 판단: 이 PR 은 (가)만(D).** (나) "부팅 읽기와 로컬 로그아웃이 둘 다 실패할 때의 출구"(A 기존 복구 동의 화면 승격 · B 비파괴 재시도 화면 · C 둘 다 · 변형 A' 영구 계열 오류만 승격)는 저장소 계층이 테스트로 못박은 방침("일시 장애에는 파괴적 복구를 제안하지 않는다")을 건드리는 제품·보안 설계라 Simon 결정으로 올린다. 추가 검증 요청: 깨진 상태 위 덮어 설치로 pm clear 없이 복구되는가 · 로그인 상태 force-stop 뒤 세션 유지. 다음: 수정 PR 게이트 뒤 머지 → **수정이 든 QA APK 로 공개 릴리스 교체**. 뒤집는 조건: 덮어 설치로 복구가 안 되면 (나)를 이 PR 에 묶는 것을 다시 본다.
26.09.19 10:52 · **[r13 수확 — Android 복호화 결함 수정 draft PR #1833 · CI 초록 · 게이트 대기]** 워커 r13-boot-hang(`ctx_63b463a1d113`, `claude-fable-5-1` @max, 63분, G8 남은 것 0 · 터미널 닫음 · 원장 1행 CAS). 커밋 `5931f140` · 2파일(제품 `src/lib/storage/encrypted-native-storage.ts` +37/−2 · 테스트 +32/−8) · verify rc=0(749 스위트 · 9,023) · CI lint · verify · web-export-smoke pass. 코디네이터가 제품 diff 직접 읽음: 엄격한 정규 base64 디코더(`isCanonicalBase64` 검증 뒤 바이트로) + `AESSealedData.fromCombined(<Uint8Array>)` · 인터페이스 타입도 `Uint8Array` 로. 실측 예외 원문 "Cannot convert … Value is a string, expected an Object". 에뮬레이터: 재현 → 수정 뒤 force-stop · 재부팅 모두 로그인 화면/세션 유지 · **버그 빌드로 깨진 기기에 `adb install -r` 덮어 설치하면 pm clear 없이 버그 빌드가 쓴 세션(2,189 B)이 그대로 복호화돼 홈까지 복구**(공개 QA APK 받은 기기의 복구 경로). 로그인 직후 프로필 확인 실패도 같은 결함이었다. 미검증: iOS · Android 실기기. 워커 실수 1건(보고됨): `qa_apk_build.py --help` 가 실제 빌드를 시작해 빌드 워크트리 `android/` 가 prebuild --clean 으로 재생성됨 — 공개 APK 무손상 · 3분 뒤 종료 → 스크립트에 `--help` 가드를 넣는다. (나) 출구 설계는 result.md §6(A · A′ · A″ 지속성 기준 · B · C — 오류 코드로는 영구/일시를 못 가른다 · 재시도는 auth-js 가 실패한 initializePromise 를 캐시해 클라이언트 리셋이 필수 · 같은 구멍이 웹에도 있음) → Simon 결정으로. 후속(진행 중): QA 통합 브랜치 `qa/integration-260919`(`640db5bd` = 260918 + #1833, origin 에 push) 로 arm64 · x86_64 재빌드 → 에뮬레이터 확인 → 새 공개 사전 릴리스로 교체. 뒤집는 조건: 게이트가 디코더에서 결함을 찾으면 수정 라운드.
26.09.19 11:04 · **[수정이 든 QA APK 공개 — 사전 릴리스 `qa-260919-640db5bd` · 09-18 릴리스는 새 릴리스로 안내]** 09-18 Simon 확정("최신 상태를 APK 릴리즈 · QA용 · 공개")의 연장: 공개해 둔 09-18 APK 가 두 번째 실행부터 멈추는 빌드라 수정본으로 교체했다. 브랜치 `qa/integration-260919`(`640db5bd` = 260918 + #1833, origin push · 태그가 이 커밋을 가리킴) · arm64 69,902,632 B(sha256 `62bd42d26487…`) · x86_64 71,429,033 B(`06901a145ceb…`) · 두 APK JS 번들 동일(`e0f697d1…`) · prerelease 라 v0.8.0 이 Latest 유지 · 토큰 없이 API 200 + 체크섬 파일 내려받아 대조. 코디네이터 재확인(에뮬레이터 Pixel_9_Pro_XL · emulator-5556 · 11:00 KST, Simon 의 5554 는 안 건드림): 09-18 빌드 새 설치 → 재실행 시 경고 3줄 + Loading(캡처 192 KB) → 새 빌드 `adb install -r`(pm clear 없음) → 실행 · force-stop 뒤 재실행 모두 로그인 화면(캡처 329 KB) · 인증 경고 0. 증거 `E:/Coding Infra/reports/qa-260919/verify-*.png`. 09-18 릴리스 안내문 경고를 "수정 빌드 나옴 → 링크"로 바꿨다(자산은 남김). 보고서 <https://claude.ai/artifact/8qYXcdSTkMYNzUaN5ZXfbh>(결정 2건: (나) 출구 설계 — 추천 A″ 연속 실패 시에만 복구 동의 화면 · gstack 업그레이드 시점) · G14 시트 `E:/Coding Infra/reports/vibe-r260919/decisions_sheet_run_e3a3e38558ab.html`. `qa_apk_build.py` 에 `--help` · 모르는 옵션 가드 추가(워커 사고 재발 방지, rc=64 확인). 뒤집는 조건: 실기기에서 덮어 설치 복구가 안 되면 릴리스 안내를 "앱 데이터 삭제 필요"로 고친다.
26.09.19 11:31 · **[Simon 지시 "남은 작업 모두 진행해. 좀있다가 usage 리셋 돼면 QA 진행할꺼야." (11:2x) → 코디네이터 실행 · /vibe 14회차]** 쿼터(11:21): claude 주간 90%(리셋 13:00) · 세션 33% · fable 0% · codex 99%(19:47) · grok 100% · gemini/antigravity 0%(워커 불가). ① **결정 시트 1번((나) 이중 실패 출구)은 Simon 이 고르지 않았다 → Simon 결정으로 기록하지 않는다.** 코디네이터 판단: "남은 작업 모두 진행"을 추천안 ① A″ 의 **draft 구현까지**로 읽는다(되돌리기 쉬움 · 머지는 Simon 확인 뒤). 워커 r14-boot-exit `ctx_aedfc1071b45`(`claude-fable-5-1` @max · run `run_e3a3e38558ab` · 워크트리 `fix-auth-boot-exit-260919` = origin/main `d0929429`). 설계 고정: `failClosedRecovery` 의 마지막 분기(로그아웃 실패 · recovery_required 아님)에 도달한 콜드 스타트를 평문 AsyncStorage 카운터로 1회씩 세고 N=3 이면 `markStorageRecoveryRequired()` → 기존 게이트 · 카운터 읽기/쓰기 실패면 즉시 승격 · 정상 부트스트랩 · 게이트 복구 뒤 0 · 새 문구 0 · 세션 승격 0. 에뮬레이터 실측은 #1833 이 없는 빌드로 이중 실패를 자연 재현(3번째 실패 실행에서 게이트). 결정 시트 2번(gstack 시점)도 미응답 → 추천대로 게이트 라운드 뒤에 한다. ② codex CLI 는 이미 최신(0.155.1 = npm latest · check_tooling 통과) — G11 조치 불필요. ③ QA 준비: 웹 QA 를 `qa/integration-260919`(`640db5bd`)로 다시 빌드(expo export rc=0 · 11:26) → <http://127.0.0.1:8765/2nd-B/> 200 · 딥 링크 200. Orca 에뮬레이터 5554(`2ndB_Codex_API36_260727`)의 v0.8.0 은 **그대로 둔다** — QA 빌드 덮어 설치가 `INSTALL_FAILED_VERSION_DOWNGRADE`(40 < 51)로 거부됐고, 되려면 삭제(기기 데이터 소실)가 필요해 Simon 의 기기에서 하지 않았다. 최신 QA 빌드는 `Pixel_9_Pro_XL` 에 있다. ④ 기록: HANDOFF 09-19 블록 prepend(94,439 B < 100 KB · `^## Latest` 1개) · CLAUDE.md 앱 화면 수 100 → **101**(origin/main `.tsx` 105 − `_layout` 2 − `+` 2, 줄 중립) · STATE §6 의 "0148~0150 적용 대기" 는 HANDOFF 610줄이 이미 정정해 두었다(항목 닫음). 뒤집는 조건: Simon 이 시트 1번에서 ①이 아닌 것을 고르면 r14 PR 을 닫는다.
26.09.19 11:48 · **[Simon 확정 1 · 결정 시트(Android 멈춤 보고) 1번] 저장소 읽기 · 로컬 로그아웃 이중 실패 때의 출구 = ① 연속 N번 실패하면 그때 복구 동의 화면(A″) · 무게: 확정** · 이유(시트 추천 근거): 이번 사고가 "데이터는 멀쩡한데 앱이 못 읽은" 경우였다 — 즉시 승격(②)이면 멀쩡한 데이터를 지우라고 권했을 것이다. N 은 Simon 이 따로 정하지 않았다 → 시트 예시대로 **N=3** 으로 구현 중(r14 워커에 11:4x 알림 — PR 본문 머지 조건을 "Simon 확정 · 보안 두 레인 통과 뒤"로). 11:31 의 "Simon 결정으로 적지 않는다"는 이 줄로 대체된다. 뒤집는 조건: 실사용에서 일시 장애가 3회 안에 풀리지 않는 사례가 잦으면 N 을 다시 본다.
26.09.19 11:48 · **[Simon 확정 2 · 같은 시트 2번] gstack 업그레이드 = ② 지금 올린다 · 무게: 확정** · 코디네이터 추천(게이트 라운드 뒤)과 달랐다 — 상충 점검: 공용 스킬 폴더를 쓰는 워커는 r14 하나였고 gstack 스킬을 쓰지 않는다 · 게이트 라운드(19:53)는 /vibe 라 무관 → 바로 실행. 결과: `/gstack-upgrade` 로 **1.84.1.0 → 1.87.4.0**(global-git · 로컬 변경 0 · 미푸시 커밋 0 · fast-forward · setup 성공 · 마이그레이션 1.86.0.0 1건 · 브라우저 데몬 없음 · 업데이트 확인 = 최신). ⚠ setup 이 "편집된 SKILL.md 40개"를 `~/.gstack/backups/skills/20260919T114445` 로 옮기고 새 판을 깔았다 — 코디네이터 대조: 40개 전부 **이전 gstack(71f6048) 이 생성한 파일과 바이트 동일**(차이 0 · gstack 트리에 없는 것 0) → 사용자 수정 손실 0, 감지 오탐. ⚠ `/cso` 는 이 PC 에서 "not assessed" 로만 돈다(네이티브 도우미 빌드에 Visual Studio 2022 Build Tools 필요) — 보안 게이트는 /vibe 두 레인이라 영향 없음. 주요 변경: `/claude` → `/claude-code` · 바깥 리뷰를 하네스별로 라우팅 · `/review` `/ship` 이 리뷰한 트리에 증거를 묶음 · `/health` 커버리지 공개 · sharp/adm-zip 취약점 오버라이드. 결정 시트 응답은 G14 시트가 아니라 보고서 페이지로 왔다(`decisions_run_run_e3a3e38558ab.json` 없음 — 채택 원장은 손으로 채우지 않는다). 뒤집는 조건: 새 판 스킬에서 회귀가 나면 `git -C ~/.claude/skills/gstack reset` 대신 백업 폴더와 `71f6048` 로 되돌린다(Simon 확인 뒤).
26.09.19 12:25 · **[r14 수확 — 이중 실패 출구 draft PR #1835 · CI 초록 · 게이트 대기(Simon 확정 ① · N=3)]** 워커 r14-boot-exit(`ctx_aedfc1071b45`, `claude-fable-5-1` @max, 55분, G8 남은 것 0 · worker-release · 터미널 닫음 · 원장 1행 CAS). 커밋 `ea1c3592`(기능) + `9fc411c1`(코디네이터가 승인한 주석 전용 4번째 파일 `storage-recovery-gate.tsx` — 게이트가 뜨는 조건 서술을 맞춤, 줄 중립) · 제품 `AuthContext.tsx` +26/−6 · `storage-recovery.ts` +21/−1 · 신규 `fail-closed-persistence.ts` 172줄 · 테스트 2개 · verify rc=0(750 스위트 · 9,058) · CI 3/3 · 변이 12종 전부 해당 테스트만 빨강 · 법무 인용 줄 전후 동일. 코디네이터가 AuthContext diff 를 직접 읽음: `bootstrapped = true` 4곳을 `markBootstrapped()`(0 되돌리기, 부팅을 막지 않는 best-effort) 한 곳으로 · 마지막 fail-closed 분기에서 잠금을 먼저 게시한 뒤 `escalateFailClosedLockIfPersistent` → 3회째에 `phase=fail-closed-escalate` 경고 + `markStorageRecoveryRequired()` · `refresh()` 재시도 없음 · 동의 폐기 성공 뒤 카운터 삭제(best-effort). 에뮬레이터(워커 기기 emulator-5556, Simon 의 5554 무접촉): #1833 없는 빌드에서 실패 1 · 2회차 Loading → 3회차 게이트 → 2단계 동의 → 폐기 → 로그인 화면 · 폐기 뒤와 #1833 포함 빌드 정상 부팅 뒤 다음 실패가 다시 Loading(0 되돌리기 행동 확인) · 로그인 상태 재실행 정상 · 한 번의 실패는 세션을 지우지 않음. 한계(result.md §7-8): 카운터 값 직접 판독 불가(Play 이미지 · 행동 증거로 대체) · 앱 사용 중 생긴 이중 실패는 다음 콜드 스타트부터 셈 · 웹의 같은 구멍 범위 밖. 워커 지적: 코디네이터가 준 `expo export` 확인 명령은 조회 셸 자신의 명령줄이 걸려 0 이 안 나온다(자기 매칭) — 다음부터는 자기 PID 를 빼고 센다. QA 워크트리는 `qa/integration-260919` · 깨끗 · `dist/` 11:26 그대로 · 웹 QA 200. 보고서 v2(확정 2건 결과 · #1835) <https://claude.ai/artifact/8qYXcdSTkMYNzUaN5ZXfbh> · G14 시트 재생성(항목 3). 게이트 대기 draft PR 은 이제 12개. 뒤집는 조건: 게이트가 카운터 · 승격 경로에서 high 이상을 찾으면 수정 라운드.
26.09.19 20:06 · **[보안 게이트 라운드 시작 — 19:53 자기 예약 · Simon 확정 D2 ① · X-2 · N1 ① · 09-19 11:4x ①]** 쿼터: `orca account list` 의 codex 값은 12:02 에 멈춘 낡은 값(100% · 19:47 리셋 표시, updatedAt 기준 7.8시간 전)이라 **G12 실호출로 확인** — `codex exec -m gpt-5.6-luna` 한 줄 → rc=0 "OK"(19:54). codex CLI 0.155.1 = npm latest · check_tooling 최신(G11) · 도는 codex 프로세스 0. claude 주간 1%(13:00 리셋) · fable 0%. 대상 draft PR 12개 = 브랜치 팁 12/12 가 PR HEAD 와 일치. **4묶음 × 두 레인 = 워커 8**(run `run_e3a3e38558ab` · 공용 읽기 전용 워크트리 `gate-review-260919` = origin/main detached · fetch 금지 · 산출 `E:/Coding Infra/reports/vibe-r260919/gate-<묶음>-<레인>/findings.md`): A 인증 · 저장소 · 복구 #1833 · #1835 · #1828 · #1831 · #1826 / B 서버 · 결제 · CI #1825 · #1827 · #1829 · #1830 / C 네이티브 · 홈 UI #1819 · #1810 / D 자동 저장 재설계 #1814(게이트 노트 "새 대화 뒤 저장은 D-1 ② 의도된 동작"). 레인: 생성물 `gpt-daybreak-blue-latest` @xhigh · 인가 `gpt-6-astra` @xhigh(코딩 레인 전부 claude → G1 충족 · validate_plan 위반 0). 디스패치 8/8 ok · codex 입력창 멈춤 대비 Enter 뒤 화면 Working=True 8/8. 판정 규칙: critical · high 0 = PASS → `gh pr ready` + `--auto` squash 머지(main strict — 머지 뒤 `state=MERGED` 확인) · 쌓인 #1831 · #1830 은 아래 PR 머지 뒤 base 를 main 으로 · high 이상은 수정 라운드. 뒤집는 조건: 게이트 산출이 차단 문구("This content can't be shown")로 조용히 실패하면 terminal read 로 확인해 재발주.
26.09.19 20:20 · **[보안 게이트 판정 8/8 수확 (20:09~20:18) — 워커 8 전부 G8 kill · release · 터미널 닫음 · 원장]** 묶음 A(인증 · 저장소 · 복구): #1833 PASS/PASS · #1828 PASS/PASS · #1831 PASS/PASS · #1826 생성물 PASS WITH CONCERNS(AA-1826-1 medium: 네이티브 HIBP 응답을 다 받은 뒤에야 256 KiB 검사 — 신뢰 제3자 오동작 전제 · AA-1826-2 low: 딥링크 전체 길이 검사가 파서보다 늦음)/인가 PASS · #1835 생성물 medium(AA-1835-1 카운터 I/O 무기한 대기면 출구가 안 열림)/인가 medium(AZ-1835-1 부분 손상에서도 게이트가 뜨는데 동의 문구는 "읽을 수 없는 데이터만" · 실제 폐기는 관리 키 전부 + 마스터 키). 묶음 B(서버 · 결제 · CI): #1825 · #1827 · #1830 PASS/PASS · #1829 생성물 low(Content-Type 매개변수를 서명 검증 전에 415)/인가 low(샌드박스 실제 Content-Type 미확인 — 같은 뿌리). 묶음 C: #1819 · #1810 PASS/PASS(발견 0). 묶음 D: **#1814 BLOCK/BLOCK** — DA-1814-1 · DZ-1814-3(High/Medium, 같은 뿌리) 손 담기와 되돌리기 drain 경쟁으로 사용자가 남기기로 한 대화가 성공 표시 뒤 삭제 · DZ-1814-1(High) 복구 조회 중 철회하면 취소된 작업이 kept 확정 · DA-1814-2 · DZ-1814-2(Medium) 대기 기록 저장 실패 무시 · DA-1814-3(Low) 파서 상한 · O(n²). **코디네이터 판단**: ① critical · high 0 인 PR 10개는 머지(PASS WITH CONCERNS 포함) — #1826 의 두 건과 #1829 의 low 는 **후속**(#1829 는 운영 승격 전 Paddle 샌드박스 확인 = Simon, AA-1826-1 은 스트리밍 전송 또는 앱 소유 프록시가 필요해 이 PR 범위를 넘는다). ② **#1835 는 medium 2건을 머지 전에 고친다** — 둘 다 이 PR 의 목적(출구 · 명시 동의)과 맞닿음 → r16-fix1835(`claude-opus-5` 표준 · `ctx_a54dd6fb27ab`): 카운터 I/O 시간 상한 + 세대 울타리 · 동의 문구를 실제 삭제 범위와 같게(동작은 안 바꿈 · 5개 로케일) → 두 레인 재게이트. ③ **#1814 는 수정 라운드** → r17-fix1814(`claude-opus-5` ultracode · `ctx_aad8c3323faa`): 뿌리 넷(경쟁 직렬화 · 철회 세대 확인 · 대기 기록 내구성 · 파서 상한) + 결정적 회귀 테스트 → 두 레인 재게이트. 머지 진행: **#1819 MERGED `1d93e3bd`(20:17)** — "#1819 머지 전 main 에 [ota]/[release] 금지 · Android 릴리스 금지" 제한이 풀렸다. 이어서 #1810 → (#1833 · #1828 · #1831 · #1826 · #1825 · #1827 · #1829 · #1830) 순서 기차. 뒤집는 조건: 재게이트에서 high 가 다시 나오면 해당 PR 은 머지하지 않고 Simon 에게 올린다.
26.09.19 20:32 · **[머지 진행 · 쌓인 PR 충돌 해소 · 코디네이터 사고 1건]** 머지됨: #1819 `1d93e3bd`(20:17) · #1810 `9f3f3462`(20:20) · **#1833 `dab26472`(20:24 — main 의 Android 두 번째 실행 멈춤 해소)** · #1828 `1b65fbfb`(20:27). #1831(base = #1828 브랜치)은 base 를 main 으로 바꾸자 DIRTY — #1828 squash 로 병합 기준점이 옛 main 이 돼 양쪽이 #1828 의 줄을 서로 다르게 추가한 꼴. 해소: 임시 워크트리 `merge-1831-260919`(브랜치 `merge/1831-260919`)에서 origin/main 병합 → 충돌 2파일은 main 판 → #1831 자기 커밋(e4d6a69f..e6e6ebd2)만 재적용 → **순 diff 가 게이트가 본 diff 와 동일**(+/- 100줄 = 100줄 · 2파일 +94/−6) · `recovery-proof-store.test.ts` 22/22 → merge 커밋 `c1400fd2` fast-forward push(force 없음). 머지 기차 검사 결함 수정: "게이트 뒤 새 커밋"을 `gated..head` 로 세면 main 을 합친 브랜치에서 main 의 squash 커밋까지 잡혀 거짓 정지 → `--no-merges gated..head ^origin/main` 로 바꿈(#1831 = 0). #1830(base = #1829)도 같은 모양이 예상된다 — 같은 방법으로 푼다. ⚠ **코디네이터 사고**: 첫 `git worktree add` 가 로컬 브랜치 이름 충돌로 실패했는데 이어진 `cd` 도 실패해 `git merge --no-commit --no-ff origin/main` 이 **정본 체크아웃 `E:/2ndB`(main)** 에서 돌았다(병합 진행 · 스테이징 7파일) → 즉시 `git merge --abort`(추적 파일 변경 0 · MERGE_HEAD 없음 · 추적 안 되는 파일 그대로 확인). 재발 방지: `cd <경로> || exit 1` / `git -C` — 메모리 `feedback_2ndb_worktree_fleet` 에 적음. r17(#1814) 질문 답: 철회 뒤 삭제와 대기 기록 쓰기가 둘 다 실패한 경우의 표시는 **B 새 문구(5개 로케일 · 상한 밖 승인)** — A(keepFailed 재사용)는 "다시 담아 주세요"가 철회 맥락에서 사실과 반대(기록은 서버에 아직 남아 있음)라 기각.
26.09.19 20:50 · **[머지 9개 · #1830 충돌 해소 · #1835 수정 수확 → 재게이트]** 머지: #1831 `562bba7d`(20:33) · #1826 `ecd9ce86`(20:36) · #1825 `b2cbb35e`(20:40) · #1827 `26eae75a`(20:42) · #1829 `a0db207b`(20:45) — 누적 9개(#1819 · #1810 · #1833 · #1828 · #1831 · #1826 · #1825 · #1827 · #1829). #1830(base = #1829)도 예상대로 DIRTY → #1831 과 같은 방법: 임시 워크트리 `merge-1830-260919` · 충돌 2파일(`request-json.ts` · 그 테스트)만 main 판 + 자기 커밋(21a75b69..81b2525a) 재적용 · 나머지 2파일은 자동 병합 → **순 diff = 게이트가 본 diff**(+/- 135줄 = 135줄 · 4파일 +130/−5) · 관련 스위트 2개 116/116 → merge 커밋 `e1c687d5` fast-forward push → 머지 기차 재개(20:47). r16-fix1835 수확(`ctx_a54dd6fb27ab`, `claude-opus-5` 표준, 29분, G8 · release · 닫음 · 원장): 커밋 `b6ff9729`(9fc411c1 위 fast-forward) — AA-1835-1: 카운터 읽기 · 쓰기 · 재읽기 · 지우기마다 2초 상한 `FAIL_CLOSED_COUNTER_IO_TIMEOUT_MS` + 앞선 호출이 끝난 뒤에만 발행하는 순서 울타리(멈춘 I/O 도 상한 안에 승격 · 늦은 쓰기가 0 되돌리기를 못 되살림) · AZ-1835-1: 5개 로케일 `storageRecovery` 를 실제 폐기 범위(관리 키 전부 + 마스터 키, 어댑터 실행으로 6/6 삭제 확인)와 같게 — ko "보호된 앱 데이터는 아직 읽을 수 있는 것까지 모두 삭제돼요 · 로그인 상태 · 아직 기록으로 저장하지 않은 초안 · 가져오기 이력 … 다시 로그인해야 해요"(코디네이터가 ko/en 전후를 직접 읽음 · 해요체) · 키 · 동작 · 문턱 변경 0 · 8파일 · 빨강→초록 · 변이 5종 · verify rc=0(750 스위트 · 9,070). 후속 1건: 폐기 자체에는 시간 상한이 없다. → **재게이트 두 레인 발주**(regate-1835-artifact `ctx_883f5aea6c47` · regate-1835-bizlogic `ctx_6f304353e730`, 둘 다 Working 확인): 두 발견이 닫혔는지 + 수정이 만든 새 결함.
26.09.19 21:03 · **[게이트 통과 PR 10개 전부 머지 · main 진단 APK 빌드 복구(#1836) · #1835 재게이트 생성물 통과]** #1830 `6b0455a3`(20:50) 로 게이트 통과분 10개 머지 완료. 그런데 main push 진단 APK(`android-release.yml`)가 오늘 10회 모두 `Setup Android SDK` 에서 실패(`Warning: Failed to find package 'tools'` → sdkmanager exit 1 · 마지막 성공 09-13 · 러너 cmdline-tools 16.0) — 우리 코드와 무관한 러너 쪽 변화. 코디네이터가 직접 1줄 수정 PR **#1836**(`packages: platform-tools` + 이유 주석 · 액션 고정 커밋 그대로 · 커밋 `3e275cb4`) → 게이트 두 레인 **PASS/PASS(발견 0)** — 권한 · 고정 핀 · 서명 · ABI 검사 · 산출물 업로드 경계 유지(액션 dist · 실패 로그 대조) → 머지 `7db32826`(21:01) → main push 실행 35441701242 에서 `Setup Android SDK` **성공** 확인(빌드 진행 중). #1835 재게이트 생성물(regate-1835-artifact `ctx_883f5aea6c47`): **PASS WITH CONCERNS — 1차 AA-1835-1 · AZ-1835-1 둘 다 닫힘**, 새 low 2(EA-1835-1 settle 안 된 호출 하나가 이후 clear 발행을 계속 막을 수 있음 · EA-1835-2 로케일 테스트가 실제 삭제 범위의 긍정 의미를 고정하지 않음) — critical/high/medium 0 → 인가 재게이트 결과 뒤 머지 판단. 뒤집는 조건: 진단 APK 빌드가 다른 단계에서 실패하면 그 원인을 따로 연다.
26.09.19 21:10 · **[#1835 머지 — Simon 확정 ① 이중 실패 출구 main 착지]** 재게이트: 생성물 PASS WITH CONCERNS(1차 medium 2건 닫힘 · 새 low 2 EA-1835-1 · EA-1835-2 → 후속) · 인가 PASS(발견 0 · 카운터 18시나리오 · 부분 손상 · 명시 동의 · 5개 언어 대조로 두 발견 해소 독립 확인 · regate-1835-bizlogic `ctx_6f304353e730`). → `gh pr ready` · auto-merge · update-branch(충돌 없음) · CI → **MERGED `e60081f7`(21:07)**. 이로써 오늘 머지 12개(#1819 · #1810 · #1833 · #1828 · #1831 · #1826 · #1825 · #1827 · #1829 · #1830 · #1836 · #1835), 남은 draft 는 #1814(수정 라운드 r17 진행 중). main push 진단 APK: 7db32826 실행(35441701242)이 빌드 단계 · e60081f7 실행(35441956394) 대기 — 둘 다 끝나면 에뮬레이터에서 설치 → 실행 → force-stop → 재실행으로 확인. 후속(묶음): #1835 low 2 · 폐기 자체의 시간 상한 · #1826 AA-1826-1/2 · #1829 Paddle 샌드박스 Content-Type(Simon).
26.09.19 21:28 · **[main 진단 APK 빌드 복구 확인 · x86_64 진단 빌드 발주]** #1836 뒤 첫 main push 실행 35441701242(`7db32826`)가 **09-13 이후 첫 초록**(21:23 · 산출 `2ndb-7db3282.apk` 69,868,216 B · arm64-v8a 전용 · 번들에 #1833 표식 있음). 에뮬레이터 Pixel_9_Pro_XL(x86_64, abilist `x86_64,arm64-v8a`)에 깔았으나 시작하자마자 `SoLoader couldn't find DSO to load: libreactnative.so`(APK 안 lib/x86_64 를 찾음)로 죽는다 — 앱 결함이 아니라 arm64 전용 APK 를 x86_64 에뮬레이터에서 돌릴 수 없는 것(번역 레이어가 이 앱의 네이티브 로딩을 못 받침). 그래서 **main(`e60081f7` = #1833 + #1835 포함)을 `android_abi=x86_64` 로 workflow_dispatch**(실행 35442914312 · 산출물 업로드만, 배포 없음) → 끝나면 설치 → 실행 → force-stop → 재실행 3회. 에뮬레이터에는 QA 빌드 `640db5bd` 를 다시 깔아 두었다. 참고: push 빌드는 arm64 고정이라 "main push 진단 APK = 공짜 부팅 검사"는 **실기기 또는 arm64 에뮬레이터에서만** 성립한다(메모리 보정 대상).
26.09.19 21:41 · **[Simon 지시 (21:3x) "중간 등급도 머지 전에 고치고 진행해"]** — 게이트 판정 규칙을 바꾼다: 이제 **medium 이상은 머지 전에 고친다**(이전 규칙 "critical · high 0 이면 머지 · medium 은 코디네이터가 후속으로 돌릴 수 있음"을 대체). 적용: ① #1814 재게이트 잔여 medium 4건(GA-1814-1 ≡ GZ-1814-3 자동 저장 정확 중복 ↔ drain 경쟁 · GZ-1814-1 저장소 getter 거부 시 메모리 기록을 내구 성공으로 오인 · GZ-1814-2 부분 삭제 뒤 손 담기가 본문 없는 행을 성공으로 확정 · GA-1814-2 undo_unrecorded 재시도가 메모리 전용이라 재시작 뒤 유실)을 수정 라운드 r19 로 · 같은 파일의 low 3건(GZ-1814-4 A→B→A 전환 · GA-1814-3 계정 줄 시간 경계 없음 · GZ-1814-5 안내 문구의 재시도 약속)도 함께. ② **이미 머지된 #1826 의 medium AA-1826-1**(네이티브 HIBP 응답을 다 받은 뒤에야 256 KiB 검사)은 이 규칙이 나오기 전에 코디네이터가 후속으로 돌렸던 것 → 규칙의 취지대로 **곧바로 후속 수정 PR**(r20, main 기준) + 같은 곳 low AA-1826-2(딥링크 길이 검사가 파서보다 늦음). 둘 다 두 레인 재게이트 뒤 머지. low 만 남은 것(#1835 EA-1835-1/2 · #1829 Paddle 샌드박스 확인)은 후속 목록에 그대로. 뒤집는 조건: Simon 이 medium 을 개별로 후속 처리하라고 하면 그 건만 예외.
26.09.19 21:58 · **[main 네이티브 확인 끝 — #1833 · #1835 가 든 main 에서 Android 재실행 멈춤이 사라졌다]** x86_64 진단 빌드(workflow_dispatch 35442914312 · main `e60081f7` · 산출 71,389,265 B · ABI x86_64 · 번들에 #1833 · #1835 표식 둘 다)를 에뮬레이터 Pixel_9_Pro_XL(emulator-5556 · Simon 의 5554 무접촉)에 새로 설치: 첫 실행 → **로그아웃 상태 force-stop 재실행 3회 모두 로그인 화면**(`phase=bootstrap-marker-read` 0 · `fail-closed-escalate` 0 · FATAL 0) → 공용 QA 계정(.env.test · 값은 어디에도 옮겨 적지 않음)으로 로그인 → 소개 화면 → **로그인 상태 재실행 2회 모두 소개 화면(세션 유지 · Loading 없음)**. 로그인 직후 `[auth] profile probe failed 'JWT issued at future'` 1줄(D3 의 알려진 서버 시각 캐시 · #1811 재시도가 흡수). 캡처 `E:/Coding Infra/reports/qa-260919/main-e60081f7-{first,relaunch3,after-login,signedin-relaunch2}.png`. 에뮬레이터에는 이제 main 진단 빌드(CI 진단 키)가 깔려 있고 QA 계정으로 로그인된 상태 · 에뮬레이터는 껐다. 메모리 `reference_2ndb_android_emu_dead` 보정(push 빌드 arm64 전용 → 에뮬은 x86_64 디스패치 · SDK 설치 실패 #1836).
26.09.19 22:10 · **[r20(#1826 후속) 전제 정정 + 코디네이터 판단 A]** 워커 소스 조사(expo 56.0.13 · `E:/Coding Infra/reports/vibe-r260919/r20-fix1826/probe-summary-aa-1826-1.md`): 네이티브 전역 fetch 는 **이미 `expo/fetch`**(`runtime.native.ts:41-52`, `EXPO_PUBLIC_USE_RN_FETCH` 미설정)라 기기에서는 reader 루프를 탄다 — 게이트가 말한 `res.text()` 버퍼링은 jest · 옵트아웃 빌드에서만. 그러나 결함은 다른 모양으로 실재한다: `expo/fetch` 네이티브는 헤더 수신 ~ 첫 read 사이를 상한 없는 네이티브 싱크에 쌓고 백프레셔가 없으며, 읽기 전 `cancel()` 은 no-op · iOS `reader.cancel()` 은 전송을 안 멈추고 · 비-OK 반환은 abort 없이 본문을 다 받는다 → **전송을 실제로 끊는 유일한 수단은 `controller.abort()`**, JS 만으로는 바이트 하드 상한 불가. 판단 **A**: 모든 조기 종료(비-OK · 선언 길이 초과 · 누적 초과 · 스트림 없음)에서 abort · 무한 `text()` 폴백 제거(스트림 없으면 읽지 않고 기존 fail-open 정책) · 가짜 계수 스트림 + abort 단언 테스트 · 핀 줄 · 반환값 · 웹 동작 불변. **AA-1826-1 은 완화이지 종결이 아니다** — 남는 창(헤더 ~ 첫 read, 5초 × 처리량)의 하드 상한은 앱 소유 프록시(엣지 배포) 또는 네이티브 코드가 필요 → Simon 결정 후속(조사 항목: `Accept-Encoding: identity` 로 OkHttp 투명 해제를 끄는지). AA-1826-2(low)는 끝(빨강 5 → 초록 31/31 · 변이 7/7). 재게이트가 완화 후 잔여 심각도를 다시 매긴다 — medium 이 남으면 Simon 규칙대로 머지 전에 Simon 에게 올린다.
26.09.19 22:38 · **[r19 · r20 수확 → #1814 3차 재게이트 · #1837 게이트 결과 → r21]** r19-fix1814b(`ctx_2beeae8f85f8` · `claude-opus-5` ultracode): 커밋 `0a7616c0`(a3ee7851 위 · 12파일 +911/−100) — 2차 발견 8건(GA-1814-1~3 · GZ-1814-1~5) 닫음: 자동 저장 정확 중복은 계정 줄에서 대기 삭제를 먼저 끝낸 뒤 새로 담음 · 손 담기는 원문을 되살린 뒤에만 담김 · 메모리에만 쓴 기록은 기기 기록으로 치지 않음 · 손 담기는 누른 순간의 계정 임대 · 줄의 일 하나에 30초 상한 · 안내 문구를 실제 조건과 배송 화면 이름(위키/Wiki)으로 5개 로케일 · 빨강 17 → 초록 · 변이 18종 · verify rc=0(759 스위트 · 9,287). 남긴 것: 행 없이 원문만 남는 undo_unrecorded 는 앱 안 삭제 경로가 없어 서버 S3 몫. → **3차 재게이트 두 레인 발주**(regate-1814-*-r3 · `ctx_9dddb9a654c1` · `ctx_34038e541bb4`). r20-fix1826 → draft **PR #1837**(`fc17971d`) 게이트: 생성물 PASS WITH CONCERNS(HA-1837-1 medium 네이티브 선버퍼 하드 상한 없음 · HA-1837-2 low 시간 제한 경합) · 인가 PASS WITH CONCERNS(**HZ-1837-1 medium** 5초 abort 가 첫 스트리밍보다 먼저면 HIBP Promise 가 정착하지 않아 가입 · 비밀번호 변경이 대기 · HZ-1837-2 low 선버퍼 — 완화 뒤 Low 로 하향) · AA-1826-2 닫힘. 두 레인이 같은 두 건을 서로 다르게 매겼다(선버퍼 M/L · 경합 L/M). Simon 규칙대로 **고칠 수 있는 medium(경합)은 머지 전에** → r21-fix1837(`ctx_7999e702c402`): 타이머가 JS 의 HIBP Promise 도 반드시 정착 · 결정적 회귀 테스트. **선버퍼 하드 상한은 앱 소유 프록시(엣지 배포) 또는 네이티브 코드가 필요한 설계 결정 → Simon 결정 항목**(한 레인이 medium 으로 봄). #1837 은 main 에 이미 있는 위험을 줄이기만 한다(새로 만들지 않음).
26.09.19 22:45 · **[/vibe (Simon 22:4x) "codex · grok usage 가 살아났다. 점검하고 할 수 있는 것을 하자" — 점검 결과와 발주]** 쿼터(orca · 22:41): claude 주간 7% · 세션 26% · codex 주간 5%(다음 리셋 토 19:54) · gemini/antigravity 0% · **grok = status error "Grok sign-in expired — run grok on the computer running Orca; sign in if prompted"**. **G12 실호출**(`adversarial_eval.py --preflight`): claude ✅ · codex ✅ · gemini ✅(CLI 직행 · Orca 워커 경로는 D-28 대로 unavailable) · **grok ❌ 402(잔액 소진)** → grok 레인은 아직 못 쓴다(쿼터 %가 아니라 로그인 · 잔액 문제 — G12 가 잡은 그대로). Simon 몫: 이 PC 터미널에서 `grok` 을 한 번 실행해 로그인 · xAI 콘솔에서 잔액 확인. 발주(run `run_e3a3e38558ab`): **r22-research**(`ctx_686474bd174c` · 웹 리서치 수집 C-realtime → 1순위 grok 불가 → 2순위 `gpt-5.6-sol` @ultra · 반증 YES) — Q1 Paddle 웹훅 요청의 실제 `Content-Type`(#1829 파서 415 위험) · Q2 Supabase 사건 6q5902p2xd9f · PostgREST v14.18 서울 적용 현황(D3) 1차 근거 · 읽기 전용. **r23-fix-recovery-lows**(`ctx_3b0ddd4f52fb` · `claude-opus-5` 표준) — #1835 low EA-1835-1(울타리 영구 점유) · EA-1835-2(로케일 긍정 계약 테스트) · 폐기 시간 상한(기존 실패 안내로) → main 기준 새 draft PR → 두 레인 게이트. 탐색 슬롯 없음(리서치가 이미 2순위 레인). 진행 중: #1814 3차 재게이트 두 레인 · r21(#1837 시간 제한 경합).
26.09.19 23:05 · **[r22 리서치 수확 — 두 결정의 1차 근거]** 워커 r22-research(`ctx_686474bd174c` · `gpt-5.6-sol` @ultra · 보고서 `E:/Coding Infra/reports/vibe-r260919/r22-research/result.md` 16 KB). ⚠ 워커가 띄운 하위 작업자가 Q2 요약으로 먼저 worker_done 을 보내 task 가 completed 가 됐고, 본 워커의 최종 worker_done 은 "capability is revoked" 로 거부됐다(보고서는 정상 기록 — 원장 · 수확은 보고서 기준). 워커가 정책 때문에 못 지운 임시 SDK 복제(`%TEMP%/r22-paddle-…`, 26 MB · 공개 저장소 6개 · 정션 0)는 코디네이터가 지웠다. **Q1 Paddle 웹훅 Content-Type**: 공식 문서 · 시뮬레이터 모델 · 공식 SDK 4종(node · python · go · php) · OpenAPI · 알림 로그 어디에도 발신 `Content-Type` 줄이나 매개변수 계약이 없다 → **정확한 값은 1차 자료로 불명(확신 높음)** · 공개 근거는 모두 `application/json` 쪽이고 제3자 수신 통합(Hookbase)은 정확히 `application/json` 이라 함 → #1829 파서의 415 잔여 위험은 **낮아 보임**(삼분 판정은 불명 · 확신 중간~높음). 완전히 닫으려면 실제 또는 시뮬레이터 전달 1건의 원시 헤더 = Simon(Paddle 로그인). **Q2 D3**: 사건 `6q5902p2xd9f` 는 여전히 identified · PostgREST v14.18 수정본과 Supabase 배포 아티팩트는 09-18 공개 · 최신 공지는 **프로젝트별 대시보드 업그레이드**를 요구 · 서울 자동 적용 문장 없음 → 우리 프로젝트가 수정본을 이미 받았을 가능성 **낮음(확신 중간 · 약 75%)**. Simon 몫: 대시보드에서 PostgREST 버전 확인 → 14.18 미만이면 **대시보드 업그레이드**(D3 ① 의 '지원 요청' 전에 해볼 수 있는 조치 — 운영 쓰기라 Simon 승인 필요).
26.09.19 23:18 · **[r21 수확 — #1837 시간 제한 경합 수정 → 2차 재게이트]** r21-fix1837(`ctx_7999e702c402` · `claude-opus-5`): 커밋 `a1b9668c`(fc17971d 위 · 제품 1 + 테스트 1) — `isPasswordBreached` 가 실제 조회를 기존 `withTimeout` 으로 감싸 5초에 abort 하고 false(fail-open)로 반드시 정착 · abort 뒤 새 read 시작 안 함 · 핀 줄 · 반환값 · 웹 불변. 수정 전 새 테스트 2건이 5초 · 60초 뒤에도 영구 pending(빨강) · 대조군 초록 → 수정 후 16/16 · 변이 4/4 · verify rc=0(759 · 9,496) · CI pass. 남은 것: 선버퍼 하드 상한(Simon 결정 대기) · 실기기 재현. → 2차 재게이트 두 레인(regate-1837-*-r2 · `ctx_04a70e1b5b08` · `ctx_699094b6c8e2`).
26.09.19 23:22 · **[r23 수확 — #1835 후속 draft PR #1838 → 게이트 두 레인]** r23-fix-recovery-lows(`ctx_3b0ddd4f52fb` · `claude-opus-5` 표준): 커밋 `08159c7f`(main `e60081f7` 위 · 제품 2 + 테스트 2 · 문구 · 문턱 · 삭제 범위 · AuthContext 변경 0) — EA-1835-1 상한을 넘긴 카운터 호출이 울타리를 영구 점유하던 것을 상한에서 놓고 늦은 답이 최신 의도와 다르면 최신 상태 재기록 · EA-1835-2 5개 로케일 × 개념 7개 긍정 핵심어 표 · 동의 뒤 폐기 시간 상한 `ENCRYPTED_STORAGE_RECOVERY_TIMEOUT_MS`=15초 → 기존 `auth:storageRecovery.failed` · 상한 뒤 후속 단계 금지. 원 하네스 재현(removeCalls 0 · 두 번째 count escalate) → 수정 뒤 clear 즉시 · 두 번째 count locked · 빨강 4/83 → 초록 87/87 · 변이 12종 · verify rc=0(759 · 9,495) · CI pass. 남긴 것: 실기기 · AsyncStorage FIFO 여부 · 15초 근거 · 호출 순서를 바꾸는 저장소에서만 생기는 잔여 2경우. → 게이트 두 레인(regate-1838-* · `ctx_14878b71523b` · `ctx_f429aacc70c2`).
26.09.19 23:30 · **[#1837 머지 — #1826 후속 · 가입/비밀번호 변경 무한 대기 해소]** 2차 재게이트: 생성물 PASS WITH CONCERNS(C0/H0/**M1**/L0 — HZ-1837-1 닫힘 · 신규 결함 0 · 유일한 medium 은 **원래 있던** 네이티브 선버퍼 하드 상한 = Simon 결정 대기라 "이번 수정의 머지 차단 근거 밖"으로 명시) · 인가 PASS WITH CONCERNS(C0/H0/M0/L1 — HZ-1837-1 독립 확인 · 선버퍼는 Low). **코디네이터 판단: 머지.** Simon 규칙("medium 은 머지 전에")의 취지는 알려진 medium 을 새로 싣지 않는 것인데, #1837 은 main 에 이미 있는 medium 두 건 중 하나(HZ-1837-1 시간 제한 경합 → 가입 · 비밀번호 변경 무한 대기)를 없애고 다른 하나(선버퍼)를 줄이기만 한다 — 붙잡으면 main 이 더 나쁜 상태로 남는다. → `gh pr ready` · auto-merge → **MERGED `ffd5cf1d`(23:30)**. 남은 선버퍼 하드 상한(앱 소유 프록시 · 네이티브 코드 · 그대로 두기)은 Simon 결정 항목으로 올린다. 뒤집는 조건: Simon 이 "잔여 medium 이 있으면 어떤 경우에도 머지 전 확인"이라고 하면 이후부터 그렇게 한다.
26.09.19 23:36 · **[#1838 게이트 → r25 수정]** 생성물 PASS WITH CONCERNS(**IA-1838-1 medium**: 시간 초과한 폐기 뒤 재시도가 실제 `maintenanceTail` 직렬화 뒤에 막히고 만료된 폐기를 큐에 한 번 더 넣음 · 재시도 테스트는 즉시 실행을 가정해 못 잡음) · 인가 PASS WITH CONCERNS(IZ-1838-1 low 같은 뿌리) · 기존 EA-1835-1/-2 는 두 레인 모두 닫힘 확인. Simon 규칙대로 머지 전에 → r25-fix1838(`claude-opus-5` ultracode): 재시도가 영구히 막히지 않고 중복 폐기가 쌓이지 않게 + **실제 큐를 쓰는 회귀 테스트** → 두 레인 재게이트.
26.09.19 23:43 · **[#1814 4차 재게이트 → r26]** 생성물 PASS WITH CONCERNS(medium 0 · **G3A-1814-1 low** 실행 중 drain 과 겹친 초점 복귀가 후속 재시도를 버림 · 3차 발견 셋 재현으로 닫힘) · 인가 PASS WITH CONCERNS(**G3Z-1814-1 medium** 자료 상세의 수동 삭제가 복구 업로드 보호를 우회해 삭제 완료 뒤 원문만 되살림 · 3차 지정 경로는 닫힘). G2Z-1814-2(서버 S3)는 두 레인 모두 '잔여 · Simon 결정 대기'로만 표시. Simon 규칙대로 → r26-fix1814d(`ctx_129f78cdeabe` · `claude-opus-5` ultracode): 사용자가 직접 지우는 모든 경로를 같은 조정 경계(계정 줄 · restoringNow)로 · 겹친 트리거는 후속 1회 예약 → 5차 재게이트. 라운드가 거듭될수록 발견이 좁아지고 있다(1차 high 2 → 2차 medium 4 → 3차 medium 2 → 4차 medium 1).
26.09.20 00:09 · **[r26 질문 답 · 별건 발견]** r26(#1814 · G3Z-1814-1): 사용자 삭제 경로 전수 조사 결과 복구와 겹쳐 '행 없는 원문'을 새로 만드는 길은 기록 상세 한 건 삭제(`deleteCapturedSource`) 하나 → 계정 줄 · `restoringNow` 에 태움(화면 코드 0 · 새 문구 0 · 되살리기 중이면 기존 `deleteSourceFailed` 로 답하고 업로드 뒤 마저 삭제). 일괄 삭제 2종(`deleteUningestedSources` · `deleteAllUserData`)은 줄에 태우지 않고 근거만 기록(제품 파일 4 유지) — 코디네이터 승인. ⚠ **별건(후속 조사 필요)**: 워커 보고로는 설정의 일괄 삭제 두 종이 **원래 raw-clippings 본문(원문)을 지우지 않는다** — 사용자가 '전체 삭제'를 눌러도 원문이 저장소에 남을 수 있는 개인정보 쪽 잠재 결함(이 PR 밖 · 기존 동작). 서버 캐스케이드 · 저장소 수명 규칙 · 계정 삭제(0188)가 대신 지우는지 코드 확인 범위를 result.md 에 적게 했다 — 고치지는 않음. 결과를 보고 별도 PR 로 연다.
26.09.20 00:37 · **[r25 · r26 수확 → 재게이트 + 별건 두 개 발주]** r25-fix1838(`ctx_8686ab7a5a2f`): 커밋 `66023b77` — 어댑터가 폐기를 두 큐 맨 앞에서 한 번 `stillAwaited` 로 확인해 이미 실패로 답한 시도의 폐기는 시작하지 않음 · 재시도는 자기 폐기 요청 · 실제 `maintenanceTail` 회귀 5건 포함 6 failed/51 → 85/85 · 변이 6/6 · verify rc=0(759 · 9,502) · CI 초록. 워커가 1차 합치기 설계(`609c6e3c`)를 철회한 이유가 **별건 A**: 동의를 가로지른 옛 Supabase 클라이언트의 refresh 저장이 폐기 · `resetSupabaseClient` 뒤에 착지해 새 클라이언트가 그 세션을 게시할 수 있다 — **복구 세션 승격 가능성**(이 PR 이전부터 · client.ts · session-mutation.ts · 코드 읽기 판정 possible · 재현 없음). r26-fix1814d(`ctx_129f78cdeabe`): 커밋 `e020313d` — 기록 상세 `deleteCapturedSource` 를 계정 줄 · restoringNow · deletingNow 로 · 겹친 비우기 부름은 합쳐 한 번 더 · 테스트 +10 · 변이 15종 · verify rc=0(759 · 9,307) · CI 통과. **별건 B(개인정보)**: 설정 일괄 삭제 · 한 건 삭제 · 보존 크론 0056 이 `raw-clippings` 원문을 남기고(서버 트리거 · 수명 규칙 · 정리 크론 0) 계정 삭제만 지우며, 데이터 내보내기(`export-account`)는 그 폴더를 내보낸다 → '전체 삭제' 뒤에도 원문이 남고 내보내기에 나온다(추정) · 안내 문구도 원문 잔존을 말하지 않음. 발주: 재게이트 #1838 2차(`ctx_8c4a0707c163` · `ctx_624ecec632fe`) · #1814 5차(`ctx_ddb337f2aa64` · `ctx_9c4c1832caa1`) · **r27-verify-refresh**(`ctx_da7332aa6cf4` · A-verify `gpt-6-astra` @xhigh · 읽기 전용 + 임시 하네스 — 별건 A 실재 · 영향 · 심각도 판정) · **r28-fix-raw-clippings**(`ctx_34ef49558882` · `claude-opus-5` ultracode · 새 워크트리 `fix-raw-clippings-erase-260920` = main `ffd5cf1d` — 별건 B 의 클라이언트 몫: 한 건 · 일괄 삭제가 원문 객체도 지움 · 실패 순서 · 문구 사실화 · **draft 까지, 머지는 게이트 뒤 Simon 확인** · 이미 남은 원문 정리(운영 쓰기) · 서버 S3 · 보존 크론은 범위 밖). ⚠ 코디네이터 실수(경미): 공용 읽기 전용 워크트리 `gate-review-260919` 를 게이트 4명이 읽는 중에 main 최신으로 옮겼다(e60081f7 → ffd5cf1d · 차이는 #1837 의 HIBP 파일뿐이라 진행 중 게이트 대상과 무관) — 다음부터는 게이트가 도는 동안 그 워크트리를 건드리지 않는다.
26.09.20 00:47 · **[#1838 머지 · r28 설계 답]** #1838(#1835 후속) 2차 재게이트 두 레인 **PASS/PASS(발견 0)** — IA-1838-1 · IZ-1838-1 닫힘 · 합치기 설계 철회 판단 확인 · 옛 refresh 경로를 넓히지 않음 → **MERGED `2d688ef0`(00:47)**. r28(원문 삭제) 설계 질문 답: Q1 `deleteSourcesByIds`(가져오기 철회 · 이력 삭제)도 같은 결함이라 포함 · Q2 위키 페이지가 가리키는 ingested=false 행(CHECK `wiki_pages_source_kind_pair` 로 행 삭제가 막힘)은 원문 · 행 모두 건드리지 않고 나머지만 지운 뒤 지운/남긴 개수를 돌려줌 — **조건: 사용자에게 거짓 '다 지웠다'를 보이지 않는다**(기존 부분 실패 문구, 없으면 최소 새 문구 5개 로케일 허용) · Q3 이번 PR 은 원문 범위만. **별건 C(개인정보)**: '전체 삭제' 뒤에도 personas · memorized_patterns · xp_events 가 남는다(`delete-bulk.ts:185-189` · 클라이언트 삭제 RLS 정책 없음)는데 안내는 "개인 콘텐츠를 모두 지워요" — 서버 삭제 정책 추가 vs 문구 사실화는 Simon 결정. #1814 5차 재게이트: 생성물 PASS(발견 0) · 인가 대기.
26.09.20 00:54 · **[#1814 5차 재게이트 → r29]** 생성물 **PASS(발견 0)** — 4차 두 건 닫힘 · 조정 훅 미설치 · 이중 등록 · 합치기 굶주림에서 새 결함 0. 인가 PASS WITH CONCERNS — **G4Z-1814-1(M, r26 의 회귀)** 상세 삭제의 30초 상한이 실제 삭제 완료 전에 승격 잠금을 풀어 원문만 되살릴 수 있음 · G4Z-1814-2(L) 일반 손 담기 중복 재시도가 삭제 중 표식보다 먼저 성공 반환(둘 다 실제 제품 함수로 재현). → r29-fix1814e(`ctx_22bf304b6ed2` · `claude-opus-5` ultracode): 상한은 줄만 풀고 잠금 · 확정은 실제 삭제 뒤(복구 업로드 추적과 대칭) · 중복 재시도도 deletingNow 먼저 · **같은 계열 조합 전수 표**를 요구. 코디네이터 관찰: #1814 는 오늘 밤 수정 5회 · 재게이트 5회(high 2 → M4 → M2 → M1 → M1) — 수렴하지만 매 수정이 새 경계 조합을 만든다. 클라이언트 쪽 다중 경로 조정 자체가 취약할 수 있어, 서버 트랜잭션(S3 계열)로 삭제 · 복구를 한 곳에 모으는 설계를 Simon 결정 항목(G2Z-1814-2 와 함께)으로 올린다.
26.09.20 01:23 · **[r28 수확 → #1839 게이트 · r27 레인 교체]** r28-fix-raw-clippings(`ctx_34ef49558882`): draft PR **#1839** 커밋 `f66ae064`(origin/main `ffd5cf1d` 위 · 11파일 +823/−49) — 새 경계 `src/lib/wiki/source-erasure.ts`: 골라 지우기(정리 안 한 캡처 · 가져오기 철회 · 옛 한 건 삭제)는 원문 먼저 · 행 나중(실패하면 행이 재시도 포인터), 위키 페이지가 가리키는 행은 원문 · 행 모두 남기고 kept 로 알림(새 문구 `capturesKeptWithWiki` 5개 로케일), 전체 삭제는 행 먼저 · `raw-clippings/<uid>/` 폴더 쓸기 · verify rc=0(760 스위트 · 9,521 테스트). 게이트 두 레인 디스패치(artifact `ctx_84b565d62d14` · bizlogic `ctx_56fb1ec6740f`) — 머지는 게이트 통과 + Simon 확인 뒤(사용자 원문 삭제 동작 변경 · 되돌릴 수 없음). r27(옛 클라이언트 refresh → 복구 세션 승격 검증)은 `gpt-6-astra` 가 codex 사이버 출력 필터에 막혀(조용한 실패 · 2,350초) 코디네이터가 종료 → G13 `revalidate_for_retry` OK → 2순위 `claude-fable-5-1` @max 로 재배정(`ctx_8b133781f03e` · 기준 origin/main `2d688ef0`). 뒤집는 조건: fable 도 막히면 3순위 opus.
26.09.20 01:27 · **[운영 실측 — 이미 남은 원문은 0건 · QA 빌드 main 으로 교체]** 운영 `2nd-brain`(zoacryukmdeivmolvyhj) 읽기 전용 집계(SELECT 만 · 01:3x): Storage 버킷은 `raw-clippings` 하나(비공개) · 객체 **3개 · 소유 폴더 2 · 240바이트** · 최신 2026-08-09 · `sources.storage_path` 와 짝 없는 **고아 원문 0** · 계정 없는 폴더 0 · `sources` 3행(2명) 전부 원문과 짝 · `_storage_pending` 0 · 최신 캡처 2026-07-18. → #1839 결함은 코드상 실재하지만 운영 노출은 0이다(미출시 · 테스트 계정뿐). Simon 결정 대기였던 '이미 남은 원문의 운영 정리(운영 쓰기)'는 대상이 없어 **폐기 제안**(결정 아님 — Simon 확인 전까지 결정 시트에 '폐기 제안'으로 남긴다). QA: 웹 QA 서버(127.0.0.1:8765/2nd-B/)를 옛 통합 브랜치 `640db5bd`(main 보다 16 커밋 뒤) 빌드에서 **main `2d688ef0`** 빌드로 교체(새 워크트리 `qa-integration-260920` · upstream 없음 · 정션 reparse=True · export rc=0) · arm64 APK `main-2d688ef0-arm64`(push 실행 35452876393) 수령 · 에뮬레이터용 x86_64 빌드 디스패치(35454934771). 뒤집는 조건: #1814 를 QA 에 넣어야 하면 그 PR 이 게이트를 통과한 헤드를 QA 브랜치에 병합해 다시 빌드.
26.09.20 01:34 · **[#1839 1차 게이트 두 레인 BLOCK → r30]** 생성물(`ctx_84b565d62d14` · 발견 H1 M2): **JA-1839-1(H)** 전체 삭제가 빈 폴더를 확인한 뒤 받은편지함 `promotePendingUploads()` 가 예전 본문을 다시 올림(0행 UPDATE 를 성공 처리 `queries.ts:186-192`) · **JA-1839-2(M)** 선택 삭제의 위키 참조 확인과 원문 삭제 사이에 위키 생성이 끼면 보존할 원문만 지워짐 · **JA-1839-3(M)** 파괴 작업이 확인 당시 계정에 안 묶여 RLS 빈 결과를 성공으로 읽음. 인가(`ctx_56fb1ec6740f` · H1 M1): **JZ-1839-1(H)** `SAFE_NAME{1,255}` 때문에 정상 담기로 생기는 256자 이름이 선택 삭제에서 원문만 빠지고 행은 지워져 거짓 성공 · 전체 삭제는 매번 던져 뒤 단계까지 막힘(메모리 재현) · **JZ-1839-2(M)** = JA-1839-3 같은 뿌리. 두 레인 모두 타인 원문 삭제 경로는 반증(0). Simon 규칙(medium+ 머지 전 수정)대로 → r30-fix1839(`ctx_f4c6a6ff4a9b` · `claude-opus-5` ultracode): 이름 계약을 저장 계약(키 UTF-8 바이트)에 맞춤 · 계정 삭제가 쓰는 `AuthSessionExpectation`/`runMutation` 재사용 · 선택 삭제/위키 생성/승격 사이 조건부 UPDATE 선점(선례 `_storage_pending`) · 승격 0행이면 방금 올린 객체 보상 삭제. **코디네이터 사전 결정: 이 PR 에 마이그레이션 · 엣지 · RLS 변경을 넣지 않는다** — 서버 울타리(삭제 세대 · outbox)는 보안 담당 서버 S3 트랙이고 #1814 G2Z-1814-2 와 같은 Simon 결정 항목이다. 남는 창(보상 삭제 전 앱 종료 · 다른 기기)은 PR 본문에 명시. 뒤집는 조건: 워커가 스키마 없이 JA-1839-2 를 닫을 수 없다고 판단하면 question → Simon 결정으로 올린다.
26.09.20 01:50 · **[r27 수확(fable) — 복구 세션 승격 재현 · Low → r31 수정 발주 · 워크트리 7개 정리]** r27-verify-refresh(`ctx_8b133781f03e` · `claude-fable-5-1` @max · astra 가 사이버 필터에 막혀 옮긴 2순위): main `2d688ef0` + auth-js 2.106.1 실제 모듈 하네스로 **재현** — 동의 전에 세션을 읽고 동의 뒤 저장하는 옛 클라이언트 refresh 가 폐기 · `resetSupabaseClient` 뒤 착지 → 새 클라이언트가 복구 세션을 **잠금 없이 일반 세션으로 게시**(다음 콜드 스타트에도 유지 · A2: 네트워크 불안정이면 동의 24초 뒤 착지). 대조: 로그아웃 뒤 되살아남 0 · 다른 계정 섞임 0(같은 S 잠금 직렬화) · #1838 은 막지 않음 · 입구는 #1835 fail-closed 승격 1곳(`AuthContext.tsx:566-574`). **심각도 Low**(복구 울타리가 UI 전용 · 서버에 `amr` 검사 0 · 보유자는 어차피 비밀번호 변경 가능). 워커 임시 사본 울타리 실험(+19/−2)이 A · A2 를 닫고 대조 불변. 코디네이터 판단: Low 라 규칙상 필수는 아니지만 #1835 불변식 파손이고 비용이 작아 닫는다 → r31-fix-recovery-fence(`ctx_f95918192465` · `claude-opus-5` ultracode · 새 워크트리 `fix-recovery-fence-260920` main 기준): 폐기를 큐에 넣는 같은 동기 블록에서 옛 runtime `gatedStorage` 은퇴 · 실패 시 회전 토큰 소실은 수용(이미 폐기 동의). 정리: 머지 끝난 워크트리 7개 제거(브랜치 유지 · 매 제거마다 공용 node_modules 745 확인 · QA 옛 워크트리는 정션 아닌 5.3 GB 실사본 · 안쪽 reparse 0 확인 뒤 제거) · E: 56 → 64 GB. ⚠ 코디네이터 실수: 3개를 한 번에 병렬로 제거(원칙은 하나씩) — 피해 0 · 이후 순차. 뒤집는 조건: r31 게이트가 울타리의 부작용(정상 refresh 유실)을 medium 이상으로 보면 설계 재검토.
26.09.20 01:52 · **[r29 범위 질문 2 답 — A]** r29-fix1814e(`ctx_22bf304b6ed2`) 중간 보고: 커밋 `1132cb84`(G4Z-1814-1 · G4Z-1814-2 + 승격 행 표식) push · CI 3 pass · verify 0. 같은 계열 조합 조사(읽기 전용 3회)에서 **이번 회귀가 아닌 기존 잔여**가 더 나옴: 실행기만으로 닫히는 것 — M5(손 담기가 기기 기록 null 을 지울 차례로 읽어, 늦은 복구 뒤 철회 안 한 자동 저장 행을 지움 = **데이터 손실** · 저장소 막힌 웹) · M14(표식 분기에서 undo_unrecorded 안내가 안 거둬짐) · 늦은 표식(상한 뒤 keptRows) / 설계 선택 — M2(손 담기 상한이 턴 잠금을 풀어 같은 답 자동 저장 재시도와 23505 경합 → 원문 고아) · M3(capture 가 INSERT 실패 때 올린 원문을 안 지움) · raw_removed 뒤 같은 내용 담기는 원문 없이 · **가져오기 이력이 정확 중복 행 id 를 적어 철회가 다른 행을 지움**. 코디네이터 답(01:5x): **A** — 실행기 3건은 두 번째 커밋으로 닫음(결정적 테스트 · 변이 · 1~5차 대조 불변) · M2 는 설계 선택이라 결정 1번(삭제 · 복구 조정)과 같은 줄로 Simon 에게 · M3 · raw_removed · 가져오기 중복 id 는 #1814 범위 밖 기존 결함 → result.md 별도 절(재현 · file:line · 영향 · #1839 가 원문까지 지우게 되는지) → 코디네이터가 별도 PR 로. 일괄 삭제는 #1839 와 겹쳐 손대지 않음. 뒤집는 조건: 가져오기 중복 id 철회가 #1839 머지 전에 닫혀야 한다고 판단되면(원문까지 지우게 되는 경우) #1839 머지 순서를 그 수정 뒤로.
26.09.20 01:57 · **[코디네이터 실수 — STATE 가 '남긴 것'으로 적은 QA 워크트리를 대조 없이 제거 · 피해 범위 확인]** 01:4x 워크트리 정리에서 `qa-integration-260918`(브랜치 `qa/integration-260919` · `640db5bd`)을 지웠는데, STATE.md(09-19 12:25 판) '7. 주인이 따로 있어 남긴 것'에 "공개 QA 릴리스 태그의 출처 · 로컬 빌드용 실제 node_modules"로 남겨 둔 항목이었다 — 정리 전 STATE 대조를 안 했다. 피해 확인: 브랜치(로컬 · origin) · 커밋 · 릴리스 태그 `qa-260919-640db5bd` 그대로 · **QA 서명 키는 워크트리 밖**(`E:/Coding Infra/tools/qa-test.keystore`, `qa_apk_build.py:109` 가 도구 폴더 기준) → 앞으로 만드는 QA APK 도 기존 QA 앱 위에 덮어 설치 가능. 잃은 것은 다시 만들 수 있는 빌드 환경뿐(실제 node_modules 5.3 GB · gradle 산출물 · 옛 dist). 웹 QA 는 이미 `qa-integration-260920`(main)로 옮긴 뒤였다. ⚠ 재발 방지 두 가지: ① 워크트리 정리 전에 STATE · HANDOFF 의 '남긴 것'을 대조한다 ② `qa_apk_build.py --reinstall` 을 **정션 워크트리에 돌리지 않는다** — `npm ci` 가 node_modules 를 비우면서 정션을 따라가 공용 설치를 지울 수 있다(도구는 node_modules 가 없을 때만 npm ci 를 돈다 · `:66-67`). 폰용 QA APK 를 main 으로 다시 만들 때는 새 워크트리의 정션을 먼저 안전하게 떼고(비재귀 Delete · 없어졌는지 확인) 실제 설치로 빌드한다.
26.09.20 02:13 · **[r29 수확 → #1814 6차 재게이트 · 가져오기 허브 중복 id 철회 → r32]** r29-fix1814e(`ctx_22bf304b6ed2`): 커밋 두 개(force 없음) — `1132cb84` G4Z-1814-1(M) 줄의 30초 상한은 줄만 넘기고 상세 삭제의 잠금 · 답은 실제 삭제 뒤 · `delete-captured-source.ts` 행 표식(지우는 중 · 지운 행 · 되살리는 중)으로 승격과 삭제가 양방향 비껴감 · G4Z-1814-2(L) 손 담기 중복은 지우는 중 · 지운 행을 먼저 거절 / `3cc4cc3f` 조사에서 나온 기존 잔여 M5(**데이터 손실** — 저장소 막힌 웹에서 늦은 재판정이 철회 안 한 자동 저장 행을 지움) · M14 · 늦은 표식. 새 테스트 18 빨강 → 초록 · 변이 17종(동치 1) · verify 0(759 · 9,325) · CI 3 pass · draft 유지. 남긴 것: M2(손 담기 상한 × 자동 저장 재시도 → 원문 고아 · 제안 2안 → Simon 결정 1번 줄) · M3 · G2Z-1814-2 · 일괄 삭제(#1839). → **6차 재게이트 두 레인**(regate-1814-*-r6 · `ctx_0165ca22af2b` · `ctx_bcbafd0ee4cd` · 5차 두 건 닫힘 확인 + 행 표식 누수 · 예외 경로 해제 · `removed` 증가 · M5/M14 새 조합). r29 §3-6 **가져오기 허브 이력이 정확 중복 행 id 를 적어 철회가 앞 가져오기의 행을 지움**(같은 계정 · 파일 가져오기 `dds-import-inbox-screens.tsx:294-297` 에는 있는 규율이 허브에 없음 · #1839 가 붙으면 원문까지 되돌릴 수 없게 지워짐) → **#1839 보다 먼저 닫는다**: r32-fix-import-hub-dedup(`ctx_833db12fb6ec` · `claude-opus-5` 표준 · 새 워크트리 `fix-import-hub-dedup-260920` main 기준): 비준에서 정확 중복 id 는 이력에 안 적고 사실대로 안내 · 철회는 다른 이력이 가리키는 id 를 지우지 않음(이미 적힌 이력 보호) · 판정은 순수 함수. 뒤집는 조건: r32 게이트가 보호 규칙의 부작용(정당한 철회가 남는 것)을 medium 이상으로 보면 재설계.
26.09.20 02:18 · **[r32 범위 답 — (b) 두 화면 모두]** r32(`ctx_833db12fb6ec`) 질문: 철회 이력 저장소 `import.history:<uid>` 를 가져오기 허브와 파일 가져오기(`dds-import-inbox-screens.tsx`)가 함께 쓰고 둘 다 이력 전체에 철회 버튼을 단다(`:250-251` · 허브 `:657-666` · /import `:607-623`) — 허브만 고치면 이미 적힌 옛 허브 이력 E2=[R] 을 /import 에서 철회할 때 `revokeImport`(`:329-354`)가 R 을 똑같이 지운다(#1839 뒤엔 원문까지). 코디네이터 답(02:18): **(b)** — 제품 상한 3(허브 · 새 순수 함수 · /import 몇 줄) · 두 화면이 같은 순수 함수 하나로 '남은 다른 이력이 가리키는 id 는 지우지 않음' 판정 · 새 문구 · 로케일 0(남긴 행은 같은 목록의 다른 이력이 계속 가리켜 목록이 사실을 보여 줌) · /import 쪽 재현 테스트 1건 · 파일 가져오기의 기존 규율 불변. 이유: #1839 전에 되돌릴 수 없는 삭제 문 둘을 다 닫는다.
26.09.20 02:36 · **[r30 수확 → #1839 2차 재게이트 · #1814 6차 판정 → r33]** r30-fix1839(`ctx_f4c6a6ff4a9b`): 커밋 `c7bd381a`(force 없음 · draft 유지 · 제품 6 + 테스트 5 · **마이그레이션 · 엣지 · RLS 0**) — 이름은 전체 키 UTF-8 1,024바이트 계약 · 본인 경로인데 계약 밖이면 행을 남기고 명시적 실패 · 전체 삭제는 원문 폴더 실패에도 끝까지 간 뒤 불완료 / 파괴 작업 4종 `runMutation` + `assertExpectedSessionInsideMutation` 시작 세션 고정 / frontmatter 선점 `_erasing` · `_generating` 조건부 UPDATE(CAS) · phase1 · 승격 쓰기도 표식 보존 CAS · 10분 만료 / 승격은 업로드 전 재확인 + 0행이면 방금 올린 객체 되돌림 · 재현 21 빨강 → 초록 · 변이 11 · verify 0(760 · 9,564) · CI pass. 남는 창(업로드 ↔ 되돌리기 사이 앱 종료 · 옛 빌드 · 만료 · 시계 차) = 서버 S3. ⚠ **#1814 head `3cc4cc3f` 와 `promote-pending.ts` 내용 충돌**(이 PR 이 지운 `updateSourceFrontmatter` 를 #1814 가 부름) — 나중 머지 쪽이 두 보호를 합친다. → **2차 재게이트 두 레인**(`ctx_eb95f8b385e7` · `ctx_793f649e4f17` · PostgREST JSON 경로 필터 · count 의미는 설치 소스 · 공식 문서로만 대조). #1814 6차: 생성물 **PASS(0)** · 인가 PASS WITH CONCERNS — **G5Z-1814-1(M, r29 회귀)** `removed` 표식이 소유자 임대 없이 확정돼 계정 전환 뒤 살아 있는 행의 복구 · 승격을 런타임 내내 막음(상한 유무 두 순서 재현) · G5Z-1814-2(L) 실행기 최초 로드 전 직접 삭제를 손 담기가 모름(expo-router 56.2.12 지연 평가로 조건 실재 확인). Simon 규칙대로 → r33-fix1814f(`ctx_25bc19b9d15e` · `claude-opus-5` ultracode): 같은 유효 소유자 임대로 부재 확인 때만 표식 · 파괴 요청 사이 임대 재확인 · 손 담기 성공 직전 공통 모듈의 '지우는 중' 확인 · #1839 와 합칠 계약 한 단락. 코디네이터 관찰: #1814 · #1839 가 같은 삭제 · 승격 경계를 서로 다른 장치(행 표식 vs frontmatter 선점 · 계정 임대 vs 세션 고정)로 풀고 있다 — 머지 순서와 통합 방식을 결정 1번(서버 조정)과 함께 Simon 에게 올린다. 뒤집는 조건: r33 뒤에도 같은 계열 medium 이 또 나오면 #1814 를 #1839 위로 올려 한 장치로 합치는 재설계를 제안.
26.09.20 02:40 · **[r31 수확 — 복구 울타리 draft PR #1840 → 첫 게이트 두 레인]** r31-fix-recovery-fence(`ctx_f95918192465` · `claude-opus-5` ultracode): 커밋 `cb994948`(main `2d688ef0` 위 · 5파일 +1032/−9 · 제품 3 중 `client.ts` 는 주석만) — `AuthStorageRuntime.retire()`(읽기 null · 쓰기/삭제 무시 · 잠금 유지) · `gatedStorage` 가 `await ready()` 뒤 어댑터 직전에 은퇴 검사 · `storage-recovery.ts` 가 폐기를 큐에 넣는 **같은 동기 턴**에 `retireStorage()` · `resetAuthStorageRuntime()` 도 은퇴. r27 하네스를 실제 모듈 + 실제 auth-js 2.106.1 jest 스위트로 이식 — main 에서 A · A2 · **새 A3**(reset 시점 울타리만으로는 첫 쓰기가 이미 큐에 있을 때 샘) 빨강 → 초록 · 대조 C0 · C1 · C2 · D · E 불변 · 변이 5 · verify 0(760 · 9,530) · CI pass. 워커 판단: 보완 `stopAutoRefresh()` 는 막는 것이 없어 넣지 않음 · 은퇴 런타임 `ready()` 의 멱등 이관 재실행은 세션 부활 경로가 아니라 범위 밖. → **#1840 첫 게이트 두 레인**(`regate-1840-*` · 틈 · 정상 흐름 차단 여부 · auth-js null 처리 독립 확인 · virtual mock 영향 · 법무 줄 인용). ⚠ astra 가 인증 검증에서 사이버 필터에 막힌 전례(r27)가 있어 진행이 멈추면 터미널을 읽고 fable 로 옮긴다.
26.09.20 02:47 · **[r32 핀 질문 답 — (a) 재고정 절차대로]** r32 verify 1건 빨강: `src/lib/inbox/__tests__/signals.test.ts:274-315` 가 `dds-import-inbox-screens.tsx` 전체의 EOL 정규화 sha256 을 박아 둠(기대 `dbf58244…` → `16e11c58…`) — (b) 로 /import 철회에 넣은 판정 한 줄 · import 한 줄 · 주석이 깬다. 핀이 스스로 적은 절차 "이후 재고정하는 사람은 여기에 자기 줄을 추가한다"(09-07 · 09-13 · 09-14 기록 있음)대로 코디네이터 답(02:47): **(a)** 테스트 상한 3 · 해시 교체 + 기록 한 줄만(그 파일 다른 단언 · 동작 테스트 변경 0) · 재고정 뒤 스위트와 전체 verify 0 직접 확인 · PR 본문 · result.md 에 이유와 이전/새 해시 앞 8자 · 살아남은 변이 1개(15 중)가 동치인지 구멍인지 적고 구멍이면 테스트 1건. 이유: 앞선 결정 (b)(두 화면 모두 닫기)를 되돌리지 않고, 핀이 정한 절차를 따른다.
26.09.20 02:53 · **[#1839 2차 재게이트 → 클라이언트 라운드 멈춤 · 결정 1번 근거 갱신(코디네이터 판단)]** 인가(`ctx_793f649e4f17`) **BLOCK H1 M1 L1**: J2Z-1839-1(H) 늦은 승격의 되돌리기 remove 오류를 삼키고 재시도 포인터를 잃어 '전체 삭제 완료' 뒤 원문이 남음(앱이 살아 있는 정상 흐름 — S3 잔여 창과 다름) · J2Z-1839-2(M) 두 번째 생성이 살아 있는 `_generating` 을 덮어쓰고 실패하며 첫 생성의 선점까지 풀어 보존할 원문이 지워짐 · J2Z-1839-3(L) JSON null 과 SQL NULL 비교 불일치로 일부 가져온 행이 계속 바쁨. 생성물(`ctx_eb95f8b385e7`) **PASS WITH CONCERNS M3**: J2A-1839-1 선점 표식만 비교하는 frontmatter 전체 UPDATE 가 phase1 · 승격 사이에서 서로의 키를 되살리거나 지움(lost update) · J2A-1839-2 되돌리기가 같은 내용을 새로 담은 업로드(행 INSERT 전)를 지움 · J2A-1839-3 외부 Markdown 이 내부 예약 키(`_erasing` · `_generating` · `_storage_pending` · `_body_fallback` · `__phase1__`)를 주입하면 선택 삭제 · 생성이 영구히 바쁨. 1차 다섯 건은 두 레인 모두 닫힘 확인(재현 109 · 134). 추이: 1차 H2 M3 → 2차 새 H1 M4 L1. **코디네이터 판단: #1839 의 클라이언트 전용 수정 라운드를 여기서 멈춘다** — 여러 클라이언트 경로(삭제 · 승격 · 위키 생성 · phase1)가 같은 행 · 원문을 frontmatter CAS 로 조율하는 구조라 고칠 때마다 새 틈(lost update · 예약 키 · 업로드 세대)이 생긴다. 서버 쪽 원자 처리(참조 확인 + 행 삭제를 한 SQL 로 · 키 단위 `jsonb_set` · 삭제 의도 outbox · 업로드 세대)가 이 계열 대부분을 뿌리에서 닫는다. #1839 는 어차피 Simon 확인 뒤 머지라 규칙('medium 은 머지 전')과도 충돌하지 않는다. → 보고서 결정 1번 추천을 '② 클라이언트 고아 청소'에서 **'① 서버 조정(S3)'** 으로 바꾸고 근거(두 PR 의 회차 추이)를 적는다. #1814 는 r33(6차 회귀 수정)만 마저 수확한다. 뒤집는 조건: Simon 이 "클라이언트로 계속"을 고르면 r34 로 여섯 건을 받는다(예약 키 제거 · 살아 있는 생성 선점 거부 · null 비교는 작고 독립적).
26.09.20 02:56 · **[#1840 게이트 두 레인 통과 → 머지 · KZ-1840-1(L)은 후속]** 생성물(`ctx_d182231db52a`) **PASS(0/0/0/0)** — 은퇴 울타리의 await · 큐 순서 · 정상 인증 흐름 · auth-js 2.106.1 null 읽기 동작 독립 추적 · 저장물 · 비밀 · 공급망 · 네이티브 · 로케일 변경 0 · 테스트가 실제 경로와 변이 5종을 문다. 인가(`ctx_926ce934cd73`) **PASS WITH CONCERNS(C0 H0 M0 L1)** — refresh 울타리 타당 · **KZ-1840-1(L, main 에도 있음 · 조건부)** 진행 중 v1→v2 이관의 raw 쓰기(`session-mutation.ts:226-257`)가 은퇴 검사 밖이라 폐기를 가로질러 옛 세션을 다시 쓸 수 있음(전제: 미완료 legacy 이관 + 첫 키 읽기 실패 뒤 같은 키 재가용 + 경합 — 실기기 빈도 미검증 · 워커가 범위 밖으로 둔 `ready()` 재이관 지점과 같은 곳). 규칙(critical · high · medium 0)상 머지 가능 → 머지 기차(`1840:cb994948` · CLEAN · 체크 4 성공). KZ-1840-1 은 후속 작은 PR(이관 raw 쓰기 직전 은퇴 검사 · await 없이 · M/S 직렬화 유지)로 닫는다. 뒤집는 조건: 머지 기차가 게이트 뒤 새 커밋 · CI 실패로 멈추면 원인 확인 뒤 재게이트.
26.09.20 02:56 · **[#1840 MERGED `c423ba88`(02:55:58)]** 머지 기차: draft 해제 → auto-merge → 1초 만에 MERGED(CLEAN · 최신). main 에서 r27 의 복구 세션 승격 경로가 닫혔다. 다음 main 빌드 · 웹 QA 재빌드는 r32(가져오기 허브 중복 id) 머지 뒤 한 번에 — 빌드 두 번을 피한다. KZ-1840-1(L) 후속 대기.
26.09.20 03:02 · **[r32 수확 — 가져오기 허브 중복 id draft PR #1841 → 첫 게이트 두 레인]** r32-fix-import-hub-dedup(`ctx_833db12fb6ec` · `claude-opus-5` 표준): 커밋 `f075834b`(main `2d688ef0` 위 · 6파일 +559/−15) — 새 순수 모듈 `src/lib/import/history-ownership.ts`: 비준은 정확 중복 id 를 이력에 안 적음 · /import-hub 와 /import 두 화면의 철회가 공유 로그에서 다른 항목이 가리키는 행을 지우지 않음(한 함수) · 새 문구 · 로케일 0 · `signals.test.ts` 해시 핀 재고정(`dbf58244`→`16e11c58` + 기록 한 줄, 코디네이터 (a) 승인). 재현 9 빨강 → 초록 · 변이 16 전부(살아남던 M15 렌더 조건은 구멍 → 테스트 추가) · verify 0(760 · 9,547) · CI pass. → **#1841 첫 게이트 두 레인**(정당한 철회를 막지 않는가 · 두 탭 동시 철회 · 50건 상한 · 결과 사실성 · 핀 재고정 영향). 통과하면 #1839 보다 먼저 머지.
26.09.20 03:03 · **[KZ-1840-1 후속 → r34]** #1840 인가 게이트의 Low(main 에도 있던 조건부): `session-mutation.ts` `migrate()` 가 은퇴 검사 없이 raw 어댑터에 legacy 값을 써(`:226-257`), 진행 중 v1→v2 이관이 동의 폐기를 가로지르면 폐기 뒤 옛 세션을 v2 키로 다시 쓴다(게이트 프로브 · 실제 auth-js 로 부활 확인). r34-fix-migrate-fence(`ctx_529e5acab8b8` · `claude-opus-5` ultracode · 새 워크트리 `fix-migrate-fence-260920` main `c423ba88` 기준): raw 쓰기 직전 은퇴 검사(await 없이) · 은퇴한 런타임의 재이관 조기 종료(M/S 직렬화 유지) · 게이트 프로브 순서를 #1840 하네스 위 결정적 테스트로 빨강 → 초록 · 정상 이관 대조 불변. 이유: 비용이 작고 방금 머지한 울타리의 마지막 조건부 구멍이다.
26.09.20 03:13 · **[r33 수확 → #1814 7차 재게이트]** r33-fix1814f(`ctx_25bc19b9d15e`): 커밋 `4a55e582`(force 없음 · draft · 제품 2 + 테스트 2) — 한 건 삭제가 시작 시 계정 임대(`captureAccountOwnerLease`)를 쥐고 요청 전후마다 재확인 · '지운 행' 표식은 같은 임대로 없음 확인 또는 지운 수 > 0 일 때만 · `capturedSourceRemoving` 을 손 담기와 자동 저장 `settleDuplicate` 가 봄. 새 테스트 15 빨강 → 18 초록 · 변이 14 + M0 · verify 0(759 · 9,343) · CI pass · 게이트 probe A · B 는 회귀 전(`e020313d`)과 같고 C(cold)=D(warm). **워커가 지시와 다르게 한 것 4가지**(지운 수 > 0 은 전환이 끼어도 사실 — 모름이면 G4Z-1814-1 재개방, 변이 D9 로 증명 · 원문 응답 뒤 전환은 `not_deleted` · 자동 저장 `settleDuplicate` 도 · #1839 세션 고정 대신 account-epoch 임대)는 7차 게이트가 판단하도록 발주서에 명시. 남은 것: 토큰 교체 ↔ 전환 알림 사이 틈(#1839 세션 계약 몫) · cold 겹친 삭제(멱등). → **7차 재게이트 두 레인**(`regate-1814-*-r7`).
26.09.20 03:18 · **[#1841 1차 게이트 → r35]** 생성물(`ctx_816458e9a9d6`) **BLOCK H1 M1**: LA-1841-1(H) 로컬 로그의 "다른 포인터 없음"을 소유권 증명으로 써서, 수정 전에 잘못 적힌 중복 항목 E2 만 남고 진짜 주인 E1 이 50건 상한으로 밀렸거나 다른 기기에 있으면 E2 철회가 E1 의 행을 지움(#1839 뒤엔 원문까지) · LA-1841-2(M) 판정 → 삭제 → 로그 제거가 원자적이지 않아 두 탭 동시 철회가 행을 남기고 포인터를 다 없앰. 인가(`ctx_83fb52e17f81`) **PASS WITH CONCERNS M2 L1**: LZ-1841-1 = LA-1841-2 · LZ-1841-2(M) 웹 로그 읽기 실패가 빈 목록(`history.ts:159-170`)이 되어 공유 행을 지움 · LZ-1841-3(L) 공유 원문을 남겨도 "원본 제거" 안내. 해시 핀 재고정 · 로케일 · #1839 세션 고정 접점은 두 레인 모두 문제없음. Simon 규칙대로 → r35-fix1841b(`ctx_bc6a81daeda3` · `claude-opus-5` ultracode): 소유권은 로컬 참조 수 대신 (a) 새 항목의 소유 증명 + 옛 항목 fail-closed 또는 (b) `sources.captured_at`(DEFAULT now()) 과 항목 시각 대조 중 택해 이유 · 옛 철회 영향을 적게 · 한 소유자 범위의 원자 철회(런타임 직렬화 + 탭 간 잠금) · 철회용 authoritative 읽기(실패를 빈 로그와 구별) · 안내 사실화. 뒤집는 조건: (a)(b) 모두 정당한 옛 철회를 넓게 막으면 Simon 에게 트레이드오프로 올림.
26.09.20 03:29 · **[#1814 7차 생성물 PASS · r34 수확 → #1842 첫 게이트]** #1814 7차 생성물(`ctx_6c16d24a6019`) **PASS(0/0/0/0)** — 6차 두 건 probe A~D 로 닫힘 · '지운 수 > 0 = 사실' 규칙 · epoch 임대의 #1839 잔여 · cold/줄 삭제 겹침 · `not_deleted` 문구를 반증 검토. 인가 7차 대기. r34-fix-migrate-fence(`ctx_529e5acab8b8`): draft PR **#1842**(`df1b9827` · 3파일 +338/−3 · 제품 1) — `migrate()` 가 raw 쓰기 직전마다 은퇴 검사 · 은퇴한 런타임은 이관을 시작하지 않음(ready · 잠금 불변 · M 직렬화 유지) · 게이트 프로브 순서가 main 에서 빨강(옛 세션 게시) → 초록 · 대조 3 + #1840 스위트 8 계속 초록 · 변이 6 · verify 0(760 · 9,538). → **#1842 첫 게이트 두 레인**. 워커 스윕의 같은 계열 후보(`recovery-proof-store.ts:309-313` 네이티브 직접 쓰기 · `import-pending.ts` runImport 늦은 `replacePendingCaptures` — 폐기 전 목록이 새 저장소에 되살아날 수 있음, medium~low 추정)는 공통 설계(읽을 때 저장소 epoch · 쓸 때 대조)가 필요해 후속 항목으로 둔다.
26.09.20 03:35 · **[main 에뮬레이터 스모크 통과 · #1814 7차 판정 → r36]** 스모크(03:31~03:33 · 제 AVD Pixel_9_Pro_XL · emulator-5556 창 없이 · Simon 의 5554 무접촉): x86_64 진단 빌드 main **`c423ba88`**(#1833 · #1835 · #1837 · #1838 · #1840 포함 · 실행 35459916943 · 71,406,761 B)를 기존 데이터 유지한 채 `install -r` → 실행 1 + 강제 종료 뒤 재실행 3 = **4회 모두 MainActivity · Loading 없음 · 세션 유지(소개 화면)** · `FATAL EXCEPTION` 0 · 인증 경고 표식(bootstrap-marker-read · fail-closed · storage_recovery · JWT issued at future) 0 · 캡처 `E:/Coding Infra/reports/qa-260919/main-c423ba88-launch{1..4}.png` · 에뮬레이터는 끔. #1814 7차 인가(`ctx_de311be70769`) PASS WITH CONCERNS **C0 H0 M1 L1**: G6Z-1814-1(M, 전체 PR 잔여 — r33 이 '멱등'이라 둔 것이 반증됨) 실행기 로드 전 cold 삭제와 로드 뒤 줄 삭제가 겹치면 같은 해시 경로에 새로 담은 S2 의 원문을 늦은 remove 가 지움 · G6Z-1814-2(L, 조건부) 전환 알림이 늦으면 B 토큰의 0행을 부재로 확정(실제 auth-js 로 알림 전 getSession=B 확인). 규칙대로 → r36-fix1814g(`ctx_d348b6d97adb` · `claude-opus-5` ultracode): 같은 자료 삭제를 공통 모듈에서 한 Promise 로 합치기(진입점 전부) · 한 건 삭제 확정을 저장 세션(사용자 + 세션 id)에 묶기(#1839 와 같은 계약 · 맞지 않으면 Low 잔여로).
26.09.20 03:41 · **[#1842 게이트 두 레인 PASS(0/0) → MERGED `c91ebcbb`(03:40:38)]** 생성물(`ctx_69ff082de9e2`) 0/0/0/0 — 검사 ↔ raw 어댑터 호출 await 틈 · 정상/중단 이관 · M 직렬화 · auth-js 테스트 계약 · 비밀 · 공급망 · 문구 확인. 인가(`ctx_a6a4a7e5bf3b`) 0/0/0/0 — base 에서 옛 세션 재저장을 실제 어댑터 + auth-js 로 재현한 뒤 HEAD 의 새 SDK · 다음 부트에서 차단 확인 · 정상 이관 · 재시도 · 중단 경계 43곳의 M/S 직렬화와 복구 검증. **KZ-1840-1 닫힘.** 머지 기차: draft 해제 → auto-merge → 1초(CLEAN). 오늘 밤 머지 16개. 남은 같은 계열 후보(`recovery-proof-store` 직접 쓰기 · `import-pending` runImport)는 공통 설계(읽을 때 저장소 epoch · 쓸 때 대조) 후속.
26.09.20 04:17 · **[r35 수확 → #1841 2차 재게이트 · 운영 ingest_log RLS 확인]** r35-fix1841b(`ctx_bc6a81daeda3`): 커밋 `e31e35c6`(force 없음 · draft) — 소유권 = 새 항목 owned 표지 · 파일 항목 원래 소유 · **옛 허브 항목은 서버 `ingest_log` 정확 중복 기록**(없으면 지움 · 1+1 이 다 보이면 올림 · 그 밖은 남기고 안내 · 읽기 실패면 지우지 않고 `revokeFailed`) · (b) captured_at 창은 시계 차 때문에 버림 · 철회 전체를 계정마다 한 임계 구역(웹 Web Lock · 네이티브 큐) · 철회용 엄격 읽기 · `ds.import.revokeKept` 5개 로케일. 재현 30/48 빨강 → 72/72 초록 · 변이 28/28 · verify 0(760 · 9,580) · CI pass. 정당한 옛 철회가 막히는 경우(같은 본문 재가져오기 · 세 번 비준 등)는 행을 남기고 안내 — 모집단은 #1612(09-05) 뒤 허브 항목뿐. **코디네이터 운영 확인(읽기 전용 SQL)**: `public.ingest_log` RLS 켜짐 · `ingest_log_select_own`(authenticated · `user_id = auth.uid()`) · `ingest_log_insert_own` · 열 `id · user_id · content_hash · stage · reason · survivor_id · dropped_at` — 워커의 운영 RLS 전제 성립. → **#1841 2차 재게이트 두 레인**. ⚠ 코디네이터 실수: 감시 스크립트 두 개가 03:38~04:13 동시에 돌았다(재무장 전 생존 확인 누락) — 피해 0, 이후 재무장 전 확인.
26.09.20 04:28 · **[r36 수확 → #1814 8차 재게이트]** r36-fix1814g(`ctx_d348b6d97adb`): 커밋 `93152b9e`(force 없음 · draft) — G6Z-1814-1(M) 같은 (계정, 행) 삭제를 공통 모듈에서 행마다 한 줄로(앞선 삭제가 다 지웠으면 요청 없이 deleted) · 실행기 진입점 셋이 `capturedSourceRemoving` 을 보고 기다리지 않고 거절(기다리면 계정 줄 멈춤 — 변이 R1 반증) · G6Z-1814-2(L) 0행 뒤 '없음' 확정만 session-mutation `*InsideMutation` 계약(M 잠금 · 상한 10초). 새 테스트 10(M0 정확히 10 빨강) · 변이 9 · probe A~D 유지 · verify 0(759 · 9,353) · CI pass. 남긴 것: 세션 계약은 0행 확인에만(요청 전부를 M 에 넣으면 자동 비우기가 모든 탭 로그인/로그아웃을 붙잡음) · 같은 부류 `undoWrites` · `afterFailure` 의 `sourceRowExists`. → **8차 재게이트 두 레인**.
26.09.20 04:32 · **[#1841 2차 재게이트 → r37]** 생성물(`ctx_1530b75042c5`) PASS WITH CONCERNS **C0 H0 M2 L2**: 1차 High 닫힘 · L2A-1841-1(M) Web Locks 없는 브라우저의 두 탭 철회가 다시 포인터를 다 없앰 · L2A-1841-2(M) 철회가 인증 세션 소유자에 안 묶임 · L2A-1841-3(L) 보존 기한 가드가 기기 시계 · L2A-1841-4(L) 서버 호출 기한 없음. 인가(`ctx_a74031e73207`) PASS WITH CONCERNS **C0 H0 M2 L2**(재현 · 관련 122 통과 · 반증 6): L2Z-1841-1(M) = L2A-1841-2(대기 중 계정 전환 → RLS 빈 답을 삭제 완료로) · L2Z-1841-2(M) 이력 제거와 owned 승격이 다른 저장이라 두 번째 실패를 삼키면 1+1 철회를 영영 못 끝냄 · L2Z-1841-3(L) 보존 기한 · 정리 예약 전제 · L2Z-1841-4(L) 안내 단정. 규칙대로 → r37-fix1841c(`claude-opus-5` ultracode): Web Locks 없으면 fail-closed(또는 저장소 CAS) · 철회를 main `session-mutation` 계약(기대 사용자 + 세션)에 묶기 · 제거 + 승격을 한 read-modify-write 로 · 기한 · 결과 중심 안내(5개 로케일). 코디네이터 관찰: #1841 도 회차마다 좁아지는 중(1차 H1 M2 L1 → 2차 M2 L2 · 같은 계열).
26.09.20 04:47 · **[#1814 8차 재게이트 → 클라이언트 라운드 멈춤(코디네이터 판단)]** 생성물(`ctx_2933301dc796`) PASS WITH CONCERNS **C0 H0 M2 L2**: 7차 두 건 닫힘 · G7A-1814-1(M) 지연된 전환 알림 중 `sourceRowExists` 0행을 믿어 철회 큐를 잃음 · **G7A-1814-2(M, 8차 수정의 회귀)** 10초 상한이 실제 Supabase SDK 잠금을 취소하지 못해 이후 로그인 · 로그아웃이 멈출 수 있음 · G7A-1814-3(L) 첫 삭제가 멈추면 follower · 표식이 끝없이 남음 · G7A-1814-4(L) Web Locks 없으면 process-only 잠금으로 강등. 인가(`ctx_4f62f5c1dce4`) PASS WITH CONCERNS **M1**: G7Z-1814-1 `undoWrites` · `afterFailure` 의 다른 저장 세션 부재 오판정이 철회 기록 소실 · 원문 삭제로(구 · 신 HEAD 모두 재현 — 전체 PR 잔여). **코디네이터 판단: #1814 의 클라이언트 수정 라운드를 8차에서 멈춘다** — ① 남은 발견이 전부 같은 계열(클라이언트 세션 · 소유권 경합)이라 결정 1번의 서버 조정(JWT 에 묶인 서버 트랜잭션)이 뿌리에서 닫는다 ② 마지막 수정이 새 회귀(SDK 잠금 멈춤)를 만들었다 — 회차를 더 돌리면 틈을 옮길 위험이 커졌다 ③ 어차피 G2Z-1814-2(S3) · M2 결정 없이는 머지하지 않는다 · draft 라 사용자 영향 0. 추이: 1차 H2 M3 → 2차 M5 → 3차 M2 → 4 · 5 · 6 · 7차 M1 → 8차 M3(회귀 1 포함). 보고서 결정 1번에 근거 추가. 뒤집는 조건: Simon 이 "클라이언트로 계속"을 고르면 9차는 r36 의 M 잠금 사용부터 되돌리고(G7A-1814-2) `sourceRowExists` 경로 전체에 잠금 없는 세션 대조를 건다.
26.09.20 04:51 · **[폰용 QA 빌드 공개 — 사전 릴리스 `qa-260920-c91ebcbb`(09-18 Simon 확정 "최신 상태를 APK 릴리즈 · QA용 · 공개"의 연장)]** 로컬 빌드(`E:/Coding Infra/tools/qa_apk_build.py --wt E:/2ndB/.worktrees/qa-apk-260920 --abi arm64-v8a --tag qa` · 새 워크트리에 **정션 없이 실제 설치**(npm ci) → prebuild → gradle 826초) → `2ndb-qa-c91ebcbb-arm64.apk` 66.6 MB · arm64-v8a · 번들 10.9 MB. **서명 인증서 SHA-256 `6053a4c7…` · versionCode 40 = 09-19 QA 빌드와 같음**(apksigner · aapt2 대조) → 기존 QA 앱 위에 데이터 유지한 채 덮어 설치. SHA256 `87874a35…`. 릴리스 본문에 **#1814(자동 저장 재설계)가 빠졌다**(main 미머지 · 09-19 QA 빌드엔 있었음)를 명시. 에뮬레이터용 x86_64 는 CI 진단 빌드(`reports/apk/main-c423ba88-x86_64/`) · 서명이 달라 별개. 빌드 환경 워크트리 `qa-apk-260920`(브랜치 `qa/apk-260920` · 실제 node_modules)은 다음 QA 빌드용으로 **남긴다**(STATE '남긴 것'에 기록 — 09-20 01:4x 실수 재발 방지). 뒤집는 조건: Simon 이 이전처럼 통합 브랜치(머지 대기 PR 포함) 빌드를 원하면 다시 만든다.
26.09.20 05:11 · **[r37 핀 질문 답 — 승인]** r37(`ctx_6158f245784f`) 질문: `src/screens/deepspace/__tests__/records-import-integrity.test.ts:190` 이 화면 소스에 `removeImportHistory(userId, entry.id)` 문자열을 박제 — L2Z-1841-2 수정으로 항목 제거가 `history.ts` 의 단일 read-modify-write(제거 + 승격)로 옮겨가 그 한 줄만 빨강(관련 20 스위트 227 초록). 코디네이터 답(05:1x): 테스트 상한 4 · 단언을 `withdrawImportHistoryEntry(` 호출로 옮기고 이유 주석 1줄 · **옛 문자열을 주석에 남겨 통과시키지 않음**(검사가 산문을 증거로 읽는 함정 — 워커 스스로 제안) · 새 단언은 주석이 아닌 실제 호출 줄을 잡을 것 · result.md 에 핀 이동 기록.
26.09.20 05:24 · **[r37 수확 → #1841 3차 재게이트]** r37-fix1841c: 커밋 `ad9ae087`(force 없음 · draft) — Web Locks 없으면 철회 전체 거절 · 차례 안 세션 고정 + 서버 단계마다 세션 확인(M 은 확인 순간만 — 게이트의 'M 을 철회 전체에' 권고는 #1839 삭제 도우미와 합칠 때 교착이라 따르지 않음 · 조합 테스트) · 제거 + 승격 단일 엄격 RMW · 보존 기한 서버 시계(`captured_at` × 토큰 `exp`) · 30초 기한 · 까닭별 안내(새 키 4 × 5 로케일) · 핀 이동(승인). 재현 29 + 렌더 1 빨강 → 초록(123/123) · 변이 7/7 · verify 0(760 · 9,631). → **#1841 3차 재게이트 두 레인**(게이트가 워커의 권고 불채택 근거를 판단하도록 발주서에 명시).
26.09.20 05:43 · **[#1841 3차 재게이트 통과 → MERGED `2dce7ead`(05:41) · 라운드 마감 정리]** 3차: 생성물(`ctx_4c1e7df1bd7a`) **PASS 0/0/0/0**(2차 M2 L2 · 인가 중복 지적 재현 123/123 로 닫힘) · 인가(`ctx_a176205ec0ba`) PASS WITH CONCERNS **C0 H0 M0 L1**(기존 153 테스트 + 프로브 6 · 2차 medium · 보존 · 문구 닫힘 · Low: 기한 초과 뒤 인증 잠금이 남음 → 후속). 머지 기차: draft 해제 → auto-merge → BEHIND 라 update-branch → CI 188초 → **MERGED `2dce7ead`**. **오늘 밤 머지 17개**(#1841 이 #1839 보다 먼저 — 가져오기 철회가 원문까지 지우기 전에 막음). 정리: 머지 끝난 워크트리 3개(#1840 · #1842 · #1841) 하나씩 제거(STATE '남긴 것' 대조 · 정션 해제 확인 · 공용 745 매번 · expo 로드). G14 결정 시트 `E:/Coding Infra/reports/vibe-r260919/decision-sheet-run_e3a3e38558ab.html`(항목 15 · 레인 5 — 브라우저로 열어 저장, Artifact 는 내려받기가 막혀 파일로). 남은 draft 둘(#1814 8차 · #1839 2차에서 클라이언트 라운드 멈춤)은 결정 1번 대기. 후속: KZ-계열 저장소 epoch 공통 설계 · #1841 Low(기한 뒤 인증 잠금).
