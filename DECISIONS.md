# DECISIONS

**append-only.** 요약·재작성·삭제 금지. 새 줄은 맨 아래에 붙인다.
한 줄 = `YY.MM.DD HH:MM · 결정 · 이유 · 뒤집는 조건`

이 파일이 하는 일: 결정이 난 그 턴에 남긴다. 대화는 버퍼고, 파일에 없으면 없는 것이다.
상태(지금 어디까지 / 다음 하나 / 막힌 것)는 여기 쓰지 않는다 — `STATE.md` 가 소유한다.
100KB 에 닿으면 요약하지 말고 기간으로 쪼갠다(`DECISIONS-2026H2.md`).

기간 보관(닫힌 파일 — 줄을 그대로 옮김 · 2026-09-19 09:54 KST 분할 · 분할 전 sha256 `ace3826318ba`):
- `DECISIONS-2026-09-13.md` — 26.09.13 ~ 26.09.13
- `DECISIONS-2026-09-14.md` — 26.09.14 ~ 26.09.14
- `DECISIONS-2026-09-15_16.md` — 26.09.15 ~ 26.09.16
- `DECISIONS-2026-09-17_18.md` — 26.09.17 ~ 26.09.18 (2026-09-20 01:52 KST 분할 · 분할 전 sha256 `1bb273c78fab`)
- `DECISIONS-2026-09-19.md` — 26.09.19 ~ 26.09.19 (2026-09-20 08:47 KST 분할 · 분할 전 sha256 `f3a89d3cabfb`)

---

26.09.20 00:09 · **[r26 질문 답 · 별건 발견]** r26(#1814 · G3Z-1814-1): 사용자 삭제 경로 전수 조사 결과 복구와 겹쳐 '행 없는 원문'을 새로 만드는 길은 기록 상세 한 건 삭제(`deleteCapturedSource`) 하나 → 계정 줄 · `restoringNow` 에 태움(화면 코드 0 · 새 문구 0 · 되살리기 중이면 기존 `deleteSourceFailed` 로 답하고 업로드 뒤 마저 삭제). 일괄 삭제 2종(`deleteUningestedSources` · `deleteAllUserData`)은 줄에 태우지 않고 근거만 기록(제품 파일 4 유지) — 코디네이터 승인. ⚠ **별건(후속 조사 필요)**: 워커 보고로는 설정의 일괄 삭제 두 종이 **원래 raw-clippings 본문(원문)을 지우지 않는다** — 사용자가 '전체 삭제'를 눌러도 원문이 저장소에 남을 수 있는 개인정보 쪽 잠재 결함(이 PR 밖 · 기존 동작). 서버 캐스케이드 · 저장소 수명 규칙 · 계정 삭제(0188)가 대신 지우는지 코드 확인 범위를 result.md 에 적게 했다 — 고치지는 않음. 결과를 보고 별도 PR 로 연다.
26.09.20 00:37 · **[r25 · r26 수확 → 재게이트 + 별건 두 개 발주]** r25-fix1838(`ctx_8686ab7a5a2f`): 커밋 `66023b77` — 어댑터가 폐기를 두 큐 맨 앞에서 한 번 `stillAwaited` 로 확인해 이미 실패로 답한 시도의 폐기는 시작하지 않음 · 재시도는 자기 폐기 요청 · 실제 `maintenanceTail` 회귀 5건 포함 6 failed/51 → 85/85 · 변이 6/6 · verify rc=0(759 · 9,502) · CI 초록. 워커가 1차 합치기 설계(`609c6e3c`)를 철회한 이유가 **별건 A**: 동의를 가로지른 옛 Supabase 클라이언트의 refresh 저장이 폐기 · `resetSupabaseClient` 뒤에 착지해 새 클라이언트가 그 세션을 게시할 수 있다 — **복구 세션 승격 가능성**(이 PR 이전부터 · client.ts · session-mutation.ts · 코드 읽기 판정 possible · 재현 없음). r26-fix1814d(`ctx_129f78cdeabe`): 커밋 `e020313d` — 기록 상세 `deleteCapturedSource` 를 계정 줄 · restoringNow · deletingNow 로 · 겹친 비우기 부름은 합쳐 한 번 더 · 테스트 +10 · 변이 15종 · verify rc=0(759 · 9,307) · CI 통과. **별건 B(개인정보)**: 설정 일괄 삭제 · 한 건 삭제 · 보존 크론 0056 이 `raw-clippings` 원문을 남기고(서버 트리거 · 수명 규칙 · 정리 크론 0) 계정 삭제만 지우며, 데이터 내보내기(`export-account`)는 그 폴더를 내보낸다 → '전체 삭제' 뒤에도 원문이 남고 내보내기에 나온다(추정) · 안내 문구도 원문 잔존을 말하지 않음. 발주: 재게이트 #1838 2차(`ctx_8c4a0707c163` · `ctx_624ecec632fe`) · #1814 5차(`ctx_ddb337f2aa64` · `ctx_9c4c1832caa1`) · **r27-verify-refresh**(`ctx_da7332aa6cf4` · A-verify `gpt-6-astra` @xhigh · 읽기 전용 + 임시 하네스 — 별건 A 실재 · 영향 · 심각도 판정) · **r28-fix-raw-clippings**(`ctx_34ef49558882` · `claude-opus-5` ultracode · 새 워크트리 `fix-raw-clippings-erase-260920` = main `ffd5cf1d` — 별건 B 의 클라이언트 몫: 한 건 · 일괄 삭제가 원문 객체도 지움 · 실패 순서 · 문구 사실화 · **draft 까지, 머지는 게이트 뒤 Simon 확인** · 이미 남은 원문 정리(운영 쓰기) · 서버 S3 · 보존 크론은 범위 밖). ⚠ 코디네이터 실수(경미): 공용 읽기 전용 워크트리 `gate-review-260919` 를 게이트 4명이 읽는 중에 main 최신으로 옮겼다(e60081f7 → ffd5cf1d · 차이는 #1837 의 HIBP 파일뿐이라 진행 중 게이트 대상과 무관) — 다음부터는 게이트가 도는 동안 그 워크트리를 건드리지 않는다.
26.09.20 00:47 · **[#1838 머지 · r28 설계 답]** #1838(#1835 후속) 2차 재게이트 두 레인 **PASS/PASS(발견 0)** — IA-1838-1 · IZ-1838-1 닫힘 · 합치기 설계 철회 판단 확인 · 옛 refresh 경로를 넓히지 않음 → **MERGED `2d688ef0`(00:47)**. r28(원문 삭제) 설계 질문 답: Q1 `deleteSourcesByIds`(가져오기 철회 · 이력 삭제)도 같은 결함이라 포함 · Q2 위키 페이지가 가리키는 ingested=false 행(CHECK `wiki_pages_source_kind_pair` 로 행 삭제가 막힘)은 원문 · 행 모두 건드리지 않고 나머지만 지운 뒤 지운/남긴 개수를 돌려줌 — **조건: 사용자에게 거짓 '다 지웠다'를 보이지 않는다**(기존 부분 실패 문구, 없으면 최소 새 문구 5개 로케일 허용) · Q3 이번 PR 은 원문 범위만. **별건 C(개인정보)**: '전체 삭제' 뒤에도 personas · memorized_patterns · xp_events 가 남는다(`delete-bulk.ts:185-189` · 클라이언트 삭제 RLS 정책 없음)는데 안내는 "개인 콘텐츠를 모두 지워요" — 서버 삭제 정책 추가 vs 문구 사실화는 Simon 결정. #1814 5차 재게이트: 생성물 PASS(발견 0) · 인가 대기.
26.09.20 00:54 · **[#1814 5차 재게이트 → r29]** 생성물 **PASS(발견 0)** — 4차 두 건 닫힘 · 조정 훅 미설치 · 이중 등록 · 합치기 굶주림에서 새 결함 0. 인가 PASS WITH CONCERNS — **G4Z-1814-1(M, r26 의 회귀)** 상세 삭제의 30초 상한이 실제 삭제 완료 전에 승격 잠금을 풀어 원문만 되살릴 수 있음 · G4Z-1814-2(L) 일반 손 담기 중복 재시도가 삭제 중 표식보다 먼저 성공 반환(둘 다 실제 제품 함수로 재현). → r29-fix1814e(`ctx_22bf304b6ed2` · `claude-opus-5` ultracode): 상한은 줄만 풀고 잠금 · 확정은 실제 삭제 뒤(복구 업로드 추적과 대칭) · 중복 재시도도 deletingNow 먼저 · **같은 계열 조합 전수 표**를 요구. 코디네이터 관찰: #1814 는 오늘 밤 수정 5회 · 재게이트 5회(high 2 → M4 → M2 → M1 → M1) — 수렴하지만 매 수정이 새 경계 조합을 만든다. 클라이언트 쪽 다중 경로 조정 자체가 취약할 수 있어, 서버 트랜잭션(S3 계열)로 삭제 · 복구를 한 곳에 모으는 설계를 Simon 결정 항목(G2Z-1814-2 와 함께)으로 올린다.
26.09.20 01:23 · **[r28 수확 → #1839 게이트 · r27 레인 교체]** r28-fix-raw-clippings(`ctx_34ef49558882`): draft PR **#1839** 커밋 `f66ae064`(origin/main `ffd5cf1d` 위 · 11파일 +823/−49) — 새 경계 `src/lib/wiki/source-erasure.ts`: 골라 지우기(정리 안 한 캡처 · 가져오기 철회 · 옛 한 건 삭제)는 원문 먼저 · 행 나중(실패하면 행이 재시도 포인터), 위키 페이지가 가리키는 행은 원문 · 행 모두 남기고 kept 로 알림(새 문구 `capturesKeptWithWiki` 5개 로케일), 전체 삭제는 행 먼저 · `raw-clippings/<uid>/` 폴더 쓸기 · verify rc=0(760 스위트 · 9,521 테스트). 게이트 두 레인 디스패치(artifact `ctx_84b565d62d14` · bizlogic `ctx_56fb1ec6740f`) — 머지는 게이트 통과 + Simon 확인 뒤(사용자 원문 삭제 동작 변경 · 되돌릴 수 없음). r27(옛 클라이언트 refresh → 복구 세션 승격 검증)은 `gpt-6-astra` 가 codex 사이버 출력 필터에 막혀(조용한 실패 · 2,350초) 코디네이터가 종료 → G13 `revalidate_for_retry` OK → 2순위 `claude-fable-5-1` @max 로 재배정(`ctx_8b133781f03e` · 기준 origin/main `2d688ef0`). 뒤집는 조건: fable 도 막히면 3순위 opus.
26.09.20 01:27 · **[운영 실측 — 이미 남은 원문은 0건 · QA 빌드 main 으로 교체]** 운영 `2nd-brain`(zoacryukmdeivmolvyhj) 읽기 전용 집계(SELECT 만 · 01:3x): Storage 버킷은 `raw-clippings` 하나(비공개) · 객체 **3개 · 소유 폴더 2 · 240바이트** · 최신 2026-08-09 · `sources.storage_path` 와 짝 없는 **고아 원문 0** · 계정 없는 폴더 0 · `sources` 3행(2명) 전부 원문과 짝 · `_storage_pending` 0 · 최신 캡처 2026-07-18. → #1839 결함은 코드상 실재하지만 운영 노출은 0이다(미출시 · 테스트 계정뿐). Simon 결정 대기였던 '이미 남은 원문의 운영 정리(운영 쓰기)'는 대상이 없어 **폐기 제안**(결정 아님 — Simon 확인 전까지 결정 시트에 '폐기 제안'으로 남긴다). QA: 웹 QA 서버(127.0.0.1:8765/2nd-B/)를 옛 통합 브랜치 `640db5bd`(main 보다 16 커밋 뒤) 빌드에서 **main `2d688ef0`** 빌드로 교체(새 워크트리 `qa-integration-260920` · upstream 없음 · 정션 reparse=True · export rc=0) · arm64 APK `main-2d688ef0-arm64`(push 실행 35452876393) 수령 · 에뮬레이터용 x86_64 빌드 디스패치(35454934771). 뒤집는 조건: #1814 를 QA 에 넣어야 하면 그 PR 이 게이트를 통과한 헤드를 QA 브랜치에 병합해 다시 빌드.
26.09.20 01:34 · **[#1839 1차 게이트 두 레인 BLOCK → r30]** 생성물(`ctx_84b565d62d14` · 발견 H1 M2): **JA-1839-1(H)** 전체 삭제가 빈 폴더를 확인한 뒤 받은편지함 `promotePendingUploads()` 가 예전 본문을 다시 올림(0행 UPDATE 를 성공 처리 `queries.ts:186-192`) · **JA-1839-2(M)** 선택 삭제의 위키 참조 확인과 원문 삭제 사이에 위키 생성이 끼면 보존할 원문만 지워짐 · **JA-1839-3(M)** 파괴 작업이 확인 당시 계정에 안 묶여 RLS 빈 결과를 성공으로 읽음. 인가(`ctx_56fb1ec6740f` · H1 M1): **JZ-1839-1(H)** `SAFE_NAME{1,255}` 때문에 정상 담기로 생기는 256자 이름이 선택 삭제에서 원문만 빠지고 행은 지워져 거짓 성공 · 전체 삭제는 매번 던져 뒤 단계까지 막힘(메모리 재현) · **JZ-1839-2(M)** = JA-1839-3 같은 뿌리. 두 레인 모두 타인 원문 삭제 경로는 반증(0). Simon 규칙(medium+ 머지 전 수정)대로 → r30-fix1839(`ctx_f4c6a6ff4a9b` · `claude-opus-5` ultracode): 이름 계약을 저장 계약(키 UTF-8 바이트)에 맞춤 · 계정 삭제가 쓰는 `AuthSessionExpectation`/`runMutation` 재사용 · 선택 삭제/위키 생성/승격 사이 조건부 UPDATE 선점(선례 `_storage_pending`) · 승격 0행이면 방금 올린 객체 보상 삭제. **코디네이터 사전 결정: 이 PR 에 마이그레이션 · 엣지 · RLS 변경을 넣지 않는다** — 서버 울타리(삭제 세대 · outbox)는 보안 담당 서버 S3 트랙이고 #1814 G2Z-1814-2 와 같은 Simon 결정 항목이다. 남는 창(보상 삭제 전 앱 종료 · 다른 기기)은 PR 본문에 명시. 뒤집는 조건: 워커가 스키마 없이 JA-1839-2 를 닫을 수 없다고 판단하면 question → Simon 결정으로 올린다.
26.09.20 01:50 · **[r27 수확(fable) — 복구 세션 승격 재현 · Low → r31 수정 발주 · 워크트리 7개 정리]** r27-verify-refresh(`ctx_8b133781f03e` · `claude-fable-5-1` @max · astra 가 사이버 필터에 막혀 옮긴 2순위): main `2d688ef0` + auth-js 2.106.1 실제 모듈 하네스로 **재현** — 동의 전에 세션을 읽고 동의 뒤 저장하는 옛 클라이언트 refresh 가 폐기 · `resetSupabaseClient` 뒤 착지 → 새 클라이언트가 복구 세션을 **잠금 없이 일반 세션으로 게시**(다음 콜드 스타트에도 유지 · A2: 네트워크 불안정이면 동의 24초 뒤 착지). 대조: 로그아웃 뒤 되살아남 0 · 다른 계정 섞임 0(같은 S 잠금 직렬화) · #1838 은 막지 않음 · 입구는 #1835 fail-closed 승격 1곳(`AuthContext.tsx:566-574`). **심각도 Low**(복구 울타리가 UI 전용 · 서버에 `amr` 검사 0 · 보유자는 어차피 비밀번호 변경 가능). 워커 임시 사본 울타리 실험(+19/−2)이 A · A2 를 닫고 대조 불변. 코디네이터 판단: Low 라 규칙상 필수는 아니지만 #1835 불변식 파손이고 비용이 작아 닫는다 → r31-fix-recovery-fence(`ctx_f95918192465` · `claude-opus-5` ultracode · 새 워크트리 `fix-recovery-fence-260920` main 기준): 폐기를 큐에 넣는 같은 동기 블록에서 옛 runtime `gatedStorage` 은퇴 · 실패 시 회전 토큰 소실은 수용(이미 폐기 동의). 정리: 머지 끝난 워크트리 7개 제거(브랜치 유지 · 매 제거마다 공용 node_modules 745 확인 · QA 옛 워크트리는 정션 아닌 5.3 GB 실사본 · 안쪽 reparse 0 확인 뒤 제거) · E: 56 → 64 GB. ⚠ 코디네이터 실수: 3개를 한 번에 병렬로 제거(원칙은 하나씩) — 피해 0 · 이후 순차. 뒤집는 조건: r31 게이트가 울타리의 부작용(정상 refresh 유실)을 medium 이상으로 보면 설계 재검토.
26.09.20 01:52 · **[r29 범위 질문 2 답 — A]** r29-fix1814e(`ctx_22bf304b6ed2`) 중간 보고: 커밋 `1132cb84`(G4Z-1814-1 · G4Z-1814-2 + 승격 행 표식) push · CI 3 pass · verify 0. 같은 계열 조합 조사(읽기 전용 3회)에서 **이번 회귀가 아닌 기존 잔여**가 더 나옴: 실행기만으로 닫히는 것 — M5(손 담기가 기기 기록 null 을 지울 차례로 읽어, 늦은 복구 뒤 철회 안 한 자동 저장 행을 지움 = **데이터 손실** · 저장소 막힌 웹) · M14(표식 분기에서 undo_unrecorded 안내가 안 거둬짐) · 늦은 표식(상한 뒤 keptRows) / 설계 선택 — M2(손 담기 상한이 턴 잠금을 풀어 같은 답 자동 저장 재시도와 23505 경합 → 원문 고아) · M3(capture 가 INSERT 실패 때 올린 원문을 안 지움) · raw_removed 뒤 같은 내용 담기는 원문 없이 · **가져오기 이력이 정확 중복 행 id 를 적어 철회가 다른 행을 지움**. 코디네이터 답(01:5x): **A** — 실행기 3건은 두 번째 커밋으로 닫음(결정적 테스트 · 변이 · 1~5차 대조 불변) · M2 는 설계 선택이라 결정 1번(삭제 · 복구 조정)과 같은 줄로 Simon 에게 · M3 · raw_removed · 가져오기 중복 id 는 #1814 범위 밖 기존 결함 → result.md 별도 절(재현 · file:line · 영향 · #1839 가 원문까지 지우게 되는지) → 코디네이터가 별도 PR 로. 일괄 삭제는 #1839 와 겹쳐 손대지 않음. 뒤집는 조건: 가져오기 중복 id 철회가 #1839 머지 전에 닫혀야 한다고 판단되면(원문까지 지우게 되는 경우) #1839 머지 순서를 그 수정 뒤로.
26.09.20 01:57 · **[코디네이터 실수 — STATE 가 '남긴 것'으로 적은 QA 워크트리를 대조 없이 제거 · 피해 범위 확인]** 01:4x 워크트리 정리에서 `qa-integration-260918`(브랜치 `qa/integration-260919` · `640db5bd`)을 지웠는데, STATE.md(09-19 12:25 판) '7. 주인이 따로 있어 남긴 것'에 "공개 QA 릴리스 태그의 출처 · 로컬 빌드용 실제 node_modules"로 남겨 둔 항목이었다 — 정리 전 STATE 대조를 안 했다. 피해 확인: 브랜치(로컬 · origin) · 커밋 · 릴리스 태그 `qa-260919-640db5bd` 그대로 · **QA 서명 키는 워크트리 밖**(`E:/Coding Infra/tools/qa-test.keystore`, `qa_apk_build.py:109` 가 도구 폴더 기준) → 앞으로 만드는 QA APK 도 기존 QA 앱 위에 덮어 설치 가능. 잃은 것은 다시 만들 수 있는 빌드 환경뿐(실제 node_modules 5.3 GB · gradle 산출물 · 옛 dist). 웹 QA 는 이미 `qa-integration-260920`(main)로 옮긴 뒤였다. ⚠ 재발 방지 두 가지: ① 워크트리 정리 전에 STATE · HANDOFF 의 '남긴 것'을 대조한다 ② `qa_apk_build.py --reinstall` 을 **정션 워크트리에 돌리지 않는다** — `npm ci` 가 node_modules 를 비우면서 정션을 따라가 공용 설치를 지울 수 있다(도구는 node_modules 가 없을 때만 npm ci 를 돈다 · `:66-67`). 폰용 QA APK 를 main 으로 다시 만들 때는 새 워크트리의 정션을 먼저 안전하게 떼고(비재귀 Delete · 없어졌는지 확인) 실제 설치로 빌드한다.
26.09.20 02:13 · **[r29 수확 → #1814 6차 재게이트 · 가져오기 허브 중복 id 철회 → r32]** r29-fix1814e(`ctx_22bf304b6ed2`): 커밋 두 개(force 없음) — `1132cb84` G4Z-1814-1(M) 줄의 30초 상한은 줄만 넘기고 상세 삭제의 잠금 · 답은 실제 삭제 뒤 · `delete-captured-source.ts` 행 표식(지우는 중 · 지운 행 · 되살리는 중)으로 승격과 삭제가 양방향 비껴감 · G4Z-1814-2(L) 손 담기 중복은 지우는 중 · 지운 행을 먼저 거절 / `3cc4cc3f` 조사에서 나온 기존 잔여 M5(**데이터 손실** — 저장소 막힌 웹에서 늦은 재판정이 철회 안 한 자동 저장 행을 지움) · M14 · 늦은 표식. 새 테스트 18 빨강 → 초록 · 변이 17종(동치 1) · verify 0(759 · 9,325) · CI 3 pass · draft 유지. 남긴 것: M2(손 담기 상한 × 자동 저장 재시도 → 원문 고아 · 제안 2안 → Simon 결정 1번 줄) · M3 · G2Z-1814-2 · 일괄 삭제(#1839). → **6차 재게이트 두 레인**(regate-1814-*-r6 · `ctx_0165ca22af2b` · `ctx_bcbafd0ee4cd` · 5차 두 건 닫힘 확인 + 행 표식 누수 · 예외 경로 해제 · `removed` 증가 · M5/M14 새 조합). r29 §3-6 **가져오기 허브 이력이 정확 중복 행 id 를 적어 철회가 앞 가져오기의 행을 지움**(같은 계정 · 파일 가져오기 `dds-import-inbox-screens.tsx:294-297` 에는 있는 규율이 허브에 없음 · #1839 가 붙으면 원문까지 되돌릴 수 없게 지워짐) → **#1839 보다 먼저 닫는다**: r32-fix-import-hub-dedup(`ctx_833db12fb6ec` · `claude-opus-5` 표준 · 새 워크트리 `fix-import-hub-dedup-260920` main 기준): 비준에서 정확 중복 id 는 이력에 안 적고 사실대로 안내 · 철회는 다른 이력이 가리키는 id 를 지우지 않음(이미 적힌 이력 보호) · 판정은 순수 함수. 뒤집는 조건: r32 게이트가 보호 규칙의 부작용(정당한 철회가 남는 것)을 medium 이상으로 보면 재설계.
26.09.20 02:18 · **[r32 범위 답 — (b) 두 화면 모두]** r32(`ctx_833db12fb6ec`) 질문: 철회 이력 저장소 `import.history:<uid>` 를 가져오기 허브와 파일 가져오기(`dds-import-inbox-screens.tsx`)가 함께 쓰고 둘 다 이력 전체에 철회 버튼을 단다(`:250-251` · 허브 `:657-666` · /import `:607-623`) — 허브만 고치면 이미 적힌 옛 허브 이력 E2=[R] 을 /import 에서 철회할 때 `revokeImport`(`:329-354`)가 R 을 똑같이 지운다(#1839 뒤엔 원문까지). 코디네이터 답(02:18): **(b)** — 제품 상한 3(허브 · 새 순수 함수 · /import 몇 줄) · 두 화면이 같은 순수 함수 하나로 '남은 다른 이력이 가리키는 id 는 지우지 않음' 판정 · 새 문구 · 로케일 0(남긴 행은 같은 목록의 다른 이력이 계속 가리켜 목록이 사실을 보여 줌) · /import 쪽 재현 테스트 1건 · 파일 가져오기의 기존 규율 불변. 이유: #1839 전에 되돌릴 수 없는 삭제 문 둘을 다 닫는다.
26.09.20 02:36 · **[r30 수확 → #1839 2차 재게이트 · #1814 6차 판정 → r33]** r30-fix1839(`ctx_f4c6a6ff4a9b`): 커밋 `c7bd381a`(force 없음 · draft 유지 · 제품 6 + 테스트 5 · **마이그레이션 · 엣지 · RLS 0**) — 이름은 전체 키 UTF-8 1,024바이트 계약 · 본인 경로인데 계약 밖이면 행을 남기고 명시적 실패 · 전체 삭제는 원문 폴더 실패에도 끝까지 간 뒤 불완료 / 파괴 작업 4종 `runMutation` + `assertExpectedSessionInsideMutation` 시작 세션 고정 / frontmatter 선점 `_erasing` · `_generating` 조건부 UPDATE(CAS) · phase1 · 승격 쓰기도 표식 보존 CAS · 10분 만료 / 승격은 업로드 전 재확인 + 0행이면 방금 올린 객체 되돌림 · 재현 21 빨강 → 초록 · 변이 11 · verify 0(760 · 9,564) · CI pass. 남는 창(업로드 ↔ 되돌리기 사이 앱 종료 · 옛 빌드 · 만료 · 시계 차) = 서버 S3. ⚠ **#1814 head `3cc4cc3f` 와 `promote-pending.ts` 내용 충돌**(이 PR 이 지운 `updateSourceFrontmatter` 를 #1814 가 부름) — 나중 머지 쪽이 두 보호를 합친다. → **2차 재게이트 두 레인**(`ctx_eb95f8b385e7` · `ctx_793f649e4f17` · PostgREST JSON 경로 필터 · count 의미는 설치 소스 · 공식 문서로만 대조). #1814 6차: 생성물 **PASS(0)** · 인가 PASS WITH CONCERNS — **G5Z-1814-1(M, r29 회귀)** `removed` 표식이 소유자 임대 없이 확정돼 계정 전환 뒤 살아 있는 행의 복구 · 승격을 런타임 내내 막음(상한 유무 두 순서 재현) · G5Z-1814-2(L) 실행기 최초 로드 전 직접 삭제를 손 담기가 모름(expo-router 56.2.12 지연 평가로 조건 실재 확인). Simon 규칙대로 → r33-fix1814f(`ctx_25bc19b9d15e` · `claude-opus-5` ultracode): 같은 유효 소유자 임대로 부재 확인 때만 표식 · 파괴 요청 사이 임대 재확인 · 손 담기 성공 직전 공통 모듈의 '지우는 중' 확인 · #1839 와 합칠 계약 한 단락. 코디네이터 관찰: #1814 · #1839 가 같은 삭제 · 승격 경계를 서로 다른 장치(행 표식 vs frontmatter 선점 · 계정 임대 vs 세션 고정)로 풀고 있다 — 머지 순서와 통합 방식을 결정 1번(서버 조정)과 함께 Simon 에게 올린다. 뒤집는 조건: r33 뒤에도 같은 계열 medium 이 또 나오면 #1814 를 #1839 위로 올려 한 장치로 합치는 재설계를 제안.
26.09.20 02:40 · **[r31 수확 — 복구 울타리 draft PR #1840 → 첫 게이트 두 레인]** r31-fix-recovery-fence(`ctx_f95918192465` · `claude-opus-5` ultracode): 커밋 `cb994948`(main `2d688ef0` 위 · 5파일 +1032/−9 · 제품 3 중 `client.ts` 는 주석만) — `AuthStorageRuntime.retire()`(읽기 null · 쓰기/삭제 무시 · 잠금 유지) · `gatedStorage` 가 `await ready()` 뒤 어댑터 직전에 은퇴 검사 · `storage-recovery.ts` 가 폐기를 큐에 넣는 **같은 동기 턴**에 `retireStorage()` · `resetAuthStorageRuntime()` 도 은퇴. r27 하네스를 실제 모듈 + 실제 auth-js 2.106.1 jest 스위트로 이식 — main 에서 A · A2 · **새 A3**(reset 시점 울타리만으로는 첫 쓰기가 이미 큐에 있을 때 샘) 빨강 → 초록 · 대조 C0 · C1 · C2 · D · E 불변 · 변이 5 · verify 0(760 · 9,530) · CI pass. 워커 판단: 보완 `stopAutoRefresh()` 는 막는 것이 없어 넣지 않음 · 은퇴 런타임 `ready()` 의 멱등 이관 재실행은 세션 부활 경로가 아니라 범위 밖. → **#1840 첫 게이트 두 레인**(`regate-1840-*` · 틈 · 정상 흐름 차단 여부 · auth-js null 처리 독립 확인 · virtual mock 영향 · 법무 줄 인용). ⚠ astra 가 인증 검증에서 사이버 필터에 막힌 전례(r27)가 있어 진행이 멈추면 터미널을 읽고 fable 로 옮긴다.
26.09.20 02:47 · **[r32 핀 질문 답 — (a) 재고정 절차대로]** r32 verify 1건 빨강: `src/lib/inbox/__tests__/signals.test.ts:274-315` 가 `dds-import-inbox-screens.tsx` 전체의 EOL 정규화 sha256 을 박아 둠(기대 `dbf58244…` → `16e11c58…`) — (b) 로 /import 철회에 넣은 판정 한 줄 · import 한 줄 · 주석이 깬다. 핀이 스스로 적은 절차 "이후 재고정하는 사람은 여기에 자기 줄을 추가한다"(09-07 · 09-13 · 09-14 기록 있음)대로 코디네이터 답(02:47): **(a)** 테스트 상한 3 · 해시 교체 + 기록 한 줄만(그 파일 다른 단언 · 동작 테스트 변경 0) · 재고정 뒤 스위트와 전체 verify 0 직접 확인 · PR 본문 · result.md 에 이유와 이전/새 해시 앞 8자 · 살아남은 변이 1개(15 중)가 동치인지 구멍인지 적고 구멍이면 테스트 1건. 이유: 앞선 결정 (b)(두 화면 모두 닫기)를 되돌리지 않고, 핀이 정한 절차를 따른다.
26.09.20 02:53 · **[#1839 2차 재게이트 → 클라이언트 라운드 멈춤 · 결정 1번 근거 갱신(코디네이터 판단)]** 인가(`ctx_793f649e4f17`) **BLOCK H1 M1 L1**: J2Z-1839-1(H) 늦은 승격의 되돌리기 remove 오류를 삼키고 재시도 포인터를 잃어 '전체 삭제 완료' 뒤 원문이 남음(앱이 살아 있는 정상 흐름 — S3 잔여 창과 다름) · J2Z-1839-2(M) 두 번째 생성이 살아 있는 `_generating` 을 덮어쓰고 실패하며 첫 생성의 선점까지 풀어 보존할 원문이 지워짐 · J2Z-1839-3(L) JSON null 과 SQL NULL 비교 불일치로 일부 가져온 행이 계속 바쁨. 생성물(`ctx_eb95f8b385e7`) **PASS WITH CONCERNS M3**: J2A-1839-1 선점 표식만 비교하는 frontmatter 전체 UPDATE 가 phase1 · 승격 사이에서 서로의 키를 되살리거나 지움(lost update) · J2A-1839-2 되돌리기가 같은 내용을 새로 담은 업로드(행 INSERT 전)를 지움 · J2A-1839-3 외부 Markdown 이 내부 예약 키(`_erasing` · `_generating` · `_storage_pending` · `_body_fallback` · `__phase1__`)를 주입하면 선택 삭제 · 생성이 영구히 바쁨. 1차 다섯 건은 두 레인 모두 닫힘 확인(재현 109 · 134). 추이: 1차 H2 M3 → 2차 새 H1 M4 L1. **코디네이터 판단: #1839 의 클라이언트 전용 수정 라운드를 여기서 멈춘다** — 여러 클라이언트 경로(삭제 · 승격 · 위키 생성 · phase1)가 같은 행 · 원문을 frontmatter CAS 로 조율하는 구조라 고칠 때마다 새 틈(lost update · 예약 키 · 업로드 세대)이 생긴다. 서버 쪽 원자 처리(참조 확인 + 행 삭제를 한 SQL 로 · 키 단위 `jsonb_set` · 삭제 의도 outbox · 업로드 세대)가 이 계열 대부분을 뿌리에서 닫는다. #1839 는 어차피 Simon 확인 뒤 머지라 규칙('medium 은 머지 전')과도 충돌하지 않는다. → 보고서 결정 1번 추천을 '② 클라이언트 고아 청소'에서 **'① 서버 조정(S3)'** 으로 바꾸고 근거(두 PR 의 회차 추이)를 적는다. #1814 는 r33(6차 회귀 수정)만 마저 수확한다. 뒤집는 조건: Simon 이 "클라이언트로 계속"을 고르면 r34 로 여섯 건을 받는다(예약 키 제거 · 살아 있는 생성 선점 거부 · null 비교는 작고 독립적).
26.09.20 02:56 · **[#1840 게이트 두 레인 통과 → 머지 · KZ-1840-1(L)은 후속]** 생성물(`ctx_d182231db52a`) **PASS(0/0/0/0)** — 은퇴 울타리의 await · 큐 순서 · 정상 인증 흐름 · auth-js 2.106.1 null 읽기 동작 독립 추적 · 저장물 · 비밀 · 공급망 · 네이티브 · 로케일 변경 0 · 테스트가 실제 경로와 변이 5종을 문다. 인가(`ctx_926ce934cd73`) **PASS WITH CONCERNS(C0 H0 M0 L1)** — refresh 울타리 타당 · **KZ-1840-1(L, main 에도 있음 · 조건부)** 진행 중 v1→v2 이관의 raw 쓰기(`session-mutation.ts:226-257`)가 은퇴 검사 밖이라 폐기를 가로질러 옛 세션을 다시 쓸 수 있음(전제: 미완료 legacy 이관 + 첫 키 읽기 실패 뒤 같은 키 재가용 + 경합 — 실기기 빈도 미검증 · 워커가 범위 밖으로 둔 `ready()` 재이관 지점과 같은 곳). 규칙(critical · high · medium 0)상 머지 가능 → 머지 기차(`1840:cb994948` · CLEAN · 체크 4 성공). KZ-1840-1 은 후속 작은 PR(이관 raw 쓰기 직전 은퇴 검사 · await 없이 · M/S 직렬화 유지)로 닫는다. 뒤집는 조건: 머지 기차가 게이트 뒤 새 커밋 · CI 실패로 멈추면 원인 확인 뒤 재게이트.
26.09.20 02:56 · **[#1840 MERGED `c423ba88`(02:55:58)]** 머지 기차: draft 해제 → auto-merge → 1초 만에 MERGED(CLEAN · 최신). main 에서 r27 의 복구 세션 승격 경로가 닫혔다. 다음 main 빌드 · 웹 QA 재빌드는 r32(가져오기 허브 중복 id) 머지 뒤 한 번에 — 빌드 두 번을 피한다. KZ-1840-1(L) 후속 대기.
26.09.20 03:02 · **[r32 수확 — 가져오기 허브 중복 id draft PR #1841 → 첫 게이트 두 레인]** r32-fix-import-hub-dedup(`ctx_833db12fb6ec` · `claude-opus-5` 표준): 커밋 `f075834b`(main `2d688ef0` 위 · 6파일 +559/−15) — 새 순수 모듈 `src/lib/import/history-ownership.ts`: 비준은 정확 중복 id 를 이력에 안 적음 · /import-hub 와 /import 두 화면의 철회가 공유 로그에서 다른 항목이 가리키는 행을 지우지 않음(한 함수) · 새 문구 · 로케일 0 · `signals.test.ts` 해시 핀 재고정(`dbf58244`→`16e11c58` + 기록 한 줄, 코디네이터 (a) 승인). 재현 9 빨강 → 초록 · 변이 16 전부(살아남던 M15 렌더 조건은 구멍 → 테스트 추가) · verify 0(760 · 9,547) · CI pass. → **#1841 첫 게이트 두 레인**(정당한 철회를 막지 않는가 · 두 탭 동시 철회 · 50건 상한 · 결과 사실성 · 핀 재고정 영향). 통과하면 #1839 보다 먼저 머지.
26.09.20 03:03 · **[KZ-1840-1 후속 → r34]** #1840 인가 게이트의 Low(main 에도 있던 조건부): `session-mutation.ts` `migrate()` 가 은퇴 검사 없이 raw 어댑터에 legacy 값을 써(`:226-257`), 진행 중 v1→v2 이관이 동의 폐기를 가로지르면 폐기 뒤 옛 세션을 v2 키로 다시 쓴다(게이트 프로브 · 실제 auth-js 로 부활 확인). r34-fix-migrate-fence(`ctx_529e5acab8b8` · `claude-opus-5` ultracode · 새 워크트리 `fix-migrate-fence-260920` main `c423ba88` 기준): raw 쓰기 직전 은퇴 검사(await 없이) · 은퇴한 런타임의 재이관 조기 종료(M/S 직렬화 유지) · 게이트 프로브 순서를 #1840 하네스 위 결정적 테스트로 빨강 → 초록 · 정상 이관 대조 불변. 이유: 비용이 작고 방금 머지한 울타리의 마지막 조건부 구멍이다.
26.09.20 03:13 · **[r33 수확 → #1814 7차 재게이트]** r33-fix1814f(`ctx_25bc19b9d15e`): 커밋 `4a55e582`(force 없음 · draft · 제품 2 + 테스트 2) — 한 건 삭제가 시작 시 계정 임대(`captureAccountOwnerLease`)를 쥐고 요청 전후마다 재확인 · '지운 행' 표식은 같은 임대로 없음 확인 또는 지운 수 > 0 일 때만 · `capturedSourceRemoving` 을 손 담기와 자동 저장 `settleDuplicate` 가 봄. 새 테스트 15 빨강 → 18 초록 · 변이 14 + M0 · verify 0(759 · 9,343) · CI pass · 게이트 probe A · B 는 회귀 전(`e020313d`)과 같고 C(cold)=D(warm). **워커가 지시와 다르게 한 것 4가지**(지운 수 > 0 은 전환이 끼어도 사실 — 모름이면 G4Z-1814-1 재개방, 변이 D9 로 증명 · 원문 응답 뒤 전환은 `not_deleted` · 자동 저장 `settleDuplicate` 도 · #1839 세션 고정 대신 account-epoch 임대)는 7차 게이트가 판단하도록 발주서에 명시. 남은 것: 토큰 교체 ↔ 전환 알림 사이 틈(#1839 세션 계약 몫) · cold 겹친 삭제(멱등). → **7차 재게이트 두 레인**(`regate-1814-*-r7`).
26.09.20 03:18 · **[#1841 1차 게이트 → r35]** 생성물(`ctx_816458e9a9d6`) **BLOCK H1 M1**: LA-1841-1(H) 로컬 로그의 "다른 포인터 없음"을 소유권 증명으로 써서, 수정 전에 잘못 적힌 중복 항목 E2 만 남고 진짜 주인 E1 이 50건 상한으로 밀렸거나 다른 기기에 있으면 E2 철회가 E1 의 행을 지움(#1839 뒤엔 원문까지) · LA-1841-2(M) 판정 → 삭제 → 로그 제거가 원자적이지 않아 두 탭 동시 철회가 행을 남기고 포인터를 다 없앰. 인가(`ctx_83fb52e17f81`) **PASS WITH CONCERNS M2 L1**: LZ-1841-1 = LA-1841-2 · LZ-1841-2(M) 웹 로그 읽기 실패가 빈 목록(`history.ts:159-170`)이 되어 공유 행을 지움 · LZ-1841-3(L) 공유 원문을 남겨도 "원본 제거" 안내. 해시 핀 재고정 · 로케일 · #1839 세션 고정 접점은 두 레인 모두 문제없음. Simon 규칙대로 → r35-fix1841b(`ctx_bc6a81daeda3` · `claude-opus-5` ultracode): 소유권은 로컬 참조 수 대신 (a) 새 항목의 소유 증명 + 옛 항목 fail-closed 또는 (b) `sources.captured_at`(DEFAULT now()) 과 항목 시각 대조 중 택해 이유 · 옛 철회 영향을 적게 · 한 소유자 범위의 원자 철회(런타임 직렬화 + 탭 간 잠금) · 철회용 authoritative 읽기(실패를 빈 로그와 구별) · 안내 사실화. 뒤집는 조건: (a)(b) 모두 정당한 옛 철회를 넓게 막으면 Simon 에게 트레이드오프로 올림.
26.09.20 03:29 · **[#1814 7차 생성물 PASS · r34 수확 → #1842 첫 게이트]** #1814 7차 생성물(`ctx_6c16d24a6019`) **PASS(0/0/0/0)** — 6차 두 건 probe A~D 로 닫힘 · '지운 수 > 0 = 사실' 규칙 · epoch 임대의 #1839 잔여 · cold/줄 삭제 겹침 · `not_deleted` 문구를 반증 검토. 인가 7차 대기. r34-fix-migrate-fence(`ctx_529e5acab8b8`): draft PR **#1842**(`df1b9827` · 3파일 +338/−3 · 제품 1) — `migrate()` 가 raw 쓰기 직전마다 은퇴 검사 · 은퇴한 런타임은 이관을 시작하지 않음(ready · 잠금 불변 · M 직렬화 유지) · 게이트 프로브 순서가 main 에서 빨강(옛 세션 게시) → 초록 · 대조 3 + #1840 스위트 8 계속 초록 · 변이 6 · verify 0(760 · 9,538). → **#1842 첫 게이트 두 레인**. 워커 스윕의 같은 계열 후보(`recovery-proof-store.ts:309-313` 네이티브 직접 쓰기 · `import-pending.ts` runImport 늦은 `replacePendingCaptures` — 폐기 전 목록이 새 저장소에 되살아날 수 있음, medium~low 추정)는 공통 설계(읽을 때 저장소 epoch · 쓸 때 대조)가 필요해 후속 항목으로 둔다.
26.09.20 03:35 · **[main 에뮬레이터 스모크 통과 · #1814 7차 판정 → r36]** 스모크(03:31~03:33 · 제 AVD Pixel_9_Pro_XL · emulator-5556 창 없이 · Simon 의 5554 무접촉): x86_64 진단 빌드 main **`c423ba88`**(#1833 · #1835 · #1837 · #1838 · #1840 포함 · 실행 35459916943 · 71,406,761 B)를 기존 데이터 유지한 채 `install -r` → 실행 1 + 강제 종료 뒤 재실행 3 = **4회 모두 MainActivity · Loading 없음 · 세션 유지(소개 화면)** · `FATAL EXCEPTION` 0 · 인증 경고 표식(bootstrap-marker-read · fail-closed · storage_recovery · JWT issued at future) 0 · 캡처 `E:/Coding Infra/reports/qa-260919/main-c423ba88-launch{1..4}.png` · 에뮬레이터는 끔. #1814 7차 인가(`ctx_de311be70769`) PASS WITH CONCERNS **C0 H0 M1 L1**: G6Z-1814-1(M, 전체 PR 잔여 — r33 이 '멱등'이라 둔 것이 반증됨) 실행기 로드 전 cold 삭제와 로드 뒤 줄 삭제가 겹치면 같은 해시 경로에 새로 담은 S2 의 원문을 늦은 remove 가 지움 · G6Z-1814-2(L, 조건부) 전환 알림이 늦으면 B 토큰의 0행을 부재로 확정(실제 auth-js 로 알림 전 getSession=B 확인). 규칙대로 → r36-fix1814g(`ctx_d348b6d97adb` · `claude-opus-5` ultracode): 같은 자료 삭제를 공통 모듈에서 한 Promise 로 합치기(진입점 전부) · 한 건 삭제 확정을 저장 세션(사용자 + 세션 id)에 묶기(#1839 와 같은 계약 · 맞지 않으면 Low 잔여로).
26.09.20 03:41 · **[#1842 게이트 두 레인 PASS(0/0) → MERGED `c91ebcbb`(03:40:38)]** 생성물(`ctx_69ff082de9e2`) 0/0/0/0 — 검사 ↔ raw 어댑터 호출 await 틈 · 정상/중단 이관 · M 직렬화 · auth-js 테스트 계약 · 비밀 · 공급망 · 문구 확인. 인가(`ctx_a6a4a7e5bf3b`) 0/0/0/0 — base 에서 옛 세션 재저장을 실제 어댑터 + auth-js 로 재현한 뒤 HEAD 의 새 SDK · 다음 부트에서 차단 확인 · 정상 이관 · 재시도 · 중단 경계 43곳의 M/S 직렬화와 복구 검증. **KZ-1840-1 닫힘.** 머지 기차: draft 해제 → auto-merge → 1초(CLEAN). 오늘 밤 머지 16개. 남은 같은 계열 후보(`recovery-proof-store` 직접 쓰기 · `import-pending` runImport)는 공통 설계(읽을 때 저장소 epoch · 쓸 때 대조) 후속.
26.09.20 04:17 · **[r35 수확 → #1841 2차 재게이트 · 운영 ingest_log RLS 확인]** r35-fix1841b(`ctx_bc6a81daeda3`): 커밋 `e31e35c6`(force 없음 · draft) — 소유권 = 새 항목 owned 표지 · 파일 항목 원래 소유 · **옛 허브 항목은 서버 `ingest_log` 정확 중복 기록**(없으면 지움 · 1+1 이 다 보이면 올림 · 그 밖은 남기고 안내 · 읽기 실패면 지우지 않고 `revokeFailed`) · (b) captured_at 창은 시계 차 때문에 버림 · 철회 전체를 계정마다 한 임계 구역(웹 Web Lock · 네이티브 큐) · 철회용 엄격 읽기 · `ds.import.revokeKept` 5개 로케일. 재현 30/48 빨강 → 72/72 초록 · 변이 28/28 · verify 0(760 · 9,580) · CI pass. 정당한 옛 철회가 막히는 경우(같은 본문 재가져오기 · 세 번 비준 등)는 행을 남기고 안내 — 모집단은 #1612(09-05) 뒤 허브 항목뿐. **코디네이터 운영 확인(읽기 전용 SQL)**: `public.ingest_log` RLS 켜짐 · `ingest_log_select_own`(authenticated · `user_id = auth.uid()`) · `ingest_log_insert_own` · 열 `id · user_id · content_hash · stage · reason · survivor_id · dropped_at` — 워커의 운영 RLS 전제 성립. → **#1841 2차 재게이트 두 레인**. ⚠ 코디네이터 실수: 감시 스크립트 두 개가 03:38~04:13 동시에 돌았다(재무장 전 생존 확인 누락) — 피해 0, 이후 재무장 전 확인.
26.09.20 04:28 · **[r36 수확 → #1814 8차 재게이트]** r36-fix1814g(`ctx_d348b6d97adb`): 커밋 `93152b9e`(force 없음 · draft) — G6Z-1814-1(M) 같은 (계정, 행) 삭제를 공통 모듈에서 행마다 한 줄로(앞선 삭제가 다 지웠으면 요청 없이 deleted) · 실행기 진입점 셋이 `capturedSourceRemoving` 을 보고 기다리지 않고 거절(기다리면 계정 줄 멈춤 — 변이 R1 반증) · G6Z-1814-2(L) 0행 뒤 '없음' 확정만 session-mutation `*InsideMutation` 계약(M 잠금 · 상한 10초). 새 테스트 10(M0 정확히 10 빨강) · 변이 9 · probe A~D 유지 · verify 0(759 · 9,353) · CI pass. 남긴 것: 세션 계약은 0행 확인에만(요청 전부를 M 에 넣으면 자동 비우기가 모든 탭 로그인/로그아웃을 붙잡음) · 같은 부류 `undoWrites` · `afterFailure` 의 `sourceRowExists`. → **8차 재게이트 두 레인**.
26.09.20 04:32 · **[#1841 2차 재게이트 → r37]** 생성물(`ctx_1530b75042c5`) PASS WITH CONCERNS **C0 H0 M2 L2**: 1차 High 닫힘 · L2A-1841-1(M) Web Locks 없는 브라우저의 두 탭 철회가 다시 포인터를 다 없앰 · L2A-1841-2(M) 철회가 인증 세션 소유자에 안 묶임 · L2A-1841-3(L) 보존 기한 가드가 기기 시계 · L2A-1841-4(L) 서버 호출 기한 없음. 인가(`ctx_a74031e73207`) PASS WITH CONCERNS **C0 H0 M2 L2**(재현 · 관련 122 통과 · 반증 6): L2Z-1841-1(M) = L2A-1841-2(대기 중 계정 전환 → RLS 빈 답을 삭제 완료로) · L2Z-1841-2(M) 이력 제거와 owned 승격이 다른 저장이라 두 번째 실패를 삼키면 1+1 철회를 영영 못 끝냄 · L2Z-1841-3(L) 보존 기한 · 정리 예약 전제 · L2Z-1841-4(L) 안내 단정. 규칙대로 → r37-fix1841c(`claude-opus-5` ultracode): Web Locks 없으면 fail-closed(또는 저장소 CAS) · 철회를 main `session-mutation` 계약(기대 사용자 + 세션)에 묶기 · 제거 + 승격을 한 read-modify-write 로 · 기한 · 결과 중심 안내(5개 로케일). 코디네이터 관찰: #1841 도 회차마다 좁아지는 중(1차 H1 M2 L1 → 2차 M2 L2 · 같은 계열).
26.09.20 04:47 · **[#1814 8차 재게이트 → 클라이언트 라운드 멈춤(코디네이터 판단)]** 생성물(`ctx_2933301dc796`) PASS WITH CONCERNS **C0 H0 M2 L2**: 7차 두 건 닫힘 · G7A-1814-1(M) 지연된 전환 알림 중 `sourceRowExists` 0행을 믿어 철회 큐를 잃음 · **G7A-1814-2(M, 8차 수정의 회귀)** 10초 상한이 실제 Supabase SDK 잠금을 취소하지 못해 이후 로그인 · 로그아웃이 멈출 수 있음 · G7A-1814-3(L) 첫 삭제가 멈추면 follower · 표식이 끝없이 남음 · G7A-1814-4(L) Web Locks 없으면 process-only 잠금으로 강등. 인가(`ctx_4f62f5c1dce4`) PASS WITH CONCERNS **M1**: G7Z-1814-1 `undoWrites` · `afterFailure` 의 다른 저장 세션 부재 오판정이 철회 기록 소실 · 원문 삭제로(구 · 신 HEAD 모두 재현 — 전체 PR 잔여). **코디네이터 판단: #1814 의 클라이언트 수정 라운드를 8차에서 멈춘다** — ① 남은 발견이 전부 같은 계열(클라이언트 세션 · 소유권 경합)이라 결정 1번의 서버 조정(JWT 에 묶인 서버 트랜잭션)이 뿌리에서 닫는다 ② 마지막 수정이 새 회귀(SDK 잠금 멈춤)를 만들었다 — 회차를 더 돌리면 틈을 옮길 위험이 커졌다 ③ 어차피 G2Z-1814-2(S3) · M2 결정 없이는 머지하지 않는다 · draft 라 사용자 영향 0. 추이: 1차 H2 M3 → 2차 M5 → 3차 M2 → 4 · 5 · 6 · 7차 M1 → 8차 M3(회귀 1 포함). 보고서 결정 1번에 근거 추가. 뒤집는 조건: Simon 이 "클라이언트로 계속"을 고르면 9차는 r36 의 M 잠금 사용부터 되돌리고(G7A-1814-2) `sourceRowExists` 경로 전체에 잠금 없는 세션 대조를 건다.
26.09.20 04:51 · **[폰용 QA 빌드 공개 — 사전 릴리스 `qa-260920-c91ebcbb`(09-18 Simon 확정 "최신 상태를 APK 릴리즈 · QA용 · 공개"의 연장)]** 로컬 빌드(`E:/Coding Infra/tools/qa_apk_build.py --wt E:/2ndB/.worktrees/qa-apk-260920 --abi arm64-v8a --tag qa` · 새 워크트리에 **정션 없이 실제 설치**(npm ci) → prebuild → gradle 826초) → `2ndb-qa-c91ebcbb-arm64.apk` 66.6 MB · arm64-v8a · 번들 10.9 MB. **서명 인증서 SHA-256 `6053a4c7…` · versionCode 40 = 09-19 QA 빌드와 같음**(apksigner · aapt2 대조) → 기존 QA 앱 위에 데이터 유지한 채 덮어 설치. SHA256 `87874a35…`. 릴리스 본문에 **#1814(자동 저장 재설계)가 빠졌다**(main 미머지 · 09-19 QA 빌드엔 있었음)를 명시. 에뮬레이터용 x86_64 는 CI 진단 빌드(`reports/apk/main-c423ba88-x86_64/`) · 서명이 달라 별개. 빌드 환경 워크트리 `qa-apk-260920`(브랜치 `qa/apk-260920` · 실제 node_modules)은 다음 QA 빌드용으로 **남긴다**(STATE '남긴 것'에 기록 — 09-20 01:4x 실수 재발 방지). 뒤집는 조건: Simon 이 이전처럼 통합 브랜치(머지 대기 PR 포함) 빌드를 원하면 다시 만든다.
26.09.20 05:11 · **[r37 핀 질문 답 — 승인]** r37(`ctx_6158f245784f`) 질문: `src/screens/deepspace/__tests__/records-import-integrity.test.ts:190` 이 화면 소스에 `removeImportHistory(userId, entry.id)` 문자열을 박제 — L2Z-1841-2 수정으로 항목 제거가 `history.ts` 의 단일 read-modify-write(제거 + 승격)로 옮겨가 그 한 줄만 빨강(관련 20 스위트 227 초록). 코디네이터 답(05:1x): 테스트 상한 4 · 단언을 `withdrawImportHistoryEntry(` 호출로 옮기고 이유 주석 1줄 · **옛 문자열을 주석에 남겨 통과시키지 않음**(검사가 산문을 증거로 읽는 함정 — 워커 스스로 제안) · 새 단언은 주석이 아닌 실제 호출 줄을 잡을 것 · result.md 에 핀 이동 기록.
26.09.20 05:24 · **[r37 수확 → #1841 3차 재게이트]** r37-fix1841c: 커밋 `ad9ae087`(force 없음 · draft) — Web Locks 없으면 철회 전체 거절 · 차례 안 세션 고정 + 서버 단계마다 세션 확인(M 은 확인 순간만 — 게이트의 'M 을 철회 전체에' 권고는 #1839 삭제 도우미와 합칠 때 교착이라 따르지 않음 · 조합 테스트) · 제거 + 승격 단일 엄격 RMW · 보존 기한 서버 시계(`captured_at` × 토큰 `exp`) · 30초 기한 · 까닭별 안내(새 키 4 × 5 로케일) · 핀 이동(승인). 재현 29 + 렌더 1 빨강 → 초록(123/123) · 변이 7/7 · verify 0(760 · 9,631). → **#1841 3차 재게이트 두 레인**(게이트가 워커의 권고 불채택 근거를 판단하도록 발주서에 명시).
26.09.20 05:43 · **[#1841 3차 재게이트 통과 → MERGED `2dce7ead`(05:41) · 라운드 마감 정리]** 3차: 생성물(`ctx_4c1e7df1bd7a`) **PASS 0/0/0/0**(2차 M2 L2 · 인가 중복 지적 재현 123/123 로 닫힘) · 인가(`ctx_a176205ec0ba`) PASS WITH CONCERNS **C0 H0 M0 L1**(기존 153 테스트 + 프로브 6 · 2차 medium · 보존 · 문구 닫힘 · Low: 기한 초과 뒤 인증 잠금이 남음 → 후속). 머지 기차: draft 해제 → auto-merge → BEHIND 라 update-branch → CI 188초 → **MERGED `2dce7ead`**. **오늘 밤 머지 17개**(#1841 이 #1839 보다 먼저 — 가져오기 철회가 원문까지 지우기 전에 막음). 정리: 머지 끝난 워크트리 3개(#1840 · #1842 · #1841) 하나씩 제거(STATE '남긴 것' 대조 · 정션 해제 확인 · 공용 745 매번 · expo 로드). G14 결정 시트 `E:/Coding Infra/reports/vibe-r260919/decision-sheet-run_e3a3e38558ab.html`(항목 15 · 레인 5 — 브라우저로 열어 저장, Artifact 는 내려받기가 막혀 파일로). 남은 draft 둘(#1814 8차 · #1839 2차에서 클라이언트 라운드 멈춤)은 결정 1번 대기. 후속: KZ-계열 저장소 epoch 공통 설계 · #1841 Low(기한 뒤 인증 잠금).
26.09.20 06:00 · **[QA 산출물을 최신 main `585aac5f` 로 맞춤]** #1841 머지(`2dce7ead`)와 기록 PR #1843(`585aac5f`) 뒤에도 QA 웹 · APK 가 `c91ebcbb` 에 멈춰 있어, Simon 의 QA 가 #1841 이전 동작(가져오기 철회가 다른 가져오기의 원본까지 삭제)을 보게 될 상태였다. 두 QA 워크트리(`qa-integration-260920` · `qa-apk-260920`)를 origin/main 으로 fast-forward(둘 다 변경 0 · `app.json` · `package.json` · `android/` · `patches/` 무변경 확인) → 웹 `expo export --clear` 재빌드(347 파일 · 40MB · 새 로케일 키가 5개 언어 몫 번들에 들어간 것 확인) → 8765 서버 재기동(`entry-a8d8d24d`) → headless Chrome 으로 로그인 화면 렌더 · 콘솔 오류 0 → APK `--skip-prebuild` 재빌드(gradle 134초 · 66.6MB · arm64-v8a · 번들에 새 키 확인) → **서명 `6053a4c7…` · versionCode 40 동일**(apksigner · aapt2 대조) → 사전 릴리스 **`qa-260920-585aac5f`** 공개 · 직전 `qa-260920-c91ebcbb` 본문 맨 위에 대체 안내. 이번 커밋 자체의 에뮬레이터 확인은 하지 않았고(네이티브 무변경 · `c423ba88` 확인으로 대신) 릴리스 본문에 그렇게 적었다. 뒤집는 조건: Simon 이 '머지 대기 PR 포함 통합 빌드'를 원하면 통합 브랜치로 다시 만든다.
26.09.20 08:45 · **[Simon 확정 1/8 — 삭제 · 복구를 서버가 조정한다(S3 착수)]** #1814(자동 저장 되돌리기) · #1839(설정 삭제가 원문까지)의 클라이언트 수정 라운드를 접고 **서버 쪽 원자화**로 간다: ① 참조 확인 + 행 삭제를 한 SQL(RPC) 안에서 ② frontmatter 는 문서 통째가 아니라 **키 단위** 갱신 ③ 삭제 의도는 **대기열**(중간에 끊겨도 이어서 끝냄) ④ 업로드는 **세대(generation)** 를 달아 늦게 도착한 쓰기를 거절. 이유: 8차 · 2차까지 회차마다 같은 계열(클라이언트 세션 · 소유권 경합)의 새 medium 이 나왔고 마지막 수정은 회귀를 만들었다 — 클라이언트 표식으로는 수렴하지 않는다. 뒤집는 조건: 서버 RPC 설계가 RLS · 기존 마이그레이션(0151~0187 중 0165 만 운영 적용)과 충돌해 적용 경로가 막히면 클라이언트 최소안으로 되돌린다.
26.09.20 08:45 · **[Simon 확정 2/8 — HIBP 조회를 앱 소유 프록시로]** 유출 비밀번호 조회를 클라이언트가 직접 HIBP 에 하지 않고 **우리 엣지 함수**를 통해서 한다(응답 크기 · 시간 상한을 서버가 강제). 이유: 네이티브 전역 fetch(expo/fetch)에는 선버퍼 · 백프레셔가 없어 클라이언트에서 '헤더~첫 read' 사이를 끊을 수단이 abort 뿐이고, 그 abort 가 대기 중 read 를 깨운다는 보장이 없다(#1837 HZ-1837-1). 뒤집는 조건: 프록시가 k-익명성 접두사까지 우리 로그에 남기는 설계가 되면(=개인정보 표면 증가) 중단하고 클라이언트 기한안(#1837 현행)으로 남긴다. ⚠ 순서: **엣지 배포가 먼저, 클라이언트 플립이 나중**(배포 전에 플립하면 가입 · 비밀번호 변경이 전부 실패).
26.09.20 08:45 · **[Simon 확정 3/8 — 규칙 해석: 새로 싣는 medium 만 막는다]** "중간 등급도 머지 전에 고치고 진행해"는 **그 PR 이 새로 들여오는** medium 에 적용한다. 원래부터 있던(=main 에 이미 있는) medium 은 머지를 막지 않고 후속으로 넘긴다. 이유: 아니면 남의 결함까지 한 PR 이 떠안아 라운드가 끝나지 않는다(#1837 을 이 해석으로 머지했다). 뒤집는 조건: 후속 목록이 쌓이기만 하고 처리되지 않으면 해석을 조인다.
26.09.20 08:45 · **[Simon 확정 4/8 — 전체 삭제 뒤 남는 데이터는 서버 삭제 정책으로]** 계정 전체 삭제가 personas 등을 남기고 chat_usage 삭제가 0행이던 것을 **클라이언트 삭제 목록이 아니라 서버 정책**(단일 삭제 RPC + 테이블 등록부)으로 닫는다. 이유: 표를 클라이언트가 들고 있으면 테이블이 늘 때마다 조용히 빠진다. **결정 1과 같은 설계에 넣는다**(따로 만들면 마이그레이션이 두 벌). 뒤집는 조건: 없음 — 범위만 조정.
26.09.20 08:45 · **[Simon 확정 5/8 — 이미 남은 원문은 폐기]** 운영에 남아 있는 raw-clippings 원문(객체 3 · 고아 0)을 폐기한다. ⚠ **선행조건: 백업.** `db-backup.yml` 이 **09-14~09-19 6회 연속 실패**(환경 비밀값 `BACKUP_PGDUMP_*` 빈 값, 오늘 08:0x 실측)라 지금은 되돌릴 수단이 없다. 그래서 **비밀값 복구 → 백업 1회 성공 확인 → 폐기** 순서로만 집행한다. 집행 주체도 이 세션이 아니다(운영은 읽기 전용) — §4 카드 또는 보안 담당 트랙. 뒤집는 조건: 폐기 대상이 다른 기능에서 참조되는 것이 확인되면 중단.
26.09.20 08:45 · **[Simon 확정 6/8 — PostgREST 는 버전 확인 후 낮으면 업그레이드]** `JWT issued at future`(시각 캐시) 수정본이 들어왔는지 **대시보드 Infrastructure 에서 버전을 먼저 확인**하고, 14.18 미만이면 업그레이드한다. 이유: 로그 문구 검색은 거짓 0 을 준다(401 로 세야 한다). 확인은 GUI 라 Simon 카드. 뒤집는 조건: 업그레이드가 다른 확장 · 함수와 충돌하면 보류하고 사건번호로 추적.
26.09.20 08:45 · **[Simon 확정 7/8 — Paddle 웹훅 Content-Type 은 샌드박스로 1회 확인]** 1차 자료로 확정하지 못한 Paddle 의 실제 Content-Type 을 **샌드박스 웹훅 1회 발사**로 직접 관측한다. 이유: 추측으로 파서를 좁히면 운영 결제가 조용히 깨진다. 로그인 필요 → Simon 카드(관측 후 파서 고정). 뒤집는 조건: 관측값이 현행 파서와 같으면 코드 변경 없이 기록만 남긴다.
26.09.20 08:45 · **[Simon 확정 8/8 — 이월 Q-S1(출시 법역)은 이번에 답한다]** Simon: "이번엔 답한다(메모에 적음)". ⚠ **그 메모 내용이 이 세션에 닿지 않았다** — 붙여넣은 응답 블록에 메모 절이 없고, 결정 시트가 내려주는 `decisions_run_*.json` 도 다운로드 폴더에 없으며(08:4x 실측), 보고서 아티팩트에 코멘트 0건. 그래서 **Q-S1 은 답이 있다는 사실만 기록하고 내용은 비워 둔다**(이월 3회차 규칙의 "기본값으로 진행"을 적용하지 않는다 — 답이 존재하므로). 다음 턴에 한 줄 받는다. 뒤집는 조건: 없음.
26.09.20 16:45 · **[운영 실측 — public 표 56/68 이 anon · authenticated 에 TRUNCATE 를 열어 두고 있다(보안 트랙으로 넘김)]** r42 워커가 "0179 의 권한 사후조건이 CI 스텁의 결핍 덕분에 초록"이라고 범위 밖 보고를 해서 코디네이터가 운영을 직접 쟀다(읽기 전용 SELECT). 결과: `public` 실표 68개 중 **56개가 anon 과 authenticated 양쪽에 TRUNCATE**, 50개가 anon 에 DELETE 를 준다(`relacl` = `anon=arwdDxtm/postgres` — Supabase `ALTER DEFAULT PRIVILEGES` 의 기본값이고, 저장소가 이미 함수 쪽에서 겪은 그 함정이다). RLS 는 68개 전부 켜져 있지만 **TRUNCATE 는 RLS 를 우회한다.** ⚠ 다만 **도달 경로는 확인되지 않았다**: `prosrc` 에 truncate 를 담은 public 함수 **0건**이고 PostgREST 에는 TRUNCATE 동사가 없다 — 즉 지금 뚫려 있는 구멍이 아니라 **방어 깊이의 결손**이다. 또한 `{public}` 역할에 걸린 정책 10개는 전부 소유자 결속(`= auth.uid()`)이라 anon(uid NULL)은 아무 행도 만나지 못한다. 이 세션은 운영 읽기 전용이므로 **고치지 않고 보안 담당 트랙으로 넘긴다**(권한 회수는 마이그레이션이고 그 트랙 소유다). 뒤집는 조건: 도달 경로(TRUNCATE 를 실행하는 SECURITY INVOKER 함수 · 직접 DB 접속 노출)가 하나라도 발견되면 방어 깊이가 아니라 즉시 대응 대상으로 올린다.
26.09.21 00:35 · **[Simon 확정 — 이월 Q-S1(출시 법역) 답: **global**]** Simon 원문 "global로 갈꺼야". 이월 3회차였고 기본값으로 닫을 수 없던 질문이 닫혔다. 이로써 DPIA · 스토어 등록 · 빌드 8종이 "법역 미정" 때문에 막히던 상태는 해소된다. ⚠ 다만 **global 은 기존의 잠재 위험 둘을 실재 차단 요소로 바꾼다**(코디네이터가 main `409347a6` 코드로 직접 확인, 읽기 전용):
① **매핑되지 않은 나라가 한국 기준으로 떨어진다.** `jurisdictionForCountry`(`src/lib/auth/consent-age.ts:44`)는 **KR · US · EEA+UK(31개)만** 매핑하고 나머지는 `null` 을 돌려준다. 그런데 `resolveJurisdiction()` 의 미인식 폴백은 보수적 `DEFAULT`(16)가 아니라 **`"KR"`(14)** 이다. 그래서 일본 · 브라질 · 인도 · 캐나다 · 호주 등의 **14세가 한국 기준으로 자기동의**한다. 그 폴백 설계는 "KR 우선 출시" 에서는 옳았다(주석이 근거를 적어 뒀다 — 신호 실패로 가입이 막히면 안 된다) — **global 이 그 전제를 뒤집는다**(미매핑이 예외가 아니라 세계의 대부분이 된다). `DIGITAL_CONSENT_AGE` 실측: KR 14 · US 13 · EU 16 · DEFAULT 16.
② **결제에 미성년 게이트가 아예 없다.** `src/lib/billing/`(`paddle-checkout.ts` · `subscription-manage.ts`)에 `isMinor` · 연령 참조 **0건**(실측). CLAUDE.md 가 적어 둔 구멍이 그대로다 — 민법 제5조 취소권(최장 8년)은 한국 기준이고, global 이면 나라별 미성년 계약 규율이 더 붙는다.
또한 `consent-age.ts` 의 `TODO(legal)` 이 **"EU 회원국별 값과 법역 신호 자체가 비-KR 시장에 쓰이기 전에 법무 확인이 필요하다"** 고 적고 있다 — global 결정으로 그 조건이 발동한다. 뒤집는 조건: Simon 이 "우선 KR 만 열고 순차 확장" 으로 바꾸면 ①의 긴급도가 내려간다(②는 그대로 남는다).
26.09.21 02:05 · **[Simon 확정 ① 집행 완료 — PR #1847 머지 `623cb0a9`(주장을 줄이고, RPC 는 잠근 채)]** 8차까지 간 게이트가 두 레인 모두 "수정 라운드는 여기서 종료하고 Simon 의 머지 판단으로 넘긴다" 로 닫았고 Simon 이 **①**(주장을 줄이고 머지)을 골랐다. 집행(r46 · 커밋 `23eb1772`): ① `0189` 에서 `GRANT EXECUTE ... TO authenticated` 를 **없앴다** — `erase_my_data` 는 **권한 0 으로 배송**된다(REVOKE 는 유지 → `check:definer-grants` 규칙 B 가 172 마이그레이션에서 PASS). 이유는 생성물 게이트 8차 M1: 26표 순차 sweep 중 **같은 사용자의 다른 세션이 이미 지나간 표에 넣은 행이 살아남아 `status=ok` 영수증이 거짓**이 된다. 서버 울타리 · 세대(설계서 S3-C · S3-D)가 들어온 뒤에 연다. ② 회귀에 블록 (L) 신설 — 배송 상태를 **카탈로그와 '실제 거절된 호출' 양쪽**으로 먼저 단언하고, 호출이 필요한 블록만 자기 트랜잭션에서 EXECUTE 를 주고 롤백한다(0150 휴면 RPC 와 같은 모양). ③ `check-erasure-registry.ts` 머리에 **증명하는 것 / 증명하지 않는 것 / 알려진 사각 전량(A1~A10 · B1~B12 · 소유권을 열 이름으로 읽는 한계 · 소유자 열을 안 든 FK) / "THIS LIST IS NOT COMPLETE" / 초록을 삭제 인가 PASS 로 인용 금지**를 적었다. 워커가 발주서의 잘못된 가정 둘도 잡았다 — 블록 8/8c/8d 는 RPC 를 부르지 않고(직접 DELETE 관측), **호출 검사는 `authenticated` 가 public 스키마 USAGE 를 못 가진 DB 에서 GRANT 를 되살려도 42501 이라 초록**이 될 뻔했다(`has_schema_privilege` 선행 가드로 닫음). 근거: 변이 A/B/C RED · 대조군 D GREEN · verify 764/9,780 · 분류 66=26/27/13 무변경 · CI 4/4 · sql 로그 `ERASURE REGRESSION PASS erase_my_data: SHIPS LOCKED`. 뒤집는 조건: 서버 울타리(S3-C)가 들어오면 그때 GRANT 를 열고, 그 PR 에서 동시 쓰기 회귀(두 세션)를 먼저 빨갛게 만든다.
26.09.21 02:05 · **[출시 차단 실측 41행 — 막음 7 · 불명 17 · 안막음 17(r47 · astra @xhigh 읽기 전용)]** global 결정 뒤 재정렬: **L1(Q-S1 법역)은 해소**(Simon 답), **L2 DPIA 완료 · 서명**(초안 · 검토자 TBD · `:717` 이 EU/UK 노출을 차단 · 서명란 빔)와 **M2 국가 신호 · 미인식 시 KR14**(BR/null → KR14 재현)가 **필수로 승격**, **R10 운영 백업**(09-13 부터 실패)은 이미 Simon 카드. 나머지 셋(R3 운영 draft 7개 · 낡은 Edge / R4 LLM 동의 v2 / R9 Rewarded SSV)은 **그 기능을 켤 때** 조건이라 출시 자체를 막지 않는다. ⚠ 워커가 **낡은 진단 셋을 반증**했다 — "법역 판정이 항상 KR"(아님: 기기 지역을 읽고 미매핑만 KR) · "0179 필수 의존"(아님) · "스토어 자료 전무"(아님: 일부 있음). 뒤집는 조건: DPIA 검토자가 EU/UK 범위를 좁히면 L2 의 범위가 줄어든다.
26.09.21 02:05 · **[버그 인벤토리 51행 → 코디네이터 분류(r47 · sonnet @medium · 탐색 슬롯)]** main 에 있고 · 사용자에게 닿고 · Simon 결정 없이 고칠 수 있는 것 **넷**만 골라 r48 로 발주했다(#24 출처 카드가 위키 페이지로 못 감 — 코드가 스스로 적은 TODO · #32 복구 동의 뒤 "초기화 중…" 무한 대기(폐기에 시간 상한 없음) · #45 RN 기본 `Image` 2곳 · #50 Android 미디어 권한 어긋남). ⚠ #50 은 **그냥 권한을 추가하지 말라**고 못박았다 — Play 사진·동영상 권한은 별도 정책 선언이 필요해 심사에 바로 걸린다. 나머지: draft PR 안의 12건은 그 PR 들이 서버 설계 위에서 다시 만들어질 때 · Simon 결정 대기 2건(전체 삭제 잔여 · 신고 연쇄 삭제) · Android 규율 15건 이상은 **grep 후보일 뿐 미판정**이라 판정 라운드가 따로 필요하다. 뒤집는 조건: QA 에서 더 급한 증상이 나오면 순서를 바꾼다.
26.09.20 20:15 · **[기록 시각 정정 — `DECISIONS.md` 의 `26.09.21` 네 줄은 오기다]** 그 네 줄(`00:35` 1 · `02:05` 3)과 바로 위 `26.09.20 16:45` 줄은 **커밋 `9d2f1b8b`(author 2026-09-20 19:26 KST)** 안에서 쓰였다. 즉 09-21 에 일어난 일이 아니다 — 쓸 때의 시계가 약 **+6시간 40분** 앞서 있었다. 교차 확인: #1847 머지 커밋 `623cb0a9` 의 author 시각은 **09-20 19:20 KST** 인데 그 줄은 `26.09.21 02:05` 로 적혀 있다. **원문은 고치지 않는다**(append-only) — 읽는 사람은 이 줄을 보고 그 다섯 줄의 날짜를 09-20 저녁으로 읽으면 된다. 뒤집는 조건: git author 시각이 신뢰할 수 없다는 근거가 나오면. 재발 방지: 시각은 `powershell -NoProfile -Command "(Get-Date).ToString('yyyy-MM-dd HH:mm:ss')"` 실측값만 쓴다(`TZ=Asia/Seoul date` 는 이 PC 에서 UTC 를 낸다).
26.09.20 20:15 · **[R48 게이트 둘 착지 — PR #1849 는 머지 보류]** 생성물(daybreak @xhigh): **high 1 · medium 1 · low 1**. 비즈로직(astra @xhigh): **신규 critical·high·medium 0 · low 2 + 기존 관찰 1**. high(F-01)는 실제 결함이다 — 최근 200행 **밖**의 인용을 연 뒤 계정이 A→B 로 바뀌면 A 의 위키 제목·96자 snippet·태그가 B 화면에 그대로 남는다(`dds-wiki-records-screens.tsx:697,708-723,733-738`; `/wiki` 에 `key={userId}` 가 없고 `linkedPage`·`honouredFocusRef` 가 소유자에 안 묶임). RLS 와 `.eq(user_id)` 는 **최초 조회만** 지킨다. Simon 규율(중간 등급도 머지 전에 고친다)대로 **고친 뒤 머지**한다. 뒤집는 조건: F-01 재현이 실패로 판명되면.
26.09.20 20:15 · **[#1847 의 "잠근 채 배송" 은 거짓이었다 — 운영 실측으로 확인]** 생성물 게이트 F-02(medium)를 코디네이터가 운영(`zoacryukmdeivmolvyhj`)에서 읽기 전용으로 대조했다: public 함수 **291개 중 242개**의 `proacl` 에 `authenticated=X` 가 명시적으로 있고, 마이그레이션이 `FROM anon` 만 REVOKE 한 함수 6개(`community_is_member`·`credit_summary_self`·`has_app_role`·`log_ai_audit`·`match_wiki_pages`·`t5_seen_aggregate`) **전부** authenticated 는 여전히 실행 가능하다. 즉 `0189:403` 의 `REVOKE ... FROM PUBLIC, anon` 은 authenticated 를 막지 못한다. **아직 살아 있는 구멍은 아니다** — 운영에 `0189` 적용 **0건**, `public.erase_my_data` **미존재**. 더 나쁜 것은 r46 이 넣은 `has_function_privilege('authenticated',...)=false` 단언이 CI scratch DB 에서 **공허하게 초록**이었다는 점이다(부트스트랩이 Supabase 의 default privilege 바닥을 안 깐다). 뒤집는 조건: 운영 ACL 재측정이 다른 값을 내면.
26.09.20 20:15 · **[r49 발주 — 코딩 워커 2 + 게이트 2]** (A) `r49-wiki-owner-fence`(claude-opus-5 @ultracode · 워크트리 `fix-bugs-260921` · PR #1849): F-01 high + OBS-01 + F-03(변이가 살아남은 공허한 테스트) + BL-01·BL-02(문서·주석 사실 오류). (B) `r49-lock-erase-authenticated`(claude-fable-5-1 @max · 워크트리 `s3-erasure-260920` · 새 브랜치 `claude/lock-erase-authenticated-260920`): 새 마이그레이션 **0190** 으로 authenticated REVOKE + **scratch DB 에 default privilege 바닥을 깔아 단언을 공허하지 않게** 만들고 변이 (가)(나) 출력을 증거로 낸다. **0189 는 고치지 않는다**(이미 머지됨 — 정정은 새 번호로). 쿼터는 이 orca 판에 조회 명령이 없어 **미확인**으로 두고 1순위 유지; 대신 G12(실호출)는 네 레인이 19:54~19:58 에 산출물을 내 충족했다. 뒤집는 조건: 0190 대신 0189 수정이 안전하다는 근거가 나오면.
26.09.20 20:15 · **[법역 결정 시트 발행 — Simon 결정 4건 대기]** global 확정 뒤 남은 값 하나(미인식·미매핑 폴백)와 셋. r48 조사(11개 시장 + EU 31개국 1차 원문 · 371줄)가 코드 주석 둘을 뒤집었다: **"DEFAULT 16 이 가장 보수적" 은 거짓**(인도 18 · 인도네시아 17), **Paddle 구매자 약관에 연령 조항 0건**(원문 24,254자 기계 검색 · 환불·차지백은 MSA 10.4 상 우리 부담). 코디네이터 추천: 결정1 = (가) 16(한 줄 · 되돌리기 쉬움 · 판독 불가 구멍을 완전히 닫음), 목적지는 (다). **(나) 매핑 확대 단독은 반대** — 지역이 안 읽히는 경우는 매핑으로 못 고친다. 이건 결정이 아니라 **대화 재료**다(Simon 확정 전까지 결정으로 기록하지 않는다).
26.09.20 20:25 · **[r47 · r48 상충 해소 — 미성년 결제 고지는 있다. 다만 나이가 어긋난다]** r48 은 "결제 경로에 나이 참조 0건", r47 은 "고지가 있다" 로 갈렸다. 코디네이터가 직접 확인: **둘 다 맞다.** r48 이 본 파일 넷(`src/lib/billing/` · `src/lib/payments/` · `src/app/plans.tsx` · `src/app/subscription.tsx`)에는 정말 0건인데, **실제 결제 화면은 `src/screens/deepspace/dds-plans-screen.tsx` 이고 `:799` 가 `minorPurchaseNotice` 를 그린다.** 그리고 거기 **확정된 결함**이 있다 — 문구는 "만 19세 미만"(`locales/ko/deepspace.json:830` · en · es 동일)인데 표시 조건은 `ADULT_AGE = 18`(`src/lib/auth/useSignUpForm.ts:49`) 이라 **만 18세는 이 고지를 영영 못 본다.** 한국 민법상 성년은 만 19세라 그 사람의 결제야말로 취소 대상이다. **이건 결정이 아니라 버그라 결정 2 의 강도와 무관하게 고친다.** ⚠ global 이라 "성년"도 나라마다 다르다(한국 19 · 일본 · 독일 · 대만 18 · 인도네시아 21) — 고지 대상 연령은 **결정 1 의 법역 신호와 같은 입력**을 써야 하므로 그 답이 나온 뒤에 붙이는 편이 낫다. 뒤집는 조건: `dds-plans-screen.tsx` 가 실제 결제 화면이 아니라는 근거가 나오면.
26.09.20 20:25 · **[법역 결정 시트 정정본 발송]** 첫 판이 결정 2 를 "나이 참조 0건" 으로 적어 **틀렸다.** 정정본은 위 상충 해소를 담고, 결정 2 에 선택지 (D) "고지 연령 불일치만 고치고 강도는 나중에"를 추가했다(추천). 99.7KB · 외부 참조 0 · 라이트/다크 헤드리스 확인. **이 시트의 내용은 결정이 아니라 대화 재료다** — Simon 이 「확정」을 누르기 전까지 결정으로 기록하지 않는다.
26.09.20 21:13 · **[r49 수정 라운드 완주 — PR 둘, 둘 다 draft · CI 초록]** (A) **#1849** `e8784e40` 7파일: R48 발견 넷을 닫았다. ⚠ **F-01(high)에서 두 게이트가 정반대로 판정했고 둘 다 부분적으로 옳았다** — 생성물은 화면 **본체 로직**(본체만 떼어 A→B 를 먹이면 `listedPages=['a-out','b-in']` 로 재현), 비즈로직은 **배송된 트리**(`_layout.tsx` 의 `AccountScope` 가 계정 epoch 마다 전 제품 화면을 리마운트해 그 상태가 B 에게 읽히기 전 폐기된다). **오늘의 라이브 유출은 아니다** — 장부에 "유출을 고쳤다"로 적지 말 것. 다만 그 껍데기 방어는 **강제되지 않는다**(`<Stack.Screen layout>`/`<Stack.Group>` 로 우회해도 `account-scope.test.ts` 는 네비게이터 문자열만 본다). 워커가 최소 패치를 넘어 `pages`/`edges` 까지 울타리를 넓혔다(계정이 바뎀도 이전 계정의 200행 전체를 그리고 있었다). 변이 7건을 **짝으로** 보고했고 바이트 델타로 no-op 이 아님을 증명했다. ⚠ 로컬 `verify` 는 하네스 메모리 부족으로 **강제 종료**돼 결론을 못 냈고, 코디네이터 지시로 **CI 를 정본 게이트**로 삼았다(4종 초록). 뒤집는 조건: 껍데기 리마운트가 실제로는 안 도는 경로가 나오면(그때는 라이브 결함이 된다).
26.09.20 21:13 · **[r49 (B) — #1851 `c3f5ce5d` · 공허하던 검사에 바닥을 깔았다]** 새 마이그레이션 **0190** 이 `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated` 하고 **적용 시점에 스스로 끝 상태를 확인하는 `DO` 블록**을 둔다(클라이언트 역할 중 하나라도 남으면 마이그레이션이 예외로 멈춘다). `0189` 는 고치지 않았다. `service_role` 은 의도적으로 안 걷고 주장을 **"아무에게도 없다"가 아니라 "클라이언트 역할에게 없다"**로 좁혔다. 바닥은 세 겹이다: ① `supabase-dry-run.yml` Seed 끝(첫 마이그레이션 **전**)에 `ALTER DEFAULT PRIVILEGES ... GRANT EXECUTE ON FUNCTIONS TO anon, authenticated` ② 회귀 블록 (L) 이 프로브 함수의 `aclexplode(proacl)` 로 **바닥이 실재함을 먼저 증명**한 뒤에만 결론 ③ 정적 핀. **변이 8변종을 임시 PostgreSQL 18.3 클러스터에서 돌린 출력 전문**이 보고서에 있다 — 핵심 한 줄: **바닥 OFF + REVOKE 제거 → 초록이고 `SHIPS LOCKED` 를 인쇄한다(= main 의 오늘 상태)**. 뒤집는 조건: 바닥을 깔은 것이 다른 레인을 깨뜨리는 것이 드러나면.
26.09.20 21:13 · **[r49 이 새로 찾은 미수정 둘 — 보안 트랙에 넘길 것]** ① `rollback/0189_down.sql` 이 ledger 에서 **자기 행만** 지우므로, 되돌린 뒤 `db push` 는 0189 만 다시 적용하고 0190 은 "이미 적용됨"으로 건너뛰어 **구멍이 되살아난다**(추론 · 미검증). ② **`0189` 와 `0190` 은 반드시 함께 적용한다 — 0189 만 올리는 순간이 곷 F-02 다.** 운영 번들에 timestamp version 을 매할 때 둘을 연속 번호로 묶을 것.
26.09.20 21:57 · **[r49 게이트 둘 착지 — 신규 medium 이상 0 · 둘 다 독립적으로 같은 결론]** 비즈로직(astra @xhigh): 신규 critical·high·medium **0** · 신규 low 1(A-N01). 생성물(daybreak @xhigh): 판정 `HOLD` · 신규 low 1(A-NEW-01 · 같은 것) + 기존 medium 2. **두 게이트가 각자 임시 PostgreSQL 18.3 클러스터를 세워 같은 것을 재현했다** — 변이 (가)와 B-K01 둘 다. A 의 수정 다섯은 실행으로 닫힘 확인(owner 비교 제거 변이 실패 · 정상 이동 보존). 비즈로직 게이트도 **F-01 을 "배송 트리에서 유출됐다"로 판정하지 않는다**고 명시했다 — 코디네이터의 정정과 같다.
26.09.20 21:57 · **[PR #1849 · #1851 머지 판정 — 머진다]** 근거 셋: ① Simon 확정 3번(새로 싱는 medium 만 막는다)에 따라 기존 계열 medium 2건(B-EX-01 rollback ledger · B-EX-02 적용 원자성)은 머지를 막지 않는다 ② 둘 다 **운영에 아직 적용되지 않은** 마이그레이션의 성질이다(0189·0190 모두 미적용) ③ **`erase_my_data` 를 부르는 클라이언트 코드가 `src/`·`supabase/` 에 0건**이라 B-EX-02 의 배포 중 창은 손으로 만든 PostgREST 호출로만 닿고 그것도 자기 데이터만 지운다. 뒤집는 조건: 클라이언트가 이 RPC 를 부르기 시작하면 B-EX-02 는 즉시 medium 이 아니라 머지 차단 사유가 된다.
26.09.20 21:57 · **[다음 라운드 묶음 다섯 (r50 후보)]** ① **B-EX-01** `rollback/0189_down.sql` 이 ledger 에서 `erasure_registry` 와 `lock_erase_my_data_authenticated` 두 행을 **같은 트랜잭션에서** 지우게 하고 "0189+0190 적용 → down → push" 회귀를 넣는다(medium) ② **A-NEW-01** `ANDROID_QA_GUIDELINES.md:38-39` 를 "Android 차단은 `cameraPermission`·`microphonePermission` 의 false 만 · `photosPermission` 은 false 포함 iOS 전용"으로 좁힌다(low · 이 문서는 연속 두 번 틀렸다) ③ **A-EX-01** `<Stack.Screen layout>`·`<Stack.Group>` 로 `AccountScope` 를 우회해도 울지 않는 것을 AST·실행 수준 검사로 닫는다 ④ `DeepSpaceDesignScreens.tsx:219` 의 **`useWikiGraphData` 사본**에 같은 울타리(두 화면이 쓴다) ⑤ `storage-recovery.ts:12` 의 법무 인용 오기 정정(실제 인용 대상은 `AuthContext.tsx:53·153`).
26.09.20 21:57 · **[보안 담당 트랙으로 넘길 것 — B-EX-02 적용 원자성]** Supabase CLI 2.116.0 은 **파일마다 각각 트랜잭션**을 돌리고 바깥 트랜잭션이 없다. 그래서 **"0189 와 0190 을 함께 넘기면 된다" 는 충분하지 않다** — 게이트 둘이 각각 실행으로 보였다(0189 커밋 직후 `erase_my_data('content') → status=ok`, 0190 뒤 `permission denied`). 처방 후보: ① 두 파일을 **한 psql 세션의 한 트랜잭션**으로 수동 적용 ② 적용 직전에 `ALTER DEFAULT PRIVILEGES ... REVOKE EXECUTE ON FUNCTIONS` 로 바닥을 잠시 걷었다가 복원. 생성물 게이트는 세번째로 **0189:403 에도 authenticated 를 추가**하는 안을 제안했으나 그건 머지된 마이그레이션 수정이라 **Simon 확인 없이 하지 않는다.**
26.09.21 00:17 · **[Simon 목표 설정 — 마지막까지 가는 한 줄]** 원문: *"/vibe /vibe-bot /persona-simulation 디버깅과 QA 및 남은 작업에 대해 개선하고 최선으로 구현을 진행하자. 다 진행하고 나서는 Local host 업데이트, 깃허브머지 및 릴리즈를 통한 abb, apk, ips 업로드 까지 진행해줘."* 순서를 이렇게 잡았다: ① r52 게이트 medium 2 수정 ② #1853·#1854 머지 ③ r51 국가별 연령표 → 법역 코드 ④ QA(persona-simulation 포함) ⑤ 로컬호스트 재빌드 ⑥ `chore(release): cut X.Y.Z` → android-release + eas-ios-build → github-release. **⑥은 유료 EAS 빌드를 쓴다** — Simon 목표문이 명시했으므로 그것을 인가로 본다. ⚠ "업로드"는 **GitHub Release 첨부**로 읽는다(저장소 관례 · `github-release.yml` 이 build id 를 받아 첨부). **스토어 제출(Play/ASC)은 별건**이고 Simon 확인 없이 하지 않는다. 뒤집는 조건: Simon 이 "업로드"를 스토어 제출로 의도했다고 밝히면.
26.09.21 00:17 · **[릴리스 버전 — 0.8.0 → 0.9.0 으로 올린다(코디네이터 판단)]** 정식 릴리스는 `v0.8.0`(2026-09-07 · `e5c9f478`)이 마지막이다. 그 뒤 2주간 보안 저장소 · 삭제 등록부 · 계정 울타리 등 구조 변경이 들어갔으므로 minor 가 맞다. 컷 커밋은 **`app.json` 버전 한 줄 + `CHANGELOG.md`** 만 바꾼다(`e5c9f478` 실측 — versionCode 는 CI 가 올린다). 뒤집는 조건: Simon 이 다른 번호를 지정하면.
26.09.21 00:55 · **[r51 국가별 연령표 착지 — 63개국 · 값 있는 행 1차 출처 100%]** **한국은 14 가 맞다 — 이번엔 원문으로 확인했다**: 개인정보 보호법 제22조의2 제1항 「만 14세 미만」, 현행판 [시행 2026. 9. 11.] [법률 제21445호]. r48 이 법령 사이트 JS 렌더링 때문에 못 했던 확인이다. 분포: 13세 12개국 · **14세 10개국(KR 포함)** · 15세 6 · 16세 12 · 18세 22 · **20세 1(태국)**. 근거 유형: 법률 조문 45 · 감독기관 지침 3 · **민법 행위능력 15**(개인정보법에 숫자가 없어 성년으로 돌아가는 보수적 해석값). ⚠ **r48 의 "조사 범위에서 18 을 넘는 나라는 없었다"가 반증됐다** — 태국 PDPA 제20조가 민상법 성년(만 20세)에 묶는다. 내가 Simon 결정 시트에 그 문장을 실었으므로 함께 정정한다. 산출: `r51-age-table/age-table.json`(기계 판독본 · `effectiveAge` 가 서버 하한 14 를 이미 반영) · `findings.md` · `rows-detail.md` · `evidence-cited.zip`. 뒤집는 조건: 인용한 조문이 개정되면(표에 `watch` 플래그가 붙은 행부터).
26.09.21 00:55 · **[폴백 18 로 간다 — 코디네이터 판단, Simon 확정 아님]** 근거는 셈이다: 폴백 16 이면 표에서 그보다 높은 행이 **23개**(인도·브라질·멕시코·태국…), 18 이면 **1개**(태국 20), 20 이면 거의 모든 나라의 성인 18~19세를 판독 불가라는 이유로 막는다. Simon 이 "일관 14세는 안 된다"고 했으므로 14 는 후보가 아니다. ⚠ **숫자로는 "지역 판독 불가"를 못 닫는다** — 그 사람이 어느 나라인지 모르기 때문이다. 그걸 닫는 것은 **가입할 때 거주국을 묻는 것**이고 그건 후속이다. **지금 폴백 18 만 넣으면 웹의 14~17세가 막힌다.** 그런데 이 앱은 어느 스토어에도 출시된 적이 없고 웹도 사실상 사용자가 없어 지금은 실해가 없다 — **출시 전에 거주국 질문을 붙여야 한다.** 뒤집는 조건: Simon 이 다른 값을 고르거나, 거주국 질문을 먼저 넣기로 하면.
26.09.21 00:55 · **[r52 착지 — r50 게이트의 medium 2 를 닫았다]** (A) #1854 `97340ee6` · CI 4종 초록. 워커가 **게이트 제안을 그대로 따르지 않았다**: "`Stack.Screen`/`Stack.Group` 의 모든 `JsxSpreadAttribute` 거부"는 spread 자체를 죄로 만들므로, **위험한 prop 을 싣는 spread 만** 잡는 스캔으로 바꾸고 이유를 적었다(변이 6개 양방향 — spread 변이 빨강 · 정상 spread 초록). ⚠ **워커가 스스로 한계를 보고했다**: 새 describe 다섯 중 **둘만** 실제로 감지기를 검사한다(나머지는 clean-route 기대값이 `[]` 라 아무것도 못 보는 스캔도 통과한다) — 그 사실을 파일 주석에 적었다. ⚠ **r50 이 원래 맞던 인용 2건을 깨뜨렸고 r52 가 되돌렸다**(`GEMINI-RETIREMENT-INVENTORY.md:158,159`). 그리고 **게이트가 준 좌표 하나도 틀렸다**(`FIDELITY_AUDIT.md:453` `/ops`). (B) #1853 은 같은 라운드에서 fingerprint 보강 중.
26.09.21 01:28 · **[r52 수정 착지 — 두 워커가 각각 게이트를 정정했다]** (A) **#1854** `97340ee6`. 게이트 제안("`Stack.Screen`/`Stack.Group` 의 모든 `JsxSpreadAttribute` 거부")을 **따르지 않고** **위험한 prop 을 싣는 spread 만** 잡는 스캔으로 바꿨다 — spread 자체를 죄로 만들면 안 되기 때문이다. 변이 6개 양방향. ⚠ 워커가 **자기 한계를 스스로 보고**했다: 새 describe 다섯 중 **둘만** 감지기를 검사하고 나머지는 clean-route 기대값이 `[]` 라 아무것도 못 보는 스캔도 통과한다 — 그 사실을 파일 주석에 적었다. ⚠ **r50 이 원래 맞던 인용 2건을 깨뜨렸고 r52 가 역사 좌표로 되돌렸다**(`GEMINI-RETIREMENT-INVENTORY.md:158,159`). **게이트가 준 좌표 하나도 틀렸다**(`FIDELITY_AUDIT.md:453` `/ops`). 숫자가 아니라 **내용 앵커를 검사하는 테스트**를 신설해 연쇄를 끊었다. (B) **#1853** `bc303d75` · 파일 3개. 옛 fingerprint 는 **22종 중 12종에 눈이 멀어** 있었다(실측). 정적 규칙(실행되는 SQL 로 두 객체를 부르는 마이그레이션은 rollback 삭제 집합에 있어야 한다) + fingerprint 확대 두 겹. ⚠ 워커가 **게이트 진술 하나를 실측으로 정정**했다 — "옛 fingerprint 가 `ADD COLUMN` 을 못 본다"는 틀렸고 실제로는 본다(rc=1). **발견 자체는 유효**하다(못 보는 12종 중 CHECK 제약으로 같은 시나리오가 성립). 자기 초안 결함 3건도 돌려 봐서 잡았다.
26.09.21 01:28 · **[r53 법역 표 착지 — PR #1855 · 3덩어리에서 63개국으로]** 커밋 `f4b4882b` · **11파일 · +881/-238** · `verify` 종료코드 **0**(766 suites / 9,827 tests) · CI 3종 초록 · 변이 3건 전부 빨강. **`src/lib/auth/consent-age-table.ts` 는 생성물이다** — 첫 줄이 `// 생성물 — 손으로 고치지 말 것`이고 원본 `age-table.json` 의 sha256 `f0967a0f…` · 생성 스크립트 경로 · 생성 시각이 머리에 박혀 있다(§0-2). 생성기가 원본을 먼저 검사한다(63행 · cc 중복 없음 · 18 미만 행의 1차 근거 · `effectiveAge = max(age, 14)`). **두 값을 갈라 뒀다**: 법이 말하는 `age` 와 서버 하한이 반영된 `effectiveAge` — 서버가 바뀌면 법값이 살아난다. **경우 셋(판독 불가 / 표에 없음 / 표에 있음)을 같은 코드 경로로 뭉개지 않았다** — 판독 불가만 거주국 자기신고로 구제할 수 있기 때문이다. ⚠ **파일 11개는 상한 8 초과**인데 계획한 8개는 그대로 8개고, 늘어난 셋은 **이 변경이 깨뜨려서 고친 것**이다: 지역 목이 14 를 전제하던 테스트 둘(`dob.test.ts` · `ensureUserProfile.test.ts`)과 **DPIA 앵커 표**(`dpia-crisis-rail-anchors.test.ts`). 12번째가 될 뻔한 것은 파일을 안 늘리고 막았다. 뒤집는 조건: 게이트가 신규 medium 이상을 내면.
26.09.21 03:10 · **[약관 본문 변경을 되돌렸다 — 판본을 못 올리는 라운드에서는 본문도 안 바꾼다(코디네이터 판단)]** #1855 재게이트 **둘이 각자** 같은 결함을 잡았다(생성물 F-01 · 비즈로직 R56-BIZ-01): 약관 제4조 ② 를 국가별 하한으로 바꾸면서 시행일 · 최종 개정일 · `TERMS_VERSION = "2026-08-16"` 을 그대로 뒀다 → **내용이 다른 두 약관이 한 판본으로 식별된다.** 판본을 올리려면 signup revision + 서버 허용 튜플(`0150` `email-v3`) 마이그레이션이 함께 움직여야 하는데 이번 라운드는 마이그레이션 금지였다. 선택지는 둘뿐이었다 — (가) 틀린 본문을 판본과 함께 유지 (나) 맞는 본문을 판본 없이 출하. **(가) 를 골랐다**: 이 앱은 어느 스토어에도 출시된 적이 없어 지금 약관에 동의하는 사람이 사실상 없고, 원장의 식별 가능성은 한 번 깨지면 되돌릴 수 없다. 되돌린 것은 세 표현(정본 md · 인앱 스냅샷 · 생성 HTML)의 같은 조문 하나뿐. 뒤집는 조건: 약관 판본 + 서버 계약 마이그레이션을 한 단위로 묶는 후속 라운드가 열리면(그때 `consent.json:29,37` 도 같이).
26.09.21 03:10 · **[게이트가 내 전제 하나를 정정했다 — `CONSENT_VERSION` 을 바꿔도 기존 가입자 재고지는 자동으로 안 걸린다]** 나는 `consent.json` 의 연령 밴드 문구를 미룬 이유로 *"CONSENT_VERSION 을 건드리면 기존 가입자 재고지가 걸린다"* 를 들었다. **코드와 다르다** — `src/lib/supabase/consent.ts:55-58,84-85` 가 재동의 흐름이 없음을 명시한다. 그 상수는 원장 쓰기와 검사에만 쓰인다. **새 판본 기록과 기존 가입자 재고지는 별개 문제**다. 미루는 판단 자체는 유지한다(서버 계약 튜플 때문) — 다만 **이유가 달랐다.** 뒤집는 조건: 없음(사실 정정).
26.09.21 03:10 · **[#1855 착지 — 재게이트 신규 medium 3 · low 2 를 닫고 머지(`225cf7eb`)]** 런타임 판정 로직(`consent-age.ts` · `consent-age-table.ts`)은 **두 라운드 모두 한 글자도 안 바뀌었다** — 게이트가 통과시킨 그대로다. 닫은 것: 약관 판본(위) · **접근성 검사가 실제 바인딩을 안 봤다**(게이트가 변이로 증명 — `BirthDateField.tsx:57` hint 를 `passwordHint` 로 바꿔도 10/10 초록. 렌더러가 없어 JSX 속성 AST + 실제 `initI18n()` 두 쪽을 한 it 에 묶었다) · **DPIA `:598` 에 새 법적 결론이 섞였다**(빼고 `[COUNSEL TO CONFIRM]` 질문으로) · README 자가당착 · **US 행 누락으로 표값 13 → 실효 14 경계가 안 걸렸다**. 변이 2건으로 공허하지 않음 증명(sha256 동일 복구). ⚠ **발주 때 적은 "실효 8파일" 주장은 철회한다** — 게이트 실측으로 재수정만 21파일이었다. 최종 27파일.
26.09.21 03:20 · **[#1853 세 번째 라운드 유통기한 발동 — 규칙을 더 깁지 말고 줄인다]** 내가 r55 발주 때 *"또 새 계열 medium 이 나오면 규칙을 걷어내고 DB 검사만 남기는 쪽이 맞는지 판정에 포함하라"* 를 걸어 뒀고, **나왔다.** 게이트 둘이 각자 판정을 내고 같은 방향으로 수렴했다 — 생성물: *"'10자 reason 이 c_names 를 쓰레기통이 되지 않게 막는다'는 장치는 직접 반증됐다. 주석은 설명이지 증거가 아니다"* · 비즈로직: *"일반화된 정적 의존 추론과 수동 예외 판정 규칙은 철회하고 DB 왕복 중심으로 축소하라."* 신규 medium 2 는 **둘 다 '양쪽 게이트가 함께 초록'** 계열이었다: (가) 무관한 migration 을 `c_names` 에 넣고 재적용하면 다른 표의 값이 1→2 가 되어도 fingerprint 동일 — 게다가 테스트가 **그 허용을 못 박고 있다**(`:770-803`) (나) 소유 시퀀스의 `RESTART WITH` 상태와 **ACL** 이 fingerprint 밖(평문 GRANT 만으로 정적 검사도 통과). R57 로 발주. 뒤집는 조건: 축소 뒤에도 같은 계열이 또 나오면 DB 왕복 자체의 보장 범위를 다시 정의한다.
26.09.21 03:30 · **[0.9.0 컷 · 머지 완료(`8f7743c4` · #1857) — 전달은 GitHub Release, 스토어 아님]** v0.8.0 이후 **172개**(머지 커밋 제외) 중 **69개(40%)** 가 보안 · 법무 · 인증 · 가드(법무 28 · 보안 18 · 가드 12 · 인증 11) — 센 값이다. ⚠ **초안에 적었던 "169개 · security 16 · legal 9 · auth 9 · guards 6" 은 틀렸다**(낡은 측정). 바꾼 파일은 둘 — `app.json` version 한 줄 + `CHANGELOG.md`. `versionCode`·`buildNumber` 는 건드리지 않았다(`eas.json` `appVersionSource: remote` → EAS 소유). CHANGELOG 의 주장은 전부 커밋으로 확인했다(Android Loading `dab26472`#1833 · 출구 `e60081f7`#1835 · 인용/초기화 `3b787951`#1849 · 뮤지엄 43 `6c48f526` · 공유 카드 `8780df6f` · 마이그레이션 0171-0187 실측 17개 · 계정 울타리 `d34d0710`#1854).
26.09.21 03:33 · **[병렬 jest 에 기존 레이스가 있다 — 별도 후속]** `npm run verify` 가 두 번 연속 빨강이었고 **매번 다른 스위트**였다. 원인은 내 변경이 아니라 `scripts/__tests__/check-no-emdash.test.ts:24` 가 `src/lib/emdash-guard-probe.generated.ts` 를 만들었다 지우는 동안 `src/**` 를 훑는 다른 스위트가 그 파일을 집어 `ENOENT` 로 죽는 것이다(`canon-icon-crash.test.ts:202` · `lenses/__tests__/migration-readiness.test.ts`). `--runInBand` 는 767 suites / 9,838 tests **전부 초록**. ⚠ `src/lib/persona/__tests__/one-seven.test.ts:68` 이 **같은 함정을 이미 이름으로 제외하고 있다** — 저장소가 한 번 맞고 한 자리만 고친 것이다. 계열 수정(=`src/**` 열거자가 `*.generated.ts` 를 건너뛰게)은 별도 PR. 뒤집는 조건: 없음(실측).
26.09.26 14:02 · **[0172 운영 적용 · Simon GO "적용해."]** 코딩 LLM 이 `apply_migration(name=0172_reward_authorization_hardening)` 으로 main 파일 바이트 그대로 적용했다(원장 `20260926050258`, CR 제거 sha256 `0f0618136558bac2`). 이유: 오늘 12:15~12:16 에 0177·hardening 이 0172 없이 들어갔다(원장 밖 authenticated 회수가 hardening 전제 검사를 통과시킴). 그래서 티켓 발급 뒤 광고 동의를 끈 사용자에게도 SSV 콜백이 지급되는 상태였고, 운영-충실 로컬 재생(함수 13/13 일치)으로 확인했다. 적용 직전 운영 지문 = 13:47 기준 13/13, 적용 후 = 설계 순서 재생 13/13, 바뀐 줄 5, Edge 응답 200 유지, 권한 오류 로그 0. `REWARD_SSV_ENABLED` 는 그대로 `true`(서버 OFF)이고 `=1` 카나리아는 별도 GO. 뒤집는 조건: 0172 뒤 커뮤니티 RLS 나 보상 정산에서 권한 오류가 관측되면 직전 정의(`E:/Coding Infra/reports/vibe-r260926/r0172-prod-order/apply/`)로 앞으로 고친다(down 금지).
26.09.26 14:19 · **[`REWARD_SSV_ENABLED` = 정확히 `1` · Simon GO]** Clavius(Keys)가 실행했다(secret `updated_at` 05:19:20Z). GO source = Relay 증명 `relay/outbox/simon-go-attested-reward-ssv-enabled-1.md`(Relay turn `t170s982`) · 실행 결과 `keys/outbox/vb-simon-go-enabled-1.result.md` · Simon 이 코딩 세션에서 14:40 에 "내가 직접 go 한거야" 로 확인. 이유: 0172(14:02:58)로 동의 철회 뒤 지급 구멍을 닫은 다음이라 켤 수 있었다. 인증 없는 POST 가 401 을 받는 것으로 켜짐을 확인했고(`index.ts:243` 은 `:228` 스위치 판정 뒤), 14:29 까지 실사용 호출은 0 이다. 광고 ON · 카나리아 주체는 별도. 뒤집는 조건: 지급 이상·서명 검증 실패가 관측되면 값을 `true` 로 되돌린다(Simon GO).
26.09.26 14:47 · **[SimonK-stack #50 머지 `c4d5c95a` · Simon "반영해."]** `/vibe`·`/vibe-bot` 에 Relay 양방향 핸드셰이크(claim 20분 뒤 재촉 1회 · ALERT/CODING/NOISE · 결과 `교훈` 블록 · 두 경로 GO 는 claim 먼저 · **증명된 Simon GO 는 되묻지 않는다**)와 `bus_watch.py`(+오프라인 테스트 6)를 넣었다. 설치본에도 적용(앞머리 2.11.6/0.9.1 유지, 백업 `E:/Coding Infra/reports/vibe-r260926/skill-install-backup-144801/`). 확인: run_ci 141 PASS · selftest 93/180 · 설치본 재검사 동일. 뒤집는 조건: 협업 루프가 봇 쪽 소음이나 재촉 과다를 만들면 규칙을 줄인다.
26.09.26 23:44~23:50 · **[Simon 결정 3건 · 운영 마이그레이션]** 코딩 LLM 채팅 선택지에서: ① B-EX-02(0189·0190 사이 창) = **③ 0189:403 에 `authenticated` 를 더하는 한 줄 수정** ② 묶음 A(0191·0192·0193·0194·0197·0200) **지금 적용** ③ 묶음 B(0171·0173~0176·0178~0187, 이어서 0199) **지금 적용**(재현본 회귀 레인 없이). 이유: 로컬 운영 순서 재현본에서 계획 순서 26번 적용이 전부 성공했고, Relay 독립 검토 `vb-44a85c06` 가 ③ 에 동의했다. 뒤집는 조건: 적용 중 파일 사후 검사 실패·지문 불일치·파일 머리말의 배포 조건 미충족이면 그 파일은 멈추고 다시 묻는다.
26.09.27 00:14 · **[운영 적용 결과 — 17개 적용 · 5개 보류]** MCP `apply_migration` 파일 하나씩, 파일마다 함수 지문(시그니처·ACL·주석 뺀 본문, `collate "C"`)이 재현본과 일치: 0171 · 0173 · 0174 · 0175 · 0176 · 0178 · 0180 · 0182 · 0183 · 0184 · 0185 · 0186 · 0187 · 0191 · 0193 · 0199 · 0200. **보류**: 0179·0181(운영 감사 표에 anon·authenticated 쓰기 권한 = Supabase 표 기본 권한 → 0179 사후 검사가 실패하고 통째로 롤백됨, 걷는 쪽은 0181:36) · 0192·0194(`storage.objects` 트리거, 머리말이 배포된 Storage 리허설을 요구) · 0197(옛 paddle-webhook 과 새 SQL 은 의도적 실패 닫힘 → webhook 재배포와 한 창에서). 뒤집는 조건: 해당 결정(0179 수정 방식 · Storage 리허설 · 0197+webhook 동시 GO)이 나면 다시 적용한다. ⚠ main 의 LLM 프록시는 0181 의 `ai_audit_log.event_source` 에 쓰므로 0181 전 재배포 금지(#1869 가드는 칼럼을 보지 않는다).
26.09.27 00:22 · **[결정 ①=③ 구현 — PR #1874 (draft)]** `0189:403` 을 `FROM PUBLIC, anon, authenticated;` 로(403행 유지, 설명은 그 아래). CI 롤백 왕복 레인은 원장 규율을 지키는 레인이라 ③ 뒤 대조군이 공허해지므로 그 레인의 스테이징 사본만 ③ 이전 줄로 되돌렸다(한 줄만 바뀌었는지 검사). 이유: 적용 방식과 무관하게 함수를 만드는 파일에서 잠근다. 뒤집는 조건: CI sql 레인 빨강 또는 독립 검토(`vb-bdefb26b`) FAIL.
26.09.27 02:46 · **[Simon 결정 · 0179/ACL 재정렬 GO]** Relay 증명 GO(`relay/outbox/simon-go-attested-0179-acl-reorder-0181-before-0179.md`, ATTESTED · FINAL): "0181 을 0179 앞으로 재정렬 후 재시도". 범위는 보류분 재적용이고 0189 · 0190 · 0195 · 0198 · 0201 은 계속 제외. 이유: 옛 0179 가 운영 표 기본 권한(anon · authenticated arwdDxtm) 때문에 사후 검사에서 실패하고 통째로 롤백됐다. 뒤집는 조건: 적용 중 사후 검사 실패 · 지문 불일치면 멈추고 다시 묻는다.
26.09.27 02:54 · **[운영 적용 — 0179 · 0181, 22개 중 19개 적용]** 재정렬은 #1878(`5c95b6d6`)이 이미 회수 두 줄을 0179 검사 앞으로 옮긴 판을 그대로 썼다(새 파일 · 번호 변경 없음, 저장소 순서 0179 → 0181 유지). MCP `apply_migration` 파일 하나씩: 0179 02:52:27 지문 `2 fdfbef04…` · 0181 02:54:27 `7 53d8d5ed…` 가 재현본과 일치했고, 표 ACL · RLS · 칼럼 기본값 · 제약 · 인덱스 · 뷰 정의도 일치했다. 재현본 대조군(옛 0179)은 운영과 같은 오류로 실패해 재현본이 공허하지 않음을 확인했다. 이유: GO 범위 안에서 저장소 · CI 순서를 그대로 지킨다. ⚠ 09-07 배포 프록시는 `event_source` 를 넣지 않아 재배포 전까지 `client_unverified` 로 찍힌다(범위 CHECK 는 통과) · "0181 전 프록시 재배포 금지"는 풀렸다. 남은 보류: 0192 · 0194(Storage 리허설) · 0197(webhook 동시 GO) · 묶음 C. 뒤집는 조건: 새 경로로 들어온 첫 감사 행이 거부되거나 실사용에서 `client_audit_rate_limited` 가 나오면 되돌림을 검토한다.
26.09.27 03:56 · **[Simon 결정 · 운영 잔여분 + Edge 재배포 GO]** Relay 증명 GO(`relay/outbox/simon-go-attested-prod-mig-remaining-edge-redeploy.md`, ATTESTED · FINAL): ① 0192 · 0194(배포된 Storage 리허설 기록 확인 → 없으면 무비용 리허설 → 실패 시 멈춤) ② 0197 적용 + paddle-webhook 재배포를 한 창에서 ③ 묶음 C(0189 · 0190 · 0195 · 0198 · 0201) ④ 옛 AI 프록시 재배포(openai-proxy 는 0195 뒤 가드 통과 확인). GO 아님: EAS · 광고 · 스토어 · 라이브 게시 · 결제 · 목록 밖 Edge. 이유: 운영에 밀린 마이그레이션과 main 프록시를 정리한다. 뒤집는 조건: 파일 머리말·안전 순서 문서의 선행 조건이 GO 밖이면 그 항목은 멈추고 보고한다.
26.09.27 04:10 · **[운영 적용 — 0189 · 0190 · 0201, 원장 174행]** MCP `apply_migration` 파일 하나씩. 0189(#1874판)는 함수 지문 `1 88bc308b…`, `erase_my_data` 실행 권한은 anon · authenticated false / service_role true(0189 와 0190 사이에 틈 없음)였고, 0201 뒤 삭제 등록부 67행이 행별 `to_jsonb` 해시로 재현본과 일치했다. **멈춘 것**: 0192 · 0194(리허설 기록 없음) · 0195 · 0198(0192 전제, 재현본에서 각각 `polaris_erasure_prerequisites_missing` · `erasure_additions_owner_table_missing`) · 0197 + paddle-webhook(main webhook 의 RELEASE HOLD, docs/SESSION-OWNERSHIP.md 13단계 중 웹훅 OFF · 바인딩 시크릿 · subscription-manage 선배포 · Paddle 토큰 교체 · 공개 게시가 GO 밖). 이유: 재현본 사전 검증을 통과한 파일만 올리고, 선행 조건이 GO 밖인 것은 올리지 않는다. 뒤집는 조건: 0192 Storage 리허설 통과 → 0192 · 0194 → 0195 · 0198 → openai-proxy 순으로 재개한다.
26.09.27 06:04 · **[Edge 재배포 — claude · gemini · xai 프록시, main 711b7eaf]** 처음 두 번의 디스패치는 배포 전 게이트에서 멈췄다(배포 0). 한 번은 승인 직전 다른 문서 머지로 `checkout-is-not-current-remote-main`, 한 번은 GitHub `Production` 환경에 시크릿이 0개라 `access-token-required` 였다. Simon 이 `PRODUCTION_SUPABASE_PROJECT_REF` · `PRODUCTION_SUPABASE_ACCESS_TOKEN` 을 넣은 뒤 Relay 가 디스패치하고 Simon 이 승인한 런 36271305645 · 36271390493 · 36271394597 이 성공했다(claude v132 · gemini v152 · xai v68). 코딩 LLM 은 Simon 계정 토큰으로 대리 승인하지 않았다. 이유: 필수 검토자는 사람이 확인하라고 둔 장치다. 뒤집는 조건: Simon 이 대리 승인을 명시적으로 허락하면 그 범위에서만 한다. 남음: openai-proxy(0195 대기) · paddle-webhook(0197 · RELEASE HOLD).
