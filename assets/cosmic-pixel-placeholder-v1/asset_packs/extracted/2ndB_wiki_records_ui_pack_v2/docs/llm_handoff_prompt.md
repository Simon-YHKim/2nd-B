# LLM handoff prompt

You are continuing implementation of 2ndB. Implement the `/wiki` and records browsing surfaces using the attached `2ndB_wiki_records_ui_pack_v2` asset pack.

Keep the current data model and routes where possible. User-facing language should use:

- `/wiki` as `지식 창고`
- records as `조각 기록`
- sources/references as `참고한 조각` or `원본 조각`

The screen should not look like a plain file manager. It should feel like stored shards inside the Cosmic Pixel Graph Village design system.

Must connect to:

- `/journal` saved daily records
- `/capture` memo/link/clip/OCR/file sources
- `/imagine` saved dream shards, if available
- main graph highlight handoff
- `/jarvis` context handoff

Do not use clinical or therapy vocabulary. Do not expose internal RAG/vector terminology to users.
