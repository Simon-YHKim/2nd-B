// Legacy UI track removed (2026-06-23): the canonical deep-space surface is the
// only build. This route renders DeepSpaceReviewScreen.

import { DeepSpaceReviewScreen } from "@/screens/deepspace/DeepSpaceDesignScreens";

export default function ReviewScreen() {
  return <DeepSpaceReviewScreen />;
}
