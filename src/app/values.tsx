// 가치관 체크 (rev2 P3b): dedicated Schwartz-anchored values reflection check.
// Thin route over the shared AxisCheckScreen (deep-space lens chrome). The
// prototype's ranked values spectrum renders later, once these reflections
// accumulate as its honest signal source.
import { AxisCheckScreen } from "@/components/deep-space/AxisCheck";

export default function ValuesCheck() {
  return <AxisCheckScreen axis="values" />;
}
