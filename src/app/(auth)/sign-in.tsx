// Legacy UI track removed (2026-06-23): the canonical deep-space surface is the
// only build. This route renders DeepSpaceSignInDesignScreen.

import { DeepSpaceSignInDesignScreen } from "@/screens/deepspace/DeepSpaceDesignScreens";

export default function SignIn() {
  return <DeepSpaceSignInDesignScreen />;
}
