// Legacy UI track removed (2026-06-23): the canonical deep-space surface is the
// only build. This route renders DeepSpaceThemeScreen.

import { DeepSpaceThemeScreen } from "@/screens/deepspace/DeepSpaceDesignScreens";

export default function ThemeScreen() {
  return <DeepSpaceThemeScreen />;
}
