// /trends - 트렌드 · 관심 상승 (SCREEN_TREE_SPEC §3). Rising-interest surface
// derived from the user's own recent records. Entry: in-app flow map (hub).
import { TrendsScreen } from "@/screens/deepspace/trends/TrendsScreen";

export default function Trends() {
  return <TrendsScreen />;
}
