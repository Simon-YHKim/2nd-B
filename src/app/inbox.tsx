// Legacy UI track removed (2026-06-23): the canonical deep-space surface is the
// only build. This route renders DeepSpaceInboxScreen.

import { DeepSpaceInboxScreen } from "@/screens/deepspace/DeepSpaceDesignScreens";

export default function Inbox() {
  return <DeepSpaceInboxScreen />;
}
