// /ttfv - First-day TTFV "첫날 자기이해 한 컷" (first-day self-understanding)
// onboarding (deep-space, propose -> ratify). Reachable directly at /ttfv.
// TODO: trigger this automatically on the user's first day post-signup (the
// auto-show trigger is out of scope for this screen).
import { TTFVScreen } from "@/screens/deepspace/onboarding/TTFVScreen";

export default function Ttfv() {
  return <TTFVScreen />;
}
