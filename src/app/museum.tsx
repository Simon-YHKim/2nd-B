// /museum - AI Museum. GT7-brand-museum style: 8 category tabs + a horizontal
// card rail of milestone moments in AI history (1950 → now), deep-space cyan.
// Language follows i18n with a header toggle. New moments are added to the CATS
// array in AiMuseumScreen (one row per moment, ko/en inline).
import { AiMuseumScreen } from "@/screens/deepspace/museum/AiMuseumScreen";

export default function Museum() {
  return <AiMuseumScreen />;
}
