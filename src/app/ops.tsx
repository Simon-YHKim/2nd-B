// Legacy UI track removed (2026-06-23): the canonical deep-space surface is the
// only build. This route renders DeepSpaceOpsScreen.

import { DeepSpaceOpsScreen } from "@/screens/deepspace/DeepSpaceDesignScreens";

export default function Ops() {
  return <DeepSpaceOpsScreen />;
}
