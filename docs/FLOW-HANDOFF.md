# 2nd-B — 구조 핸드오프

> 새 세션은 **이 파일만 읽으면** 앱 구조를 안다. 2분이면 된다.
> 개별 화면을 건드릴 땐 [`docs/flow-map.json`](docs/flow-map.json) 를 조회한다(아래 §5).
> 자동 생성 — 손으로 고치지 말고 `make-handoff.js` 로 재생성할 것.

**86개 화면 · 483개 동작 · 서버/데이터 97종 · AI 14종**  
코드 좌표 986개 전부 실제 소스와 대조: **✔ 함수까지 확인 509** · **· 파일·줄만 확인 477** · ⚠ 0

### 0. 먼저 — 이 문서가 아직 맞는지 30초 안에 확인

이 지도는 커밋 `?` (+ 커밋 안 된 변경) 의 코드를 읽고 만들었다.
그 뒤로 코드가 바뀌었다면 아래 좌표들은 **틀린 채로 자신 있어 보인다.** 바로 확인할 것:

```bash
node <flow-debugger>/scripts/check-stale.js docs/flow-map.json . --strict
```
- **exit 0** — 앵커한 파일 168개가 그대로다. 이 문서를 믿고 시작해도 된다.
- **exit 1** — 바뀐 파일 목록이 그대로 출력된다. **그 화면들만** 다시 스캔하면 된다(§6 재생성).

---

## 1. 코드 만지기 전에 반드시 아는 3가지

이 셋을 모르고 시작하면 반나절을 버린다. 실제로 그렇게 됐다.

### 1. `src/app/*.tsx` 를 고치면 **화면이 안 바뀐다**

**`isDeepSpaceUI()`** (`src/lib/ui-mode.ts:36`) 가 어느 UI를 그릴지 고른다 — 라우트 4개가 이걸로 갈라진다.

사용자가 보는 건 **위임된 쪽**이다. 라우트 파일의 본문은 프로덕션에서 렌더되지 않는다.
거기를 고치면 **빌드는 초록인데 화면은 그대로**다.

**고칠 파일 찾는 법** — 화면 81개가 위임한다. 그 화면의 진짜 파일:

```bash
jq -r '.screens[] | select(.route=="/sign-in") | .rendersInProduction' docs/flow-map.json
```

### 2. 이 5개 화면은 **사용자가 못 연다**

배포판에서 열리지 않는다. **여기서 찾은 "버그"는 실사용자에게 안 보인다** — 고치기 전에 그것부터 확인할 것.
(전에 이걸 안 물어서 "저장 버튼이 가짜다, 데이터가 사라진다"는 확신에 찬 **허위 신고 4건**이 나갔다.)

| 화면 | 왜 | 근거 |
|---|---|---|
| `/deepspace-home` | 개발 전용 — 배포판에서는 열리지 않아요 | `src/app/deepspace-home.tsx:6` |
| `/deepspace-hub` | 개발 전용 — 배포판에서는 열리지 않아요 | `src/app/deepspace-hub.tsx:6` |
| `/deepspace-preview` | 개발 전용 — 배포판에서는 열리지 않아요 | `src/app/deepspace-preview.tsx:6` |
| `/deepspace-flowmap` | 개발 전용 — 배포판에서는 열리지 않아요 | `src/app/deepspace-flowmap.tsx:6` |
| `/trends` | 개발 전용 — 배포판에서는 열리지 않아요 | `src/app/trends.tsx:18` |

### 3. 겉보기와 다른 함수 — **호출 한 줄에 AI가 숨어 있다**

화면 코드엔 `createRecord(...)` 한 줄뿐인데 그 안에서 **AI를 부른다.** 화면 파일만 읽으면 절대 안 보인다.
(이걸 안 따라가서 서버 호출 66건·AI 7건이 통째로 지도에서 빠졌었다.)

**AI를 부르는 함수 26개** — 화면에 이 호출이 보이면 AI·비용·지연을 계산에 넣어라:

| 함수 | 위치 | 경유 |
|---|---|---|
| `useImportPendingCaptures()` | `src/lib/capture/use-import-pending.ts:15` | `createRecord` |
| `sendChatMessage()` | `src/lib/chat/conversation.ts:166` | `readChatUsage` |
| `retrieveChatContext()` | `src/lib/chat/rag.ts:40` | `embedTexts` |
| `buildIdenDoc()` | `src/lib/iden/build-iden.ts:257` | `buildPersona` |
| `exportIden()` | `src/lib/iden/iden-export.ts:89` | `buildIdenDoc` |
| `nextProbe()` | `src/lib/interview/probe.ts:239` | `callGemini` |
| `callGemini()` | `src/lib/llm/gemini.ts:464` | `callAdvisor` |
| `embedTexts()` | `src/lib/llm/gemini.ts:903` | 직접 |
| `transcribeAudio()` | `src/lib/llm/gemini.ts:1046` | `classifySafety` |
| `callAdvisor()` | `src/lib/llm/gemini.ts:1152` | `classifySafety` |
| `classifySafety()` | `src/lib/llm/safety.ts:254` | `insertAiAuditLog` |
| `buildOpsDailyBrief()` | `src/lib/ops/daily-brief.ts:142` | `callGemini` |

전체 26개: `jq '.aiHelpers | keys' docs/flow-map.json`

---

## 2. 앱 기능 지도

| 영역 | 화면 | 동작 | 주요 화면 |
|---|---|---|---|
| **인증·시작** | 8 | 39 | 로그인 `/sign-in` · 회원가입 `/sign-up` · 가입 마무리(생년월일·동의) `/complete-profile` · 비밀번호 재설정 `/reset-password` |
| **메인 화면** | 10 | 45 | 홈 별자리 `/` · 허브 도크 (개발용 미리보기) `/deepspace-hub` · 도메인 별 렌즈 `/star/[domain]` · 북극성 문장 `/northstar` |
| **기록·수집** | 6 | 53 | 전체 담기 `/capture-full` · 기록 보관소 `/records` · 애착 유형 `/attachment` · 담기 `/capture` |
| **설정·계정** | 13 | 65 | 설정 `/settings` · 요금제 `/plans` · 오늘의 비서 (루틴 허브) `/ops` · 내보내기 형식 `/formats` |
| **영역·사람** | 9 | 49 | 커리어 타임라인 `/career` · 나의 변화 (주간 성장) `/growth` · 커리어 드릴다운 `/career-drilldown` · 휴식 담기 `/rest` |
| **AI 비서** | 9 | 49 | 세컨비 대화 `/secondb` · 리서치 (연결 찾기) `/research` · 위키 `/wiki` · 심층 인터뷰 `/interview` |
| **검사·진단** | 9 | 60 | 성격 5요인 검사 `/big-five` · 동기 자기보고 `/motivation` · 성격 정밀검사 (120문항) `/ipip-neo` · 가치 자기보고 `/values` |
| **성장·인사이트** | 10 | 54 | AI 뮤지엄 `/museum` · 집중 타이머 `/focus` · 인사이트 `/insights` · 오늘의 정리 `/digest` |
| **가져오기·구독** | 9 | 50 | 가져오기 허브 `/import-hub` · 통화 돌아보기 (앱 안에 들어가는 길이 없는 화면) `/call-reflection` · 파일 가져오기 `/import` · 지인 응답 페이지 `/peer/[token]` |
| **구버전 화면** | 3 | 19 | 북극성 (나의 종합 요약) `/core-brain` · 내 두뇌 지도 `/graph` · 내 영역 (4영역) `/trinity` |

전체 화면 목록: `jq -r '.screens[] | "\(.groupKo)  \(.title)  \(.route)"' docs/flow-map.json`

---

## 3. 알려진 문제 41건 (검증됨)

스캔이 코드에서 확인한 결함이다. **손대기 전에 여기 있는지 먼저 본다.**

| 화면 | 안 되는 것 | 증상 | 코드 |
|---|---|---|---|
| `/star/[domain]` | 담기 버튼 누르기 | 담고 돌아왔는데 이 별의 목록에 그 기록이 없어요 (다른 별로 붙었어요) | `src/lib/records/detect-domain.ts:129` · |
| `/northstar` | 세컨비 제안 받기 | 인터넷이 끊기거나 AI가 형식에 안 맞는 답을 해도 화면은 똑같이 '기록이 부족해요'라고만 알려줘요 — 기록… | `src/lib/persona/northstar.ts:77` · |
| `/capture-full` | 4W1H·할 일·음성 담기 | 저장이 실패하면 '별가루를 담지 못했어요' 카드가 뜨고 쓴 내용은 남아 있어요 ('다시 시도'를 누르면 돼요… | `src/lib/records/create.ts:100` · |
| `/capture-full` | 음성 녹음하고 받아쓰기 | 현재 배포된 실제 앱에서는 받아쓰기가 항상 '녹음을 글로 바꾸지 못했어요…'로 끝나요. 음성 받아쓰기만 안전… | `src/lib/llm/gemini.ts:1046` · |
| `/records` | 조각 하나 열기 | 담아둔 자료를 누르면 상세 내용이 없어서 '찾을 수 없어요' 화면이 떠요 (목록에는 보이는데 열리지 않는 진… | `src/screens/deepspace/dds-wiki-records-screens.tsx:402` ✔ |
| `/record/[id]` | 조각 불러오기 | 약점: 인터넷이 끊겨서 못 불러온 것과 진짜로 지워진 것을 구분하지 않아요. 둘 다 똑같이 '찾을 수 없어요… | `src/lib/records/create.ts:332` · |
| `/record/[id]` | 검사 결과 보러 가기 | 약점: 검사 이름표가 다섯 가지(동기·강점·가치·성격·애착) 중 하나와 정확히 맞지 않으면 버튼이 아예 안 … | `src/screens/deepspace/dds-wiki-records-screens.tsx:710 (assessmentRoute)` ✔ |
| `/attachment` | 저장된 검사 결과 불러오기 (실패해도 빈 화면으로 보임) | 버그: 불러오기가 실패해도 오류 안내가 안 나오고 '아직 이 별은 어두워요' 빈 화면이 떠요. '불러오지 못… | `src/lib/persona/build.ts:232` · |
| `/attachment` | 첫 저장 뒤 세컨비 대화로 자동 이동 | 버그(앱 전용): 앱을 껐다 켜면 '처음' 표시가 초기화돼서, 이미 한 번 안내를 받은 사람도 다음 저장 때… | `src/app/attachment.tsx:227` ✔ |
| `/manual` | 화면 열기 | 검색창을 눌러도 글자가 입력되지 않아요 (원래 동작하지 않는 장식이에요) | `src/screens/deepspace/DeepSpaceDesignScreens.tsx:1074` ✔ |
| `/formats` | 원본 기록 포함 스위치 | 실제 결함: .iden / PDF / JSON을 고른 상태에서 이 스위치를 켜도 아무 차이가 없어요. 코드가… | `src/lib/wiki/export.ts:219 (includeRecords)` ✔ |
| `/formats` | 내보내기 실행 | 실패하면 '내보내기를 만들지 못했어요. 다시 시도해 주세요' 문구가 뜨고 미리보기는 안 나와요 | `src/lib/iden/iden-export.ts:89` · |
| `/career-drilldown` | 드릴다운 제출하기 | 실제 결함: 저장이 실패해도 오류 안내 없이 화면은 그냥 세컨비로 넘어가서, 사용자가 기록이 날아간 걸 모른… | `src/lib/records/create.ts:100` · |
| `/ledger` | 화면 열기 (이번 달·지난 달 내역 불러오기) | 인터넷이 끊기면 '잠시 불러오지 못했어요' 안내와 다시 시도 버튼이 떠요 (이번 달 불러오기가 실패한 경우에… | `src/lib/finance/ledger.ts:127` · |
| `/ledger` | 빠른 기록 추가하기 (0원 자리표시 줄만 생김) | 실제 금액을 입력할 수 없어 가계부로 쓸 수 없고, 잘못 생긴 줄을 지울 수도 없어요 | `src/lib/finance/ledger.ts:109` · |
| `/ledger` | 불러오기 실패했을 때 다시 시도 누르기 | 여전히 인터넷이 없으면 같은 오류 화면이 다시 떠요 | `src/lib/finance/ledger.ts:127` · |
| `/secondb` | 화면 열기 (인사 모달 + 오늘 쓴 횟수 불러오기) | 휴대폰 앱에서는 '오늘은 그만 볼래요' 를 눌러도 저장이 안 돼서, 들어올 때마다 인사 창이 계속 다시 떠요… | `src/lib/chat/usage.ts:6` · |
| `/secondb` | 근거 보기 (참고한 기록 열기) | 누른 그 페이지로 이동하지 않고 위키 목록만 열려요. 어떤 기록을 눌렀는지 정보가 그냥 버려집니다 (코드에 … | `src/lib/chat/sources.ts:26` · |
| `/secondb` | 답변을 길게 눌러 복사하기 | 휴대폰 앱에서는 꾹 눌러도 자동 복사가 안 돼요 — 자동 복사는 웹 브라우저에서만 동작해요 (src/app/… | `src/app/secondb.tsx:716` ✔ |
| `/beyond` | 음성으로 담기 (마이크 버튼) | 마이크 버튼을 눌러도 녹음이 안 시작돼요 — 그냥 글 쓰는 담기 화면만 열려요 | `src/app/beyond.tsx:98` · |
| `/discover` | 화면 열기 | 화면의 +32% / +18% 는 내 데이터로 계산한 값이 아니라 코드에 박아둔 고정 숫자예요. 모든 사용자에… | `src/screens/deepspace/DeepSpaceDesignScreens.tsx:1220 (DeepSpaceDiscoverScreen)` ✔ |
| `/research` | 페이지 열어 보기 | 위키 페이지 번호를 기록 화면에 그대로 넘겨요 — 두 번호는 서로 다른 체계라 거의 항상 '찾을 수 없음' … | `src/screens/deepspace/DeepSpaceDesignScreens.tsx:1523` ✔ |
| `/wiki` | 연결 그림에서 점을 눌러 선택하고, 한 번 더 눌러 페이지 열기 | 한 번만 누르면 아무 일도 안 일어난 것처럼 보여요 (선택만 된 상태) | `src/screens/deepspace/dds-wiki-records-screens.tsx:1333 (onOpenPage)` ✔ |
| `/big-five` | 저장된 성격 점수 불러오기 | 버그(기본 화면에서 실제로 겪음): 인터넷이 끊기거나 서버가 안 되면 오류 화면 대신 '아직 이 별은 어두워… | `src/lib/persona/build.ts:263` · |
| `/ipip-neo` | 120문항 답하기 | 버그(기본 화면에서 실제로 겪음): 답하는 중에 안드로이드 뒤로가기를 누르면 '그만둘까요?' 확인 없이 화면… | `src/lib/persona/ipip-neo.ts:182` · |
| `/ipip-neo` | 120문항 답변 저장하기 | 저장에 실패하면 빨간 알림이 뜨고 답변은 화면에 그대로 남아요 — 다시 저장을 누르면 돼요 (src/app/… | `src/lib/records/create.ts:100` · |
| `/rlss` | 6문항에 1~7점으로 답하기 | 답하는 중에 안드로이드 뒤로가기를 누르면 확인 창도 없이 화면이 닫히고 지금까지 고른 답이 전부 사라져요 (… | `src/app/rlss.tsx:181` · |
| `/values` | 화면 열기 (저장된 가치 결과 불러오기) | 버그(기본 화면에서 실제로 겪음): 불러오기가 실패해도 오류 표시나 다시 시도 버튼 없이 '결과 없음'으로 … | `src/lib/persona/build.ts:302` · |
| `/strengths` | 화면 열기 (저장된 강점 결과 불러오기) | 버그(기본 화면에서 실제로 겪음): 불러오기가 실패해도 오류 표시가 없이 '결과 없음'으로 처리돼서, 결과가… | `src/lib/persona/build.ts:350` · |
| `/strengths` | '다음' 눌러 다음 문항 페이지로 넘어가기 | 게이트: 마지막 장이 아니면 저장 버튼 자체가 없어서, 다 답했는데도 저장을 못 찾는 것처럼 보여요 | `src/components/quant/QuantPager.tsx:68` · |
| `/insights` | 화면 열기 (내 기록 불러오기) | 인터넷이 끊기면 화면 대신 오류 문구와 다시 시도 버튼만 보여요 | `src/lib/records/create.ts:309` · |
| `/milestones` | 목표 추가 (＋ 버튼) | 저장이 실패해도 오류 문구가 전혀 안 떠요 (조용한 실패 — screens.tsx:409-411 의 빈 ca… | `src/lib/ops/milestones.ts:88` · |
| `/milestones` | 상태 칩 눌러 진행 상태 바꾸기 | 저장이 실패해도 오류 안내가 없어서 그냥 안 눌린 것처럼 보여요 (screens.tsx:423-425 의 빈… | `src/lib/ops/milestones.ts:134` · |
| `/focus` | '어떤 별을 위해?' 별 고르기 | 고른 별이 저장되지 않아 별 밝기나 영역별 통계에 전혀 반영되지 않아요 | `src/screens/deepspace/DeepSpaceDesignScreens.tsx:2423` · |
| `/import` | 오늘 건강 데이터 반영 | [버그] 폰에 건강 앱 연결이 없거나 권한을 '거부'하면, 알리지도 않고 가짜 값(걸음 9000·수면 420… | `src/lib/health/ingest.ts:62` · |
| `/import-hub` | 고른 항목 기록에 반영 | 저장이 실패해도 오류 없이 첫 화면으로 돌아가서 성공한 것처럼 보여요 (실제 버그) | `src/lib/wiki/capture.ts:78` · |
| `/peer-invites` | 초대 회수하기 | 회수가 실패해도 오류 안내가 전혀 없고 목록도 그대로예요 (실패했다는 걸 알 방법이 없어요) | `src/lib/peer/invite.ts:86` · |
| `/peer/[token]` | 내 응답 철회하기 | 서버에서 취소가 실패해도(500·404) 화면은 '철회됐어요'로 바뀌어요 - 실제로는 내 응답이 그대로 남아… | `supabase/functions/peer-respond/index.ts:97` · |
| `/ratifications` | 결정 종류로 거르기 | 승인 기록이 있는데도 보류·거절을 누르면 '기록이 하나도 없다'는 안내가 떠서 기록이 사라진 줄 알게 돼요 | `src/app/ratifications.tsx:168` ✔ |
| `/plans` | 무료 요금제(별바라기) 카드의 버튼 누르기 — 유료 이용자에게만 눌리는 버튼 | 버튼을 눌러도 아무 반응이 없습니다 (앱이 멈춘 것처럼 보입니다) | `src/screens/deepspace/dds-plans-screen.tsx:308` · |
| `/core-brain` | '참고한 별가루' 목록 열고 조각 눌러보기 | 담은 자료를 누르면 상세 화면이 '기록' 쪽에서만 찾아서 무조건 '기록을 찾을 수 없어요. 보관소로 돌아가 … | `src/app/core-brain.tsx:281` · |

---

## 4. 화면 이동 (157개 연결)

<details><summary>mermaid 그래프 펼치기</summary>

```mermaid
flowchart LR
  S1["로그인"] -->|화면 열기(로그인 상태 확인)| S2["홈 별자리"]
  S1["로그인"] -->|네이버로 로그인| S3["소셜 로그인 처리 중"]
  S1["로그인"] -->|회원가입 화면으로 가기| S4["회원가입"]
  S4["회원가입"] -->|화면 열기(로그인 상태 확인)| S2["홈 별자리"]
  S4["회원가입"] -->|구글·애플·카카오 등으로 가입| S5["가입 마무리(생년월일·동의)"]
  S4["회원가입"] -->|네이버로 가입| S3["소셜 로그인 처리 중"]
  S4["회원가입"] -->|이미 계정이 있어요 → 로그인으로| S1["로그인"]
  S5["가입 마무리(생년월일·동의)"] -->|화면 열기(로그인·프로필 확인)| S2["홈 별자리"]
  S5["가입 마무리(생년월일·동의)"] -->|취소하기(로그아웃)| S1["로그인"]
  S6["비밀번호 재설정"] -->|완료 후 앱으로 들어가기| S2["홈 별자리"]
  S6["비밀번호 재설정"] -->|로그인 화면으로 돌아가기| S1["로그인"]
  S3["소셜 로그인 처리 중"] -->|화면 열기(네이버 로그인 마무리)| S2["홈 별자리"]
  S3["소셜 로그인 처리 중"] -->|실패 시 로그인 화면으로| S1["로그인"]
  S7["가입 전 한 줄 메모"] -->|계정 만들러 가기| S4["회원가입"]
  S8["첫 소개 화면"] -->|화면 열기(소개를 봤는지 확인)| S2["홈 별자리"]
  S8["첫 소개 화면"] -->|시작하기(로그인으로)| S1["로그인"]
  S2["홈 별자리"] -->|도메인 별 누르고 여행하기| S9["도메인 별 렌즈"]
  S2["홈 별자리"] -->|북극성 누르기| S10["북극성 (나의 종합 요약)"]
  S2["홈 별자리"] -->|세컨비 머리 눌러 챗봇/비서 열기| S11["세컨비 대화"]
  S2["홈 별자리"] -->|알림 종 누르기| S12["알림"]
  S2["홈 별자리"] -->|아래 탭(별자리·담기·세컨비·위키·설정) 누르기| S13["담기"]
  S2["홈 별자리"] -->|앱 열 때 홈이 뜰지 다른 화면으로 튕길지 결정되는 관문 통과하기| S5["가입 마무리(생년월일·동의)"]
  S2["홈 별자리"] -->|가입 첫날 홈 대신 '첫 별 점등' 화면으로 한 번 보내지기| S14["첫 통찰"]
  S15["홈 별자리 (개발용 미리보기)"] -->|별 누르기| S9["도메인 별 렌즈"]
  S15["홈 별자리 (개발용 미리보기)"] -->|알림 종 누르기| S12["알림"]
  S15["홈 별자리 (개발용 미리보기)"] -->|세컨비 머리 눌러 챗봇/비서 열기| S11["세컨비 대화"]
  S16["허브 도크 (개발용 미리보기)"] -->|오늘의 정리 열기| S17["오늘의 정리"]
  S9["도메인 별 렌즈"] -->|담기 버튼 누르기| S13["담기"]
  S9["도메인 별 렌즈"] -->|한 걸음 더 들어가기 버튼 누르기| S18["커리어 타임라인"]
  S9["도메인 별 렌즈"] -->|기록 하나 누르기| S19["조각 상세"]
  S20["북극성 문장"] -->|세컨비 제안 받기| S21["요금제"]
  S22["밝기 변화"] -->|북극성 보러 가기| S10["북극성 (나의 종합 요약)"]
  S22["밝기 변화"] -->|승인 기록 열기| S23["승인 이력"]
  S13["담기"] -->|전체 담기 화면 열기 (사진·음성)| S24["전체 담기"]
  S13["담기"] -->|저장한 뒤 기록 보관소 열기| S25["기록 보관소"]
  S24["전체 담기"] -->|'조언 받기' 켜기 (잠겨 있으면 누르면 요금제 화면으로 가요)| S21["요금제"]
  S24["전체 담기"] -->|AI가 만든 형식 저장하기 ('내 것만' / '모두에게 공유' 중 선택)| S26["내보내기 형식"]
  S25["기록 보관소"] -->|조각 하나 열기| S19["조각 상세"]
  S25["기록 보관소"] -->|분류함 열기| S12["알림"]
  S25["기록 보관소"] -->|이 별 채워넣기| S27["관계 인물맵"]
  S25["기록 보관소"] -->|담으러 가기| S13["담기"]
  S19["조각 상세"] -->|연결된 기록 열기| S19["조각 상세"]
  S19["조각 상세"] -->|근거 보러 가기| S10["북극성 (나의 종합 요약)"]
  S19["조각 상세"] -->|검사 결과 보러 가기| S28["동기 자기보고"]
  S12["알림"] -->|로그인 안 했으면 로그인 화면으로 보내기| S1["로그인"]
  S12["알림"] -->|아래 탭(별자리·담기·세컨비·위키·설정) 누르기| S13["담기"]
  S29["일기 (이동)"] -->|화면 열기| S13["담기"]
  S30["애착 유형"] -->|첫 저장 뒤 세컨비 대화로 자동 이동| S11["세컨비 대화"]
  S30["애착 유형"] -->|인터뷰로 이동| S31["심층 인터뷰"]
  S30["애착 유형"] -->|성격 5요인 검사로 이동| S32["성격 5요인 검사"]
  S33["매뉴얼 · 도움말"] -->|'시작하기' 질문 누르기| S34["지원"]
  S33["매뉴얼 · 도움말"] -->|세컨비에게 직접 물어보기| S11["세컨비 대화"]
  S18["커리어 타임라인"] -->|드릴다운 화면으로 이동| S35["커리어 드릴다운"]
  S18["커리어 타임라인"] -->|성과 카드 눌러 상세 보기| S19["조각 상세"]
  S35["커리어 드릴다운"] -->|화면 열기 (로그인 확인)| S1["로그인"]
  S35["커리어 드릴다운"] -->|아래 탭바로 다른 화면 가기| S2["홈 별자리"]
  S35["커리어 드릴다운"] -->|드릴다운 제출하기| S11["세컨비 대화"]
  S36["나의 변화 (주간 성장)"] -->|다시 분석하기| S36["나의 변화 (주간 성장)"]
  S36["나의 변화 (주간 성장)"] -->|근거 칩 눌러 렌즈 화면으로 이동| S32["성격 5요인 검사"]
  S36["나의 변화 (주간 성장)"] -->|꿈을 한 걸음으로 (상상 화면 열기)| S37["공상하기"]
  S36["나의 변화 (주간 성장)"] -->|첫 주 안내에서 오늘 담기| S13["담기"]
  S36["나의 변화 (주간 성장)"] -->|첫 주 안내에서 루틴 시작하기| S38["오늘의 비서 (루틴 허브)"]
  S39["자비스 (옛 대화 주소)"] -->|화면 열기| S11["세컨비 대화"]
  S11["세컨비 대화"] -->|근거 보기 (참고한 기록 열기)| S40["위키"]
  S11["세컨비 대화"] -->|트위비가 준 갈래 담기 (기록으로 넘기기)| S13["담기"]
  S11["세컨비 대화"] -->|'플랜 보기' 누르기 (오늘 횟수 다 씀)| S21["요금제"]
  S41["북극성 (나의 종합)"] -->|북극성 다듬기| S20["북극성 문장"]
  S41["북극성 (나의 종합)"] -->|영역 별 하나 눌러 보기| S25["기록 보관소"]
  S41["북극성 (나의 종합)"] -->|검증(성격 검사) 보기| S32["성격 5요인 검사"]
  S41["북극성 (나의 종합)"] -->|별자리 홈으로 돌아가기| S2["홈 별자리"]
  S31["심층 인터뷰"] -->|화면 열기 (로그인 확인 + 시기 값 읽기)| S1["로그인"]
  S31["심층 인터뷰"] -->|이렇게 반영할게요 (저장)| S32["성격 5요인 검사"]
  S37["공상하기"] -->|다음 한 걸음 하나 담기로 보내기| S13["담기"]
  S37["공상하기"] -->|세컨비와 더| S11["세컨비 대화"]
  S42["앱 밖에서"] -->|화면 열기| S1["로그인"]
  S42["앱 밖에서"] -->|담기 (+ 버튼)| S13["담기"]
  S42["앱 밖에서"] -->|알림 설정 열기| S43["설정"]
  S44["트렌드"] -->|관계 검사 시작 (첫 번째 카드)| S30["애착 유형"]
  S44["트렌드"] -->|담으러 가기 (두 번째 카드)| S13["담기"]
  S44["트렌드"] -->|뒤로 가기 (왼쪽 위 화살표)| S2["홈 별자리"]
  S45["리서치 (연결 찾기)"] -->|첫 조각 추가하기 (빈 화면일 때)| S13["담기"]
  S45["리서치 (연결 찾기)"] -->|페이지 열어 보기| S19["조각 상세"]
  S40["위키"] -->|원문 열어 보기| S19["조각 상세"]
  S40["위키"] -->|담으러 가기| S13["담기"]
  S32["성격 5요인 검사"] -->|'데이터 추가' 눌러 담기 화면으로 가기| S13["담기"]
  S32["성격 5요인 검사"] -->|'다른 검증틀 · 애착 유형 보기' 카드 누르기| S30["애착 유형"]
  S46["MBTI (없어진 화면)"] -->|화면 열기 (바로 페르소나로 이동)| S41["북극성 (나의 종합)"]
  S47["지금 이 순간 체크인"] -->|홈으로 돌아가기| S2["홈 별자리"]
  S48["삶의 만족도 검사"] -->|6문항 답변 저장하기| S41["북극성 (나의 종합)"]
  S49["가치 자기보고"] -->|가치 인터뷰로 넘어가기| S31["심층 인터뷰"]
  S49["가치 자기보고"] -->|다른 검증틀 보기 (성격 5요인)| S32["성격 5요인 검사"]
  S50["강점 자기보고"] -->|화면 열기 (저장된 강점 결과 불러오기)| S1["로그인"]
  S50["강점 자기보고"] -->|강점 살리기 (세컨비에게 물어보기)| S11["세컨비 대화"]
  S50["강점 자기보고"] -->|동기 보기| S28["동기 자기보고"]
  S28["동기 자기보고"] -->|강점 확인하러 가기| S50["강점 자기보고"]
  S28["동기 자기보고"] -->|성격(빅파이브) 확인하러 가기| S32["성격 5요인 검사"]
  S51["나를 파일로 내보내기"] -->|'AI에 전달' 카드 누르기| S52["데이터 연동"]
  S51["나를 파일로 내보내기"] -->|'담으러 가기' 버튼 누르기 (로그아웃 상태에서만 보여요)| S31["심층 인터뷰"]
  S53["인사이트"] -->|이번 주 카드 눌러 기록 목록 열기| S25["기록 보관소"]
  S53["인사이트"] -->|발견 카드 눌러 연구 화면 열기| S45["리서치 (연결 찾기)"]
  S53["인사이트"] -->|기록하러 가기 버튼 (첫 주일 때만)| S13["담기"]
  S53["인사이트"] -->|왼쪽 위 뒤로가기 칩 누르기| S2["홈 별자리"]
  S54["밝기 추이"] -->|로그인 안 했으면 로그인 화면으로 보내기| S1["로그인"]
  S54["밝기 추이"] -->|공유 카드 만들기 버튼| S55["공유 카드"]
  S54["밝기 추이"] -->|아래 탭(별자리·담기·세컨비·위키·설정) 누르기| S13["담기"]
  S17["오늘의 정리"] -->|제안 줄 눌러 원본 기록 열기 (지금은 항상 '기록을 찾을 수 없어요'가 떠요)| S19["조각 상세"]
  S17["오늘의 정리"] -->|다시 시도(오류) / 기록하러 가기(빈 화면)| S13["담기"]
  S56["리뷰 (제안 승인)"] -->|근거 기록 눌러 원문 확인| S19["조각 상세"]
  S56["리뷰 (제안 승인)"] -->|뒤로 가기 (왼쪽 위 화살표)| S2["홈 별자리"]
  S57["AI 뮤지엄"] -->|별자리 홈으로 돌아가기 ('두 번째 뇌' 사건에서만)| S2["홈 별자리"]
  S58["보여지는 나"] -->|설문하러 가기| S31["심층 인터뷰"]
  S58["보여지는 나"] -->|지인에게 보내기| S59["지인 초대"]
  S14["첫 통찰"] -->|별자리 홈으로 들어가기| S2["홈 별자리"]
  S52["데이터 연동"] -->|소스 연결 누르기| S60["가져오기 허브"]
  S61["통화 돌아보기 (앱 안에 들어가는 길이 없는 화면)"] -->|기록으로 저장| S25["기록 보관소"]
  S61["통화 돌아보기 (앱 안에 들어가는 길이 없는 화면)"] -->|버리기| S2["홈 별자리"]
  S61["통화 돌아보기 (앱 안에 들어가는 길이 없는 화면)"] -->|나중에 하기| S43["설정"]
  S62["예약된 알림"] -->|'오늘의 비서에서 추가' 누르기| S38["오늘의 비서 (루틴 허브)"]
  S59["지인 초대"] -->|화면 열기 (로그인 확인 + 초대 목록 불러오기)| S1["로그인"]
  S23["승인 이력"] -->|북극성으로 가기| S10["북극성 (나의 종합 요약)"]
  S43["설정"] -->|하위 설정 메뉴 열기 (프로필·개인정보·계정·테마·데이터·기록·지원·연동)| S63["프로필"]
  S43["설정"] -->|코치마크(첫 사용 안내) 다시 보기| S2["홈 별자리"]
  S43["설정"] -->|전체 삭제 (DELETE 입력 후 실행)| S13["담기"]
  S43["설정"] -->|로그아웃| S1["로그인"]
  S64["나 (계정)"] -->|프로필·설정·데이터·IDEN·비욘드 열기| S63["프로필"]
  S64["나 (계정)"] -->|계정 삭제 (지금은 개인정보 화면에 있음)| S1["로그인"]
  S63["프로필"] -->|뒤로 가기 (왼쪽 위 화살표)| S2["홈 별자리"]
  S63["프로필"] -->|설정 아이콘 누르기| S43["설정"]
  S63["프로필"] -->|요금제 카드 누르기| S21["요금제"]
  S63["프로필"] -->|목록에서 항목 눌러 그 화면 열기| S41["북극성 (나의 종합)"]
  S21["요금제"] -->|문의하기| S34["지원"]
  S65["개인정보"] -->|계정 영구 삭제 (DELETE 입력)| S1["로그인"]
  S66["데이터"] -->|내 권리 항목 누르기 (내보내기 / 파생 신호 초기화 / 계정 삭제)| S65["개인정보"]
  S34["지원"] -->|세컨비에게 묻기 · 사용설명서 보기| S11["세컨비 대화"]
  S67["과거의 나 (시기별 돌아보기)"] -->|시기 카드 누르기| S31["심층 인터뷰"]
  S68["내 두뇌 지도"] -->|가운데 '나' 별 누르기| S64["나 (계정)"]
  S68["내 두뇌 지도"] -->|'군집 보기' 버튼| S25["기록 보관소"]
  S68["내 두뇌 지도"] -->|'연결 찾기' 버튼| S45["리서치 (연결 찾기)"]
  S10["북극성 (나의 종합 요약)"] -->|첫 카드의 바로가기 3개 (밝기 변화 · 승인 이력 · 공유 카드)| S22["밝기 변화"]
  S10["북극성 (나의 종합 요약)"] -->|'자주 보이는 나의 모습' 칸 눌러 채우러 가기| S41["북극성 (나의 종합)"]
  S10["북극성 (나의 종합 요약)"] -->|'참고한 별가루' 목록 열고 조각 눌러보기| S25["기록 보관소"]
  S10["북극성 (나의 종합 요약)"] -->|'다음 한 걸음' 카드의 버튼 3개| S11["세컨비 대화"]
  S10["북극성 (나의 종합 요약)"] -->|기록이 하나도 없을 때 뜨는 시작 버튼 2개| S30["애착 유형"]
  S69["내 영역 (4영역)"] -->|영역 카드 눌러 기록 보기| S25["기록 보관소"]
  S69["내 영역 (4영역)"] -->|'데이터 추가' 버튼| S13["담기"]
```

</details>

---

## 5. 필요할 때 찾아보는 법

읽는 건 여기까지다. 나머지는 **찾아 쓴다** — [`docs/flow-map.json`](docs/flow-map.json) 에 483개 동작 전부 있다.

```bash
# 한 화면이 무슨 일을 하는가
jq '.screens[] | select(.route=="/capture")' docs/flow-map.json

# 이 화면을 고치려면 어느 파일인가 (프로덕션 렌더 파일)
jq -r '.screens[] | select(.route=="/capture") | .rendersInProduction' docs/flow-map.json

# 어떤 동작이 이 테이블을 건드리나
jq -r '.screens[].actions[] | select(.apis[]? | contains("records")) | .action' docs/flow-map.json

# AI 쓰는 동작 전부
jq -r '.screens[] as $s | $s.actions[] | select(.ai) | "\($s.route)  \(.action)  \(.ai.purpose)"' docs/flow-map.json
```

**클릭해서 보기:** [`docs/flow-debugger.html`](docs/flow-debugger.html) — 화면별 플로우 / 시스템 플로우, 버그 신고서 생성.

---

## 6. 이 지도를 얼마나 믿어도 되나

| | 수 | 뜻 |
|---|---|---|
| **✔** | 509 | 그 줄에 **그 함수가 실제로 있음** — 출발점으로 신뢰해도 됨 |
| **·** | 477 | 파일·줄은 실재. **대조할 함수명이 없어 그 줄이 맞는지는 확인 못 함** — 근처를 읽고 판단 |
| **~** | 0 | 빈 줄/import/주석 — 로직은 다른 줄 |
| **⚠** | 0 | 대조 실패 — 믿지 말 것 |
| 위임 트랩 | 0 | 앵커가 가리키는 파일이 프로덕션에선 다른 걸 그림 |

**앱을 고쳤으면 지도도 갱신한다** — 안 하면 이 파일이 거짓말을 시작한다:

```bash
# flow-debugger 스킬 폴더에서 (scan-prompts.md "RESCAN / PATCH" 참조)
node scripts/verify-anchors.js <graph.json> "E:\2ndB" --fix <graph.json> --strict
node scripts/make-handoff.js <graph.json> "E:\2ndB" --out docs/FLOW-HANDOFF.md --json docs/flow-map.json
```

