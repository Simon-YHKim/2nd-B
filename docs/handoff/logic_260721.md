# 로직 감사 (logic) — 2026-07-21

세션ID `logic` · 임무: "된다고 적혀 있는 것"과 "실제로 되는 것"의 차이를 찾아 없앤다.

---

# 4차 (cowork 07:11 KST — 신규 발주 없음 · P2 위험도 재검토만, 수정 금지)

## 수신

- ① 부킹 실패 경고 종결 확인(부분 실패 카피 분기 판단 인정). ③ 카운트 정정 종결.
- **② 캐논 잔존 레벨 = 보류 확정. 진행하지 않는다.** 근거는 3차의 무영향 증명(강제층 드리프트 0,
  프로토 열람층 전용) + design 세션 작업 중 충돌 회피. 3차 범위 회신(두 mock.json:96 +
  sb-screens-know.jsx:325,336-337 · sb-wikigraph.jsx:620은 실데이터라 제외)이 design 인계서로 전환됨.
- 신규 발주 없음. 기존 P2 3건을 "실제로 위험한지" 기준으로 재검토, **우선순위만 회신 · 수정 금지**.
- 규약: `date '+%Y-%m-%d %H:%M:%S %Z%z'` = `2026-07-21 07:39:52 +0900` (+0900, 환산 불필요).
  `git rev-parse main origin/main` = `582c5b0a` ≠ `8f59fd2e` — 로컬 main 여전히 스테일(behind), 무접촉.
  검증·문서 모두 origin/main(`8f59fd2e` = #1122 팁) 기준.

## 작업

읽기 전용 재검증(코드 수정 0). 세 건 모두 origin/main 팁에서 주장 유지 확인:
- A) `useAppFeatures().connections`(cal/health/notion) 소비 렌더러 0 — `git grep "connections\." origin/main -- src/app src/screens src/components` 히트 2건은 무관한 산문 카피(AutoReasoningIntroSheet.tsx:59, DeepSpaceDesignScreens.tsx:893).
- B) `git grep -il revenuecat origin/main -- supabase/functions` = 0건 — RevenueCat 웹훅→edge→revenue_events 경로 여전히 부재(purchases.ts TODO(IAP-webhook) 유효).
- C) ChatView·RhythmLensView·RecallLensView·RelationalLensView·PossibleLensView 마운트 0 — src/app·src/screens 히트는 주석 언급뿐(esm.tsx:325, imagine.tsx:5).

## 회신 — P2 위험도 우선순위 (열린 P0 없음, 만들지 않음)

1. **B. purchases.ts IAP-webhook 부재 — 유일하게 실위험 벡터 보유. 최우선.**
   오늘 위험 0(스토어 키·상품 미설정, 클라이언트 전 경로 fail-closed 확인 완료 — 1차 통과 판정).
   그러나 스토어 GO 순간: (a) 실결제 발생 (b) `revenue_events`(C4) 미적재 — **무증상 실패가 첫 매출과
   동시에 시작**되고 심사 기간과 겹친다. rewarded.native.ts의 프로덕션 가드("Replacing this guard with
   the real unit id is the launch change")와 동형의 런칭 시점 결합 위험.
   권고(코드 아님, 절차): **"스토어 제품 활성화 전 웹훅 착지"를 런칭 차단 항목으로 등재** — 루트 TODO.md
   정본 또는 스토어 런치 러너(S1/S2 트랙) 소관자에게. 등재만 되면 이 항목은 위험 0으로 강등된다.
2. **C. DeepSpaceViews 미배선 참조 뷰 — 현재 위험 0, 미래 오배선 위험 중하.**
   전부 마운트 0이므로 사용자 접점 없음. 다만 ChatView(:528)는 정적 유저/AI 버블이라 다섯 중 유일하게
   "동작하는 것처럼 보이는" 코드인데 UNWIRED 주석이 없다(Rhythm/Recall/Relational은 있음). 다음 수정
   사이클에서 주석 1줄이면 위험 소거. 지금은 무조치.
3. **A. app-features.ts connections 죽은 pref — 위험 최하.**
   렌더러 0 = 사용자 접점 0 = 기만 0. /settings 재유입 주경로는 3차 네거티브 가드가 차단하나 그 가드는
   settings.tsx 한정임을 명시해 둔다(타 화면에서 새로 렌더하면 미차단 — 그래도 렌더+미배선 이중 실수가
   필요해 확률 낮음). 삭제는 정리 취향의 문제로 D-27 우선순위 없음.
- 인접 1줄(신규 발주 요청 아님): 같은 심의 kakao 인물 upsert(:238) 스왈로우는 사용자가 '고른' 행이 아닌
  파생 신호이고 재임포트로 재생성 가능 — 낮음, 조치 불요 판단.
- 결론: **로직 트랙에 열린 P0 없음.** 다음 사이클 대기.

---

# 3차 (cowork 04:29 KST 발주 — dbl 독립검증 PASS 18 / FAIL 4 / UNVERIFIABLE 1)

## 수신

- dbl 정정 수용: **L4 무접촉**(결과 gate + SoT 근거 있음 — cowork 04:01 판정은 스테일 로컬 기반이라 파기),
  attachment L3만 유효 FAIL(이번 발주 목록에는 미포함 — 무접촉 유지).
- 발주 3건: ① [P0] 부킹 실패 경고 미동작(resolve 계약) ② [P0] 캐논 잔존 1,2,3,4,3 — **범위 회신 후 진행 확인 대기**
  ③ [P2] "고아 스타일 5종" 오기 정정. UNVERIFIABLE(실기 UI 탭 회귀)은 실기 QA로 이관, 내 작업 없음.
- 규약 이행: 시각 `date '+%Y-%m-%d %H:%M:%S %Z%z'` = `2026-07-21 04:37:32 +0900` (오프셋 +0900, 환산 불필요 —
  환산 커맨드도 동일값 교차확인). `git rev-parse main origin/main` = `582c5b0a` ≠ `20e11763`:
  **로컬 main이 origin/main보다 뒤**(merge-base 확인, fast-forward 가능·분기 아님) → 워크트리는 origin/main 기준.

## 작업

브랜치 `logic/ledger-warn-resolve-contract`, 워크트리 `.worktrees/logic-audit-r3` (origin/main `20e11763` = #1115 기준).
수정 파일 2/5: `ImportHubScreen.tsx` · 본 보고서.

1. **① 부킹 실패 경고 — resolve 계약으로 완결** (`src/screens/deepspace/import/ImportHubScreen.tsx`)
   - 사실관계: dbl 실측(#1117 head) 정확 — `ratifyLedgerEntries`는 per-row try/catch로 `{inserted, failed}`를
     **resolve**하며 행 실패로는 reject하지 않는다(`src/lib/import/ledger-ratify.ts:36-50`). 내 r2의 `.catch()` 경고는
     그 경로에서 죽은 코드였다. 이후 **#1115가 선반영**: await + `bookedTxns === 0 → setLedgerWarn(true)`
     (전체 실패 케이스 — dbl의 2/2 실측 케이스는 이 시점에 이미 해소). 주석에 "#1117 intent" 명시돼 있음.
   - 이번 잔여 완결: `failed > 0`이면 경고(부분 실패 포함). 상태를 `LedgerRatifyResult | null`로 바꿔 카운트 보존,
     reject 방어선(`booked === null`)은 시도건수 전체를 failed로 집계.
   - **부분 실패 경고 판단 = 경고한다, 단 카피 분기.** 근거: `ledger-ratify.ts:7-10` 문서화된 한계 —
     "ops_ledger는 자연 유니크 키가 없어 같은 명세 재비준은 **중복 부킹**". 따라서 "다시 가져오라" 조언은
     전체 실패(inserted=0)에서만 안전하고, 부분 실패에서 그대로 쓰면 이미 반영된 건을 중복시키는 유해 조언이 된다.
     부분 실패 카피: "고른 거래 중 {failed}건을 적지 못했어요. {inserted}건은 반영됐어요. 같은 파일을 다시
     가져오면 이미 반영된 내역이 중복되니, 빠진 내역은 직접 추가해 주세요." (건수 치환은 이 파일의 기존
     `applyN` `.replace` 관용구.)
2. **③ 기록 정정**: 2차 작업 블록의 "고아 스타일 5종" → **6종**(목록 병기). 카운트는 이후 세고 적는다.

## 회신

- ① 완료(아래 PR). ③ 완료(본 문서 정정). UNVERIFIABLE은 실기 QA 이관 인지.
- **② 캐논 범위 회신 (진행 여부 확인 요청):**
  - 고정 레벨의 서식지: `"eras"` 배열 (1,2,3,4,3) — `design/proto_rev2/reference-app/data/core/mock.json:96`
    + 동일 내용의 서빙 사본 `public/proto/data/core/mock.json:96` (eras 블록 diff 일치 확인).
    렌더: `sb-data.jsx:48`(`ERAS = SB_D.mock.eras`) → `sb-screens-know.jsx:325,336-337`(`<Dots level>` + "또렷함 L{e.level}").
  - **코드 소비층 무영향 증명**: `src/lib/canon/index.ts`가 임포트하는 JSON 14개에 `core/mock.json` 없음,
    src 전역에 canon eras 소비자 없음 → canon.test/canon-tokens.test 무관(387 스위트 통과가 방증).
    즉 "캐논 JSON+코드 동시 변경" 규칙의 강제층에는 드리프트가 **없고**, 잔존은 프로토 열람층(참조앱)이다.
  - 제안 범위: 3파일 — 두 `mock.json`의 eras `level` 필드 제거 + `sb-screens-know.jsx` AuditScreen의
    Dots/라벨 열 제거. 앱 코드·테스트 무접촉, 프로토 전용. reference-app은 design 세션 소관과 겹치므로
    **진행 확인을 기다린다.** (참고: `sb-wikigraph.jsx:620`의 "또렷함 L{node.dom.level}"은 실데이터 동적
    표시라 조작 패턴 아님 — 범위 제외.)
- 검증: `npm run verify` 통과 후 PR·오토머지. 결과는 PR CI가 재확인.

---

# 2차 (cowork 03:16 KST 발주)

## 수신

- 발주: 잔여 P0 — /audit 캐논 "또렷함 L{n}" 하드코딩(DeepSpaceViews.tsx:1546-1552) 제거,
  권고안("레벨 표시 제거, 타임라인=내비 전용") 채택. 이어서 P1 — ImportHubScreen.tsx:248
  ratifyLedgerEntries 무통보 스왈로우에 실패 알림 추가. 경계: 5파일 한도 · 토큰/프롬프트 무접촉 ·
  워크트리→PR · verify 통과.
- 시각 기록: `TZ=Asia/Seoul date` 실행값 = `2026-07-20 18:25`. 단, 이 Windows Git Bash는 TZ 이름
  해석을 못 해 **UTC로 폴백**한 값이다(= 2026-07-21 03:25 KST). 실행값 그대로+환산 병기.

## 작업

브랜치 `logic/audit-honest-timeline`, 워크트리 `.worktrees/logic-audit-r2` (origin/main ce7ae0ab 기준).
수정 파일 4/5: `DeepSpaceViews.tsx` · `ImportHubScreen.tsx` · `audit.tsx`(주석) · 본 보고서.

1. **P0 — /audit 조작 레벨 제거** (`src/components/deep-space/DeepSpaceViews.tsx`)
   - `AUDIT_ERAS`에서 `level` 필드 삭제(고정값 1,2,3,4,3), `AUDIT_DOTS` 상수 + `AuditDots` 컴포넌트 삭제,
     렌더의 `auditEraMeta` 블록(도트 + `ds.audit.vividness` "또렷함 L{n}") 삭제, 고아 스타일 **6종** 삭제
     (auditEraMeta·auditDotRow·auditDot·auditDotOn·auditDotOff·auditVivid — 최초 기록 "5종"은 오기, 3차에서 정정. dbl FAIL 3).
   - 타임라인은 내비 전용(시대 탭 → /interview 기간 스코프 진입)으로 유지 — 기존 동작 무변경.
   - 헤더 주석을 정직성 제약("fabricated brightness 금지, 실데이터 소스가 생겨야 레벨 복귀")으로 교체.
   - `ds.audit.vividness` i18n 키는 잔존(C7 패리티 무영향). `src/app/audit.tsx`의 낡은
     "with per-era brightness" 주석도 현행화.
   - 테스트/캐논 핀 없음 확인(grep AuditDots|vividness → 컴포넌트 파일뿐).
2. **P1 — 가져오기 거래 부킹 실패 가시화** (`src/screens/deepspace/import/ImportHubScreen.tsx`)
   - `ratifyLedgerEntries(...).catch(() => undefined)` → `.catch(() => setLedgerWarn(true))`.
     best-effort 의미(임포트 본체 성공 후 비차단)는 유지, **전체 실패만** 허브에 경고로 표면화.
   - 허브 상단에 `OpsState variant="rate"`(경고 톤) 1줄: "거래 반영은 실패했어요 / 가져오기는 저장됐어요.
     다만 고른 거래 내역을 적지 못했어요. 같은 파일을 다시 가져오면 반영돼요." (이 파일의 기존 관용구인
     로컬 COPY 테이블에 ko/en 추가 — i18n 키 아님, 로케일 파일 무접촉.)
   - 재시도 시 `setLedgerWarn(false)` 리셋. kakao 인물 upsert(:238)는 발주 범위 밖 — 동일 패턴 적용은 후속 판단.

## 회신

- P0 잔여분 처리 완료: /audit 캐논은 이제 어떤 사용자에게도 지어낸 밝기를 보여주지 않는다.
  실제 시대별 커버리지 산출(기간별 audit_response 카운트 → 레벨)은 데이터 파이프라인 과제로 남김(D-27 범위 밖 동의).
- P1 처리 완료: 사용자가 고른 거래의 소실은 이제 무통보로 사라지지 않는다.
- 신규 확인 항목(P3 후보, 이번 범위 밖): `ds.lens.level`="L4"(LensView:642·BigFiveLensM3:770),
  `ds.attachment.level`="L3"(AttachmentLensM3:911) 정적 칩. persona.tsx 주석상 "검증 도구 완료=ladder L4
  직행"이 설계라면 L4 칩은 정의상 상수로 정당하나, attachment의 **L3**은 그 정의와 어긋날 수 있다.
  사다리 SoT(CONSTELLATION-DESIGN) 대조 후 판정 요망 — 근거 확보 전이라 지적이 아닌 확인 요청으로 남긴다.
- 검증: `npm run verify` 통과(아래 1차와 동일 게이트) 후 PR. 결과는 PR CI가 재확인.

---

# 1차 (초기 발주)

## 수신

- 지시: P0 거짓 UI → P1 죽은 경로 → P2 미완 → P3 계약 위반 순으로 사냥. 모든 지적에 파일:줄 + 재현 방법.
  고칠 수 없으면 삭제/비활성 제안. 세션당 수정 5파일 이하, 한 기능, `npm run verify` 통과. 과잉 지적 금지.
- 금지 준수: 디자인 토큰 변경 없음 · AI 프롬프트 무접촉 · main 직접 push 없음(워크트리+PR) · C1~C12 무완화 · 안전 분류기 무접촉.
- 읽음: CLAUDE.md, docs/CONSTRAINTS.md.

## 작업

수정은 한 기능만: **/settings 플라시보 컨트롤 6종 제거** (파일 2개: `src/app/settings.tsx`, 본 보고서).
브랜치 `logic/settings-placebo-controls`, 워크트리 `.worktrees/logic-audit-260721`.

제거한 것 (모두 로컬 pref만 저장하고 **소비처 0** — `grep features\.(notify|autotag|applock|ondevice|callrec)` 와
`grep -rn "\.palette\b"` 의 유일한 히트가 설정 화면 자기 자신의 렌더였음):

| 컨트롤 | 약속 (locales/ko/settings.json) | 실제 |
|---|---|---|
| 앱 잠금 (settings.tsx:658) | "생체 인증으로 보호" (:114-115) | 잠금 화면/게이트가 코드베이스에 존재하지 않음 |
| 온디바이스 우선 (:660, **기본 ON** app-features.ts:24) | "원문은 기기에서만 분석" (:116-117) | 모든 분석이 클라우드 Gemini 경유 (C1 단일 래퍼) — 허위 프라이버시 표시 |
| 통화 녹음 (:662-669) | "통화 내용을 **기기에서** 받아 적고" (:118-119) | 전사는 클라우드: call-reflection.tsx:28 `transcribeAudio`→gemini.ts. 토글은 화면 진입도 기능도 안 막음 |
| 제안 알림 (:643) | "새 통찰이 생기면 알려줘요" (:112-113) | 통찰 알림 경로 자체가 없음 — notifyNow 사용처는 포커스 알람뿐 (src/lib/ops/reminders.ts, DeepSpaceDesignScreens.tsx:2675) |
| 자동 분류 (:656) | "담는 즉시 별·태그로 정리" (:110-111) | 분류는 실존하나 무조건 실행 — OFF가 아무것도 안 바꿈 (제어권 거짓) |
| 강조 색 cyan/violet (:628 M3PaletteSeg) | "별빛 팔레트" | palette 소비처 0 — m3 토큰은 정적이라 바이올렛 선택해도 화면 불변 |

재현(수정 전): 설정 → 앱 잠금 ON → 앱 완전 종료 후 재시작 → 어떤 잠금도 없음.
설정 → 온디바이스 ON(기본) 확인 → /capture 저장 → `ai_audit_log`에 클라우드 호출 행 적재.

처리: 6개 행 제거 + 죽은 플럼빙(`useAppFeatures` 사용부, `M3PaletteSeg`, seg 스타일) 제거.
`src/lib/settings/app-features.ts`(pref 저장소+테스트)는 보존 — 실기능 배선 시 컨트롤을 복원한다.
같은 화면의 선례를 따름: settings.tsx:177-181 주석(M3ConnectRow 제거, "audit pattern A, fake success").
다크모드(useTheme)·크루 밀도·언어·데이터 삭제 등 실기능 컨트롤은 그대로.

미수정(다음 세션 몫): 아래 회신 P0-2, P1-1.

## 회신

### P0 (거짓 UI)

1. **[수정됨] /settings 플라시보 컨트롤 6종** — 위 표. 보안("생체 인증으로 보호")과
   프라이버시("원문은 기기에서만 분석", 기본 ON 표시) 허위 표기가 포함되어 최우선 제거.
2. **[미수정] /audit 캐논: 시대별 "또렷함 L{n}"이 전원 동일 하드코딩** —
   `src/components/deep-space/DeepSpaceViews.tsx:1546-1552` `AUDIT_ERAS` level `1,2,3,4,3` 고정,
   `:1593-1594`에서 AuditDots + `ds.audit.vividness`("또렷함 L{n}")로 렌더.
   기록 유무와 무관하게 모든 사용자가 유아기 L1·아동기 L2·청소년기 L3·청년기 L4·현재 L3을 봄.
   "정직한 밝기 L1~L5" 캐논 원칙(CLAUDE.md Visual Tier) 정면 위반. 도달 경로 다수: /persona 도구카드,
   capture.tsx:1751, profile.tsx:222, DomainStarLens.tsx:441("회상 시작하기"), star/[domain].tsx:67 등.
   재현: 신규 계정 → /audit 진입 → 기록 0인데 "또렷함 L4" 표시.
   제안: 시대별 커버리지 파이프라인이 없으므로(RecallLensView 주석 :1117-1121이 자인) **레벨 표시 제거**
   (타임라인=내비 전용화)가 정직한 최소 수정. 1파일. 실데이터 산출(auditPeriod별 audit_response 카운트)은 후속.

### P1 (죽은 경로 · 삼켜지는 에러)

1. **ImportHubScreen 가계부 부킹 실패 무통보** — `src/screens/deepspace/import/ImportHubScreen.tsx:248`
   `void ratifyLedgerEntries(userId, chosen).catch(() => undefined)`. 사용자가 체크한 거래들의 ops_ledger
   부킹이 통째로 실패해도(네트워크 등) 성공 연출로 허브 복귀. :238 kakao 인물 upsert 동일 패턴.
   임포트 본체는 성공했으니 "best-effort" 주석 의도는 이해되나, 사용자가 **선택**한 커밋의 소실은 무통보면 안 됨.
   제안: 실패 시 토스트 1줄("가계부 반영은 실패했어요. 다시 임포트하면 반영돼요").
2. 그 외 `.catch(() => {})` 다수 검토 → 대부분 로컬 설정 미러/정리 경로(무해) 또는 문서화된 fail-open
   (northstar.tsx:76 "getReasoningUsage is fail-open" 등)으로 **통과** 판정.

### P2 (미완 · 데드코드)

1. `app-features.ts` `connections`(cal/health/notion) — 렌더러 0(설정은 :344에서 features/palette만 구조分解했었음),
   죽은 pref. 유지(미래 배선) 무해하나, 배선 계획이 없으면 삭제 후보.
2. DeepSpaceViews의 미배선 참조 뷰들(ChatView:528 정적 버블, RhythmLensView:1307, RecallLensView:1114,
   RelationalLensView:1611, PossibleLensView:1333) — **마운트 0 확인**(grep, 라이브 라우트 임포트 목록 대조).
   Rhythm/Recall/Relational은 "UNWIRED ON PURPOSE + 정직한 빈 상태" 주석 있음. ChatView만 동급 주석 부재 → 1줄 권장.
3. `src/lib/payments/purchases.ts:16` TODO(IAP-webhook) — 문서화된 스캐폴드. 스토어 키/상품 미설정이라 현재
   발화 불가 경로지만, 런칭 GO 전에 구매→서버 entitlement 반영이 없으면 "결제됐는데 등급 그대로"가 됨. 런칭 게이트에 등재 요망.
4. 개발용 화면의 TODO들(DeepSpaceHubDockScreen.tsx:132 "실제 대화 저장은 다음 단계", DeepSpaceFlowMapScreen.tsx:101)
   — 전부 `DevOnlyRoute` 뒤(deepspace-hub.tsx:6 등)로 프로덕션 미노출 **확인, 통과**.

### P3 (계약 위반)

- 이번 스윕에서 신규 발견 없음. dds-plans-screen.tsx:238-241의 구 등급(soma/cortex/brain)→표시행(plus/pro)
  매핑은 의도된 브리지로 판단(과잉 지적 안 함).

### 통과 판정 (재발 방지용 기록)

- big-five.tsx / ipip-neo.tsx: `traits={null}` 명시 전달로 DUMMY_LENS_TRAITS 프리뷰 경로 차단 (big-five.tsx:320-352, ipip-neo.tsx:381-387).
- 광고: policy.ts 5규칙 전부 fail-closed, rewarded.ts 웹 스텁 `completed:false` 고정, rewarded.native.ts:74-78
  프로덕션 가드 + `EARNED_REWARD`만 완료 인정. 가짜 시청/가짜 크레딧 경로 없음.
- 페이월(dds-plans-screen): 스토어 미설정 시 정직한 안내(:245 showStoreNotice), free/pro(준비 중) 버튼은
  사실행 처리(:349-353 주석 med#16), 가격은 TIER_PRICE_KRW SoT 직결.
- SeenLensView: 실데이터 로드 + 정직한 빈 상태 + 가짜 self/other 바 없음 (:1180-1186 주석대로 동작).
- /museum: 역사 편집 콘텐츠(사실) + 의도된 오브 슬롯 — 사용자 데이터 위장 아님.
- 전 소스: 빈 catch 블록 0건, `if (true)`/`if (false)`/`|| true` 가드 0건, no-op onPress 0건.
- notion 파일 임포트: proposals.ts:175 markdown 브랜치 실존(07-18 픽스 확인).

### 검증

- `npm run verify` (lint+tsc+i18n+lexicon+legal+LLM boundary+constraints+emdash+anti-anthro+mascot+cycles+jest): 통과 — PR CI로 재확인.
- `deep-space-controls-a11y.test.ts:63` 갱신: 제거된 팔레트 세그의 a11y를 박제하던 블록을
  "설정에 플라시보 컨트롤 없음" 네거티브 가드로 교체(같은 파일의 dead font-size knob 가드 :34-37과 동일 관용구).
- i18n: 키 삭제 없음(미사용 키 잔존은 C7 패리티 무영향) → EN↔KO 패리티 유지.
- 수정 파일 3/5: `src/app/settings.tsx` · `src/lib/__tests__/deep-space-controls-a11y.test.ts` · 본 보고서.
