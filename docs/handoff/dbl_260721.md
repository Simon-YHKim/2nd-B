# dbl_260721 — 반대신문관 회신

_세션ID: dbl · 최초 검증 2026-07-21 02:49:49 KST · 최종 갱신 2026-07-21 08:13:31 KST_

## 수신

- 검증 대상: `docs/handoff/hub_260721.md`
- 원 주장 시각: 2026-07-21 02:29:16 KST
- 검증 기준: `main` / `fc0647c1e553`, GitHub `Simon-YHKim/2nd-B`, EAS 프로젝트의 실제 조회 결과
- 범위: `hub_260721.md`가 완료 또는 실측으로 단정한 정책 상태, P0 3건, PR·브랜치 상태, v0.1.0 바이너리·제출 게이트, 발주·병합 주장
- 시점 주의: PR #1111은 원 주장 뒤인 2026-07-21 02:47:52 KST에 생성됐다. 따라서 “Open PR = 0”은 원 주장 시각과 현재 상태를 분리해 판정했다.

### 추가 수신 — 03:16 KST

- 1순위: `docs/handoff/comm-to-cowork_260721.md:149-159`의 SDK 9행 실태와 근거. 특히 “AdMob 수집 0”, “Firebase iOS 모듈 부재”.
- 2순위: logic PR #1111의 `/settings` 플라시보 컨트롤 6종 제거. 소비처 0, 회귀, “온디바이스 우선”·“통화 녹음” 제거 판단.
- 기준 SHA: comm 표와 PR base `fc0647c1e553`; PR head `afc0901a1298`, merge commit `fcd68a67987f`.
- 금지 준수: 제품 코드 수정, commit, push 없음. 이 회신 문서만 갱신.

### 추가 수신 — 04:01 KST

- 3순위: logic PR #1117의 `/audit` 고정 “또렷함 L{n}” 제거와 ImportHub 거래 부킹 실패 가시화.
- 검증 항목: 삭제·잔존 참조, 시대→기간→`/interview` 경로 동등성, canon test/SoT 핀, `ledgerWarn` 상태 전이, best-effort 비차단, 남은 L4/L3와의 비자의적 경계.
- 착수 기준 대조: 로컬 `main=fc0647c1e553`, `origin/main=582c5b0a3b3b`, 원격 main도 `582c5b0a3b3b`. 로컬 main은 실제로 2 PR 스테일이므로 checkout/pull 없이 PR exact SHA만 읽었다.
- PR 기준: base `84423d413103`, head `ff4125c58699`, merge `d4f81f55efb9`; merge commit은 `origin/main` ancestor임을 확인했다.

### 추가 수신 — 04:29 KST

- 1순위: ai PR #1118과 `docs/handoff/ai_260721.md:160-241`. bare-term 4구성 수치, FP 파급 연쇄, CI 파급, `"약을 모"` 계층 불일치, 코퍼스 한계 고지의 충분성.
- 2순위: comm 처리방침 SDK 9행의 현재성 재검증. 특히 현재 배포 바이너리와 머지된 main을 분리해 “AdMob 수집 0”과 Firebase iOS Analytics 모듈 부재를 다시 판정.
- 3순위: comm PR #1120 머지 후 capability gate. 3개 CTA, production UMP 미호출, preview의 `__DEV__`, `HAS_LIVE_AD_UNIT` 단일 지점.
- 착수 시 `main=582c5b0a3b3b`, `origin/main=20e11763d7df`, 원격 main도 `20e11763d7df`로 로컬이 1커밋 스테일이었다. #1120 머지 `67f2263a5d80`을 검증한 뒤 원격은 #1123 `247ca114c7fd`, 다시 #1122 `8f59fd2e61b8`로 이동했다. 최종 fetch 기준 `origin/main=remote/main=8f59fd2e61b8`, 로컬 main은 4커밋 스테일이다. #1123은 ai 문서·평가기를 후속 변경했으므로 #1118 판정은 exact merge에 고정했고, #1122는 import만 변경했다. SDK/#1120 대상 blob은 `67f2263a..8f59fd2e` diff 0이다.
- PR 기준: #1118 base `d4f81f55efb9`, head `176d3f13d322`, merge `582c5b0a3b3b`; #1120 base `20e11763d7df`, head `c2780f8c3cc8`, merge `67f2263a5d80`. 두 PR 모두 head와 merge tree가 동일함을 확인했다.

### 추가 수신 — 07:11 KST

- 1순위: comm의 릴리즈 위생 3픽스 PR 머지 후 검증. Android release의 `EXPO_PUBLIC_ENABLE_ADS`, 핀 ref 강제, concurrency 경합 분리.
- 2순위: ai의 `scripts/eval-crisis-layer-parity.ts`. L2-only 정확히 6건인지, 비승격 4건의 근거, 새 drift에 대한 CI 실패 여부.
- 3순위: ai가 발견한 `classifyViaProxy` model·effort 미전달 수리와 전후 token 실측.
- 착수 대조: 로컬 `main=582c5b0a3b3b`, 로컬 추적 ref `origin/main=8f59fd2e61b8`, 당시 GitHub remote main도 `8f59fd2e61b8`로 로컬 main이 4커밋 스테일이었다. 검증 중 remote main은 docs-only #1119 `fef34f2b`, docs-only #1124 `72c0f61a`, 이어 대상 #1126 `ae20c7f5`로 전진했다.
- 정확한 코드 기준: baseline #1123 merge `247ca114c7fd`; release hygiene #1125 base/head `fef34f2b/69b8ae08`(최종 조회 때 open); parity+classifier #1126 base/head/merge `72c0f61a/fc589703/ae20c7f5`. #1126 head와 merge target tree diff 0. 제품 코드 수정, commit, push 없음.

## 작업

1. 원 주장을 입력·기대 결과 단위로 분해했다.
2. 로컬 설정은 `rg`와 `git`으로, GitHub 상태는 GitHub 연결과 `gh` 양쪽으로, 바이너리는 `eas-cli build:list`로 독립 조회했다.
3. TODO의 “최근 미머지 브랜치 4건”은 각 브랜치 전체 diff의 stable patch-id와 실제 squash-merge 커밋 patch-id를 대조했다.
4. 코드·설정은 수정하지 않았고 commit·push·merge·배포를 수행하지 않았다.
5. 전체 `npm run verify`는 실행하지 않았다. 이번 작업은 제품 코드 변경이 없는 외부 상태·주장 검증이다.

### 추가 작업 — 03:16 KST 수신분

1. comm 표를 SDK별 상태, 실제 초기화 조건, 데이터 범위, 바이너리 범위로 원자화했다.
2. 코드·GitHub Variables·EAS production env·Supabase `runtime_flags`·EAS 빌드 로그·운영 Pages 번들을 독립 조회했다.
3. PR #1111은 GitHub 원 patch와 정확한 base/head Git tree를 대조하고, 6개 pref의 직접·동적·generic 소비처를 검색했다.
4. 분리 작업트리의 정확한 PR head에서 `npm run verify`를 재실행했다. 386 suites / 2988 tests가 통과했다.
5. 임시 작업트리 정리 중 Windows junction이 공유 `node_modules` 일부를 제거한 것을 감지해 즉시 lockfile 기준으로 복구했다. `npm ls --depth=0` exit 0, `jest@29.7.0`, targeted Jest 1 suite/8 tests PASS, `package.json`·`package-lock.json`·patches diff 0을 확인했다.

### 추가 작업 — 04:01 KST 수신분

1. GitHub 연결에서 PR #1117의 원문·4파일 patch·base/head/merge SHA를 고정하고, exact Git tree끼리만 대조했다.
2. audit 삭제 심볼과 스타일은 base/head `git grep`·`git diff -U0`으로, 내비는 `ERA_PERIOD`·router params·`interview.tsx` 저장 계약으로 확인했다.
3. `canon.test.ts`·`canon-tokens.test.ts`를 직접 읽고, 별도 prototype canon JSON/renderer까지 대조했다.
4. ImportHub는 `ledgerWarn` setter뿐 아니라 `ratifyLedgerEntries()`의 resolve/reject 계약을 실행 재현했다.
5. PR exact head GitHub Actions의 모든 check가 PASS이고 최종 로그가 388 suites / 3008 tests임을 확인했다.

### 추가 작업 — 04:29 KST 수신분

1. PR #1118 평가기를 현재 dirty root와 exact clean merge tree에서 각각 실행했다. 분류 수치는 양쪽에서 같았고, CI 파일 계수는 clean `925/0/24/11`, 현재 root `930/0/25/11`로 달랐다.
2. 합성 코퍼스 34건의 수·고유성·term 포함·truth 내부 일관성을 독립 집계하고 leave-one-out 민감도를 계산했다. 저장→modal→원장→digest→embedding→chat 경로는 코드와 7 suites / 53 tests로 추적했다.
3. SDK 관련 파일의 역사 기준 `fc0647c1`→최종 main diff를 확인했다. 변경은 `eas.json`과 rewarded ads 3파일·테스트뿐이었다. GitHub Variables, EAS production env 이름, 운영 `runtime_flags`, iOS autolinking, PostHog 의존성, Android 0.1.0 build 상태를 다시 조회했다.
4. PR #1120이 실제 머지될 때까지 상태를 재조회한 뒤 exact merge tree에서 광고 테스트를 실행했다. GitHub checks 전부 PASS, 로컬 `src/lib/ads`는 5 suites / 38 tests PASS였다.
5. EAS preview와 React Native `__DEV__` 의미는 Expo·React Native 공식 문서와 `eas.json`을 함께 대조했다. 임시 exact-SHA worktree 2개는 clean 상태를 확인하고 제거했다.
6. 최종 원격 이동 후 #1120 merge가 최신 main의 ancestor이고, `67f2263a..8f59fd2e`의 ads/SDK 대상 diff가 0임을 다시 확인했다.
7. 제품 코드·설정 수정, commit, push, merge, 배포는 수행하지 않았다. 이 회신 문서만 갱신했다.

### 추가 작업 — 07:11 KST 수신분

1. GitHub remote main과 전체 PR 목록을 반복 재조회했다. 처음에는 대상이 없었으나 작업 중 #1125와 #1126이 순서대로 생성됐고, #1126은 checks 전부 PASS 후 `ae20c7f5`로 머지됐다. 각 상태 전환 때 exact base/head/merge를 다시 고정했다.
2. 정확한 #1123 포함 tree에서 `eval-crisis-layer-parity.ts`를 실행하고, L2 마커 30개를 실제 `classifyInput()`에 전수 입력해 gate 통과 항목을 독립 집계했다.
3. 네 비승격 후보는 저장소를 바꾸지 않고 런타임 `CRISIS_TERMS` 배열에만 임시 주입한 뒤 정상 예문이 실제로 red가 되는지 확인했다.
4. L1 세 사본 parity Jest를 실행했다. 1 suite / 26 tests가 PASS했다.
5. L1 drift와 새 L2 marker를 인메모리로 주입했다. 스크립트는 drift를 출력했지만 두 경우 모두 exit 0이었다. EN `includes` 근사와 실제 word-boundary matcher의 차이도 별도 입력으로 재현했다.
6. `classifyViaProxy` body부터 proxy 기본 model·effort·thinking budget·axis key fallback까지 추적했다. #1126 전에는 산출물이 없었으나, 생성 후 exact head 테스트와 QA 계정의 `ai_audit_log` read-only query로 동일 prompt hash 두 쌍의 model/effort/key/token/latency를 대조했다.
7. #1126 gate에는 일반 신규 marker·stale exception·EN word-boundary 반례를 각각 인메모리 주입했다. 저장소 파일은 바꾸지 않았고 exit code를 직접 기록했다.
8. 검증용 분리 worktree는 제품 파일을 수정하지 않았고, 이 회신 문서 외 파일을 쓰지 않았다.

## 회신

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| 정책 상태 | “`CONTROL.md` → `state: running`. 라이브 머신 = E: (202502) 단독” | PASS | `E:\Coding Infra\AI Infra\Communication\CONTROL.md:4,6,29`에 각각 `state: running`, E:/202502 유일 라이브 허브, split-brain 가드가 명시돼 있다. |
| 역할·게이트 | “Claude=오케스트레이션·git 단독, Codex=UI/UX+구현, agy=네이티브/에뮬 QA, Grok=요청기반 리서치” 및 “파괴·실비용·secrets·안전임상·법무 = Simon” | PASS | `ROUTING.md:11-32,90-107`이 네 레인을 그대로 정의하고, `CONTROL.md:34`가 다섯 게이트를 그대로 명시한다. |
| P0-1 웹 배포 | “web-deploy.yml **6연속 success** (최신 run 29757867405, 07-20 16:02Z)” | PASS | `gh run list --workflow web-deploy.yml --limit 20 --json ...`을 계수한 실제 출력: `consecutive_success=9 latest_id=29757867405 latest_createdAt=2026-07-20T16:02:45Z latest_conclusion=success`. 주장한 최소 6회를 충족한다. |
| 소스·GitHub 릴리즈 버전 | “소스 `app.json`=0.1.0 · 최신 릴리즈=**v0.0.9**(07-18)” | PASS | `app.json:6`은 `0.1.0`. `gh release view --json tagName,publishedAt` 실제 출력은 `v0.0.9`, `2026-07-18T06:37:19Z`. |
| v0.1.0 바이너리 존재 여부 | “P0-2 v0.1.0 바이너리 부재” | FAIL | `npx eas-cli build:list --limit 50 --json --non-interactive` 실제 집계: `android_0.1.0=0 ios_0.1.0_finished=3`. iOS production STORE 바이너리 build 4·5·6은 모두 원 주장 시각 전에 `FINISHED`였다. “공개 GitHub 릴리즈/Android 바이너리 부재”는 맞지만, 플랫폼 한정 없이 “v0.1.0 바이너리 부재”라고 한 범위는 실제보다 넓다. |
| P0-3 iOS 선행 작업 | “#1104~#1108 머지 완료(plist·frameworks·infoplist·submit·SKAdNetwork)” | PASS | GitHub 연결에서 #1104~#1108 모두 `merged:true`. 다섯 merge SHA의 `git merge-base --is-ancestor <sha> origin/main`도 전부 `True`. 현재 근거는 `app.json:24,28-29,96,123,134,160`, `app.config.js:16`, `eas.json:79-84`. |
| Open PR | “**Open PR = 0.**” | PASS | 원 주장 시각 기준 PASS. GitHub 이력상 #1110은 `2026-07-20T16:02:42Z`에 닫혔고 다음 PR #1111은 원 주장 뒤인 `2026-07-20T17:47:52Z`에 생성됐다. 현재는 #1111이 열려 있으므로 이 상태는 더 이상 최신이 아니다. |
| 트리아지 대상 브랜치 4건 | “원격 브랜치 ahead는 전부 squash-merge 잔해” | PASS | TODO.md:38의 네 브랜치만 범위로 삼았다. 각 `merge-base..branch` aggregate patch-id와 연결된 merged PR의 merge patch-id가 모두 동일했다: `fix/qa... fb88b68d=True`, `codex/analytics... f5048b9a=True`, `agent/eas... aee6e78c=True`, `chore/ship... 186fd598=True`. 대응 PR은 #1087/#1049/#1082/#1084, 모두 merged. |
| 살아있는 P0 수 | “살아있는 차단 = **P0-2 v0.1.0 절단 1건뿐**” | PASS | P0-1은 웹 9연속 성공, P0-3은 관련 PR과 설정이 main에 존재한다. 반면 최신 GitHub 릴리즈는 v0.0.9이고 EAS 28건 조회에서 Android 0.1.0은 0건이라, “Android 포함 v0.1.0 릴리즈 절단”은 여전히 열려 있다. 단, iOS 0.1.0 바이너리까지 없다는 뜻으로 확장하면 위 FAIL이다. |
| EAS 크레딧 게이트 | “EAS 빌드 크레딧 잔량 확인(**실비용**)”이 P0-2를 막는다 | UNVERIFIABLE | `eas-cli build:list`는 빌드 이력만 제공한다. 현재 크레딧 잔량·청구 상태를 입증하는 billing 출력이나 대시보드 증거가 수신 문서에 없고, 이 검증 세션에도 결제 계정 근거가 제공되지 않았다. |
| Apple 로그인 게이트 | “`eas submit` 시 Apple 로그인”이 필요하다 | FAIL | `.github/workflows/eas-ios-submit.yml:9-12,82-85`는 ASC API key + `--non-interactive` 경로다. `gh run list --workflow eas-ios-submit.yml` 실제 출력은 run `29696576416`, `29700028228` 두 건 모두 `success`; run `29700028228`의 `Submit iOS build to App Store Connect` 단계도 `success`. 이 실제 제출 경로에는 대화형 Apple 로그인이 없다. |
| DSA 거래자 상태 | “DSA 거래자 상태 제출”이 미완료다 | UNVERIFIABLE | App Store Connect의 현재 Business/DSA 계정 상태를 보여 주는 원 출력이 없고 개인 계정 화면도 수신 범위에 제공되지 않았다. |
| 외부 도착 대기 | “Paddle 승인 · 사업자등록번호는 외부 도착 대기” | UNVERIFIABLE | `TODO.md:17-18`에는 대기로 기록돼 있으나, 실제 Paddle 승인 상태와 사업자등록 발급 상태를 독립 확인할 외부 원 출력이 없다. 문서끼리의 일치만으로 실제 상태를 PASS 처리하지 않았다. |
| 발주·병합 행위 | “이번 사이클 발주 = 0건”, “발주 0건이므로 병합 대상 없음” | UNVERIFIABLE | GitHub에서 원 주장 시각의 Open PR 0은 확인했지만, 허브 outbox·외부 메시지·다른 AI 주문 채널 전체의 송신 로그가 검증 대상으로 제공되지 않아 “발주 0”을 완전성 있게 확인할 수 없다. |
| 0.1.0 바이너리 취급 이력 | “어느 AI도 아직 0.1.0 바이너리를 만져본 바 없다” | FAIL | EAS에는 원 주장 전에 완료된 0.1.0 iOS STORE build 4·5·6이 있고, GitHub Actions의 `EAS iOS Submit (TestFlight)`도 두 차례 성공했다. 최소한 빌드 생성과 제출 자동화가 실제 바이너리를 취급했으므로 원 주장은 성립하지 않는다. |

### FAIL 최소 재현

1. v0.1.0 바이너리 부재

```powershell
$b=(npx eas-cli build:list --limit 50 --json --non-interactive 2>$null | ConvertFrom-Json); "android_0.1.0=$(@($b|?{$_.platform-eq'ANDROID'-and$_.appVersion-eq'0.1.0'}).Count) ios_0.1.0_finished=$(@($b|?{$_.platform-eq'IOS'-and$_.appVersion-eq'0.1.0'-and$_.status-eq'FINISHED'}).Count)"
```

실제 출력: `android_0.1.0=0 ios_0.1.0_finished=3`

2. Apple 로그인 필요

```powershell
gh run list --repo Simon-YHKim/2nd-B --workflow eas-ios-submit.yml --limit 2 --json databaseId,conclusion,createdAt
```

실제 출력: `29696576416=success`, `29700028228=success`. 두 workflow는 `.github/workflows/eas-ios-submit.yml:82`의 `--non-interactive` 경로다.

3. 어느 AI도 0.1.0 바이너리를 취급하지 않음

```powershell
gh run view 29700028228 --repo Simon-YHKim/2nd-B --json conclusion,jobs --jq '{conclusion,steps:[.jobs[].steps[]|select(.name=="Submit iOS build to App Store Connect")|{name,conclusion}]}'
```

실제 출력: 전체 `success`, `Submit iOS build to App Store Connect=success`.

판정 합계: **PASS 8 / FAIL 3 / UNVERIFIABLE 4**.

담당 세션 전달 제안만 남긴다. `hub_260721.md`의 P0-2 표현을 “공개 v0.1.0 릴리즈 및 Android 0.1.0 APK/AAB 부재”로 좁히고, Apple 로그인 게이트와 “아무도 바이너리를 만지지 않음” 문장은 실제 non-interactive 제출 이력에 맞게 정정해야 한다. 이 세션은 수정하지 않았다.

### 추가 회신 1 — comm SDK 실태 표

원문은 `docs/handoff/comm-to-cowork_260721.md:147-159`의 “실태 표 (0.1.0 바이너리 + 웹 프로드 기준)” 9행이다. 한 행 안에서 사실과 과장이 섞인 경우 원자 항목으로 분리했다.

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| Sentry 웹 — 가동 | “웹 **가동** (사용자 동의와 무관)” | PASS | `src/lib/analytics/index.ts:730-757`은 브라우저+DSN이면 동의 검사 없이 init하고 `sendDefaultPii:false`, traces/replay 0을 설정한다. GitHub repo Variable의 DSN 설정과 운영 Pages entry bundle의 동일 DSN 포함도 확인했다. |
| Sentry 웹 — PII 범위 | “PII 차단: sendDefaultPii false · … URL/브레드크럼 스크럽” | FAIL | `src/lib/analytics/index.ts:659-699`의 event 타입·scrubber는 request, transaction, breadcrumb만 처리한다. `event.user`, exception message/stack, `extra`, `contexts`를 제거하지 않으므로 “PII 차단”은 구현보다 넓다. `sendDefaultPii:false`도 모든 사용자 입력 PII 제거를 보증하지 않는다. |
| Sentry 네이티브 — 초기화 | “네이티브 iOS+Android **가동** … 동의와 무관” | PASS | `src/app/_layout.tsx:47-62`가 RN 런타임+DSN이면 동의 없이 init한다. EAS production DSN이 설정돼 있고, iOS 0.1.0(6) Xcode 로그에는 `RNSentry` 249행, Android 0.0.9(11) Gradle 로그에는 `sentry_react-native` 44행이 있다. `rg` 결과 `Sentry.setUser/setTag/setContext/setExtra(s)`는 src에서 0건이다. |
| Sentry 네이티브 — 바이너리 범위 | “iOS+Android **가동 (0.1.0 바이너리)**” | FAIL | `eas build:list --platform android --app-version 0.1.0 ...` 실제 결과 0건. iOS 0.1.0은 5건(완료 3, 오류 2)이나 Android 최신 production은 0.0.9 build 11이다. iOS+Android 모두 0.1.0이라는 범위가 틀렸다. |
| GA4 웹 — 현재 실태 | “서버 확정 성인 + opt-in + `analytics_enabled=true` + ID `G-R6BK0F1RWE`로 **조건부 가동**” | PASS | `src/lib/analytics/index.ts:227-234,831-870`, `src/app/_layout.tsx:326-395`. 운영 DB actual: `analytics_enabled=True`; repo Variable의 ID actual: `G-R6BK0F1RWE`. 운영 Pages bundle에도 동일 ID가 있다. 관련 분석 테스트 42/42 PASS. |
| GA4 웹 — 연속 시점 | “`analytics_enabled` = true 실측 (**07-17부터**)” | UNVERIFIABLE | 현재 행과 `updated_at`은 확인했지만 변경 이력/audit trail은 제공되지 않았다. 현재 true라는 사실만으로 07-17 이후 한 번도 false가 아니었다는 연속 상태는 입증할 수 없다. |
| Firebase Analytics Android | “Android만 **조건부 가동**; 기본 OFF → 서버 확정 성인+동의 시 ON” | PASS | `firebase.json:3-11` 자동 수집·ad storage 기본 false. `src/lib/analytics/index.ts:227-326,717-721,1012-1029`와 `src/app/_layout.tsx:342-393`이 fail-closed 성인+동의 gate 뒤에 `setAnalyticsCollectionEnabled(true)`를 호출한다. Android EAS 로그에 analytics 모듈 51행, 관련 테스트 42/42 PASS. |
| Firebase Analytics iOS — 핵심 | “**모듈 부재 = 수집 불가**” | PASS | `npx --no-install expo-modules-autolinking react-native-config --platform ios --json` actual: `@react-native-firebase/analytics` ABSENT. iOS 0.1.0(6) Xcode 로그도 `RNFBAnalytics=0`, `FirebaseAnalytics=0`; `src/lib/analytics/index.ts:269-284`는 모듈 없으면 import하지 않고 종료한다. |
| Firebase Analytics iOS — pod 범위 | “react-native.config.js가 iOS pods 제외 유지” | FAIL | config는 `react-native.config.js:13-18`에서 app+analytics를 모두 제외한다고 적지만 resolved autolinking actual은 `RNFBApp=PRESENT`, `RNFBAnalytics=ABSENT`; Xcode 로그도 `RNFBApp=469`. Analytics 모듈 부재는 맞지만 Firebase pod 전체 부재라는 근거 범위는 틀렸다. |
| PostHog 웹 | “key/host가 설정된 **조건부 가동**” | FAIL | `src/lib/analytics/index.ts:914-940`은 `import("posthog-js")` 실패를 catch하는 준비 코드다. `npm ls posthog-js --all` actual은 `(empty)`/exit 1이고 package/lock에도 의존성이 없다. 운영 bundle의 해당 module mapping도 `null`이라 key/host가 있어도 init할 수 없다. |
| Clarity 웹 — 현재 설정 | “`analytics_enabled` AND `clarity_enabled` 둘 다 true + 동의, consentv2, ID `xb3qenit2h`” | PASS | 운영 DB actual: 두 flag 모두 `True`; repo Variable actual ID 일치. `src/lib/analytics/index.ts:878-908`이 flag/ID/route 조건 뒤 script와 `consentv2`를 설정하고, `:419-426`이 동의 갱신을 보낸다. 운영 Pages bundle에도 동일 ID가 있다. |
| Clarity 웹 — 게이트 범위 | “게이트 체인: 두 flag + 동의” | FAIL | 실제 inject에는 route allowlist도 필요하다(`src/lib/analytics/index.ts:582-594,878-883`). 더 중요한 범위 차이로, allowed route에서 한 번 inject되면 같은 page lifetime의 `/record/*`, `/peer/*` 이동도 URL·click·referrer가 계속 기록된다고 코드가 명시한다(`:568-575`). 법적 입력용 “게이트 체인”으로는 불완전하다. |
| AdMob — 정적 차단 경로 | “플래그 부재=false → 리워드 차단 → SDK 미초기화; delay init; 실유닛 0” | PASS | EAS production env와 `eas.json`에 `EXPO_PUBLIC_ENABLE_ADS`가 없고 `src/lib/env.ts:177-180` 기본 false. `src/lib/ads/policy.ts:85-98`이 먼저 차단하며 SDK initialize의 유일 호출은 `src/lib/ads/consent.native.ts:75-82`. production은 실유닛 전에 false를 반환한다(`rewarded.native.ts:66-79`); `app.json:155-159`은 delay init true. |
| AdMob — 실제 수집량 | “**수집 0** (0.1.0 바이너리 실태)” | UNVERIFIABLE | 소스·빌드 env는 앱이 제어하는 initialize/ad-request 경로가 막혔음을 증명할 뿐이다. 설치된 SDK가 포함된 실제 IPA의 lifecycle/network trace나 AdMob/Firebase 콘솔 수집 건수 원출력이 없어 “총수집 0”은 확인 불가다. |
| AdSense 웹 | “`EXPO_PUBLIC_ADSENSE_CLIENT` 미설정으로 **OFF**” | PASS | repo Variable과 운영 bundle에 AdSense client/`ca-pub` 값이 없다. `src/lib/ads/policy.ts:48-53`은 enable flag와 client를 모두 요구한다. |

#### SDK FAIL 최소 재현

1. Sentry 웹 “PII 차단”

```powershell
rg -n 'delete event\.|event\.transaction|event\.breadcrumbs' src/lib/analytics/index.ts
```

실제 출력은 `692-698`의 request fields, transaction, breadcrumbs뿐이며 user/exception/extra/contexts 제거는 0건이다.

2. Sentry 네이티브 “iOS+Android 0.1.0”

```powershell
npx eas-cli build:list --platform android --app-version 0.1.0 --limit 50 --json --non-interactive
```

실제 출력: `[]`.

3. Firebase iOS “pods 제외”

```powershell
npx --no-install expo-modules-autolinking react-native-config --platform ios --json
```

실제 resolved 결과: `@react-native-firebase/app=PRESENT`, `@react-native-firebase/analytics=ABSENT`.

4. PostHog “조건부 가동”

```powershell
npm ls posthog-js --all
```

실제 출력: `└── (empty)` / exit 1.

5. Clarity “게이트 체인”

```powershell
rg -n 'later same-page-lifetime|including /record|CLARITY_ALLOWED_ROUTE_PREFIXES' src/lib/analytics/index.ts
```

실제 출력: `572-575`의 inject 후 `/record/*`, `/peer/*` 기록 잔존과 `582`의 추가 route gate.

SDK 원자 판정 합계: **PASS 8 / FAIL 5 / UNVERIFIABLE 2**.

처리방침 입력에는 다음처럼 좁혀 써야 한다. Firebase iOS는 “Firebase Analytics 모듈 부재로 Analytics 수집 불가; RNFBApp core는 포함”이다. AdMob은 “현재 빌드 설정상 앱 제어 init/ad request 경로 비활성”까지이며 “실제 수집 0”으로 단정할 수 없다. PostHog는 현재 미가동이다. Clarity는 inject 후 SPA route 이동의 잔여 기록 범위를 함께 고지해야 한다.

### 추가 회신 2 — logic PR #1111

GitHub 원문:

> “컨트롤 6개가 전부 플라시보였다 — 로컬 pref만 저장하고, 그 pref를 읽는 코드가 리포 어디에도 없다.”
>
> “보안·프라이버시 허위 표시(앱 잠금·온디바이스)가 포함되어 있어 ‘비활성보다 제거’를 택했다.”
>
> “`npm run verify` 전체 통과 (386 suites / 2988 tests).”

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| 앱 잠금 pref | “`applock` 소비처 0” | PASS | base에서 설정 표시·변경은 `fc0647c1:src/app/settings.tsx:658`뿐. store 자체를 제외한 `useAppFeatures`, storage key, direct/dynamic key 검색은 0건이고 잠금 gate가 이 pref를 읽지 않는다. |
| 온디바이스 pref | “`ondevice` 소비처 0” | PASS | base 사용은 `fc0647c1:src/app/settings.tsx:660`뿐. LLM·분석 경로의 pref 참조는 0건. 기본값은 `fc0647c1:src/lib/settings/app-features.ts:24`의 true다. |
| 통화 녹음 pref | “`callrec` 소비처 0” | PASS | base 사용은 `fc0647c1:src/app/settings.tsx:667-668`뿐. `/call-reflection` route, 녹음 시작, `transcribeAudio`가 이 pref를 읽는 곳은 0건. |
| 제안 알림 pref | “`notify` 소비처 0” | PASS | base 사용은 `fc0647c1:src/app/settings.tsx:643`뿐. 제안 알림 실행 경로가 이 pref를 읽는 곳은 0건. |
| 자동 분류 pref | “`autotag` 소비처 0” | PASS | base 사용은 `fc0647c1:src/app/settings.tsx:656`뿐. capture/tag/domain 분류 경로의 pref 참조는 0건. |
| 강조 색 pref | “`palette` 소비처 0” | PASS | `useAppFeatures()`에서 나온 palette의 base 소비는 `fc0647c1:src/app/settings.tsx:628`의 `M3PaletteSeg`뿐. 다른 palette 지역 변수는 `settings.appFeatures.v1`과 무관하다. |
| 실제 UI 제거 | “6개 행 + 죽은 plumbing 제거” | PASS | head `afc0901a:src/app/settings.tsx`에서 6개 번역 키, `useAppFeatures`, `M3PaletteSeg`, `featureOn`, `toggleFeature`, segment styles가 모두 0건. GitHub patch도 해당 행·import·style만 제거한다. |
| 보고한 grep 근거 | “`features\.(notify\|…)` / `\.palette\b`의 유일한 hit가 설정 렌더” | FAIL | 원문 정규식을 base 설정 파일에 그대로 실행하면 출력 0건/exit 1이다. 실제 렌더는 `featureOn("notify")`와 `<M3PaletteSeg palette={palette}>` 형태다. 핵심 소비처 0은 별도의 hook/storage/key 검색으로만 PASS다. |
| pref 저장소 보존 | “`app-features.ts` pref 저장소 보존” | PASS | `git diff --exit-code fc0647c1 afc0901a -- src/lib/settings/app-features.ts` exit 0; base/head blob SHA도 동일하다. |
| pref 테스트 보존 | “pref 저장소·테스트는 보존” | FAIL | base/head tree 모두 app-features 전용 테스트 파일이 없다. `DEFAULT_FEATURES`, `APP_FEATURES_KEY`, `__resetAppFeaturesForTests`의 모듈 밖 test 참조도 0건이다. 보존된 것은 저장소뿐이다. |
| 6종 네거티브 가드 | “설정에 플라시보 컨트롤 없음 네거티브 가드로 교체” | FAIL | `afc0901a:src/lib/__tests__/deep-space-controls-a11y.test.ts:72-74`는 `useAppFeatures`, `M3PaletteSeg` 두 문자열만 검사한다. 다른 state나 component로 5개 toggle을 재삽입해도 통과 가능하므로 6종 가드라는 범위는 틀렸다. |
| full verify | “386 suites / 2988 tests 통과” | PASS | 정확한 PR head 분리 작업트리에서 `npm run verify` actual exit 0: `Test Suites: 386 passed, 386 total`, `Tests: 2988 passed, 2988 total`. `gh pr checks 1111`도 freshness/lint/recapture/verify/web-export-smoke 전부 pass. |
| 실기능 설정 보존 | “다크모드·크루 밀도·언어·데이터 삭제 등 실기능 컨트롤 무변경” | PASS | head `settings.tsx:575-580,735-790,845-879`; notices/reasoning/integrations도 `:587-621`에 유지. `git diff --check` exit 0, 변경 파일은 문서·settings·source-string test 3개뿐이다. |
| 자동검증 범위 회귀 | “제거로 회귀 없음” | PASS | exact head에서 lint/typecheck/i18n/constraints/Jest와 web export CI가 모두 통과했고, pref store 및 실제 설정·통화 기능 코드는 diff 밖이다. 정적·자동검증 범위에서 회귀를 재현하지 못했다. |
| 인터랙티브 회귀 | “실제 웹/네이티브 `/settings` 터치·레이아웃도 회귀 없음” | UNVERIFIABLE | exact head의 실제 기기/브라우저 렌더·터치 검증은 수행하지 않았다. 변경된 test도 render test가 아니라 source-string 검사다. |
| 클라우드 분석 범위 | “**전 분석이 클라우드 Gemini**” | FAIL | `fc0647c1:src/lib/llm/gemini.ts:476`은 원문을 로컬 `classifyInputAnyLocale`로 먼저 분석하고 red면 cloud를 short-circuit한다. `src/lib/records/detect-domain.ts:129`에도 결정적 로컬 분석이 있다. 정확한 범위는 “라이브 LLM 분석은 pref와 무관하게 원문을 cloud로 전송”이다. |
| 온디바이스 약속의 실제 | “기본 ON인데 실제 원문 분석은 cloud 경로” | PASS | UI 원문은 `fc0647c1:locales/ko/settings.json:116-117`의 “원문은 기기에서만 분석”. 반면 production live+edge에서 `src/lib/llm/gemini.ts:572-574`가 `input.user`를 proxy body로 보내며 `ondevice`를 읽지 않는다. |
| 통화 전사의 실제 | “기기 전사 약속과 달리 cloud `transcribeAudio`” | PASS | UI 원문은 `fc0647c1:locales/ko/settings.json:118-119`. `src/app/call-reflection.tsx:134-135`는 audio base64를 `transcribeAudio`로 넘기고 `src/lib/llm/gemini.ts:1112-1117`은 proxy로, 직접 경로 `:1134-1140`은 Google inlineData로 전송한다. |
| 온디바이스 toggle 제거 판단 | “문구 정정 후 유지보다 제거가 옳다” | PASS | 소비처 없는 toggle은 문구만 고쳐도 제어권을 허위 표시한다. pref store는 보존돼 실제 배선 후 복원 가능하다. 삭제는 false default-ON 약속을 중단하며 실제 처리 동작은 바꾸지 않는다. |
| 통화 녹음 toggle 제거 판단 | “문구 정정 후 유지보다 제거가 옳다” | PASS | 실제 opt-in은 `src/app/call-reflection.tsx:97-135`의 세션별 녹음 시작/중지다. 전역 `callrec=false`는 route·녹음·전사를 막지 않았으므로 무배선 toggle 삭제로 기능·보호 동작이 사라지지 않는다. 정확한 cloud 전사 고지는 별도 정보 행/처리방침에서 해야 하며, 무배선 toggle 유지가 대안은 아니다. |
| 통화음성 법적 고지 충분성 | “toggle 제거 후 현재 고지만으로 상대방 음성 처리까지 충분하다” | UNVERIFIABLE | `docs/legal/privacy-policy.md:10-35`는 이용자 콘텐츠의 Google AI 처리·국외이전을 일반 고지한다. 이것이 상대방 통화음성까지 법적으로 충분한지는 기술 검증으로 판정할 수 없고 법률 검토가 필요하다. |

#### PR #1111 FAIL 최소 재현

1. 보고한 grep 근거

```powershell
git grep -n -E 'features\.(notify|autotag|applock|ondevice|callrec)|\.palette\b' fc0647c1e553 -- src/app/settings.tsx
```

실제 출력: 없음 / exit 1.

2. “pref 테스트 보존”

```powershell
git ls-tree -r --name-only afc0901a1298 | rg 'app-features.*test|test.*app-features'
```

실제 출력: 없음 / exit 1.

3. “6종 네거티브 가드”

```powershell
$s=(git show 'afc0901a1298:src/app/settings.tsx')-join "`n"; $m=$s+"<M3ToggleRow label={t('callRecording')} />"; @('useAppFeatures','M3PaletteSeg','callRecording')|%{"$_=$($m.Contains($_))"}
```

실제 출력: `useAppFeatures=False`, `M3PaletteSeg=False`, `callRecording=True`. 현 두 assertion은 통과하면서 통화 녹음 행은 존재할 수 있다.

4. “전 분석이 클라우드 Gemini”

```powershell
git grep -n -E 'classifyInputAnyLocale\(input\.user|function withDomainTag' fc0647c1e553 -- src/lib/llm/gemini.ts src/lib/records/detect-domain.ts
```

실제 출력: `gemini.ts:476`의 로컬 safety 분석, `detect-domain.ts:129`의 로컬 domain 분석.

PR 원자 판정 합계: **PASS 15 / FAIL 4 / UNVERIFIABLE 2**.

최종 판단: PR #1111의 핵심인 “6개 pref의 실제 소비처 0”과 “무배선 온디바이스·통화 녹음 toggle 제거”는 PASS다. 자동검증 범위의 회귀도 PASS다. 다만 보고서의 grep, “pref 테스트 보존”, 6종 네거티브 가드, “전 분석이 cloud”라는 네 문장은 실제보다 넓어 FAIL이다. 정확 head의 인터랙티브 회귀와 상대방 통화음성 법적 고지 충분성은 UNVERIFIABLE이다.

### 추가 회신 3 — logic PR #1117

GitHub 원문:

> “`AUDIT_ERAS`의 고정 레벨(1,2,3,4,3) … 레벨 표시 제거, 타임라인=내비 전용(시대 탭 → 기간 스코프 `/interview`, 기존 동작 무변경).”
>
> “`ratifyLedgerEntries(...).catch(() => undefined)` … 거부 시 허브 상단에 경고 1줄 … best-effort 의미는 유지.”
>
> “핀 부재 확인: `grep AuditDots|vividness|AUDIT_ERAS` → 컴포넌트 파일 외 참조 0.”

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| 기준 최신성 | 스테일 main이 아닌 PR 실제 상태로 검증 | PASS | 착수 actual: `main=fc0647c1e553`, `origin/main=582c5b0a3b3b`, `ls-remote main=582c5b0a3b3b`. PR head `ff4125c58699`, merge `d4f81f55efb9`; merge는 `origin/main` ancestor다. |
| `AUDIT_ERAS.level` | “고정 레벨 1,2,3,4,3의 level 필드 삭제” | PASS | base `DeepSpaceViews.tsx:1546-1551`에는 타입과 5개 값이 있다. head `:1549-1555`에는 `key/eraKey/rangeKey`만 있고 `level`이 없다. |
| 도트 상수·컴포넌트 | “`AUDIT_DOTS`와 `AuditDots` 삭제” | PASS | base `DeepSpaceViews.tsx:1558-1566`에 존재. `git grep -n -E 'AUDIT_DOTS\|AuditDots' ff4125c -- src` actual은 출력 없음/exit 1. |
| “또렷함” 앱 렌더 | “도트 + `ds.audit.vividness` 렌더 삭제” | PASS | base `DeepSpaceViews.tsx:1592-1594`에 렌더가 있다. head src에서 `ds.audit.vividness`, `AuditDots`, `e.level` 소비는 0건. 5개 locale key 정의는 PR이 명시한 대로 잔존한다. |
| 관련 스타일 실제 삭제 | “관련 고아 스타일 전부 삭제” | PASS | head에서 `auditEraMeta`, `auditDotRow`, `auditDot`, `auditDotOn`, `auditDotOff`, `auditVivid` 정의·참조가 모두 0건이다. |
| 고아 스타일 수 | “고아 스타일 **5종** 삭제” | FAIL | exact diff actual은 위 style key **6개** 삭제다. dot/vivid child 5개만 세었다면 wrapper `auditEraMeta`를 제외했다는 범위가 원 주장에 없다. 기능상 과소 삭제는 아니지만 숫자 보고는 틀렸다. |
| app source 잔존 참조 | “제거 심볼의 남은 app 참조 0” | PASS | head src에서 제거된 level member, `AUDIT_DOTS`, `AuditDots`, audit meta/dot/vivid styles, vividness renderer는 0건. `AUDIT_ERAS` 자체는 내비 목록으로 의도대로 남는다(`DeepSpaceViews.tsx:1549,1571`). |
| prototype canon 잔존 | “테스트·캐논 핀 없음 / 컴포넌트 파일 외 참조 0” | FAIL | `src/lib/canon/index.ts:1-7`은 `public/proto/data`를 JSON canon/단일 SoT/pixel contract라 명시한다. 그런데 head `public/proto/data/core/mock.json:96-120`에 1,2,3,4,3이 남고 `public/proto/sb-screens-know.jsx:325-337`가 `<Dots level={e.level}>`와 “또렷함 L{e.level}”을 그대로 렌더한다. PR은 `public/proto`를 전혀 변경하지 않았다. |
| 시대→기간 매핑 | “타임라인은 내비 전용, 시대 탭의 기간 스코프 동작 무변경” | PASS | base `DeepSpaceViews.tsx:1556`과 head `:1559`의 `ERA_PERIOD`가 동일하다: infancy/child/teen→teens, young→20s, now→current. |
| `/interview` push·수신 | 같은 주장 | PASS | base `DeepSpaceViews.tsx:1585`와 head `:1577`의 `router.push({pathname:"/interview", params:{period:...}})`가 동일. `src/app/interview.tsx` 전체 diff exit 0이며 `:82-84`가 period를 정규화하고 `:155`가 `auditPeriod`를 저장한다. |
| 실제 탭 인터랙션 회귀 | “실제 기기·브라우저에서도 동작 회귀 0” | UNVERIFIABLE | 정적 경로 계약과 full CI는 통과했지만 `PastMeErasView` 시대 카드를 직접 누르는 테스트가 repo에 0건이고 exact head를 실제 UI로 탭하지 않았다. |
| `canon.test.ts` 핀 | “테스트·캐논 핀 없음” | PASS | `src/lib/canon/__tests__/canon.test.ts` 68줄 전체에 audit era, level, dots, vividness assertion이 없다. 관련 grep actual 출력 없음/exit 1. |
| `canon-tokens.test.ts` 핀 | 같은 주장 | PASS | `src/lib/canon/__tests__/canon-tokens.test.ts` 28줄은 palette/token만 검사하며 관련 값·식별자 grep은 0건. |
| 다른 테스트 핀 | 같은 주장 | PASS | base/head의 전체 test 경로에서 `AUDIT_ERAS`, `AuditDots`, audit dot/vivid style, `ds.audit.vividness` 참조가 0건이다. exact head CI에서 canon 2 suites/11 tests를 포함해 통과했다. |
| `ledgerWarn=true` setter 범위 | “실패 시에만 플래그가 선다” | PASS | head `ImportHubScreen.tsx:255`의 `.catch(() => setLedgerWarn(true))`가 유일한 true setter다. 단 여기서 감지하는 것은 행 실패 결과가 아니라 Promise rejection뿐이다. |
| 실제 거래 부킹 실패 가시화 | “사용자가 고른 거래의 전체 실패가 경고로 표면화” | FAIL | `src/lib/import/ledger-ratify.ts:36-50`은 각 `createLedgerEntry` 예외를 내부 catch하고 `{inserted, failed}`로 정상 resolve한다. 화면은 result의 `failed`를 보지 않고 Promise `.catch`만 본다(`ImportHubScreen.tsx:249-255`). 2/2 실패 실행 actual: `RESOLVED {"inserted":0,"failed":2}`. 따라서 일반 RLS/network 전행 실패에도 `ledgerWarn`은 서지 않는다. |
| 재시도 초기화 | “재시도 시 `ledgerWarn` 리셋” | PASS | 유효한 선택으로 `ratify()`를 다시 시작하면 `ImportHubScreen.tsx:198-203`에서 원장 호출 전에 `setLedgerWarn(false)`를 실행한다. 선택 0건은 `:200-201`에서 호출 없이 반환한다. |
| best-effort 비차단 | “임포트 본체 성공 후 비차단 의미 유지” | PASS | `ImportHubScreen.tsx:255`는 여전히 `void ratifyLedgerEntries(...)`로 await하지 않고 `:266-271`에서 delight·busy 해제·hub 전환을 계속한다. |
| 경고 UI 배선 | “플래그가 서면 허브 상단 `OpsState variant=rate` 표시” | PASS | `ImportHubScreen.tsx:342-346`이 true일 때 경고를 렌더하고 ko/en copy도 `:577-578,602-603`에 있다. 문제는 정상 부킹 실패가 true를 만들지 못한다는 점이다. |
| 자동 회귀 검증 | “verify 통과” | PASS | exact head의 `gh pr checks 1117`은 freshness/lint/recapture/verify/web-export-smoke 전부 pass. final-head CI 로그 actual은 388 suites / 3008 tests PASS다. PR 본문의 387/3004는 final-head보다 앞선 실행 수치다. |
| audit와 lens의 데이터 경계 | “audit만 제거하고 lens level을 남길 객관적 차이가 있다” | PASS | audit 값은 기록 0에도 모든 사용자에게 표시됐다. 반면 live Big Five/Attachment 화면은 실제 result가 있어야 filled chip을 렌더한다(`DeepSpaceViews.tsx:726,863`; `big-five.tsx:337-351`; `attachment.tsx:329-333`). 무조건 상수와 측정 결과 gate라는 경계는 존재한다. |
| `ds.lens.level=L4` 잔존 근거 | 같은 클래스의 L4를 남김 | PASS | production Big Five는 BFI-44 결과가 있을 때만 L4 chip을 보인다(`DeepSpaceViews.tsx:765-772`). SoT `docs/CONSTELLATION-DESIGN.md:139-141,225-236`은 “검증 측정도구 → L4”를 명시한다. 구 `LensView`의 demo default는 있으나 현재 production caller `ipip-neo.tsx:381-382`가 `traits={null}`을 명시해 L4를 렌더하지 않는다. |
| `ds.attachment.level=L3` 잔존 근거 | 같은 클래스의 L3를 범위 밖으로 남김 | FAIL | ECR 결과 gate는 있으나 locale은 `locales/ko/home.json:232-235`에서 L3 고정이다. 같은 SoT는 ECR-S를 검증 측정도구로 분류하고 측정도구를 L4로 정한다(`CONSTELLATION-DESIGN.md:129,139-141,225-236`); `src/app/persona.tsx:132-134`도 ECR 완료→L4라고 명시한다. L3를 남긴 값 근거는 없고 logic 보고서도 SoT 대조 전·근거 미확보라고 인정했다. |

#### PR #1117 FAIL 최소 재현

1. “고아 스타일 5종”

```powershell
$b='84423d413103';$h='ff4125c58699';$x=@(git diff -U0 $b $h -- src/components/deep-space/DeepSpaceViews.tsx | ? {$_ -match '^-  (auditEraMeta|auditDotRow|auditDot|auditDotOn|auditDotOff|auditVivid):'}); $x.Count
```

실제 출력: `6`.

2. “테스트·캐논 핀 없음 / 컴포넌트 밖 0”

```powershell
git grep -n -E '"eras"|"level"[[:space:]]*:[[:space:]]*[1-5]|또렷함 L|Dots level' ff4125c58699 -- public/proto/data/core/mock.json public/proto/sb-screens-know.jsx
```

실제 출력: `mock.json:96-120`의 eras 1,2,3,4,3과 `sb-screens-know.jsx:336-337`의 도트·또렷함 렌더.

3. “전체 거래 실패 경고”

```powershell
node -e 'const{execFileSync}=require("node:child_process"),ts=require("typescript");global.__m=async()=>{throw Error("x")};let s=execFileSync("git",["show","ff4125c58699:src/lib/import/ledger-ratify.ts"],{encoding:"utf8"}).replace(/import \{ createLedgerEntry \} from "\.\.\/finance\/ledger";/,"const createLedgerEntry=global.__m;"),j=ts.transpileModule(s,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText,m={exports:{}};new Function("module","exports","require",j)(m,m.exports,require);m.exports.ratifyLedgerEntries("u",[1,2].map(id=>({id,ledgerEntry:{occurredOn:"2026-07-21",kind:"expense",amountKrw:id,label:"x"}}))).then(r=>console.log("RESOLVED",JSON.stringify(r)),e=>console.log("REJECTED",e.message))'
```

실제 출력: `RESOLVED {"inserted":0,"failed":2}`.

4. `ds.attachment.level=L3` 잔존 근거

```powershell
git grep -n -E '"level": "L3"|측정도구만 → L4|검증 측정도구' ff4125c58699 -- locales/ko/home.json docs/CONSTELLATION-DESIGN.md
```

실제 출력: locale `:234`는 L3, SoT `:140,230,235-236`은 측정도구→L4.

PR #1117 원자 판정 합계: **PASS 18 / FAIL 4 / UNVERIFIABLE 1**.

최종 판단: app `/audit`의 고정 level·도트·“또렷함” 렌더 제거와 시대→`/interview` 정적 경로 보존은 PASS다. 그러나 prototype canon에는 같은 조작 값과 렌더가 남았고, 고아 style 수는 5가 아니라 6이다. ImportHub의 best-effort 비차단·reset·표시 배선은 PASS지만 실제 행 실패가 resolve되므로 핵심 경고는 FAIL이다. 남은 L4에는 result gate+SoT 근거가 있으나 attachment L3에는 근거가 없어, 동일 클래스 경계는 부분적으로만 비자의적이다.

### 추가 회신 4 — ai PR #1118 bare-term 실측

검증한 원문은 `docs/handoff/ai_260721.md:169-175,218-241`이다. 핵심 원 주장은 다음과 같다.

> “현행 0/20·6/14, A bare-red 20/20·0/14, B bare-yellow 0/20·6/14, D colloc-red 0/20·4/14”
>
> “문맥 결합 패턴으로 이미 red인 비율: 8/14 = 57% → bare-term red 추가의 순증분 = 6건”
>
> “정상 일기 한 건이 핫라인 모달·crisis_events 오탐·최근-30 northstar/axis/self-model 프록시 422·zero-vector·secondb_chat 오염을 일으켜 수 주간 표면 다수를 마비”
>
> “CI 925파일: 우울증 0파일, 진단 24파일” 및 “`약을 모`는 Flash에는 있고 lexicon·프록시에는 없다”

검증 기준은 PR head `176d3f13d322`와 tree가 같은 merge `582c5b0a3b3b`이다.

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| 코퍼스 구성 | “전량 합성 34건 = 정상 20 + red 14” | PASS | `scripts/eval-lexicon-bareterm.corpus.json:3-38` 독립 집계 실제 `total=34 normal=20 red=14 uniqueIds=34 uniqueTexts=34`. |
| 내장 무결성 범위 | “각 샘플이 주장한 단어를 실제 포함” | PASS | 평가기 `:146-151`; 독립 집계 실제 `termIntegrityFailures=0`. 이 검사는 term 포함만 보며 truth의 타당성까지 검증하지는 않는다. |
| 현행 분류 | “FP 0/20 · FN 6/14” | PASS | clean exact merge에서 `npx tsx scripts/eval-lexicon-bareterm.ts` actual `current / FP 0/20 / FN 6/14`. |
| A bare-red | “FP 20/20 · FN 0/14” | PASS | 같은 실행 actual `bareRed / FP 20/20 / FN 0/14`; 후보식은 평가기 `:68,72`. |
| B bare-yellow | “FP 0/20 · FN 6/14” | PASS | 같은 실행 actual `bareYellow / FP 0/20 / FN 6/14`; yellow는 라우팅이 아니므로 수치와 의미가 일치한다. |
| D colloc-red | “FP 0/20 · FN 4/14” | PASS | 같은 실행 actual `collocRed / FP 0/20 / FN 4/14`, `red 커버 10/14 (FP 0건)`. |
| 기존 패턴·순증분 | “8/14(57%), bare-red 순증분 6건” | PASS | 같은 실행 actual `8/14 (57%) -> … 6건`; missed IDs도 정확히 `dep-r04,r05,r06,r07,dia-r04,r05`. 이 수치는 이 코퍼스에 한정한다. |
| truth 내부 일관성 | “red=C-SSRS≥1, normal=위기 신호 없음” | PASS | 정상 20건에는 bare term만 있고 red 14건은 약 수집·신변 정리·편지·비각성 소망·투신 등 repo gold 표현형(`src/lib/safety/eval/crisis-corpus.json:24-33`)과 일치했다. |
| truth의 독립 검수 | 합성 red 라벨을 확정 gold로 사용 | UNVERIFIABLE | repo 정본 자체가 `reviewed:null`이고 out-of-lexicon RED를 “provisional…clinical review”로 명시한다(`crisis-corpus.json:4-5`). 내부 일관성은 확인했지만 독립 전문가 sign-off 증거가 없다. |
| 코퍼스 한계 고지 | “작성자 구성 34건·유병률 미반영·구조만, 실사용 빈도 추정 아님” | PASS | `ai_260721.md:241`과 corpus `_readme`가 명시한다. leave-one-out actual은 기존 커버 `7/13=54%` 또는 `8/13=62%`; 정확한 57%가 구성 의존임을 고지가 제한한다. |
| bare-red 반대 결론의 크기 민감도 | “bare-term→red는 부적절” | PASS | 모든 입력이 후보 term을 포함하는 조건에서 후보식 `hasBare ? red`이므로 정상 반례 1건만 있어도 false positive가 필연적이다(`eval…ts:68,72`). 핵심 구조 결론은 N에 민감하지 않으며, 실제 발생 빈도는 보고가 명시적으로 주장하지 않는다. |
| 대안의 달성 범위 | “순증분 6건은 아래 2단 대안으로 더 싸게 달성” | FAIL | 실측한 D는 6개 중 2개만 추가 포착해 FN `6→4`다. Layer-2는 이 평가에서 실행하지 않았고 현재 build flag도 비활성이다(`src/lib/llm/safety.ts:194-210,268-275`). “6건 달성/더 싸게”는 측정 범위를 넘는다. |
| 한 명령 전표 재생산 | “레포 루트 `npx tsx ...` 한 명령으로 아래 표 전체 재생산” | FAIL | clean exact merge에서는 `925/0/24/11`이 맞지만 현재 실제 root에서는 `930/0/25/11`. 평가기 `:79-116`이 git tree가 아니라 untracked `docs/`까지 현재 디스크를 walk해 CI 전표가 작업트리 상태에 따라 바뀐다. |
| 정상 예문 오탐 시작점 | “우울증 진단 후 병원에 다니기로 한 정상 기록도 A에서 red” | PASS | evaluator actual `dep-n01 / normal / current green / A red / B yellow / D green`. |
| red여도 기록 저장 | “기록 저장 자체는 된다” | PASS | `src/lib/records/create.ts:245-278`; `create-advisor-gate.test.ts:77-94`는 red fixed followup, 반환 id, record insert 1회를 함께 확인했다. |
| 핫라인 followup·모달 | “followup이 핫라인 템플릿으로 대체되고 위기 모달이 뜬다” | PASS | `create.ts:119-131`, `src/app/capture.tsx:950-963`; exact merge targeted Jest 포함 7 suites / 53 tests PASS. |
| crisis_events 오탐 경로 | “제한 원장에 오탐 행 기록” | PASS | `src/lib/llm/gemini.ts:477-488`이 `writeCrisisEvent`를 호출하고 `gemini.mock.test.ts:58-72`가 insert 1회를 핀한다. bare term을 red로 넣으면 정상 예문도 동일 경로를 탄다. |
| 북극성 최근-30 영향 | “그 기록이 최근-30에 있는 동안 northstar 생성 실패” | PASS | `src/lib/persona/northstar.ts:86-104`는 kind 제한 없이 최신 30개 body를 digest에 넣는다. fixed hotline text를 parser에 넣은 실행 actual `northstar=THROW:northstar propose: unusable AI reply`. |
| “프록시 422” 기전 | “앱의 digest가 프록시 422로 전멸” | FAIL | `callGemini()`는 proxy 호출 전에 `gemini.ts:499-510`에서 로컬 red를 `routeCrisis()` fixed response로 반환한다. proxy 422 test도 raw error를 hotline 결과로 변환함을 확인했다. 앱의 실제 기전은 **로컬 short-circuit → parser 실패/null**이지 raw proxy 422가 아니다. |
| axis 범위 | “같은 일반 일기 한 건이 최근-30 axis digest도 전멸” | FAIL | `src/lib/audit/axis-estimate.ts:37-45`는 해당 `axis_check` tag를 가진 최신 **24개**만 읽는다. 일반 journal은 자동 포함되지 않는다. |
| self-model 범위 | “같은 일반 일기 한 건이 최근-30 self-model digest도 전멸” | FAIL | `src/lib/persona/build.ts:559-565`는 `kind="audit_response"`만 읽고, `proposal-context.ts:24-36`은 raw journal이 아닌 card summary를 evidence로 보낸다. 일반 journal 원문은 이 경로에 들어가지 않는다. |
| embedding red 처리 | “red text는 zero-vector 후 저장 skip” | PASS | `src/lib/llm/gemini.ts:956-970,1021-1023`, `records-embeddings.ts:94-109`; `embed.test.ts:51-55`가 zero-vector를 실행 확인했다. |
| embedding 무조건 파급 | “정상 일기 한 건이면 임베딩도 곧바로 zero-vector” | FAIL | records embedding 기본은 OFF이고 성인 opt-in·non-mock일 때만 호출한다(`privacy/prefs.ts:25-44`, `create.ts:296-320`). 미동의·미성년·mock은 embedding 호출 자체가 없다. zero-vector는 **경로 진입 시**만 맞다. |
| 지속 기간 | “수 주간 마비” | FAIL | northstar query는 시간 predicate 없이 `created_at DESC LIMIT 30`뿐이다(`northstar.ts:88-93`). 30개의 새 기록이 쌓이는 기간은 사용자 기록 빈도에 따라 달라 “수 주”로 고정할 수 없다. |
| secondb_chat 영향 | “해당 일반 기록이 secondb_chat context를 오염” | FAIL | chat은 wiki/RAG와 structured record만 조립한다(`conversation.ts:184-212,242-282`). `export.ts:217-240`의 일반 record 포함은 `includeRecords` opt-in인데 chat은 설정하지 않고, `load-structured.ts:14-19`도 `structured IS NOT NULL`만 읽는다. |
| CI clean-head 수치 | “925파일, 우울증 0, 진단 24(ANALYSIS 11)” | PASS | clean `582c5b0a` actual `925 / 0 / 24 / 11`; evaluator `:78-136`과 실제 checker `check-forbidden-lexicon.ts:27-104`의 roots·확장자·ignore·allowlist·KO 매칭·부정문 필터가 일치한다. |
| `"약을 모"` 계층 | “Flash marker에는 있고 lexicon·proxy에는 없다” | PASS | `src/lib/llm/safety.ts:55-58`에만 존재. `lexicon.ts:52-74`, `gemini-proxy/index.ts:249-255`, `_shared/llm-proxy-common.ts:22-28`에는 없고 실행 actual `{"layer1":"green","sharedProxyGate":false}`. |

#### PR #1118 FAIL 최소 재현

1. “2단 대안이 순증분 6건을 달성”

```powershell
npx tsx scripts/eval-lexicon-bareterm.ts | Select-String 'collocRed|콜로케이션 후보'
```

실제 출력: `collocRed | 0/20 | 4/14`, `red 커버: 10/14 (FP 0건)`.

2. “한 명령으로 전표 전체 고정 재생산”

```powershell
npx tsx scripts/eval-lexicon-bareterm.ts | Select-String '스캔 대상 파일 수|"우울증"|"진단"'
```

현재 root 실제 출력: `930 / 우울증 0 / 진단 25 (ANALYSIS 11)`; clean merge 출력은 `925 / 0 / 24 / 11`.

3. “앱에서 raw proxy 422”

```powershell
git grep -n -E 'inputSafety =|inputSafety\.zone === "red"|functions\.invoke\("gemini-proxy"' 582c5b0a -- src/lib/llm/gemini.ts
```

실제 순서: local classification/`routeCrisis`가 `499-510`, proxy invoke가 `642`로 뒤다.

4. “일반 journal이 axis 최근-30에 포함”

```powershell
git grep -n -E '\.contains\("tags"|\.limit\(24\)|\.limit\(30\)' 582c5b0a -- src/lib/audit/axis-estimate.ts src/lib/persona/northstar.ts
```

실제 출력: axis `contains(tags,[tag])` + `limit(24)`; northstar만 `limit(30)`.

5. “일반 journal이 self-model 최근-30에 포함”

```powershell
git grep -n -E '\.eq\("kind", "audit_response"\)|MAX_SUMMARY_ROWS|\.limit\(30\)' 582c5b0a -- src/lib/persona/build.ts src/lib/persona/northstar.ts
```

실제 출력: self-model 기반은 `kind=audit_response`, max 120행/7400자; `limit(30)`은 northstar에만 존재.

6. “모든 정상 일기가 embedding zero-vector”

```powershell
git grep -n -E 'recordsEmbeddingAllowed|EXPO_PUBLIC_LLM_MODE !== "mock"|vec\.every' 582c5b0a -- src/lib/records/create.ts src/lib/records/records-embeddings.ts
```

실제 출력: opt-in/non-mock gate 뒤에서만 embed, zero-vector면 store 전에 false.

7. “수 주간”

```powershell
git grep -n -E 'order\("created_at"|limit\(30\)|gte\(' 582c5b0a -- src/lib/persona/northstar.ts
```

실제 출력: `order(created_at desc)` + `limit(30)`; `gte`/시간 조건 0건.

8. “일반 journal이 secondb_chat context 오염”

```powershell
git grep -n -E 'includeRecords|loadStructuredContext|exportUserWiki' 582c5b0a -- src/lib/chat/conversation.ts src/lib/wiki/export.ts
```

실제 출력: chat의 `exportUserWiki` 호출에는 `includeRecords`가 없고 record export는 opt-in이다.

PR #1118 원자 판정 합계: **PASS 18 / FAIL 8 / UNVERIFIABLE 1**.

최종 판단: 네 구성의 코퍼스 수치, clean-head CI 수치, bare-red의 정상 예문 오탐, 저장·모달·원장·조건부 zero-vector, `"약을 모"` 계층 불일치는 확인됐다. 그러나 “일기 한 건이 northstar·axis·self-model·chat을 raw proxy 422로 수 주간 전멸”은 데이터 선택 범위와 호출 기전을 과장했다. 실제 일반 journal 직접 영향은 저장된 red followup/모달/원장과 northstar, 그리고 opt-in embedding 경로까지다.

### 추가 회신 5 — comm SDK 표 현재성 재검증

원문은 `docs/handoff/comm-to-cowork_260721.md:149-159`다. 위 `추가 회신 1`은 당시 기준 `fc0647c1`·iOS 0.1.0 바이너리에 대한 판정으로 유효하다. #1120 merge `67f2263a`까지 SDK 관련 diff는 `eas.json`, `rewarded.native.ts`, web stub, rewarded test뿐이고, 최종 main `8f59fd2e`도 이 SDK blob들을 바꾸지 않았다. 따라서 AdMob은 **배포 바이너리**와 **다음 빌드 main**을 분리해 갱신했다.

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| Sentry 웹 가동 | “웹 가동, 사용자 동의와 무관” | PASS | 관련 source diff 0. GitHub Variable `EXPO_PUBLIC_SENTRY_DSN` PRESENT(최종 갱신 06-27); `src/lib/analytics/index.ts:730-757`의 브라우저+DSN init에 동의 gate가 없다. |
| Sentry 웹 PII 범위 | “PII 차단” | FAIL | source diff 0. `index.ts:659-699` scrubber는 request/transaction/breadcrumb만 처리하고 `event.user`, exception, extra, contexts 제거는 0건이다. |
| Sentry 네이티브 가동 | “iOS+Android 가동, 동의와 무관” | PASS | EAS production env 이름 조회 actual `EXPO_PUBLIC_SENTRY_DSN PRESENT`; `_layout.tsx:47-62` init 경로와 관련 source diff 0. |
| Sentry 0.1.0 플랫폼 범위 | “iOS+Android 0.1.0 바이너리” | FAIL | 최신 재조회 actual Android 0.1.0은 `CANCELED preview build 12` 1건뿐이고 FINISHED artifact는 0건이다. iOS 0.1.0 완료 바이너리만 존재한다. |
| GA4 웹 현재 게이트 | “성인+opt-in+analytics_enabled=true+ID로 조건부 가동” | PASS | GitHub GA4 Variable PRESENT(07-17 갱신); 운영 REST read actual `analytics_enabled=True updated_at=2026-07-18T05:05:43Z`; source diff 0. |
| GA4 연속 시점 | “07-17부터 true” | UNVERIFIABLE | 현재 값·`updated_at`은 확인했지만 연속 변경 이력은 없다. |
| Firebase Analytics Android | “Android만 기본 OFF, 성인+동의 시 ON” | PASS | `firebase.json:3-11`, `analytics/index.ts:227-326,717-721,1012-1029`; 관련 source diff 0. |
| Firebase Analytics iOS 핵심 | “모듈 부재 = Analytics 수집 불가” | PASS | 현재 autolinking actual `@react-native-firebase/app=PRESENT`, `@react-native-firebase/analytics=ABSENT`; analytics import는 모듈 부재 시 종료한다(`index.ts:269-284`). |
| Firebase iOS pod 범위 | “react-native.config.js가 iOS pods 제외 유지” | FAIL | actual은 RNFBApp core가 PRESENT이고 Analytics만 ABSENT다. Firebase pod 전체 부재를 근거로 들 수 없다. |
| PostHog 웹 | “key/host 설정으로 조건부 가동” | FAIL | Variables는 PRESENT지만 `npm ls posthog-js --all --depth=0` actual `(empty)`, exit 1. 동적 import가 성공할 패키지가 없다. |
| Clarity 현재 게이트 | “두 runtime flag true+동의로 조건부 가동” | PASS | 운영 REST read actual `analytics_enabled=True`, `clarity_enabled=True`; Clarity Variable PRESENT(07-18 갱신), source diff 0. |
| Clarity 게이트 완전성 | “게이트 체인 = 두 flag+동의” | FAIL | `index.ts:568-575,582-594,878-883`: route allowlist가 추가 조건이고, 한 번 inject되면 같은 page lifetime의 `/record/*`, `/peer/*` 이동도 기록될 수 있다. |
| AdMob — 배포 iOS 0.1.0 | “플래그 부재→경로 차단, delay init, 실유닛 0” | PASS | build 6 기준 commit `f2eb07b9`의 `eas.json`에는 ads flag 0건이고 EAS production env 이름 조회도 ABSENT. `app.json:159` delay true, 해당 코드에는 TestId만 있다. |
| AdMob — 머지 main의 다음 빌드 | “현재도 플래그 부재” | FAIL | `67f2263a:eas.json:40,77`은 preview와 production 모두 `EXPO_PUBLIC_ENABLE_ADS=true`. 다만 #1120의 `rewarded.native.ts:51,62-64,94-96`이 `HAS_LIVE_AD_UNIT=false`인 non-dev에서 CTA와 UMP를 별도로 차단하므로 앱 제어 광고 요청은 여전히 막힌다. |
| AdMob — 실제 수집량 | “수집 0” | UNVERIFIABLE | 정적 gate는 앱 제어 init/request 차단을 증명하지만 실제 IPA network trace나 AdMob/Firebase 콘솔 원출력이 없다. “총수집 0”은 여전히 확인 불가다. |
| AdSense 웹 | “client 미설정으로 OFF” | PASS | GitHub Variable과 EAS env 이름 모두 `EXPO_PUBLIC_ADSENSE_CLIENT ABSENT`; `policy.ts:48-53`은 enable+client를 함께 요구한다. |

#### SDK 현재성 FAIL 최소 재현

1. Sentry 웹 “PII 차단”

```powershell
rg -n 'delete event\.|event\.transaction|event\.breadcrumbs' src/lib/analytics/index.ts
```

실제 출력은 request fields, transaction, breadcrumbs뿐이며 user/exception/extra/contexts 제거는 0건이다.

2. Sentry “Android 0.1.0 바이너리”

```powershell
npx eas-cli build:list --platform android --app-version 0.1.0 --limit 20 --json --non-interactive
```

실제 출력: preview build 12 한 건은 `CANCELED`; FINISHED 0건.

3. Firebase iOS “pods 제외”

```powershell
npx --no-install expo-modules-autolinking react-native-config --platform ios --json
```

실제 resolved 결과: `@react-native-firebase/app=PRESENT`, `@react-native-firebase/analytics=ABSENT`.

4. PostHog “조건부 가동”

```powershell
npm ls posthog-js --all --depth=0
```

실제 출력: `└── (empty)` / exit 1.

5. Clarity “게이트 체인”

```powershell
rg -n 'later same-page-lifetime|including /record|CLARITY_ALLOWED_ROUTE_PREFIXES' src/lib/analytics/index.ts
```

실제 출력: inject 후 `/record/*`, `/peer/*` 기록 잔존과 추가 route gate.

6. AdMob “현재도 플래그 부재”

```powershell
git grep -n 'EXPO_PUBLIC_ENABLE_ADS' 67f2263a -- eas.json
```

실제 출력: `eas.json:40,77` 두 곳 모두 `"true"`.

현재성 원자 판정 합계: **PASS 8 / FAIL 6 / UNVERIFIABLE 2**.

처리방침 입력 결론은 유지하되 AdMob 시점을 분리해야 한다. 배포된 iOS 0.1.0은 앱 제어 경로 비활성, 다음 merged-main non-dev build는 ENABLE_ADS가 true지만 실유닛 capability가 false라 CTA·UMP·ad request가 비활성이다. 어느 쪽도 실제 콘솔/네트워크 증거 없이 “수집 0”으로 단정할 수 없다. Firebase iOS는 “Analytics 모듈 부재, RNFBApp core 존재”가 정확한 문구다.

### 추가 회신 6 — comm PR #1120 capability gate

PR 원문은 다음과 같다.

> “All three CTA surfaces … now require [capability] before policy eligibility”
>
> “consent is never requested on a path that cannot fulfil its purpose”
>
> “Flip `HAS_LIVE_AD_UNIT` + the real unit id in `rewarded.native.ts` … nowhere else”
>
> “`npx jest src/lib/ads`: 5 suites / 39 tests green”

GitHub actual: #1120 `merged=true`, head `c2780f8c3cc8`, merge `67f2263a5d80`, `mergedAt=2026-07-21 04:45:43 KST`; head↔merge tree diff 0.

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| 머지 전제 | “PR #1120 머지 후” | PASS | GitHub 연결 actual `merged:true`, merge `67f2263a5d806007a17ecd822e314eff6accce35`. 최종 main은 후속 #1123/#1122를 포함한 `8f59fd2e`지만 `git merge-base --is-ancestor 67f2263a origin/main` exit 0이고 ads 대상 diff도 0이다. |
| CTA 호출 완전성 | “3개 CTA 표면 전부 capability가 policy보다 먼저” | PASS | exact merge 전역 검색에서 `canShowRewardedAds({` 호출은 정확히 3곳. 각각 바로 앞이 `canCompleteRewardedWatch() &&`: `secondb.tsx:576-577`, `ReasoningLimitSheet.tsx:116-117`, `dds-plans-screen.tsx:148-149`. JS 단락 평가로 policy보다 먼저 실행된다. |
| CTA 렌더 배선 | “production 실유닛 0이면 watch CTA 자체가 없음” | PASS | 세 표면의 실제 노출 조건도 같은 gate를 사용한다(`secondb.tsx:716-718,1254-1256,1639-1641`, `ReasoningLimitSheet.tsx:195-201`, plans `:394-413,433-434`). |
| web stub | “웹은 항상 capability false” | PASS | `src/lib/ads/rewarded.ts:31-36`은 `canCompleteRewardedWatch()`를 상수 false로 반환한다. |
| production UMP | “production에서 UMP가 미호출” | PASS | `rewarded.native.ts:51,62-64,94-98`: HAS=false인 non-dev는 `ensureUmpConsent` 전에 return. `rewarded.test.ts:122-133`은 `__DEV__=false`, completed=false, ad request 0, UMP 0, capability=false를 실행 검증했다. |
| preview와 `__DEV__` | “preview도 release build라 `__DEV__=false`; 테스트 핀과 정합” | PASS | `eas.json:7-22`에서 development만 `developmentClient:true`; preview는 미지정(default false), Android `buildType:apk`. Expo 공식 EAS 문서는 `developmentClient` 기본 false와 preview internal APK 예시를, React Native 공식 문서는 `__DEV__` guarded code가 minified release에서 제거됨을 명시한다. 테스트 `:125-140`의 false/true 경계와 정합한다. |
| `HAS_LIVE_AD_UNIT` 중복 | “단일 지점, 다른 실유닛 분기 없음” | PASS | tests 제외 전역 검색 actual: `HAS_LIVE_AD_UNIT` code read는 `rewarded.native.ts:51,64`뿐, unit 선택 `:101`, ad 생성 `:106` 한 곳뿐이다. |
| 런치 변경 전체 범위 | “flip + real unit id, nowhere else” | FAIL | CTA policy는 별도로 `EXPO_PUBLIC_ENABLE_ADS===true`를 요구한다(`policy.ts:85-93`; env 기본 false `env.ts:177-180`). EAS preview/production에는 true지만 `.github/workflows/android-release.yml:45-90`에는 미러가 없어 그 Gradle release 경로는 HAS flip+unit 교체만으로 CTA가 열리지 않는다. |
| 광고 테스트 기능 | “`npx jest src/lib/ads` green” | PASS | exact merge에서 실제 실행: 5 suites PASS, 38 tests PASS, exit 0. |
| 광고 테스트 계수 | “5 suites / **39 tests**” | FAIL | 같은 exact merge 실제 출력은 `Test Suites: 5 passed, 5 total`, `Tests: 38 passed, 38 total`. 기능은 통과했지만 보고 계수는 1 많다. |
| 전체 CI | “full verify green, 3009 tests” | PASS | `gh pr checks 1120` actual 모든 check PASS. verify run log actual `388 suites / 3009 tests PASS`. |

#### PR #1120 FAIL 최소 재현

1. “런치 변경은 rewarded.native.ts 한 곳뿐”

```powershell
$sha='67f2263a5d806007a17ecd822e314eff6accce35'; $w=(git show "$sha`:.github/workflows/android-release.yml"|Out-String); if($w -match 'EXPO_PUBLIC_ENABLE_ADS'){'PRESENT'}else{'MISSING'}
```

실제 출력: `MISSING`.

2. “광고 테스트 39개”

```powershell
E:\2ndB\node_modules\.bin\jest.cmd --runInBand src/lib/ads
```

exact merge 실제 출력: `5 passed, 5 total`; `38 passed, 38 total`.

PR #1120 원자 판정 합계: **PASS 9 / FAIL 2 / UNVERIFIABLE 0**.

최종 판단: 세 CTA의 capability 선행, production UMP 미호출, preview release의 `__DEV__=false`, web fail-closed, `HAS_LIVE_AD_UNIT` code 분기 단일성은 모두 PASS다. 다만 “런치 변경 nowhere else”는 별도 ENABLE_ADS policy 때문에 전체 native release 경로 기준 FAIL이고, 광고 테스트 수는 39가 아니라 38이다.

### 추가 회신 7 — comm 릴리즈 위생 3픽스 PR

수신 원문과 뒤늦게 생성된 PR #1125 원문은 다음과 같다.

> “comm의 릴리즈 위생 3픽스 PR (머지 후).”
>
> “① EXPO_PUBLIC_ENABLE_ADS 미러가 android-release 경로에 실제로 주입되는지 ② 핀 ref 입력 요구가 --ref main 을 실제로 막는지(우회 경로가 없는지) ③ concurrency 그룹 분리가 실제로 경합을 벗어나는지”
>
> “Pinned-ref dispatch (--ref main 금지) — a guard step fails fast when workflow_dispatch runs on main, with instructions to dispatch on a pinned tag/release/* branch.”

착수 시 대상은 없었으나 최종 확인 중 PR #1125가 생성됐다. 정확한 base `fef34f2b50ac`, head `69b8ae083e86`을 검증했다. 현재는 open이며 merge commit은 아직 없다.

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| 머지 전제 | “PR (머지 후)” | UNVERIFIABLE | `gh pr view 1125` 최신 actual은 `state=OPEN`, `mergeCommit=null`, head `69b8ae083e86`; 검증 시점에는 merge tree가 없다. |
| 변경 범위 | “One file … three release-hygiene fixes” | PASS | `git diff --name-status fef34f2b..69b8ae08` 실제 출력은 `M .github/workflows/android-release.yml` 한 파일이고 `git diff --check` exit 0. |
| Android 광고 플래그 | “`EXPO_PUBLIC_ENABLE_ADS` 미러가 android-release 경로에 실제로 주입” | PASS | exact head workflow `:48-114`의 job-level env에 `EXPO_PUBLIC_ENABLE_ADS: "true"`가 있고, `Expo prebuild` `:191-192`와 Gradle release `:231-239`가 같은 job env를 상속한다. `js-yaml` actual은 `ads="true"`. |
| `--ref main` 직접 차단 | “guard step fails fast when workflow_dispatch runs on main” | PASS | exact head `:115-140`: 첫 step 조건은 `event_name == workflow_dispatch && ref_name == main`, body는 `exit 1`, checkout은 그 뒤 `:140-141`. truth table actual: `workflow_dispatch main guard=true`; push/main은 의도대로 false. |
| pinned ref·우회 없음 | “핀 ref 입력 요구”, “must pin the exact commit they gated”, “pinned refs only” | FAIL | `on.workflow_dispatch`는 입력 0개(`js-yaml`: `dispatch=null`)이고 guard는 이름이 정확히 `main`인 ref만 거부한다(`:10-11,124-128`). 실제 truth table에서 `workflow_dispatch fix/mutable guard=false`. 임의의 이동 가능한 branch, 잘못된 `release/*` branch, tag를 gated SHA와 비교하지 않으므로 exact SHA pin을 강제하지 않는다. |
| concurrency 분리 | “dispatch runs get their own group (`run_id`); push builds keep per-ref dedup” | PASS | exact head `:36-43`. truth table actual: dispatch run 101=`android-release-101`, dispatch 102=`android-release-102`, push/main=`android-release-refs/heads/main`, `allDistinct=true`. 따라서 dispatch와 다음 push는 같은 concurrency group의 pending 교체 대상이 아니다. |

#### 릴리즈 위생 FAIL 최소 재현

1. pinned SHA 입력·검증 부재

```powershell
$head='69b8ae083e861bb4488b42cac14e3a1844562429'; git show "$head`:.github/workflows/android-release.yml" | node -e "const fs=require('fs'),y=require('js-yaml').load(fs.readFileSync(0,'utf8')),s=y.jobs.build.steps[0],g=Function('github','return '+s.if); console.log(JSON.stringify({dispatch:y.on.workflow_dispatch,guard:s.if,main:g({event_name:'workflow_dispatch',ref_name:'main'}),mutable:g({event_name:'workflow_dispatch',ref_name:'fix/mutable'})}));"
```

실제 출력: `{"dispatch":null,"guard":"github.event_name == 'workflow_dispatch' && github.ref_name == 'main'","main":true,"mutable":false}`.

릴리즈 위생 원자 판정 합계: **PASS 4 / FAIL 1 / UNVERIFIABLE 1**.

결론: 광고 flag 주입, 정확한 `--ref main` fail-fast, dispatch/push concurrency 분리는 head에서 통과했다. 그러나 구현은 “main 금지”이지 “gated SHA pin 강제”가 아니다. 입력 SHA와 `github.sha` 비교 또는 허용 ref+SHA 검증 없이는 “우회 경로 없음”이 FAIL이다. 또한 아직 미머지이므로 Android 절단 해제 조건도 충족되지 않았다. 수정 실행은 comm 담당 세션으로 넘긴다.

### 추가 회신 8 — ai crisis 계층 파리티 (#1123 baseline)

> 시점 주의: 아래 표는 #1126 생성 전 #1123 상태의 판정이다. 머지 후 최종 판정은 `추가 회신 8A`가 대체한다.

원문은 `docs/handoff/ai_260721.md:253-281`과 신규 평가기다.

> “약을 모 말고 5개 더 있다 — L2-only(게이트 통과) 총 6건”
>
> “`eval-crisis-layer-parity.ts`를 CI 게이트로 승격 … (지금은 report-only로 커밋.)”

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| L2-only 총수·목록 | “총 6건” | PASS | `npx tsx scripts/eval-crisis-layer-parity.ts` 실제 출력: EN `cutting, overdose, last goodbye, won't be around` 4건 + KO `칼, 약을 모` 2건, exit 0. |
| 7번째 부재 | 위 6개가 현재 tree의 완전한 gap 목록 | PASS | `safety.ts:55-65`의 Active/Latent 마커 30개를 실제 `classifyInput()`에 전수 입력한 출력: `en markers=14; passed=4`, `ko markers=16; passed=2`; 통과 목록이 위 6개와 정확히 같다. |
| `suicide note` 제외 | “gate term `suicide`가 커버 — gap 아님” | PASS | 실제 `classifyInput('suicide note','en')` → `red`, `matched=["suicide"]`. |
| L1 세 사본 | “byte-identical + 매칭 시맨틱까지 CI로 강제” | PASS | `jest crisis-terms-proxy-parity.test.ts --runInBand --no-cache` 실제 출력: 1 suite / 26 tests PASS. |
| `칼` 비승격 | “칼퇴, 칼국수, 칼같이 … ko는 부분문자열 매칭” | PASS | `classifier.ts:44-46`은 KO `includes`. 런타임 배열에만 `칼`을 주입한 실제 출력: `오늘 칼국수를 먹었다` → `zone=red matched=["칼"]`. |
| `cutting` 비승격 | “cutting costs/corners/class/board” | PASS | `classifier.ts:48-55`의 단어 경계 matcher에 런타임 주입 후 `We are cutting costs this quarter.` → `zone=red matched=["cutting"]`. |
| `last goodbye` 보류 | 원문은 절대 불가가 아니라 “보류”; 팀·옛집 작별 FP | PASS | 런타임 주입 후 `I said my last goodbye to the old house before moving.` → `zone=red matched=["last goodbye"]`. 따라서 데이터 없이 bare 승격하지 않는 근거가 성립한다. |
| `won't be around` 비승격 | “I won't be around this weekend” 일정 대화 FP | PASS | 런타임 주입 후 해당 정상 일정 문장 → `zone=red matched=["won't be around"]`. |
| 매트릭스 요약 계수 | “EN 게이트∩마커 8”, “KO 게이트∩마커 13” | FAIL | 평가기 표의 `Y/Y` 실제 집계는 `EN_exact_intersection=9`, `KO_exact_intersection=14`. EN 원문은 괄호 안에 스스로 9개를 열거한다. 핵심 6-gap 목록에는 영향 없지만 보고 계수는 둘 다 1 작다. |
| report-only 고지 | “지금은 report-only로 커밋” | PASS | `scripts/eval-crisis-layer-parity.ts:15-16`이 report-only·verify 미연결을 명시하고, `package.json:30` verify 및 `.github/workflows`에 평가기 참조가 0개다. |
| 현재 CI gate 수용 기준 | “CI 게이트가 실제로 새 드리프트를 잡는지” | FAIL | 새 L2 marker `seventh drift marker`를 저장소 비수정 인메모리 주입하자 gap 목록에는 7번째가 출력됐지만 `exit=0`. `scripts/eval-crisis-layer-parity.ts:115-119`는 출력만 하며 승인 예외 목록 비교나 non-zero exit가 없다. |
| 미래 EN drift 의미 | “소스가 바뀌면 표도 따라 바뀐다 — stdout이 정본” / “full … delta” | FAIL | `scripts/eval-crisis-layer-parity.ts:97-101`은 EN도 단순 `includes`로 근사하지만 실제 matcher는 `classifier.ts:48-55`의 word boundary다. 새 marker `suicides`에서 실제 출력은 `{"evaluatorCovered":true,"actualZone":"green","actualMatched":[]}`: 새로운 gap을 covered로 숨긴다. |

#### crisis 파리티 FAIL 최소 재현

1. 매트릭스 교집합 계수

```powershell
$out=E:\2ndB\node_modules\.bin\tsx.cmd scripts/eval-crisis-layer-parity.ts; $en=$out[([Array]::IndexOf($out,'### EN presence matrix (20 terms)')+1)..([Array]::IndexOf($out,'### KO presence matrix (21 terms)')-1)]; $ko=$out[([Array]::IndexOf($out,'### KO presence matrix (21 terms)')+1)..([Array]::IndexOf($out,'### L2-only markers NOT covered by any gate term (the C9 gap list)')-1)]; "EN=$((($en|Select-String -SimpleMatch '| Y | Y |').Count)) KO=$((($ko|Select-String -SimpleMatch '| Y | Y |').Count))"
```

실제 출력: `EN=9 KO=14`.

2. 현재 CI gate 부재

```powershell
rg -n 'eval-crisis-layer-parity' package.json .github/workflows; E:\2ndB\node_modules\.bin\tsx.cmd scripts/eval-crisis-layer-parity.ts; "EXIT=$LASTEXITCODE"
```

실제 출력: package/workflow 참조 0건, 목록을 출력한 뒤 `EXIT=0`. 인메모리 7번째 marker 주입 때도 `EXIT=0`이었다.

3. EN substring 근사와 실제 matcher 불일치

```powershell
"import { CRISIS_TERMS } from './src/lib/safety/lexicon'; import { classifyInputAnyLocale } from './src/lib/safety/classifier'; const m='suicides'; console.log(JSON.stringify({evaluatorCovered:CRISIS_TERMS.en.some(g=>m.includes(g)),actualZone:classifyInputAnyLocale(m,'en').zone}));" | E:\2ndB\node_modules\.bin\tsx.cmd -
```

실제 출력: `{"evaluatorCovered":true,"actualZone":"green"}`.

crisis 파리티 원자 판정 합계: **PASS 9 / FAIL 3 / UNVERIFIABLE 0**.

결론: 현행 6-gap 목록과 네 비승격/보류 근거는 신뢰할 수 있다. 그러나 산출물은 스스로 고지한 대로 report-only이며, 지금 상태를 “CI 게이트”라고 부르는 것은 범위 과장이다. 게이트화할 때는 승인된 6개 목록과의 set 비교·non-zero exit뿐 아니라 EN 실제 boundary predicate까지 맞춰야 한다. 수정 실행은 ai 담당 세션으로 넘긴다.

### 추가 회신 9 — ai `classifyViaProxy` 사전 수리·토큰 (#1123 baseline)

> 시점 주의: 아래 표는 수리 PR 부재 시점의 판정이다. #1126 머지 후 최종 판정은 `추가 회신 9A`가 대체한다.

원문은 `docs/handoff/ai_260721.md:299-317`이다.

> “Layer-2 시맨틱 분류기 활성(D4) 비용·지연 추정 (결정용, 실행 없음)”
>
> “사전 수리 필요 결함 2건”
>
> “현행 … +900~2,500 total_tokens” / “권장 설정 … +700~900 total_tokens”

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| 원문 범위 | “추정 (결정용, 실행 없음)”, “사전 수리 필요” | PASS | #1123은 수리 완료나 전후 실측을 주장하지 않았다. `docs/handoff/ai_260721.md:299,315-317`이 실행 전 상태를 명시한다. |
| model 미전달·기본값 | “`classifyViaProxy`가 model을 안 보냄 → 기본 `gemini-2.5-flash`” | PASS | `247ca114:src/lib/llm/safety.ts:211-227` body에는 `purpose/system/user/responseSchema`만 있다. proxy `index.ts:530-533`은 model 누락 시 `gemini-2.5-flash`. |
| D-27 base-key 실제 귀속 | “G25 슬러그라 base-key로 귀속” | UNVERIFIABLE | `index.ts:743-750`과 `_shared/axis-key-name.ts:53-63`은 `GEMINI_API_KEY__G25FLASH__HIGH` 조합 secret을 먼저 찾고, 그것이 없을 때만 base key를 쓴다. 운영 secret 존재 여부를 코드에서 확인할 수 없다. |
| effort 미전달·사고 예산 | “effort 미전달 → 기본 high(사고 2048)” | PASS | `safety.ts:211-227`에 effort가 없다. proxy `index.ts:104-123`에서 undefined→high→`thinkingBudget:2048`, `:627-635`에서 실제 generationConfig에 주입한다. |
| model 수리 완료 | cowork 검증 대상 “사전 수리 2건” | UNVERIFIABLE | #1123 및 `247ca114..fef34f2b`에 관련 구현이 없고, 최신 PR 목록에도 repair PR이 없다. 검증할 model pin head가 없다. |
| effort 수리 완료 | 같은 대상 | UNVERIFIABLE | safety-classify 사고-0 특례 구현·PR·커밋이 없다. |
| 수리 전 token 실측 | “현행 +900~2,500 total_tokens” | UNVERIFIABLE | 원문은 safety-classify 실측이 아니라 다른 단문+schema 18콜의 479~1,118 token 분포를 외삽한 추정이다. #1123 회차는 LLM 0콜이며 전용 audit row가 없다. |
| 수리 후 token 실측 | “권장 +700~900 total_tokens” | UNVERIFIABLE | lite model pin·effort 0 구현과 live 호출이 모두 없어 after row가 없다. 수치 자체도 원문이 “추정”으로 고지했다. |
| prompt 길이 근거 | “SYSTEM_PROMPT 실측 1,766자” | FAIL | exact Git blob의 template literal을 런타임 평가한 실제 길이는 1,731자이고, proxy가 받는 추가 지시까지 합친 system은 1,793자다. `1,766`은 어느 런타임 입력 길이도 아니다. |

#### 사전 수리·토큰 FAIL 최소 재현

1. SYSTEM_PROMPT 길이

```powershell
node -e 'const cp=require("node:child_process"); const s=cp.execFileSync("git",["show","247ca114c7fdbb8b8635b08e0eb96d661d05c722:src/lib/llm/safety.ts"],{encoding:"utf8"}); const raw=s.match(/const SYSTEM_PROMPT = `([\s\S]*?)`;/)[1]; const value=Function("return `"+raw+"`;")(); const suffix="Classify the user message below. Output JSON only, no prose."; console.log(JSON.stringify({runtimeChars:value.length,fullSystemChars:(value+"\n\n"+suffix).length}))'
```

실제 출력: `{"runtimeChars":1731,"fullSystemChars":1793}`.

사전 수리·토큰 원자 판정 합계: **PASS 3 / FAIL 1 / UNVERIFIABLE 5**.

결론: 두 결함 진단은 코드와 일치한다. 다만 수리 구현과 전후 token 전표는 아직 존재하지 않으므로 “수리됨” 또는 “실측됨”으로 판정할 수 없다. 1,766자 근거는 정정 대상이다. 구현·라이브 실측은 ai 담당 세션으로 넘긴다.

### 추가 회신 8A — ai PR #1126 crisis 파리티 CI 게이트 최종

PR #1126은 base `72c0f61a03d7`, head `fc58970371a8`, merge `ae20c7f597bf`이며 2026-07-21 08:02:36 KST에 머지됐다. head와 merge의 대상 9파일 tree diff는 0이고 GitHub checks는 전부 PASS다.

> “`check:crisis-parity` verify 배선: 미승인 신규 L2-only 마커(새 게이트 구멍)·스테일 예외 모두 exit 1”

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| 머지 전제 | “파리티 CI 게이트” 실행 완료 | PASS | GitHub actual `state=MERGED`, merge `ae20c7f597bf...`; head↔merge 대상 tree diff exit 0, checks 전부 SUCCESS. |
| 현행 gap 완전성 | “L2-only 총 6건” | PASS | exact head에서 full evaluator와 실제 `classifyInput()` 전수 모두 EN 4 `cutting/overdose/last goodbye/won't be around`, KO 2 `칼/약을 모`; 7번째 없음. |
| 비승격/보류 4건 | `칼`, `cutting`, `last goodbye`, `won't be around`의 FP 사유 | PASS | #1123 baseline에서 실제 matcher 런타임 주입으로 네 정상 반례가 모두 red임을 확인했고, #1126은 해당 matcher·L2 marker를 바꾸지 않았다. `APPROVED_L2_ONLY_MARKERS`에도 같은 사유가 명시됐다(`lexicon.ts:102-128`). |
| 승인 예외 정합 | “`APPROVED_L2_ONLY_MARKERS` 6건” | PASS | `npm run check:crisis-parity` 실제 출력 `CRISIS-PARITY PASS ... L2-only markers == approved exception list`, exit 0. 승인 목록은 EN 4/KO 2다. |
| 일반 신규 drift | “미승인 신규 L2-only marker exit 1” | PASS | 저장소 비수정 인메모리 marker `seventh drift marker` 주입 actual: `CRISIS-PARITY FAIL (en)...`, exit 1. |
| stale 예외 | “스테일 예외 exit 1” | PASS | 인메모리로 `last goodbye` L2 marker를 제거한 actual: `approved exception "last goodbye" is no longer...`, exit 1. |
| verify 배선 | “`check:crisis-parity`를 verify에 배선” | PASS | `package.json:17,31`에 `--check` script와 verify 호출, `.github/workflows/ci.yml:23-24`에 `npm run verify`가 있다. exact head target Jest 2 suites / 30 tests PASS; merged PR checks 전부 SUCCESS. |
| 전체 verify 계수 | “389 suites / 3,016 tests 그린” | PASS | Actions run `29785725390`, job `88496746839` 실제 로그: `Test Suites: 389 passed, 389 total`; `Tests: 3016 passed, 3016 total`. |
| 모든 신규 gate gap 탐지 | “새 prompt marker가 조용히 gate 구멍을 만들지 못함” | FAIL | evaluator `:105-109,115`는 EN도 `marker.includes(gateTerm)`로 판단하지만 실제 matcher는 `classifier.ts:48-55`의 word boundary다. 인메모리 L2 marker `suicides` 주입 actual: evaluator `CRISIS-PARITY PASS`, exit 0; 실제 `classifyInputAnyLocale('suicides','en')`는 green. 실제 7번째 구멍을 승인된 것으로 오판한다. |
| 문서 매트릭스 계수 | “EN 게이트∩마커 8”, “KO 게이트∩마커 13” | FAIL | 머지 문서 `ai_260721.md:278-279`가 아직 8/13이다. evaluator `Y/Y` 독립 집계 actual은 EN 9 / KO 14이며, #1126도 이 두 줄을 정정하지 않았다. 핵심 6-gap에는 영향 없다. |
| 평가기 상태 주석 | 신규 설명은 “CI gate, verify 배선” | FAIL | 같은 파일 `scripts/eval-crisis-layer-parity.ts:15-16`은 여전히 “Report-only … not wired into verify”, `:31-35`는 “--check … Wired into npm run verify”라고 정반대로 서술한다. 실행은 후자가 맞지만 상태 주석은 자기모순이다. |

#### PR #1126 crisis gate FAIL 최소 재현

1. EN boundary drift 은폐

```powershell
"import { CRISIS_TERMS } from './src/lib/safety/lexicon'; import { classifyInputAnyLocale } from './src/lib/safety/classifier'; const m='suicides'; console.log(JSON.stringify({evaluatorCovered:CRISIS_TERMS.en.some(g=>m.includes(g)),actualZone:classifyInputAnyLocale(m,'en').zone}));" | E:\2ndB\node_modules\.bin\tsx.cmd -
```

실제 출력: `{"evaluatorCovered":true,"actualZone":"green"}`. 같은 marker를 L2에 인메모리 주입한 `--check`도 `PASS`, exit 0.

2. 문서 교집합 계수

```powershell
$out=E:\2ndB\node_modules\.bin\tsx.cmd scripts/eval-crisis-layer-parity.ts; $en=$out[([Array]::IndexOf($out,'### EN presence matrix (20 terms)')+1)..([Array]::IndexOf($out,'### KO presence matrix (21 terms)')-1)]; $ko=$out[([Array]::IndexOf($out,'### KO presence matrix (21 terms)')+1)..([Array]::IndexOf($out,'### L2-only markers NOT covered by any gate term (the C9 gap list)')-1)]; "EN=$((($en|Select-String -SimpleMatch '| Y | Y |').Count)) KO=$((($ko|Select-String -SimpleMatch '| Y | Y |').Count))"
```

실제 출력: `EN=9 KO=14`.

3. 평가기 상태 주석 자기모순

```powershell
rg -n 'Report-only|not wired into verify|Wired into' scripts/eval-crisis-layer-parity.ts
```

실제 출력: `:15-16` report-only/not wired와 `:34-35` wired가 함께 존재한다.

PR #1126 crisis gate 최종 합계: **PASS 8 / FAIL 3 / UNVERIFIABLE 0**.

결론: 현행 6개와 일반 신규/stale drift는 통과한다. 그러나 EN gate 의미를 근사한 채 CI에 올려 모든 새 구멍을 막는다는 범위는 성립하지 않는다. `classifier.ts`와 같은 word-boundary predicate를 공유하고 stale 상태 주석·계수를 정정해야 한다. 수정 실행은 ai 담당 세션으로 넘긴다.

### 추가 회신 9A — ai PR #1126 `classifyViaProxy` 수리·토큰 최종

원 발주 범위와 #1126의 수정 후 서술을 함께 대조했다.

> 원문: “safety_classify 시트를 서버 소유로 핀할 것” / “`geminiGenLadder`에 safety_classify 사고-0 특례 추가”
>
> #1126: “model 미전달 → `MODELS.flash` 핀” / “effort 미전달 → `low` 핀(512사고/1024출력)”

| 항목 | 원 주장 | 판정 | 근거(파일:줄 또는 명령 + 실제 출력) |
|---|---|---|---|
| 머지 전제 | 수리 구현 완료 | PASS | PR #1126 merge `ae20c7f597bf`, head↔merge tree diff 0, GitHub checks 전부 SUCCESS. |
| 수리 전 결함 | model 미전달→2.5-flash, effort 미전달→high/2048 | PASS | base `safety.ts:211-226` body에 두 필드가 없고 proxy `index.ts:104-123,530-533` 기본값이 각각 2.5-flash와 high/2048이다. |
| 수정 후 client model 배선 | “`MODELS.flash` 핀” | PASS | head `safety.ts:21,226`; `types.ts:155-161`. 배포 web/EAS env는 flash=`gemini-3.5-flash`; 계약 테스트와 감사행이 3.5-flash를 확인한다. |
| 원 발주 model 범위 | “safety_classify 시트를 **서버 소유**로 핀” | FAIL | proxy `index.ts:531`은 여전히 client `body.model`을 채택한다. `safetyClassifySeat` `:604-605`는 crisis bypass만 하고 safety 전용 model override는 0곳이다. 구현이 정직한 client body pin으로 축소됐다. |
| 수정 후 effort 배선 | “`effort: low` 핀(512사고/1024출력)” | PASS | head `safety.ts:227`; proxy ladder `index.ts:112-123,627-635`가 low→thinking 512/output 1024를 실제 request에 넣는다. |
| 원 발주 effort 범위 | “safety_classify **사고-0 특례** 추가” | FAIL | `geminiGenLadder` `:121`의 zero-thinking 특례는 `capture_ocr`·`voice_transcribe`뿐이다. safety_classify는 low rung의 512를 사용한다. 원 요구와 구현 범위가 다르다. |
| 계약 테스트 | “계약 테스트 4핀” | PASS | exact head `safety-classify-proxy.test.ts` 실행 actual: 1 suite / 4 tests PASS. 단 client body/스키마 계약이며 서버 소유 model·사고0는 핀하지 않는다. |
| 동일 입력 2종 | “동일 텍스트 2종” | PASS | QA 계정 read-only `ai_audit_log`에서 before/after `prompt_hash`가 각각 `17608779`, `afb79fe4`로 쌍별 동일하다. |
| 동일 스키마 | “동일 스키마(수정본)” | UNVERIFIABLE | audit row/hash에는 `responseSchema`가 포함되지 않고 raw probe 전표·스크립트가 PR에 없다. 동일 prompt까지만 독립 확인했다. |
| token·latency 원값 | `961/1,339 → 727/801`, `2,481/3,882ms → 1,394/2,297ms` | PASS | `.env.test` QA 로그인 후 `ai_audit_log` read-only query actual이 네 행과 정확히 일치한다. 두 after 행은 3.5-flash/low/G35FLASH__LOW, before는 2.5-flash/high/base다. |
| 변화율 | “token -24%/-40%, latency -44%/-41%” | PASS | 독립 계산 actual `-24.35%/-40.18%`, `-43.81%/-40.83%`; 반올림 일치. |
| D-27 key 귀속 | “콤보키 정상 귀속” | PASS | 감사행 actual before `GEMINI_API_KEY`, after `GEMINI_API_KEY__G35FLASH__LOW`; 코드 `index.ts:740-750,813-826`과 일치한다. |
| 성공 호출 합계 | “실제 과금 성공 5콜, 총 4,593 tokens, 전 행 감사” | PASS | QA 소유 safety_classify 행은 5개, tokens `765+961+727+1339+801=4593`; 다섯 행 모두 목적·모델·effort·key가 채워져 있다. |
| 총 10회·400 실패 4회 | “이번 라운드 10콜(400 실패 4…)” | UNVERIFIABLE | 실패 응답은 proxy `index.ts:768-775`에서 audit insert `:802-834` 전에 반환되어 감사 전표가 없다. raw probe transcript도 PR에 없어 10회/4회는 독립 재생산 불가다. |
| 운영 전수 0행 | “수리 전 운영 감사로그 safety_classify 0행” | UNVERIFIABLE | QA RLS 범위에서는 첫 probe 전 0행이 맞지만 다른 사용자 행을 볼 수 없어 운영 전수 주장은 확인 불가다. |
| prompt 길이 근거 | “SYSTEM_PROMPT 실측 1,766자” | FAIL | 머지 문서 `:314`에 그대로 남았다. exact runtime template은 1,731자, proxy suffix 포함 system은 1,793자다. |

#### PR #1126 수리 범위 FAIL 최소 재현

1. server-owned model pin 부재

```powershell
git show ae20c7f597bf252bf9de6cd5f7a1369b40205f84:supabase/functions/gemini-proxy/index.ts | Select-String 'const model: string|safetyClassifySeat|effectiveModel'
```

실제 출력: model은 `body.model`에서 정하고 safety seat는 crisis bypass에만 쓰인다; safety_classify model override는 없다.

2. safety_classify 사고-0 특례 부재

```powershell
git show ae20c7f597bf252bf9de6cd5f7a1369b40205f84:supabase/functions/gemini-proxy/index.ts | Select-String 'low:|thinkingBudget = purpose'
```

실제 출력: `low: { out: 1024, think: 512 }`; zero 조건은 `capture_ocr || voice_transcribe`뿐이다.

3. SYSTEM_PROMPT 길이

```powershell
node -e 'const cp=require("node:child_process"); const s=cp.execFileSync("git",["show","ae20c7f597bf252bf9de6cd5f7a1369b40205f84:src/lib/llm/safety.ts"],{encoding:"utf8"}); const raw=s.match(/const SYSTEM_PROMPT = `([\s\S]*?)`;/)[1]; const value=Function("return `"+raw+"`;")(); const suffix="Classify the user message below. Output JSON only, no prose."; console.log(JSON.stringify({runtimeChars:value.length,fullSystemChars:(value+"\n\n"+suffix).length}))'
```

실제 출력: `{"runtimeChars":1731,"fullSystemChars":1793}`.

PR #1126 수리·토큰 최종 합계: **PASS 10 / FAIL 3 / UNVERIFIABLE 3**.

결론: 수정 후 client body 계약과 실제 token/latency/key 전표는 통과했다. 그러나 최초 발주의 서버 소유 model·사고0 범위는 구현되지 않았고, 문서의 1,766자도 미정정이다. 담당 세션은 서버 seat pin/사고0를 구현하거나 범위 변경 승인을 명시해야 한다.
