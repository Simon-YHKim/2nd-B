# PR #1865 production server preflight — 2026-09-26 05:43:41 KST

**Decision: NO-GO for production writes or client activation.** This dispatch made read-only Supabase management calls and local Git/file reads only. Target identity was verified: `supabase/config.toml` ref `zoacryukmdeivmolvyhj` equals authenticated project `2nd-brain`, `ACTIVE_HEALTHY`, Seoul region. Worktree HEAD `0ce7b1c4d24823e8728d1db3f0152d6e76ce6015` equals remote PR branch; remote main is `ed2e54c8a527640726f618a16c0fe5949fda2a6c`. The server files have no diff from the manifest's source commit `2c6420e7fe011e144b49ee8719259c3a77b79399`; its manifest is `docs/qa/manifests/server-first-2c6420e7.json` (inventory SHA-256 `20a1076470b53f30eb7c7597623a6e1f48163e40d8e7a754053067f752d7d930`).

## Live evidence and decision gates

| Gate | Authenticated read-only observation | Decision |
| --- | --- | --- |
| Migration history | `list_migrations`: 148 rows; last by timestamp is `0150_signup_consent_contract_20260902`. `0165_log_ai_audit_xai_vendor` and `0188_raw_clippings_deleted_account_fence` were applied earlier. All 19 other local numbered files above 0150 are absent **by name**. | Never infer order or absence from numeric prefix alone. |
| 0165 / 0188 semantics | Live `log_ai_audit` source accepts `xai`. Live `storage.objects` INSERT and UPDATE raw-clippings policies both require an existing `users` row; `pg_policies` deparses `public.users` as `users`, so a string check for `public.users` is a false negative. Local 0181 retains `xai` in its allowlist, and 0186 recreates the storage write policies with the same live-user fence plus tighter path checks. | Treat 0165 and 0188 as already applied; do not replay. Verify 0181/0186 together in clone. |
| Remaining 19 numbered SQL files | Catalog checks for the primary object/effect of 0171–0187 and 0189–0190 were all negative: no public-data quota, reward hardening, consent RPC, capacity table, reward tickets, records request ID, audit idempotency, OAuth tables, billing limit, purpose quota, account-deletion verifier, knowledge-source HTTPS constraint, erasure registry, or `erase_my_data(text)`. The legacy direct reward grant RPCs remain executable by `service_role` and lack current-user eligibility checks. | These are real missing prerequisites, but full definitions/constraints and migration collision safety still require clone rehearsal. |
| Signup | 0148/0149/0150 are in the ledger. The `auth.users` confirmation trigger is enabled, security definer, and uses the contract. `email-v3` resolves to 2026-09-07 / 2026-09-07 / 2026-08-16; `email-v4` returns no tuple. `signup_consent_contract_status()` is absent. | Public build gate stays closed until the email-v4 draft and actual confirmation canary pass. |
| Service consent / Polaris / Reward / Paddle | No `llm_consent_receipts`, `polaris_generations`, `reward_ssv_tickets`, `account_deletion_tombstones`, service consent status/coverage/snapshot RPC, Polaris ratify RPC, reward issue/settle RPC, or Paddle adjustment-review RPC. Live `rewarded-ssv` v86 directly grants rewards; `paddle-webhook` v94 and `subscription-manage` v85 have no binding marker; four LLM proxy bundles have no new consent mode marker. | No feature activation. OFF + observed drain are mandatory before incompatible DB/Edge swaps. |
| Edge/functions | 15 active Edge functions; `service-consent` absent. Relevant current versions: claude 128, delete-account 129, gemini 148, oauth-naver 128, openai 133, paddle-webhook 94, rewarded-ssv 86, rss-proxy 89, subscription-manage 85, xai 64. Production bundles were read, not invoked. | Deploy only after exact dependent SQL and frozen source checks; record new versions and canaries. |
| Runtime configuration | `runtime_flags` has only `analytics_enabled=true`, `clarity_enabled=false`, `llm_enabled=true`. No development branches. The available Supabase connector has no secret-name / Edge environment listing method, so `REWARD_SSV_ENABLED`, `PADDLE_WEBHOOK_ENABLED`, `LLM_CONSENT_MODE`, binding secret, AdMob units, and provider keys could **not** be checked for presence. No secret values were read. | Operator must inspect **names/presence and flag states** in the Supabase dashboard before any write; do not infer from source or local `.env`. |
| Rehearsal/backup | `supabase` CLI missing; Docker executable exists but daemon socket is unavailable. No development branch/clone or verified backup/restore evidence was available to this dispatch. | Obtain restorable backup and disposable clone with extensions and auth/storage schema, then run full 19+8 forward/rollback/ledger tests before production. |

The 8 draft SQL files in the frozen inventory are `signup_consent_admob_20260925`, `account_deletion_completion_fence`, `effective_llm_consent_current_contract`, `llm_service_consent_management`, `polaris_generation_allowance`, `reward_ssv_hardening`, `paddle_refund_consequence_integrity`, and `service_contract_erasure_registry`. Three other local drafts (`oauth_naver_rate_limit_completion`, `rss_proxy_quota`, `peer_response_rate_limit`) are **outside** that inventory. The proposed new `oauth-naver` and `rss-proxy` source calls RPCs from the first two excluded drafts; current production lacks those RPCs. Therefore hold those two Edge deployments until their own SQL is numbered, rehearsed and applied, or explicitly remove them from the deployment set. `peer-respond` is outside the 11-function package and its live bundle does not call the new peer rate-limit RPC.

## Executable server-first sequence for the write dispatch

1. Recheck project ref, PR/source commit, live ledger and remote Git refs immediately before changes. Use `git ls-remote origin refs/heads/main refs/heads/fix/qa-harness-integrated-260925`; fetch remote refs in the write dispatch and scan **all** branches for proposed prefixes. Local and remote main currently max at `0190`; **0191–0198 below are candidate assignments, not reserved numbers**. Reserve by promoting/pushing a branch before apply, then check collisions again. A read-only dispatch cannot create a real reservation.
2. Confirm restorable backup, test restore, and isolated clone with `pgvector`, `auth`, `storage`, and current ledger. Run all 173 local numbered files plus promoted drafts against a fresh clone and apply only the 19 live-missing numbered files to a production clone in actual ledger order. Check current object definitions and data, `db/erasure-registry.json` forward additions, `rollback/0189_down.sql` registry replay, ACL/RLS, SQL fixtures, and CLI/MCP ledger round trip. A production `db push` over this timestamp-ledger/numbered-file history is unsafe without reconciliation. Never run down SQL in production.
3. Candidate draft numbering and dependency order: `0191_signup_consent_admob_20260925`; `0192_account_deletion_completion_fence`; `0193_effective_llm_consent_current_contract`; `0194_llm_service_consent_management`; `0195_polaris_generation_allowance`; `0196_reward_ssv_hardening`; `0197_paddle_refund_consequence_integrity`; `0198_service_contract_erasure_registry`. `0198` runs only after all four owned tables exist. If another branch has allocated any prefix, recompute the entire map and registry/rollback references. Use the connector's `apply_migration(project_id=zoacryukmdeivmolvyhj, name=<numbered file stem>, query=<exact reviewed SQL bytes>)` **one file at a time**, checking the returned timestamp ledger row and catalog after each; never use SQL Editor alone for untracked DDL.
4. Before changing LLM DB/proxies, set `runtime_flags.llm_enabled=false` and prove in-flight provider requests drained from logs. Apply 0173/0174/0176/0185 and promoted consent dependencies; deploy four frozen proxy sources with the same `LLM_CONSENT_MODE=collect`, keep management off until its Edge is installed with JWT verification. Canary legacy collect, new grant/revoke, pre/post token checks and coverage. Only `uncovered_accounts=0 AND blocked_accounts=0` permits enforce. On failure keep global LLM blocked and roll forward; do not revert to consent `off` after withdrawals.
5. Keep Reward server/client OFF; prove invocation/callback drain. Apply 0172, 0177, then 0196; verify exact service-role RPC ACLs and revoked legacy grants. Deploy frozen `rewarded-ssv`, verify disabled 503 first, then signed/tampered/replay test under a narrow private canary. Roll back with server flag OFF and forward fix, not DB down.
6. Keep Paddle webhook OFF and prove gateway/delivery/invocation drain. Configure the same server-only binding secret name on signer and verifier without exposing its value; deploy `subscription-manage` first, validate private binding-aware preview, close legacy checkout/token paths, then apply 0197 and deploy strict `paddle-webhook`. Use sandbox and narrow production replay/negative canaries before enabling. If failed, leave webhook OFF and roll forward; do not redeploy old webhook or execute `0136_down.sql`.
7. Apply deletion/erasure/Polaris prerequisites and registry-only 0198, deploy delete-account and OpenAI Polaris wrapper plus other proxies, canary delete/settlement races, then enable Polaris. Deploy `service-consent` only after SQL/collect proxy coverage. Hold oauth-naver and rss-proxy until excluded SQL dependencies are promoted. Record Edge versions, deployed source SHA, secret **presence**, flag states, canary and coverage evidence. The coding owner can publish the client only after exact signup status tuple, real email confirmation and all relevant server gates pass.

**Current hard blockers:** no verified backup/clone/rehearsal; 19 missing numbered prerequisites; 8 unreserved drafts plus incomplete erasure registry forward/rollback metadata; unknown Edge secret/flag presence; no service-consent Edge or signup status RPC; two Edge entrypoints with SQL drafts outside the frozen inventory. Current findings are metadata/semantic spot checks, not a proof that every migration can run on live data. No production/GitHub changes, tests, or canaries were performed in this dispatch.
