# S1 트랙 로그 — 2026-07-19 (앱 시스템: iOS PR / 웹 export P0 / SSV / Clarity / 파서)

> 발주: docs/session-dispatch_260719.html · 발주 시점 main = da6be790 (#1088)
> 담당: Claude Code (fable5 max effort) · 소유 경계 = src/lib/{ads,analytics,import,ops} · RewardedSheet · ReasoningLimitSheet · app.json/app.config.js/eas.json · .github/workflows · 신규 테스트
> 인수인계 규칙: ① 디스패치 프롬프트 전문 ② 이 로그 ③ docs/HANDOFF.md Latest 순서로 읽고 마지막 항목부터 재개.

---

[2026-07-19 / 09:29:45 KST] 세션 시작 — 시작 절차 수행.
- fetch 완료. origin/main = da6be790 (#1088) = 발주 시점과 일치. 로컬 main도 동일 SHA (pull 불요).
- docs/session-dispatch_260719.html 전문 + docs/HANDOFF.md "Latest — 2026-07-18 (4)" 정독 완료.
- 스태시 확인: stash@{0} = "admob-session iOS app.json WIP (UMP tracking string + GoogleService-Info ref)" (메시지 매칭 — 대상 확정). stash show -p로 diff 검토: app.json ios 블록에 googleServicesFile + NSUserTrackingUsageDescription + ITSAppUsesNonExemptEncryption:false 3키. 현재 app.json ios 블록 컨텍스트와 충돌 없음 판단 (ios 블록은 스태시 생성 후 미변경, version/plugins 변경은 다른 hunk 범위).
- 워킹트리 실측: app.config.js M (iOS GOOGLE_SERVICE_INFO_PLIST env 오버라이드) · eas.json M (submit.production.ios ascAppId 6792266942 / appleTeamId 7CP84WS5C6) — 디스패치 기술과 일치.
- 다음: S1-0 실행 (stash apply → 검증 → feat/ios-release-prereq → 3파일만 커밋 → PR).

[2026-07-19 / 09:38:00 KST] S1-0 완료 — PR #1089 오픈.
- stash apply stash@{0} (메시지 매칭 재확인 후) → auto-merge 클린, 충돌 없음. app.json diff = 정확히 3키(googleServicesFile / NSUserTrackingUsageDescription / ITSAppUsesNonExemptEncryption:false).
- git check-ignore 실증: .gitignore:128 `**/GoogleService-Info.plist` — plist는 diff에 못 들어옴.
- feat/ios-release-prereq 브랜치 생성 → app.json·app.config.js·eas.json 3파일만 명시 add (design D 2건·flow-debugger M은 unstaged 잔류 확인) → 커밋 00debb71 → push (로컬=원격 SHA 일치 검증).
- PR: https://github.com/Simon-YHKim/2nd-B/pull/1089 — gh pr view --json files로 diff 3파일 실증. 버전 bump 없음(이미 0.1.0).
- 적용 완료 diff 확인 후 해당 스태시 drop (d16e3218). 뮤지엄 WIP 스태시는 @{1}로 잔존(보존 대상 — 건드리지 않음).
- git checkout main 복귀. 캐노니컬 트리의 app.config.js·eas.json M 상태는 브랜치로 흡수돼 해소됨(의도된 결과 — 회귀분 복구).
- 다음: S1-1 워크트리 생성 + web-deploy 실패 런 분석.

[2026-07-19 / 10:05:00 KST] S1-1 근본원인 확정 + 픽스 구현 (워크트리 s1-system, 브랜치 claude/s1-system-260719).
- 워크트리 E:\2ndB\.worktrees\s1-system 생성(origin/main da6be790) + node_modules 정션 완료.
- 실패 런 29645075631 --log-failed 정독: "Importing native-only module react-native/Libraries/Utilities/codegenNativeComponent on web" — import stack = react-native-google-mobile-ads(index→BannerAd→codegenNativeComponent) ← src/lib/ads/rewarded.ts ← ReasoningLimitSheet ← /reasoning ← /capture ← require.context.
- 로컬 재현 완료(픽스 전, 워크트리에서 npx expo export --platform web): CI와 동일 에러/스택 실증. λ Bundling failed, 4270 modules.
- 근본원인: rewarded.ts/consent.ts는 런타임 lazy require 규율은 이미 지킴(#1085 설계) — 그러나 Metro는 모듈 그래프를 정적으로 추적하므로 Platform.OS 가드와 무관하게 require("react-native-google-mobile-ads")가 웹 그래프에 편입 → 번들타임 크래시. 런타임 가드는 번들타임을 못 지킴. jest/tsc는 이 클래스를 볼 수 없음 = "CI 그린인데 배포만 깨짐"의 구조적 원인.
- 픽스(RN 표준 플랫폼 분리, 디스패치 권장안 그대로):
  · src/lib/ads/types.ts 신설 — 타입 전용 공유 모듈(RewardedResult/ShowRewardedAdOptions/UmpConsentResult).
  · rewarded.native.ts / consent.native.ts — 실구현 이동(내용 동일, SDK 참조는 .native에만 존재).
  · rewarded.ts / consent.ts — 웹/기본 해석용 fail-closed 스텁(completed:false, canRequestAds:false, SDK 문자열 참조 0). 기존 웹 런타임 시맨틱과 동일(웹=광고 없음) — 이제 번들타임에 보장.
  · 콜사이트(RewardedSheet/ReasoningLimitSheet)는 "@/lib/ads/rewarded" 임포트 유지 — Metro가 플랫폼별로 해석.
- 회귀 가드 2중:
  · 신규 jest 가드 src/lib/ads/__tests__/platform-split.test.ts — ①웹 해석 파일(rewarded/consent/types/policy.ts)에 SDK 참조 형태(import/from/require/import()) 0 ②가드 자기건전성(.native에는 SDK 참조 존재해야 함) ③export surface 웹=네이티브 일치(Object.keys) + typeof 상호 대입으로 컴파일타임 시그니처 패리티 ④스텁 fail-closed 행동 고정.
  · ci.yml에 web-export-smoke 잡 신설 — PR CI에서 npx expo export --platform web 실행(기본 env, EXPO_PUBLIC_UI=deep-space). 이 클래스는 import-graph 형상 문제라 env와 무관 — 주석에 명시.
- 기존 테스트 조정: rewarded.test.ts/consent.test.ts 임포트를 ../rewarded.native, ../consent.native로 변경(행동 계약은 실구현에 계속 핀), source-pin 경로도 .native.ts로.
- tsc는 moduleSuffixes 미설정(expo/tsconfig.base 확인) → 타입 해석은 항상 스텁 기준 = 단일 타입 정본 확인.
- 검증: npx jest src/lib/ads/__tests__ → 4 suites / 34 tests 전부 PASS. 현재 post-fix expo export + npm run verify 병렬 실행 중.

[2026-07-19 / 10:24:00 KST] S1-1 DoD 게이트 2개 모두 통과 — PR 오픈 진행.
- npx expo export --platform web (픽스 후, 워크트리): exit 0, 85+ 라우트 정적 렌더 + dist/index.html 생성(92MB). 픽스 전 동일 명령은 Bundling failed였음 — before/after 실증 완료.
- npm run verify (단독 실행, 리다이렉트 없음): exit 0 — 380 suites / 2939 tests 전부 그린(베이스라인 2935 + 신규 platform-split 4).
- S1-2 사전 조사 결과(코드 실측):
  · SSV 서버측은 완성 상태: rewarded-ssv 엣지(fail-closed REWARD_SSV_ENABLED!=1→503, ECDSA 검증, custom_data "userId"/"userId|chat" 라우팅, transaction_id 멱등) + 0079(리즈닝 grant)/0091(챗 grant, 공유 rewarded_ssv_txns 원장).
  · 이중지급 가드 실태: 리즈닝 클라 지급 addRewardCredits(entitlements/usage.ts:145)에 EXPO_PUBLIC_REWARD_SSV=true 시 no-op 가드 이미 존재. 챗 클라 지급 grantChatAdBonus(src/lib/chat/usage.ts:58)에는 해당 가드 없음 = SSV 켜는 순간 챗만 이중지급. → src/lib/chat은 S1 소유 밖: [타트랙결함]로 아래 기록.
  · 미배선 실측: showRewardedAd() 무인자 호출 2곳(RewardedSheet.tsx onWatch·ReasoningLimitSheet.tsx onWatch) — customData 없이는 SSV 콜백이 no_user 400으로 죽으므로 SSV 모드 자체가 기능 불가. 배선은 두 시트(둘 다 S1 소유) 내부에서 해결 가능: ReasoningLimitSheet는 useAuth() userId 이미 보유, RewardedSheet는 useAuth() 추가(부모 파일 수정 불요 — 소유 경계 준수).
- [타트랙결함] src/lib/chat/usage.ts grantChatAdBonus: entitlements/usage.ts:139-145와 동일한 가드 필요 — 함수 첫머리에 `if (process.env.EXPO_PUBLIC_REWARD_SSV === "true") return 0;` (또는 현재 보너스 조회 후 반환). 근거: 0091 헤더가 명시하는 전환 계약(EXPO_PUBLIC_REWARD_SSV=true ↔ REWARD_SSV_ENABLED=1)에서 챗 경로만 클라+서버 동시 지급됨. S5 수거 요망(파일 소유 미배정 영역).

[2026-07-19 / 10:48:00 KST] S1-1 PR #1090 + S1-2 PR #1091 오픈 완료.
- PR #1090 (claude/s1-system-260719, 커밋 6d3b4606): 플랫폼 분리 + 가드 2중(ci web-export-smoke 잡 + platform-split.test) + 이 로그 포함. verify 380/2939 그린.
- PR #1091 (claude/s1-ssv-260719, 커밋 f78a59cc, main 기반 — #1090과 파일 교집합 0, 머지 순서 무관): ReasoningLimitSheet에 ssvCustomData:userId(리즈닝=bare), RewardedSheet에 kind 분기(`userId|chat`/bare, useAuth로 자체 소싱 — 부모 파일 무수정으로 소유 경계 준수), ssv-wiring.test.ts 핀 4종. 이 브랜치 verify 2939 그린 + tsc 클린.
- 지급 단일화 설계안은 PR #1091 본문에 전문 기재(플래그 페어 계약, 멱등 체인 transaction_id→rewarded_ssv_txns PK→cap clamp, 챗 가드 공백 [타트랙결함], 컷오버 런북, UI 지연 노트). 엣지/서버 변경 없음 → [배포요청] 없음.
- 다음: S1-5 (proposalsToMarkdown i18n, tiny) → S1-4 (P1 파서) → S1-3 체크리스트 → S1-6.

[2026-07-19 / 11:20:00 KST] S1-5 완료 (PR #1092) + S1-4 구현 완료 (verify 대기).
- S1-5: proposalsToMarkdown에 SystemLocale 3번째 파라미터(기본값 = systemLocaleFor(i18next.language), 호출 시점 평가) — 콜러 무수정으로 KO는 기존 바이트 동일·그 외 로케일 EN 제목. 로케일 JSON 키 추가 없음(C7 무접촉). 테스트 12/12 + verify 그린 → PR #1092 (claude/s1-i18n-260719, 0798cdb9).
- [관찰/후속 후보] buildProposals의 sub 라벨들("약속 → 캘린더 후보" 등)은 여전히 전 로케일 KO 고정 — 큐 D 범위 밖이라 미수정. 후속 태스크 후보로 기록만.
- S1-4 (claude/s1-parsers-260719): youtube.ts(Takeout watch-history 파서+요약, 광고 제외·채널/월별 파생 시그널만·개별 영상 제목 비노출·50k 캡) + finance-csv.ts(3계열 레이아웃: 출금/입금 분리·무부호 카드(지출 기본, 구분열 입금 인식)·부호 단일열; 인용부 천단위·전문·;/tab 구분자; skipped 정직 카운트) + detect.ts 2 kind 라우팅(보수적 스니핑 — Netflix류 CSV는 unknown 유지) + buildProposals 분기(youtube=비민감 리듬+채널 / finance=민감·proposal에 ledgerEntry 탑재) + ledger-ratify.ts(선택 행만 ops_ledger 기입, 카테고리 "명세 가져오기" 고정, per-row fail-soft).
- [소유 경계 노트] src/lib/google/tasks.ts:53 — ImportSummary 리터럴 1곳이 신규 필드(watches/transactions) 누락으로 컴파일 실패 → 기계적 2필드 보강(내 인터페이스 확장이 강제한 변경; google/은 미배정 영역). PR 본문에 명시.
- [S5 배정 요청] ImportHubScreen(스크린, S1 소유 밖) 후속 2건: ① ratify 시 finance kind에서 ratifyLedgerEntries(userId, chosen) 호출(원장 실기입 — #1075 relationSignals 후처리와 같은 위치) ② summary.watches/transactions 카운트 표시. 라이브러리 계약·테스트는 이번 PR에 완비 — 훅업만 남음.
- [설계 한계 기록] ops_ledger엔 자연 유니크 키가 없어 같은 명세 재비준 시 중복 기입됨(리뷰 화면이 현재의 dedup 권위). content-hash 가드 후속 후보.
- 테스트: youtube 6 · finance-csv 8 · ledger-ratify 3 · detect +4 · proposals +2 전부 그린, tsc 클린. verify 백그라운드 진행 중.

[2026-07-19 / 11:22:00 KST] S1-3 Clarity 활성 검증 — [대기] (선행조건: #1090 머지 + web-deploy green 첫 배포).
코드 실측으로 준비한 검증 체크리스트(배포 후 실행):
1) 주입 게이트 체인(코드 기준): EXPO_PUBLIC_CLARITY_PROJECT_ID(빌드) → runtime_flags DB 행 analytics_enabled AND clarity_enabled === true(#1054, 5s 타임아웃 fail-closed, analytics/index.ts:116,453-459) → 성인+동의(analyticsConsent) → 라우트 allow-list 정확일치/접두("/"만 정확일치): / /plans /settings /sign-in /onboarding (index.ts:582-595).
2) 검증 3점: ① allow-list 라우트에서만 <script> 주입 + documentElement에 data-clarity-mask="true" 확인 ② /record/* /peer/* 진입 시 미주입(네트워크 탭에 clarity.ms 요청 0) ③ 설정에서 동의 revoke → _clck/_clsk/_ga* 쿠키 소거(#1079) + clarity consentv2 denied 전송(index.ts:419-426).
3) runtime_flags 상태가 웹 동작에 걸리는 방식: 두 행 중 하나라도 false/부재/fetch 실패 → clarityEnabled=false로 주입 자체가 없음(fail-closed). 검증 전 prod DB에서 두 행 값 확인 필요(읽기 전용 — S5/Simon에 값 확인 요청; S1은 프로드 변경 금지).
4) 확인 방법(브라우저): https://simon-yhkim.github.io/2nd-B/ 시크릿 창 → QA 계정 로그인 → DevTools Network "clarity" 필터 + Application 쿠키 탭. Clarity 대시보드(xb3qenit2h) recording 유입은 수 분 지연 감안.

[2026-07-19 / 12:05:00 KST] S1-4 PR #1094 오픈 + S5 고속 머지 확인 + P0-1 프로덕션 해소 실증 + S1-3/S1-6 부분 수행.
- S1-4: claude/s1-parsers-260719 커밋 3dd30033 (11파일 +819) → PR #1094. 이 브랜치 verify 2958 그린(2935 베이스 + 신규 23).
- S5가 이미 머지: #1089 → bc1a8b1e, #1090 → 3dac7bd8, #1091 → 09973102 (squash, [S1] 접두 제거된 제목으로 랜딩).
- ★ P0-1 해소 실증(프로덕션): main push web-deploy 런 — #1089 시점 failure(플랫폼 분리 전, 예상대로) → **#1090 머지 푸시 run 29667968331 = SUCCESS(3m36s)** = #1086 이후 첫 성공 배포. #1091 푸시 런도 이어서 진행(확인 시점 in_progress).
- [교훈/전트랙 공유] PR 제목 밸리데이터(.github/workflows/pr-title.yml)가 `^(feat|fix|...)` 앵커라 디스패치의 "[S1] 태그"를 접두로 붙이면 lint fail. 트리거에 edited 포함 → 재제목만으로 재검증됨. 해법 = conventional 접두 먼저, [S#] 태그는 말미. #1092/#1094 재제목 완료, 양쪽 lint pass 전환 확인. (머지된 #1089~#1091은 S5가 머지 과정에서 정리한 것으로 보임.)
- S1-3 실측 진전(체크리스트 중 즉시 가능분 완료):
  · 배포 번들에 Clarity ID 탑재 확인: entry-f3b373dd*.js에 xb3qenit2h 1건(curl+grep).
  · runtime_flags 실값(프로드 읽기 전용 SELECT): analytics_enabled=true · clarity_enabled=true (둘 다 2026-07-17 20:05 UTC 설정) — DB 게이트 열림. 스키마 주의: 컬럼명 value 아니고 **enabled**.
  · 결론: 주입 선행조건 전부 충족(빌드 ID + 배포 성공 + DB 게이트 open). [잔여] 브라우저 행동 3점(허용 라우트 주입/비허용 미주입/revoke 쿠키 소거)은 실브라우저 세션 필요 — 체크리스트 위 11:22 항목 그대로 실행하면 됨.
- S1-6 부분 수행(프로드 읽기 전용 관찰): QA 계정 오염 source 실측 — id ec5792e2-48e9-46e3-b808-7021029ad43d, title "KakaoTalk 가져오기", storage_path "fcd4dec5-.../kakaotalk-가져오기-53c622cba53c.md" (한글 잔존 = **아직 미힐링**), ingested=false, captured 07-18 11:01Z. 판단: promote-pending 힐링은 앱의 인박스 로드가 트리거인데 #1087 머지 후 QA 계정으로 인박스를 연 세션이 없음 — 오염 잔존은 예상 상태. [잔여] 웹(또는 S3 유휴 에뮬)에서 QA 로그인 → 인박스 로드 → 본 행 storage_path가 ASCII 키로 교정되는지 + markdown 임포트(obsidian-qa.md) → 자동 딥런 루프 + 처음-ON 시트 + 궤도 링 + Notes 칩 시각 확인. 실행 런북은 디스패치 S1-6 원문 + 이 항목.
- [질문] S1-6 웹 실주행은 브라우저 상호작용(로그인·파일 업로드·DevTools)이 필요해 이번 세션에서 미수행. S5 검수 사이클에 붙이거나 다음 S1 세션 첫 태스크로 권장.

## [완료 요약] (2026-07-19 / 12:05:00 KST 기준)

### PR 목록 (S1 산출 5건)
| PR | 내용 | 상태(확인 시점) |
|---|---|---|
| #1089 | S1-0 iOS 선행 3파일(app.json/app.config.js/eas.json) 복구 | **머지됨** (bc1a8b1e) |
| #1090 | S1-1 P0-1 웹 export 픽스(ads 플랫폼 분리) + 가드 2중(ci web-export-smoke 잡 + platform-split 테스트) + 트랙 로그 | **머지됨** (3dac7bd8) → main web-deploy SUCCESS로 P0-1 해소 실증 |
| #1091 | S1-2 SSV customData 배선(두 시트) + 지급 단일화 설계(PR 본문) | **머지됨** (09973102) |
| #1092 | S1-5 proposalsToMarkdown 제목 i18n(큐 D) | 오픈 — lint pass, verify/smoke 진행 중 |
| #1094 | S1-4 P1 파서(YouTube Takeout + 금융 CSV → ops_ledger) | 오픈 — lint pass(재제목 후), verify/smoke 진행 중 |

### 미완 (인수인계 대상)
1. S1-3 잔여: 브라우저 행동 3점 검증(허용 라우트 주입 / record·peer 미주입 / revoke 쿠키 소거) — 선행조건은 전부 충족 상태로 만들어 둠(체크리스트 11:22 항목).
2. S1-6 잔여: 웹 실주행 시각 확인(markdown 임포트→자동 딥런 루프·처음-ON 시트·궤도 링·Notes 칩) + QA 오염 source 힐링 실측(현재 미힐링 상태 실측 완료, 인박스 로드가 트리거).
3. S1-4 훅업(스크린, S1 소유 밖 — S5 배정): ImportHubScreen에 ① finance ratify 시 ratifyLedgerEntries 호출 ② summary.watches/transactions 표시. 라이브러리 계약·테스트는 #1094에 완비.
4. 후속 후보(비긴급): buildProposals sub 라벨 전 로케일 KO 고정(관찰 기록) · ops_ledger 임포트 중복 방지 content-hash 가드 · SSV 모드 완료 후 지연 refetch(UI 폴리시).

### 타트랙결함 (S5 수거 요망)
1. **src/lib/chat/usage.ts grantChatAdBonus** — SSV 전환(EXPO_PUBLIC_REWARD_SSV=true) 시 이중지급: entitlements/usage.ts:139-145의 D2 가드가 챗 경로에 없음. 제안 패치: 함수 첫머리 `if (process.env.EXPO_PUBLIC_REWARD_SSV === "true") return 0;`. 상세 = PR #1091 본문.
2. (결함 아님/기계적 경계 노트) src/lib/google/tasks.ts:53 — ImportSummary 확장으로 컴파일 강제된 2필드 보강을 #1094에 포함(미배정 영역, 투명성 위해 기록).

### 게이트 (발생 없음)
- 파괴적 작업·비용 발생·프로드 변경 없음. 프로드 접근은 읽기 전용 SELECT 2회(runtime_flags 값, QA source 행)뿐. [배포요청] 없음(SSV 엣지/RPC는 기존 배포 상태 그대로 휴면).

---

[2026-07-19 / 11:10:00 KST] 추가 발주(콘솔 트랙 인계): iOS 빌드 워크플로 생성 + 첫 빌드 크리덴셜 경로 — 완료 (PR #1099).
- 배경 실측: 리포에 iOS 빌드 워크플로 부재(eas-preview-build.yml은 Android 전용) — #1089 머지로 설정은 갖춰졌지만 실행 대상이 없었음.
- 산출: .github/workflows/eas-ios-build.yml (workflow_dispatch 전용 = 슬롯 소모가 구조적으로 사람 게이트; profile choice preview|production 기본 production; 전용 concurrency 그룹; contents:read; EXPO_TOKEN; check:ota-runtime 게이트 유지; --non-interactive --no-wait --json + 런 서머리에 빌드 URL). Android 쌍둥이와 구조 패리티, 의도적 델타 1개 = GOOGLE_SERVICES_JSON_BASE64 복원 스텝 제거(iOS plist는 EAS 파일 시크릿 GOOGLE_SERVICE_INFO_PLIST를 빌더가 주입, app.config.js가 읽음 — 러너에 plist 비접촉). YAML은 js-yaml 로드로 파싱 검증.
- 크리덴셜 경로 판단: **(a) ASC API Key 채택**(발주 권장안과 일치). 근거: (b) 대화형은 Apple ID+2FA 의존 + 로테이션마다 재발생; (a)는 1회 셋업 후 CI 완전 비대화형 + eas submit에도 재사용. 셋업 절차는 워크플로 헤더 + PR #1099 본문에 명문화(.p8 커밋 금지 명시). eas credentials는 빌드가 아니라 슬롯 소모 없음.
- [Simon 액션 목록] ① ASC > Users and Access > Integrations > Keys에서 App Manager 키 생성, .p8(1회 다운로드)+Key ID+Issuer ID 확보 ② 로컬에서 npx eas-cli credentials --platform ios (production 프로파일) → API 키로 배포 인증서+프로비저닝 EAS 생성·저장 + 제출용 키 저장 ③ PR #1099 머지(S5) 후 Actions에서 EAS iOS Build 수동 실행(슬롯 소모 — 실행 직전 확인) ④ production 빌드 후 eas submit -p ios (타깃은 eas.json에 이미 고정).
- [타트랙 동기화 노트/ATT] 발주 경고 접수: ATT 실호출 0(expo-tracking-transparency 미설치) → App Privacy "트래킹 안 함" 선언이 현재 정합. admob-sdk-x 트랙이 ATT를 구현하는 릴리즈에서 ASC App Privacy를 Yes로 동시 갱신해야 하며, 그 전까지 IDFA 사용 금지. app.json의 NSUserTrackingUsageDescription(#1089)은 API 호출 없인 무해 — 선행 탑재 상태만 유지.
- [관찰/ENABLE_ADS=true] GitHub 변수 변경 접수. 웹: 플랫폼 분리로 SDK 자체가 웹 그래프에 없고 AdSense 변수 미설정이라 광고 없음 유지. 네이티브: rewarded.native.ts 프로덕션 가드(실유닛 부재 fail-closed)가 여전히 마지막 잠금 — 실광고는 실유닛 생성+가드 교체 릴리즈에서만.
- 워크트리 부산물 노트: verify 체인이 docs/flow-debugger.html의 STALE 스탬프 1줄을 재기록(flag-changed-screens.js) — 워크트리 사본만 git restore로 원복(캐노니컬 E:\2ndB 사본 비접촉).

[2026-07-19 / 11:35:00 KST] 추가 발주 2: iOS 크리덴셜 로컬 전환 (Apple fastlane 2FA 차단 대응) — 완료.
- 배경 접수: eas credentials의 프로비저닝 생성 경로가 Apple fastlane 2FA에서 반복 실패("Verification codes can't be sent...", 웹 로그인은 정상) → 계정 잠금 위험으로 원격 경로 폐기, Apple 로그인 자체가 불필요한 로컬 크리덴셜로 전환. 포털 웹에서 배포 인증서+App Store 프로파일(둘 다 2027-07-19 만료) 발급·p12 변환·쌍 일치 검증까지 콘솔 트랙이 완료.
- 구현 3종(같은 PR):
  · eas.json — credentialsSource:"local"을 **build.production.ios 아래(플랫폼 스코프)**로 추가. 발주 문구는 "production 프로파일에 추가"였으나 프로파일 루트에 넣으면 Android production까지 로컬로 전환돼 EAS 관리 키스토어 경로(AAB)가 깨짐 → 의도(iOS 한정)에 맞춰 스코프 조정. submit.production.ios 불변.
  · eas-ios-build.yml — 빌드 직전 "Provision iOS signing credentials" 스텝(production 프로파일 한정 if 게이트): 3개 시크릿 존재 검증 → base64 복원(ios/certs/) → 비어있음 검증 → credentials.json은 node JSON.stringify로 작성(패스워드 특수문자 JSON-이스케이프 안전, 셸 heredoc 대신) → chmod 600. 값 echo 0(env→파일 직행). 헤더 주석을 로컬 서명 스토리로 전면 교체(ASC 키 MCNQH5N3US는 submit 전용 유지 명시, preview iOS는 여전히 원격=Apple 벽 잔존 명시).
  · .gitignore — credentials.json + ios/certs/ 추가(*.p12/*.mobileprovision/*.p8/*.key는 기존 29-32행, /ios 전체 무시 71행도 기존 — credentials.json만 실질 신규 커버, 패스워드 포함 파일이라 필수).
- .easignore 확인 결과(발주 요청): credentials.json·ios/certs·*.p12·*.mobileprovision 어느 것도 제외하지 않음 — 로컬 크리덴셜 플로와 충돌 없음. (Android google-services.json 전례 같은 함정 부재.)
- 검증: 워크플로 js-yaml 파싱(6 스텝 순서 확인) + eas.json node 파싱(production.ios/android/submit 3분면 값 확인) + git check-ignore로 credentials.json 무시 실증.
- 빌드 실행 전 남은 선행조건: ① Simon GitHub 시크릿 3종 등록(IOS_DIST_P12_BASE64/IOS_PROVISIONING_PROFILE_BASE64/IOS_DIST_P12_PASSWORD — 완료 알림 대기) ② S5 이 PR 머지 ③ Simon GO 후 workflow_dispatch(production) 실행(슬롯 소모) ④ 빌드 후 eas submit -p ios(ASC 키 등록済).

[2026-07-19 / 13:55:00 KST] 추가 발주 3: iOS 빌드 run #1 실패 → 러너 plist 복원 스텝 추가 — 완료.
- 증상 접수: run 29673670150 "Submit EAS iOS build" exit 1 — 러너의 Expo CLI app-config 해석이 ios.googleServicesFile(./GoogleService-Info.plist)을 readFileSync → ENOENT. 슬롯 미소모(업로드 전 사망). 서명 스텝(PR #1102 로컬 크리덴셜)은 정상 통과 확인됨.
- 근본 원인 인정: 내 #1099 헤더 전제("plist는 러너에 닿지 않는다")가 config-해석 단계에 대해 틀렸음. eas build는 아카이브 업로드 전 러너에서 config를 해석하고, EAS 파일 타입 env는 빌더에서만 디스크에 풀림. Android 쌍둥이가 안 깨진 이유 = google-services.json 복원 스텝이 이미 있었기 때문 — iOS에만 대응 스텝 누락.
- 수정(1파일): eas-ios-build.yml에 "Provision GoogleService-Info.plist" 스텝 신설(ota-runtime 다음, 서명 스텝 앞) — GitHub 시크릿 GOOGLE_SERVICE_INFO_PLIST_BASE64 복원 + 비어있음 검증 + BUNDLE_ID=com.simonk.secondbrain node 검증. **프로파일 게이트 없음**(발주 지시대로 — config 해석은 preview에서도 일어남). 헤더 주석을 "동일 파일 2경로(러너=GitHub 시크릿/빌더=EAS 파일 env, app.config.js의 `?? 폴백`이 자동 분기)" 사실로 전면 정정. app.config.js/app.json/eas.json 무수정. .easignore는 plist 미제외 확인(복원 파일이 아카이브에 실림 — Android와 동일).
- 별건 플래그 접수 → GitHub 이슈로 등록(범위 밖 보존): plist에 CLIENT_ID/REVERSED_CLIENT_ID 부재(Firebase iOS OAuth 클라이언트 미생성) — 빌드는 생존하나 iOS URL scheme 미주입. EXPO_PUBLIC_NATIVE_SOCIAL_SDK=false라 현재 무영향, 네이티브 Google 로그인 활성화 시 Firebase 콘솔에서 iOS OAuth 클라이언트 생성→plist 재발급→시크릿 2종(EAS 파일 env + GitHub base64) 갱신 필요.
- 검증: js-yaml 파싱으로 7스텝 순서·게이트 확인 + npm run verify 그린 후 push.

[2026-07-19 / 15:25:00 KST] 추가 발주 4: iOS pod install 실패(Firebase Swift 파드 모듈맵) → useFrameworks static 채택 — 완료.
- 증상 접수: EAS 빌드 ef68af0f "Install pods" 실패 — AppCheckCore·FirebaseCoreInternal(Swift)이 non-modular GoogleUtilities 의존 → static library 링크 모델에서 CocoaPods 통합 거부. Read config(#1104)·Prepare credentials(#1102)·Prebuild는 통과 실증 — 두 PR 무접촉 유지.
- 문서 대조(발주 지시 — 착수 전 최신 문서 확인 수행):
  · rnfirebase.io Expo 설치: expo-build-properties ios.useFrameworks:"static"이 공식 요구("firebase-ios-sdk requires use_frameworks"). $RNFirebaseAsStaticFramework는 bare 전용 문단에만 존재.
  · RNGMA(docs.page/invertase): "$RNGoogleMobileAdsAsStaticFramework=true는 bare Podfile용"이고 **"Expo users may enable static frameworks by using the expo-build-properties plugin"** — Expo에서는 동일 메커니즘이 곧 공식 경로 → 추가 config plugin 불필요 판정.
  · @sentry/react-native·react-native-purchases(RevenueCat)·nitro-modules·kakao-login 공식 문서: use_frameworks static 요구/비호환 언급 전무(침묵). 방증 실측: 오늘 실패 로그에서 Firebase 계열 2파드 외 전 파드(카카오·HealthKit/Nitro·Sentry·RevenueCat 포함)는 static-library 모드조차 통과 — non-modular 의존이 없는 Swift 파드들이라 프레임워크 전환 리스크 낮음.
- 택일: **1순위(useFrameworks static)** 채택. 탈락 사유(2순위 modular headers): 전역 use_modular_headers!는 React 파드와의 알려진 충돌 클래스, GoogleUtilities 한정 :modular_headers는 Firebase/GMA 스택의 다른 non-modular 의존(nanopb·GoogleDataTransport·GoogleUserMessagingPlatform 등)으로 두더지잡기 재발 위험 + GMA SDK 12의 Swift 전환 방향과 역행 + 벤더 문서가 지정하지 않는 비정규 경로. withDangerousMod 구현 부담은 낮으나(전례 withAndroidAbiFilter) 정규 경로 존재 시 선택할 이유 없음.
- 변경: app.json expo-build-properties에 "ios": {"useFrameworks": "static"} 1키 추가. Android 블록·app.config.js·eas.json·워크플로 무수정(발주 경계 준수).
- 검증: Windows에서 iOS prebuild 자체 불가("Run from macOS or Linux" — 디스패치 예상대로 로컬 한계) → 대체로 npx expo config --type prebuild 전체 해석 실행: 플러그인 체인 평가 정상, resolved ios buildProps = {"useFrameworks":"static"}, android 무변경, ios.googleServicesFile 폴백 유지 확인. npm run verify 그린 후 push. 실검증은 EAS 재빌드(실패 빌드 슬롯 미집계 — 무비용).
- 잔여 리스크 정직 기록: 링크 모델 전역 전환이라 다음 빌드에서 컴파일 단계 신규 에러 가능성은 남음(문서상 차단 요인은 0건). 표면화 시 해당 라이브러리 단위로 후속 발주 요망.

[2026-07-20 / 01:35:00 KST] 추가 발주 5: iOS 첫 빌드 성공 → 즉시 제출 + 제출 워크플로 — 완료.
- 작업 1(즉시 제출): 성공. Submission ID cdb04f7b-feef-4b0e-a82b-e0df8dd06094 · 빌드 c517ea20 (0.1.0, build number 4) · "Submitted your app to Apple App Store Connect!" · Apple 처리 5-10분 → TestFlight (appstoreconnect.apple.com/apps/6792266942/testflight/ios). **Apple 로그인/2FA 프롬프트 미발생**(ASC API 키 MCNQH5N3US, Key Source: EAS servers).
- 시행 기록(투명성): 1차 시도는 발주 명령 그대로 E:\2ndB에서 실행 → "Set ascAppId in the submit profile" 에러. 원인 = **캐노니컬 E:\2ndB의 main이 스테일(da6be790)** — #1089의 submit.production.ios 블록이 없는 구 eas.json. 설정 검증 단계 실패라 ASC 미접촉·제출 미생성·빌드번호 무소모. 캐노니컬 pull은 S5 영역+untracked 충돌(트랙로그 등) 위험이라 회피하고, 최신 main 기반 워크트리에서 재실행(동일 프로젝트 ID·빌드 ID — 서버측 제출이라 등가). --non-interactive 1개만 추가(헤드리스 셸에서 프롬프트 발생 시 대기 대신 즉시 에러 = 중단·보고 지침 합치).
- [타트랙 노트/S5] 캐노니컬 E:\2ndB main 스테일이 이번 실패의 원인 — 다음 트리 정리 사이클에서 pull 필요(untracked docs/tracks/S1-log_260719.md가 현재 main 추적 파일과 충돌할 것 — 내용 동일(#1105 시점)이므로 로컬 untracked 삭제 후 pull이 안전).
- 작업 2(PR): .github/workflows/eas-ios-submit.yml 신규 — workflow_dispatch(build_id 선택 입력, 비우면 --latest / profile choice 기본 production), 크리덴셜 복원 스텝 **의도적 부재**(제출은 ASC API 키만 사용, IOS_DIST_* 미참조 — 헤더 명시), 슬롯 무소모 명시, set -x 없음(EXPO_TOKEN은 env→CLI만), pipefail로 tee 파이프에도 실패 전파, STEP_SUMMARY에 submission URL·ASC TestFlight 링크·CLI tail 20줄. js-yaml 파싱 검증: 4스텝(Checkout→Node22→npm ci→Submit), inputs build_id(required:false)+profile(default production), permissions contents:read, 전용 concurrency 그룹.
- 경계 준수: app.json·app.config.js·eas.json·eas-ios-build.yml·Android 무접촉. 신규 파일 1 + 트랙 로그만.

### 인프라 상태 노트
- 워크트리 E:\2ndB\.worktrees\s1-system 존치(브랜치 claude/s1-parsers-260719 체크아웃 상태, 클린). 제거 시 반드시 정션 먼저: `rmdir node_modules` → `git worktree remove`.
- E:\2ndB 본체: S1-0 이후 무접촉(잔존 M/D/untracked는 발주 전 상태 그대로).
- 이 로그의 정본 = E:\2ndB\docs\tracks\S1-log_260719.md (라이브). 리포 반영: #1090에 1차 스냅샷 머지됨, 최종본은 별도 docs PR로 제출.

[2026-07-20 / 02:15:00 KST] 추가 발주 6 (3건 일괄): ASC 90683 픽스 + #1106 리뷰 반영 + 캐노니컬 트리 정리 — 완료.
- 건1 (PR #1107, app.json만): NSHealthUpdateUsageDescription false → 정직 문구(쓰기 안 함 명시 — S5 초안에서 string→notice 1단어 교체). 전수 감사는 Windows에서 prebuild 불가라 npx expo config --type introspect(mod 인메모리 평가)로 최종 Info.plist 덤프 — 캘린더 iOS17 키(FullAccess) 주입 확인·마이크/카메라/사진/추적 전부 커스텀 문구 확인. 동일 클래스 추가 발견 = expo-calendar가 주입하는 NSReminders* 2키가 애플 템플릿 문구 → remindersPermission 정직 문구로 교체(5.1.1 왕복 예방). 관찰: SKAdNetworkItems 부재(RNGMA plugin skAdNetworkItems 미설정 — 90683 클래스 아님, 광고 GO 시점 과제로 기록). introspect용 plist 임시 사본은 사용 후 삭제.
- 건2 (#1106 push 2건 + 스레드 2건 resolve): P2 preview 프로파일 제거·production 고정(580b8977, 확장 사유 헤더 기록) + P1 build_id 필수화·--latest 제거(6d87b798 — eas submit --latest는 빌드 프로파일 무시라 애드혹 IPA 오제출 위험, 리뷰 지적 타당). 두 스레드 회신 후 GraphQL resolveReviewThread로 resolve 완료.
- 건3 (캐노니컬 트리, PR 아님): E:\2ndB main da6be790 → 57e95d9e ff-pull 완료. 과정: ①내 untracked 로그 = #1106 브랜치 사본과 바이트 동일 실증 후 삭제 ②S5 파일 2건(docs/s5-report_260719.html·docs/tracks/S5-log_260719.md)도 pull 차단 — origin/main 추적본과 바이트 동일 cmp 실증 후 삭제(무손실 no-op) ③docs/flow-debugger.html 로컬 M(1줄 STALE 스탬프)이 업스트림 변경과 충돌 → 파괴 없이 라벨 스태시 "S1 260720: flow-debugger STALE stamp ..."로 보존 후 pull(QA 세션의 unblock-pull 선례 패턴). 잔존 보고: D design zip 2건(업스트림 무접촉, 목록 보고만 — 처분은 S3 제안 대기) · 뮤지엄 WIP 스태시는 @{2}로 이동 잔존. 이후 이 로그의 라이브 정본은 워크트리 브랜치(현재 #1106)로 이관 — 캐노니컬은 추적 파일이라 직접 편집 금지.
