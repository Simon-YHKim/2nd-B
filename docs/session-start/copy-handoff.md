# 앱 문구 개선 인수 기록

2026-09-06 TTL-Work 세션에서 확인한 기록이다. 이 문서가 있는 브랜치에
아래 앱 수정이 모두 들어 있다는 뜻이 아니다. 현재 구현과 비교해서 남은 범위를 정한다.

## 남겨둔 자료

- 소유 파일 목록: `E:\2ndB\.worktrees\2ndB\TTL-Work\Output\plain-language-260906\change-manifest.json`
- 완료 보고: 같은 폴더의 `completion-report.html`
- 전체 테스트 원본: 같은 폴더의 `jest-final.json`
- 작업 전 스냅샷: 같은 폴더의 `baseline-path.txt`가 가리키는 위치
- 수정 당시 브랜치: `claude/pixelclay-auth-mascot-font-260905`

이 원본들은 미커밋 로컬 자료다. TTL-Work를 지우거나 정리하기 전에 필요한 변경과
검증 근거를 보존해야 한다. 파일이 없으면 확인 불가로 적고 Git 이력과 현재 코드로
재확인한다. 58개 소유 파일 목록은 검토 출발점이며, 파일 전체가 이 세션 소유라는 뜻은 아니다.
공유 파일에는 다른 세션의 변경도 있으므로 전체 파일 덮어쓰기나 전체 dirty diff 이식은 금지한다.

## 당시 완료한 범위

- 5개 언어(KO·EN·ES·PT·ID)의 문구 140개, 관련 프롬프트와 안내를 다듬었다.
- 어휘 검사에서 기술 설명·일상 질문을 잘못 막던 문맥과 줄 전체 부정문 면제를 고쳤다.
- 실제 AI 응답에서 근거 없는 성격 단정과 임의 답변 기한을 발견해 프롬프트를 수정했다.
- Android 암호화 저장소의 Base64 문자열·바이트 배열 전달 오류를 고쳤다.
- `npm run verify` 당시 결과: 684 suites / 8,131 tests 통과. lint 오류 0, 경고 92.
- 웹 101개 소스 경로에서 207개 화면 캡처 확인. 일부는 빈 데이터 fixture를 사용했다.
- Android API 36 x86_64 debug 확인. iOS와 출시 빌드 확인 결과로 쓰지 않는다.

커밋·push·병합·운영 배포는 이 결과에 포함되지 않는다. 해당 테스트를 현재 코드에서
다시 실행했다는 뜻도 아니다. 실제 AI 검증은 당시 QA 계정의 일일 한도에 도달했으므로
한도·비용을 확인하고 필요한 범위만 재검증한다.

## ES·PT·ID 후속 개선 — 2026-09-06

[다국어 인수 기록](multilingual-copy-260906/README.md)에 후속 변경과 패치를 보존했다.
로케일 문구 117개와 소스 안 ID 문구 4개를 다듬고, 세컨비의 UI 언어 연결과 문구 검사
패턴을 고쳤다. 위의 140개와는 별도 차수다. 같은 키를 다시 다듬은 경우도 있어 두 수를
고유 문구 개수로 합산하지 않는다. 현재 검증 결과와 미해결 검사는 후속 기록을 따른다.

스토어 [자료](../store-copy/README.md)는 KO/EN/ES/PT/ID 5개 언어로 확장했다.
공유 브랜치에 보존되어 TTL-Work 없이도 읽을 수 있다. 콘솔 대조·등록·심사 제출·공개는
하지 않았다.
