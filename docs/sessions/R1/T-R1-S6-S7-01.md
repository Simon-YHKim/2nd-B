# T-R1-S6-S7-01 — R1 잔여 후속조치 일괄 발주 (S1/S2 개별 라우팅을 S7로 통합)

발신 S6 (메타 프롬프팅) · 수신 S7 (콘솔 전담) · 2026-07-31 00:04 KST · 라운드 R1

게이트 ⑤ 법무 — 항목 1, 공개 법무 문구 확정 직전 Simon 확인 필수

상태 OPEN

## 한 줄

S5의 사후검증 티켓(`T-R1-S5-S6-01`)이 요구한 후속조치 4건을 S6가 원래 S1/S2로 나눠 BOARD에
라우팅해뒀으나, Simon 지시로 **S7이 한 번에 처리**하는 것으로 바꾼다. 코드 수정이 필요한 항목은
S7이 담당 세션에 위임하든 직접 처리하든 방식은 지정하지 않는다.

## 배경 (한 문단)

R1 PR 5건이 `f1c964a3`에서 우회 머지됐고(PROTOCOL §5 유일 예외, Simon 직접 승인), S5가 사후
재검증 결과를 `T-R1-S5-S6-01`로 보냈다. `#1141`+`#1142`(6-domain 캐논+consumer)는 CONFIRMED,
**롤백·재설계 대상 아님**. `#1140`(lifetime 철수)은 CONDITIONAL — `#1146` 완료가 조건. `#1143`
(핸드오프 완결성)은 REFUTED — `PROTOCOL.md`와 원발주 2건 원문이 `main`에 없어서다(이 결손은 S6가
별도로 LOST 확정, 재구성하지 않기로 함 — `BOOTSTRAP.md` §0-5/§0-6 참고). 전체 판정표는
`docs/sessions/R1/BOARD.md`의 "I-6"에 있다(브랜치 `claude/s6-r1-board-init` / PR `#1149`, main
미머지).

## 항목 1 — `#1146` 법무·GTM 문구 마감 + 게이트⑤

- PR `#1146` "fix(legal): retire lifetime plan and use canonical tier names"
  (`claude/s1-legal-vocab-lifetime`, 현재 DRAFT)
- 확인할 것: lifetime 표현 제거 범위, canonical tier 명칭(별바라기/항해자/북극성) 일치 여부
- 공개 약관·법무 문구를 **확정하는 시점에 게이트⑤로 멈추고 Simon 확인을 받는다**
  (BOOTSTRAP §3, 게이트 7종 중 ⑤ 법무 — 약관·개인정보처리방침·환불정책·공개 가격 고지)
- 확인 후 `gh pr checks 1146`으로 green 별도 확인, 머지 가능 상태를 S6에 회신
- 참고: 루트 랜딩(`simon-yhkim.github.io`)의 공개 가격 고지와 동시에 맞아야 한다는 메모가
  fleet-handoff에 있었다 — 머지 타이밍 조율 필요하면 반영

## 항목 2 — `north-star.ts` 오래된 주석 정정

- 파일 `src/lib/persona/north-star.ts` 상단
- 현재 주석이 "7 DOMAIN", "SAME formula"라고 돼 있는데 실제 계약은 **7-domain persona 합성과
  6-domain home headline이 분리**돼 있다(S5가 CONFIRMED, `DESIGN.md`/`docs/PRD.md`/
  `docs/CONSTELLATION-DESIGN.md` 기준)
- **로직은 바꾸지 않는다.** 주석만 현재 계약에 맞게 고친다
- 별도 PR로 낸다(다른 변경과 묶지 않음), 기존 관련 테스트 실행하고 결과를 S6에 회신

## 항목 3 — `T-R1-S1-S5-04` 원문 감사

- 2026-07-29 04:56 KST, S1→S5 발주. S5가 처리하지 못한 채로 `main`에 원문이 없다
- 둘 중 하나로 닫는다:
  1. 원문이 다른 기계·브랜치에 남아 있으면 **그대로** 복구·커밋 → `RECOVERED`
  2. 없으면 새로 지어내지 말고 `LOST / SUPERSEDED`로 기록하고, `T-R1-S5-S6-01`의 사후 판정이
     이를 대체했다고 BOARD에 연결

## 항목 4 — `T-R1-S2-S5-02` 원문 감사

- 2026-07-29 04:46 KST, S2→S5 발주. 항목 3과 같은 처리(`RECOVERED` 또는 `LOST / SUPERSEDED`)

## 하지 말 것

- `#1141` 또는 `#1142` 롤백
- Polaris 계산 로직 재수정
- 추가 production OTA 배포
- 항목 3·4의 원문을 추측으로 새로 작성
- `main` 직접 push (전부 PR로)

## 완료 조건

1. `#1146` 게이트⑤ 처리 결과(승인/보류)와 머지 가능 상태가 S6에 회신됨
2. `north-star.ts` 주석 정정 PR과 테스트 결과가 S6에 회신됨
3. 항목 3·4 각각 `RECOVERED` 또는 `LOST / SUPERSEDED`로 닫힘
4. 위 전부가 `docs/sessions/R1/BOARD.md`에 반영 가능한 형태로 회신됨(S6가 BOARD에 옮긴다)

## 회신 요청

같은 reply 파일(`docs/sessions/R1/T-R1-S6-S7-01-reply.md`)에 위 4개 항목 각각의 상태·링크·게이트⑤
결과를 적는다.
