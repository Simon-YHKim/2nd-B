# T-R1-S7-S4-06 · 발주 — Codex 사이클 173개 통합

- **FROM** S7 콘솔 전담 (Claude Cowork)
- **TO** S4 디자인 디버깅 (Codex CLI · VSCode 4) · **cc** S2 시스템 R&D · S5 적대검증 · S6 메타
- **발행** 2026-07-30 04:4x KST
- **라운드** R1
- **게이트** 없음 (코드 작업)
- **긴급도** ⚠️ **이 브랜치들은 한 대의 디스크에만 있다.** 원격에 없다.

---

## 한 줄

`E:\2ndB`(구 기계)의 Codex 사이클 루프가 밤새 만든 **173개 단일 커밋 브랜치**를 통합해 main에 올려라. **120개는 자동으로 붙지만 그것만 올리면 반드시 깨진다** — 이유는 아래에 있다.

## 왜 S4인가

내용이 **i18n·UI 카피 + 캐논 라벨 정합**이다. 손대는 파일 148~163개 중 99개가 `locales/**`, 61개가 `src/**`, 2개가 canon mirror. 이건 네 single-writer 영역이다.
그리고 충돌 해소가 **문구 판단**을 요구한다 — 나는 "코드 수정이나 설계 판단은 하지 않는다"가 경계라 여기서 멈춘다.

## S7이 실측한 것 (추정 아님)

### 1. 대상

```
사이클 브랜치 중 origin/main 대비 실질 변경 있음 = 173개
비어 있거나 이미 반영됨                          =  11개
```

committer-date 순 첫 커밋 `7e587c5e`(`codex/cycle-260726-2315`) ~ 끝 `587f16df`(`codex/cycle-20260730-034909-docs-ui-scan`).

### 2. 자동 병합률 — 내 첫 추정이 틀렸다

나는 "충돌 지옥"이라고 했는데 **재보니 아니었다.**

```
committer-date 순 cherry-pick
  120개  충돌 0  ← 기계적으로 안전
   53개  충돌
```

### 3. `-X theirs` 로 53개를 밀어붙인 결과 — **실패. 하지 마라.**

locale JSON에는 통했지만 TypeScript를 부쉈다. CI 원문:

```
src/screens/deepspace/DeepSpaceHubDockScreen.tsx(8,10):  TS2300 Duplicate identifier 'isAvailableUiLocale'
src/screens/deepspace/DeepSpaceHubDockScreen.tsx(363,7): TS2451 Cannot redeclare block-scoped variable 'CAPTURE_MODES'
src/components/deepspace/DeepSpaceHubDock.tsx(76,13):    TS17001 JSX elements cannot have multiple attributes with the same name
src/components/deepspace/CompletionToast.tsx(76,59):     TS2304 Cannot find name 't'
```

들어오는 쪽을 통째로 취하면서 블록이 중복되고 import가 날아갔다. **전략 플래그로 해결할 문제가 아니다.**

### 4. ★ 핵심 — **120개만 떼면 반드시 깨진다**

깨끗한 120개만 올려 PR `#1145`를 냈더니 **verify가 정확히 하나 실패했다:**

```
WorldviewConceptCoherence FAIL
  worldview docs/code should not regress to Iris or drift from Simon's canonical character responsibilities
```

나머지 i18n 가드는 **전부 PASS**. 원인을 팠다:

`src/lib/__tests__/worldview-naming.test.ts` 를 **사이클 커밋 3개가 강화했다** (+20줄):

```
a01153ec  fix(i18n): align core brain north star labels
f3345955  fix(i18n): rename index domain star a11y
b9ac21e7  fix(i18n): rename secondb north star hint
```

강화된 가드가 요구하는 것:

```js
expect(enCoreBrain.soulCoreEyebrow).toBe("02. North Star");
expect(koCoreBrain.myCenter).toBe("북극성");
expect(indexCopy.openPatternCore).not.toMatch(/Pattern Core|패턴 코어|Núcleo de Patrones|Núcleo de Padrões|Inti Pola/i);
//  locales/{en,ko,es,pt,id}/index.json 5개 전부
```

**가드는 120개 안에 들어왔는데, 그 가드를 만족시키는 locale 수정은 53개 안에 있다.**
루프가 "가드를 조이고 → 그에 맞춰 문구를 고치고"를 반복했기 때문이다.

→ **173개는 한 덩어리다. 쪼개면 가드와 수정이 분리돼서 깨진다.**

## 준비해둔 것

- 브랜치 `chore/codex-cycle-integration` — **원격에 푸시돼 있다.** 현재 상태 = 깨끗한 120개만 얹힌 것(`origin/main` + 120 commits).
- PR **`#1145`** — verify FAIL 상태로 열려 있다. 코멘트에 **제외된 53개 브랜치 이름 전체**를 적어뒀다.
- 워크트리 `E:\2ndB\.worktrees\s7-cycle-integrate` (구 기계에만 있음)

## S4가 할 것

1. **173개를 committer-date 순으로 통합하라.** 120개는 그냥 붙는다. 53개는 손으로 봐야 한다.
   충돌 지점은 대부분 **같은 locale 키를 두 번 고친 것**이라 후자가 맞을 가능성이 높지만, `src/**`에서는 그렇게 하면 위 TS2300/TS2451이 난다.
2. **`WorldviewConceptCoherence`가 통과하는지로 완결을 판정하라.** 그 가드가 정확히 "173개가 다 들어왔는가"를 재는 계측기다.
3. `npm run verify` **단독 실행**. 404 suites / 3139 tests 기준.
4. PR은 `#1145`를 재사용하거나 새로 열어라. 제목·본문은 네가 판단.

## ⚠️ 시간 제약

이 브랜치들은 **구 기계 디스크에만 있다.** Simon이 그 기계를 곧 끈다.
꺼도 사라지지 않지만, **그 기계를 다시 안 켜면 복구 경로가 없다.**
→ **통합 전에 173개를 원격으로 밀어두는 것을 먼저 고려하라** (`git push origin --all` 은 위험하니 사이클 브랜치만 골라서).

## 제약

- `main` 직접 push 금지 · `git add -A` 금지 · 워크트리는 리포 안 `.worktrees/`.
- `npm run verify` 파이프 금지 (exit code 마스킹).
- 커밋 메시지 본문에 큰따옴표·em-dash 금지.
- S5 적대검증 통과 후 main으로. (단 R1 PR 5건은 Simon이 07-30 01:4x에 우회를 승인했다 — 이 건도 같은 예외인지는 Simon에게 확인하라.)

## 회신요청

1. 173/173 통합 결과 + `WorldviewConceptCoherence` 판정
2. 53개 중 **버린 것이 있으면 무엇을 왜** 버렸는지
3. `npm run verify` exit code와 실행 시각
