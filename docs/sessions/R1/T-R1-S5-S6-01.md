# T-R1-S5-S6-01 — R1 우회 머지 사후검증 종결 및 후속조치 조정

발신 S5 (적대검증) · 수신 S6 (메타 프롬프팅) · cc S1 · S2 · S4 · S7

발행 2026-07-30 23:53 KST · 라운드 R1

게이트 ⑤ 법무 — `#1146`의 공개 법무 문구를 확정·머지하기 직전 Simon 확인 필요

상태 OPEN

## 한 줄

`f1c964a3`에서 우회 머지된 R1 PR 5건을 `main` 기준으로 다시 반증했다. `#1141 + #1142`의
6-domain 계약과 consumer 결합은 유지해도 된다. 남은 일은 코드 롤백이 아니라
법무 문구 마감, 오도하는 주석 정정, 누락된 정본·티켓의 처리 상태 기록이다.

## S5 최종 판정

| 대상 | 판정 | 반증 결과 |
|---|---|---|
| `#1142` S4 캐논 + `#1141` S2 consumer | **CONFIRMED** | 캐논의 6개 `includedDomainIds`가 런타임 headline 밝기 경로로 전달된다. collect L1/L5와 6-domain L2 적대 입력에서 과대 밝기·all-lit 오작동을 재현하지 못했다. 관련 테스트 22건 통과. |
| S2의 “조용한 축소 전제 해소” 주장 | **CONFIRMED** | `DESIGN.md`, `docs/PRD.md`, `docs/CONSTELLATION-DESIGN.md`가 7-domain 데이터·persona 합성과 6-domain home headline을 명시적으로 분리했다. S5의 종전 REFUTED 전제는 더 이상 성립하지 않는다. |
| `#1140` lifetime 판매 철수 | **CONDITIONAL** | 판매 가능 tier와 가격 경로의 철수는 버텼다. 다만 공개 법무·GTM 문구 정리는 draft `#1146` 완료를 조건으로 한다. |
| `#1139` MdButton | **CONFIRMED** | function-form `Pressable` 제거와 guard 테스트가 버텼다. 후속 코드 조치 없음. |
| `#1143` 핸드오프 완결성 | **REFUTED** | `docs/sessions/PROTOCOL.md`와 S5 미처리 원발주 2건의 원문이 `main`에 없다. 포인터와 보고만으로 원문 존재를 대체할 수 없다. |
| production OTA의 vc20 격리 | **CONFIRMED** | S7 보고를 독립 재검증했다. production OTA Android runtime과 vc20 fingerprint는 `c1e1f6e...`로 일치하고 vc19·기존 production 업데이트와 불일치한다. `#1148`은 이미 머지됐다. |

## 요청

### 1. S6 — 판정과 결손 상태를 BOARD에 정본화

- 이 티켓의 판정표를 `docs/sessions/R1/BOARD.md`에 반영한다.
- `#1141 + #1142`는 롤백·재설계 대상이 아니라고 명시한다.
- `docs/sessions/PROTOCOL.md` 결손은 기존 `T-R1-S7-S6-08`과 PR `#1149`의 후속으로 추적한다.
- 존재하지 않는 원문을 추측으로 복원하지 않는다.

### 2. S1 — `#1146` 법무·GTM 문구 마감

- draft `#1146`의 lifetime 표현 제거 범위와 canonical tier 명칭을 다시 확인한다.
- 공개 약관·법무 문구를 확정하는 시점에 게이트 ⑤로 멈추고 Simon 확인을 받는다.
- 확인 후 checks green을 별도 확인하고 머지 가능 상태를 S6에 회신한다.

### 3. S2 — 6-domain 계약의 오래된 주석 정정

- `src/lib/persona/north-star.ts` 상단의 `7 DOMAIN`, `SAME formula` 표현을 현재 계약과 맞춘다.
- 로직은 바꾸지 않는다. 7-domain persona 합성과 6-domain home headline의 경계를 설명하는
  최소 주석 수정만 별도 PR로 낸다.
- 기존 관련 테스트를 실행하고 결과를 회신한다.

### 4. S1·S2 — 미처리 원발주 2건의 감사 경로 결정

대상:

- `T-R1-S1-S5-04`
- `T-R1-S2-S5-02`

각 원 작성자는 다음 둘 중 하나를 명시한다.

1. 원문이 다른 기계나 브랜치에 남아 있으면 원문 그대로 복구·커밋한다.
2. 원문이 소실됐으면 새 내용을 원문인 것처럼 만들지 말고 `LOST / SUPERSEDED`로 기록하고,
   이 티켓의 사후 판정으로 대체됐다고 BOARD에 연결한다.

## 하지 말 것

- `#1141` 또는 `#1142` 롤백
- Polaris 계산 로직 재수정
- 추가 production OTA 배포
- 누락 티켓의 추측 복원
- `main` 직접 push

## 완료 조건

1. BOARD에 위 판정과 담당별 상태가 반영됨
2. `#1146`의 게이트 ⑤ 처리 결과가 기록됨
3. `north-star.ts` 주석 정정 PR과 테스트 결과가 연결됨
4. 원발주 2건이 `RECOVERED` 또는 `LOST / SUPERSEDED` 중 하나로 닫힘
5. `PROTOCOL.md` 결손의 처리 방침이 `#1149` 또는 후속 PR에 명시됨

## 회신 요청

같은 reply 파일에 다음을 회신한다.

1. BOARD 반영 위치
2. 담당별 티켓·PR 링크와 현재 상태
3. 게이트 ⑤ 승인 여부
4. 원발주 2건의 최종 감사 상태
5. `PROTOCOL.md` 복구 또는 참조 정정 결과
