2ndB HQ PREMIUM ASSET APPLY ORDER v1

You are the coding LLM currently implementing the 2ndB app.
I am uploading 2 zip files that together form the high-quality premium asset package.
These files are NOT placeholder assets. They are the new preferred production-style raster assets.

Goal:
Replace the simplified/low-detail placeholder islands and hero art with the higher-quality premium assets that are closer to the original premium reference mood.
Keep the current NavGraph, routes, pan/zoom, worker system, sheet logic, and handoffs.
Do not redesign the app. This is an asset replacement and styling upgrade pass.

Package contents and intended mapping:

core_center_premium_hq.png
- Replace current Core Brain / 나의 중심 island art.
- Use in IslandArt.tsx or the current center/core art mapping.
- This asset should be displayed slightly smaller than the current oversized center art. Target about 50 to 60 percent of the current visual footprint if needed.
- Do NOT add cyan square outlines or black crop boxes.
- Add glow in CSS/runtime, not by surrounding the image with visible rectangular wrappers.

auth_secondb_gate_hero_hq.png
- Replace the current auth/login hero art.
- Use in sign-in screen.
- Keep it floating naturally on the cosmic background.
- Do not place it inside a visible black rectangle or framed image card.

Domain island replacement mapping:
- domain_work_growth_premium_hq.png -> 일과 성장
- domain_relationship_premium_hq.png -> 관계와 사람
- domain_knowledge_premium_hq.png -> 배움과 지식
- domain_records_premium_hq.png -> 기록 보관소
- domain_imagine_premium_hq.png -> 공상 작업실
- domain_inspiration_premium_hq.png -> 취향과 영감

These islands are higher-detail premium replacements and should replace the simpler cube-like assets.
Use real text labels in the app, not baked image text.

Characters:
- characters_lineup_premium_hq.png is a reference/roster sheet.
- individual idle crops are included:
  secondb_hq_idle.png
  archi_hq_idle.png
  gadi_hq_idle.png
  lulu_hq_idle.png
  momo_hq_idle.png
  vela_hq_idle.png
  lumi_hq_idle.png

Use these idle crops to replace current static worker/fab/profile/preview portraits where appropriate.
If current walk/work sprites are still needed and the new pack does not contain full animation strips yet, keep the current motion system temporarily but use the new idle portraits for premium static states.

Visual rules:
- Keep the current domain-worker color mapping.
- Do not use robot-face generic glyphs for Tier 3/4 data nodes.
- Continue using shard icons for lower tiers.
- Keep all Korean text rendered in the app, not inside images.
- Maintain image-rendering: pixelated or nearest-neighbor display where appropriate.
- Preserve pan/zoom, route handoffs, bottom sheets, and SecondB FAB behavior.

Important cleanup:
- Remove any previous asset wrappers that create visible black or cyan rectangles.
- Ensure auth hero and core island blend naturally into the cosmic background.
- If the new islands look too large, scale them down slightly before rendering.
- If the center still dominates too much, reduce center art scale and/or increase label readability around the six domains.

Recommended apply order:
1. Replace auth hero.
2. Replace core center island.
3. Replace all six domain islands.
4. Replace static worker portraits/FAB portrait with the new premium character crops where appropriate.
5. Keep current motion sprites until a dedicated premium animation pack is generated.
6. Re-test main graph readability, domain labels, and bottom sheet alignment.

After applying, return:
1. Files replaced.
2. Components changed.
3. Any scaling adjustments made.
4. Whether any old placeholder assets remain.
5. Screenshots of login screen and main graph.
