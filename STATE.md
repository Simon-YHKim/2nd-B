# STATE

**덮어쓰기 파일.** 네 절만 — 완료 / 진행중 / 다음 / 막힌 것.
결정은 여기 쓰지 않는다. `DECISIONS.md` 가 소유한다(append-only).

최종 갱신 **2026-09-13 16:02 KST** · Claude Code (`E:/2ndB/.worktrees/handoff-split-260913`)
소유자: 이 세션 — 직전 소유자 `runbook-260907` 이 **스스로 닫힌다고 적고 닫혔다**(지명 2분 전).
이전 근거는 `DECISIONS.md` 26.09.13 15:19 줄. 다른 세션은 `DECISIONS.md` 에만 쓸 것.

---

## 완료

- **`docs/HANDOFF.md` 100KB 상한 위반 해소** — #1801 `ebf7a04a`. 732,210B(상한의 7.3배)를
  **요약 없이 기간으로** 쪼갰다: 활성 창 78KB + 월 보관 9파일, 전부 100KB 미만.
  137블록 → 137블록, 소실 0(git 오브젝트 수준 검증). `## Latest` 마커도 바로잡았다 —
  09-06 블록에 붙어 있었고 그 위에 09-13 블록이 넷이었다.
- **`/simon-handoff` 스킬 갱신**(266 → 378줄) — 굴림 절차(Step 2-B)·검사(Step 2-C)·
  실행 전 게이트·공유 워크트리 경고. 규칙만 있고 검사가 없어서 마커가 일주일 방치됐다.
- **`/vibe` 적대평가 1회차 라이브 완주** — `ae_260913_144139`, 8 probe · 24 호출 · 원장 16행.
  ⚠ **16/16 정답 = 문제 세트가 레인을 못 가른다**는 뜻이다. `--report` 가 이제 그걸 직접 말한다.
- **`/vibe` 하네스 껍데기 메움** — `--run` 이 안내문만 찍었고 `truth_post` 는 구현이 없었다
  (세는 문제에 파일 목록이 정답으로 들어갔다). 검사 29개 추가, selftest **132 PASS / 0 FAIL**.
- **공유 워크트리 구제 스냅샷** — 아래 "막힌 것" 첫 항목 참조.

## 진행중

없음. 열린 PR 은 **#1800(PKCE) 하나**이고 그건 아래 "막힌 것"이다.

## 다음 (하나만)

**P1 — 배송 홈이 `highlightRecordId` 를 읽는다.** (Q6 ④, Simon 이 "받는 쪽부터"로 순서 지정)

보내는 곳 둘(`src/app/capture.tsx:2254` · `src/app/record/[id].tsx:224`), 읽는 곳은
아카이브된 홈(`legacy/screens/index.tsx:303,308`) 하나뿐이다. 받는 쪽을 먼저 고치지 않으면
보내는 쪽을 손봐야 아무 일도 안 난다. 되살리기 큐(Q11 · P2 · P3 · P4) 전체의 선행이다.

## 막힌 것

### 1. ⛔ 공유 워크트리 TTL-Work 에 미커밋 작업이 **771건** 있다 — 기록은 "0" 이라고 적는다

```
수정 575 · 미추적 196(132.7MB) · 스태시 22   @2026-09-13 15:54 KST
docs/HANDOFF.md 는 "미push 커밋 0, 남은 워크트리 0" 이라고 적는다
```

그 기록을 믿고 정리하면 사라지는 것: `audit-write-outbox.ts` **725줄 재작성본**(main 과 다른 판) ·
`purge-local-data.ts`(main 에 부재) · Round21 회귀 310줄 · `docs/quality/` 33파일 36MB
(품질 회차 195발견의 **유일한 재현 근거**) · `SignInStorageRecoveryCard.tsx` · `batches.json`.

**구제 스냅샷을 떠 뒀다(판단 없이 보존만):**

```
E:/Coding Infra/_rescue/ttl-work-260913-1554/
  tracked.patch    3,966,891 B   수정 577파일 (재작성본은 여기 있다)
  untracked.tar  140,789,760 B   951파일 (docs/quality 34 포함)
  README.md                      복구 절차 + 함정 둘
  status.txt                     당시 git status 원본
```

⚠ **저장소 밖에 뒀다.** 앞선 백업 둘(`.worktrees/_backup/ttl-work-260907-*`)은 워크트리
안에 있어서 워크트리를 정리하면 백업까지 같이 사라진다. 같은 위험을 반복하지 않는다.
⚠ `tar -tf` 가 셸에서 **0건**을 낸다(경로에 공백). 빈 아카이브가 아니다 — python 으로 확인할 것.

**남은 일은 보존이 아니라 처분이다.** 어느 것이 완성이고 어느 것이 폐기인지는
각 작업의 소유자만 안다. 이 세션은 판단하지 않았다.

### 2. Simon 결정 8건이 나머지를 막는다

| | 무엇 | 막고 있는 것 |
|---|---|---|
| A1 | 출시 법역 확정(Q-S1) | DPIA A~H 전부 + 빌드 8종. **변호사가 아니라 제품 결정**이다 |
| A2 | 마이그레이션 0171~0187 운영 적용 승인 | 레이트리밋·인가·감사 하드닝 17건이 코드에만 있다 |
| A3 | `community_is_member` 미바인딩 처분 | 로그인한 누구나 타인의 커뮤니티 멤버십을 조회한다(보안담당) |
| A4 | 웹 게시 승인(D1) | 라이브가 main 보다 **92커밋 뒤**. 막는 건 없고 누를 사람만 없다 |
| A5 | PR #1800(PKCE) 머지 여부 | 네이티브 OAuth 가 implicit. **머지 조건이 코드리뷰가 아니라 에뮬 검증**인데 담당이 없다 |
| A6 | 미확인 보안 브랜치 방향 | origin 미머지 69갈래 중 PR 있는 건 1개 = **68갈래가 CI 를 한 번도 안 탔다** |
| A7 | `STATE.md` 소유자 확인 | 이 세션이 임시로 받았다(위 머리말). 지명하시면 즉시 이전 |
| A8 | 자살예방법 시행령 공포 관찰자 | 시행 2026-11-12(미확인)인데 누가 언제 볼지 배정이 없다 |

### 3. 답을 기다리다 서로 멈춘 것

- **Q9 `/privacy` 신뢰 문구** — 승인된 5개 언어 문구가 번들에 있는데 **어떤 배포에서도 안 뜬다**
  (`PrivacyLegacy()` 분기 · 배포 4곳이 `deep-space` 하드코딩). 자문을 실제로 의뢰한 흔적이 0건이라
  "회신 대기"가 맞는지부터 불명. ⚠ 가드 단언을 떼면 공백이 **영구히 조용해진다**.
- **기존 가입자 재고지(D5)** — `CONSENT_VERSION="2026-09-07"` 인데 비교하는 런타임 코드가 0건.
  `consent.ts:84-86` 이 스스로 "알려진 공백"이라 적는다. Q-S1 에 묶여 있다.
- **D8 `DECISIONS.md`·`STATE.md` 거처** — Simon 은 `docs/` 를 골랐는데 #1793 이 루트에 먼저
  만들었고 이미 쓰인다. 지금은 이중 소스가 **없다** — 확정 전에 누가 `docs/` 에 두 번째 벌을
  만들면 그때 §0-2 위반이 생긴다.

### 4. 기록이 사실과 다른 것 (다음 세션이 헛수고하지 않도록)

- `docs/HANDOFF.md` 활성 창이 **0148·0149·0150 을 "적용 대기"로 적는다** → 운영 적용 완료(09-07).
- 이 워크트리의 `CLAUDE.md` 는 웹 배포를 **gh-pages** 라 적는다 → main 은
  `actions/deploy-pages`(#1657). 여기서 시작하는 세션이 낡은 쪽을 지침으로 읽는다.
- `docs/WEB-PUBLISH-RUNBOOK.md` 가 게시 재현성을 **"Simon 확인 사항"** 으로 남겨 둔다 →
  D4 로 닫혔고 #1795 로 고쳐졌다.
- `docs/handoff/HANDOFF-2026-08-p4.md:1348` 이 `auth.uid()` 없는 함수 **"0건 · 수정 불요"**
  라 적는다 → 바로 그 문단이 반례를 이름으로 적고 있다(위 A3).

---

## 이 상태가 어디서 왔나

2026-09-13, 세션 8개 + 횡단 6종(1차) · 세션간 대화 528건 + Simon 프롬프트 원장 1,223행(2차)을
서브에이전트 36개로 훑었다. 1차가 세션간 대화를 통째로 빠뜨렸고 **완결성 비판이 그걸 잡아서**
2차를 돌렸다 — 소실 임박 6건은 2차에서만 나왔다.

⚠ 1차 TODO 는 이미 끝난 것 셋을 남은 일로 적었다(#1801 머지 · 적대평가 1회차 · HANDOFF 732KB).
전부 작성 직전 20분 안에 닫힌 것들이다. **감사 결과에는 잰 시각이 붙어야 한다.**
