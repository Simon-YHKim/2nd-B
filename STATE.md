# STATE

**덮어쓰기 파일.** 네 절만 — 완료 / 진행중 / 다음 / 막힌 것.
결정은 여기 쓰지 않는다. `DECISIONS.md` 가 소유한다(append-only).

최종 갱신 **2026-09-13 10:48 KST** · Claude Code
⚠ 이 워크트리(`E:/2ndB/.worktrees/2ndB/runbook-260907`)는 **닫힌다**(Simon 지시, 09-13).
아래 "다음"은 새 워크트리에서 이어받는다. 내 브랜치는 전부 origin 에 올라가 있다(미푸시 0).

---

## 완료

- **레거시 은퇴 2회차 착지** — 아카이브 17건, 죽은 핀 37(`wiki.tsx` 29 · `inbox.tsx` 8), 머지 20건.
- **Simon 결정 11건 접수**(Q1~Q11) → `DECISIONS.md`. 회신 검증 보고서:
  <https://claude.ai/code/artifact/563d43dd-e2eb-4324-9846-b159ceea4b1e>
- **#1793 머지** — `DECISIONS.md` · `STATE.md` 신설(결정이 대화보다 오래 살도록).
- **#1796** — `/sources` 신설 + Phase 1 배송 진입점. 검사 셋 통과, 자동 머지 대기.

### #1796 이 무엇을 고쳤나 (다음 사람이 다시 재지 않게)

배송 앱에 **가져온 자료를 띄우는 화면이 없어서** Phase 1(요약 + 되새김 질문 넷)의
호출부가 0건이었다. Phase 2(위키 페이지)는 멀쩡했다 — 끊긴 건 읽는 단계 하나다.

```
runPhase1 호출부        2곳, 둘 다 죽은 반쪽 안 (inbox.tsx:452 · wiki.tsx:318)
megafile               listSources·generateSourcePage·runPhase1 → import 만
eslint no-unused-vars  warn  → CI 초록
/import 주석            "land in the inbox for Phase 1/2 later ($0)"  ← 못 지킬 약속
```

새 검사 `src/lib/wiki/__tests__/phase1-has-a-shipping-caller.test.ts` 가 이 구멍을 지킨다.
그 검사를 쓰다가 **매달린 import 를 일곱 개 더** 찾았다(한 파일에 열). 변이 검증 7/7.

## 진행중

없음. `#1796` 자동 머지 대기뿐.

## 다음 (하나만)

**P1 — 홈이 `highlightRecordId` 를 읽는다.** (Q6 ④, Simon 이 "받는 쪽부터"로 지정)

보내는 곳은 둘(`src/app/capture.tsx:2254`, `src/app/record/[id].tsx:224`)인데 읽는 곳은
**아카이브된 홈**(`legacy/screens/index.tsx:303,308`) 하나뿐이다. 배송 홈이 안 읽으므로
기록을 담고 돌아와도 방금 담은 자리가 안 밝아진다. 받는 쪽을 먼저 고치지 않으면
보내는 쪽을 손봐야 아무 일도 안 난다.

그 다음 순서(보고서의 "순서" 탭과 같다):

| | 무엇 | 근거 |
|---|---|---|
| Q11 | `/import` 붙여넣기 상자 + 우리 분류기 | Simon "둘 다". `$0` 주석은 #1796 에서 이미 정정됨. 붙여넣은 것도 소스가 되므로 `/sources` 가 그대로 받는다 |
| P2 | `/data` 묶음 — 기기 전용 저장 고지 + 내보내기 거처 | Q8 + Q7③ (같은 화면이라 한 묶음) |
| P3 | 위키 — 한 장 지우기 · 검색 · 지표 상세 | Q7 ①②④. 한 장 지우기가 먼저(자동 저장 되돌리기 전제) |
| P4 | 기록 상세 셋 — 세컨비에게 묻기 · 원본 열기 · 후속 메모 | Q6 ①②③ |

`/wiki?focusSourceId=` 점프는 **P3 에서 받는 쪽을 만든 뒤에** 잇는다. 배송 위키는
`focusPageId` 를 읽으므로 지금 보내면 고아 파라미터를 하나 더 만드는 것이다(#1782).

## 막힌 것

- **Q9 자문 회신 대기** — 프라이버시 신뢰 문구. 답이 오기 전까지 "번들에 있고 화면에 없다"는
  상태를 지키는 검사 #1791 을 건드리지 않는다.
- **은퇴는 Simon 지시로 중단**(Q10). 남은 죽은 핀 37은 은퇴가 아니라 **되살리기로** 해소된다 —
  배송 화면이 계약을 갖게 되면 검사가 그쪽을 가리키고 핀이 죽은 핀이 아니게 된다.
- **`docs/HANDOFF.md` 가 708KB · 9,378줄** — 지침의 100KB 상한을 7배 넘겼다. 요약하지 말고
  기간으로 쪼개야 한다(`docs/handoff/HANDOFF-2026H2.md`). 워크트리 닫는 중에 즉흥으로 할
  일이 아니라 손대지 않았다. **새 워크트리의 첫 작업 후보.**
