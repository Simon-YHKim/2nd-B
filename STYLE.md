# 앱 문구와 답변 말투

사용자 결정: 2026-09-05. 앱에서 보이는 말은 쉽고 자연스럽게 쓴다. AI가 쓴 듯한 말투는 쓰지 않는다.

## 기본

- 안내는 해요체로 쓴다. 버튼은 `기록 저장`, `다시 시도`, `요금제 보기`처럼 할 일을 적는다.
- 첫 문장에서 결과나 필요한 행동을 말한다. 한 문장에 여러 내용을 몰아넣지 않는다.
- `당신`, `귀하`, `사용자님`을 반복하지 않는다. 주어가 없어도 뜻이 통하면 생략한다.
- 이미 자연스러운 짧은 문장은 그대로 둔다. 모든 문장을 같은 틀로 바꾸지 않는다.
- 저장 실패와 기록 없음, 저장 완료와 처리 중을 구분한다. 확인하지 못한 저장 상태를 `안전해요`라고 단정하지 않는다.

## 쓰지 않는 말투

- 내용 없는 칭찬·공감: `정말 좋은 질문이에요`, `소중한 이야기를 나눠주셔서 감사해요`로 답을 시작하지 않는다.
- 과장·홍보: `혁신적인`, `강력한`, `놀라운 통찰`, `완벽하게 이해해요`처럼 내용을 부풀리지 않는다.
- 추상적인 은유: `나를 채우는 여정`, `기록이 말을 걸어요`, `과거와 현재가 정렬돼요`로 기능을 설명하지 않는다.
- 번역투·격식체: `도움을 제공해 드립니다`, `고려해 보시는 것은 어떨까요`, `귀하의 요청이 처리되었습니다`를 일상적인 말로 바꾼다.
- 매번 세 가지 목록, 마지막 요약, 다음 질문을 붙이지 않는다. 내용에 필요할 때만 쓴다.

## 용어

| 뜻 | 화면에서 쓸 말 |
|---|---|
| plan | 요금제 |
| insight | 발견한 점 |
| reasoning 연결 기능 | 자료 연결 / Find connections |
| automatic reasoning | 자동 연결 / Automatic connections |
| persona / self-model 설명 | 나의 요약 / 기록으로 정리한 나의 모습 |
| ratify 동작 | 승인 / 확인하고 반영 |
| source 설명 | 원본 자료 |
| entity 설명 | 사람·장소·대상 등 문맥에 맞는 이름 |
| fallback / serialization 같은 구현 용어 | 사용자에게 필요한 결과나 다음 행동 |

북극성·세컨비·별가루 같은 제품 이름은 유지한다. 이름만으로 동작을 알기 어려우면 설명에서 기록·자료·요약 등 실제 대상을 말한다. 키, 경로, 모델 이름, 파일 형식, 숫자와 단위는 문구를 다듬는 과정에서 바꾸지 않는다.

## AI가 만드는 답변

- 질문에 바로 답한다. 짧은 일상어를 쓰고, 한국어는 자연스러운 해요체로 쓴다.
- 기록에 근거한 내용과 추측을 구분한다. 모르는 것은 모른다고 말한다.
- 없는 경험·감정·수치를 지어내거나 사용자를 다 안다는 듯 말하지 않는다.
- 형식적으로 위로하거나 친밀감을 주장하지 않는다. 공감이 필요하면 사용자가 실제로 말한 상황을 짚는다.
- 원문 인용, 출처, JSON 형식과 안전 안내 규칙은 그대로 지킨다.

## 수정할 때 확인할 것

로케일 키와 `{{변수}}`, 버튼의 실제 동작, 저장·삭제·결제·동의의 의미를 보존한다. 정형 설문 문항, 약관 원문, 긴급 연락처는 단순한 말투 수정으로 바꾸지 않는다. 이 문서는 기존 어휘·안전·마스코트 검사를 대체하지 않는다.

검증: `npm run verify`. 문구의 자연스러움은 코드 검사만으로 증명되지 않으므로 화면의 제목·설명·버튼을 함께 읽고 검토한다.

## 금지어를 적용하는 기준 (2026-09-06 점검)

- 단어가 등장했다는 이유만으로 같은 뜻이라고 판단하지 않는다. 기술 상태 설명과 사용자에 대한 평가는 문맥이 다르다. 어휘 검사의 좁은 문맥 예외는 `src/lib/safety/lexicon.ts`, 판정은 `scripts/lib/lexicon-copy.ts`에서 관리한다.
- 제품의 표현 정책을 법률상 금지 단어 목록이라고 설명하지 않는다. 실제 주장과 기능, 적용 범위를 함께 봐야 한다. 근거와 남은 범위는 `docs/legal/lexicon-policy-review-260906.md`에 기록한다.
- 친근한 말투, 인사, 구체적인 공감은 허용한다. 애착이나 독점적 관계를 요구하거나 사용자의 속마음을 다 안다고 단정하지 않는다.
- 인사·질문·행동 안내에 억지로 출처를 붙이지 않는다. 사용자의 특성이나 반복된 행동을 말할 때는 실제 기록을 근거로 삼는다.
- `혁신적`, `공감`, `도움` 같은 낱말을 모든 문맥에서 금지하지 않는다. 이 문서의 나쁜 예시는 상투적인 사용 방식을 가리킨다. 사용자 인용·고유명사·기술 설명은 뜻을 보존한다.
- em dash 제한은 화면 표기 취향이다. 문장부호 하나로 AI 작성 여부나 문체의 품질을 판정하지 않는다.
- 위기 신호 탐지와 동의·연령·출처 보호는 문체 검사와 별개다. 문장을 자연스럽게 고친다는 이유로 이 경계를 생략하지 않는다.

## 스페인어·포르투갈어·인도네시아어 (2026-09-06)

한국어 문장 구조를 그대로 옮기지 않는다. 기능·저장 상태·동의 범위·변수는 보존하고,
해당 언어에서 흔히 쓰는 동사로 행동을 설명한다. 짧고 자연스러운 문장은 그대로 둔다.

| 언어 | 기준 | 피할 표현 방식 | 예시 |
|---|---|---|---|
| ES | 중남미에서 읽기 쉬운 `tú` 표현. `vosotros`와 불필요한 격식체를 섞지 않음 | 기능 설명을 `viaje transformador`, `poderosas perspectivas`로 부풀리기 | `Guarda una nota y vuelve a leerla cuando la necesites.` |
| PT | 브라질의 `você` 표현. 버튼은 `Salvar`, `Abrir`, `Tentar novamente` | `jornada de transformação`, `desbloqueie seu potencial` 같은 약속 | `Salve uma nota e consulte o registro depois.` |
| ID | 기존 앱처럼 `kamu` 사용. `Anda`·`engkau`를 이유 없이 섞지 않음 | `perjalanan transformatif`, 현자·영감 큐레이터 설정, capture를 `tangkapan`으로 직역 | `Simpan catatan agar mudah ditemukan nanti.` |

ID의 저장 화면 이름은 `Simpan`, 저장한 원본은 문맥에 따라 `bahan`, 글로 남긴 기록은
`catatan`으로 쓴다. 위키 페이지와 아직 위키로 정리하지 않은 원본 자료를 구분한다.
음성을 마이크로 잡는다는 뜻의 `tertangkap`까지 일괄 치환하지 않는다.

단어를 통째로 금지하지 않는다. 다음 차이를 확인한다.

- ES: `Te quiero mostrar una nota`는 설명하려는 행동이고, `Te quiero`는 AI 자신의 애착 표현이다.
- PT: `Você sente saudades de alguém?`는 사용자의 경험을 묻는 질문이다. `Senti saudades de você`는 AI의 애착 주장이다.
- ID: `Siapa yang kamu rindukan?`는 질문이고, `Aku merindukanmu`는 AI 자신의 감정 주장이다.
- `transformar áudio em texto`는 음성의 텍스트 변환이다. `transformar`라는 동사만으로 홍보 표현이라고 판단하지 않는다.
- 사용자의 실제 인용이나 정형 설문을 문체 때문에 바꾸지 않는다. 금지 지시는 해당 구절에만 적용한다.
  같은 문장의 뒤쪽에 별도 주장이 있으면 따로 검토한다.

친밀감·독점·속마음 단정의 대표 표현은 `src/lib/safety/anthro.ts`와
`src/lib/safety/mascot-voice.ts`의 ES/PT/ID 예시로 검사한다. 이는 문구 검토용이며
모든 표현을 인식하는 언어 분류기가 아니다. 임상 서비스·효능 주장은 새 언어 문구에서도
실제 근거와 문맥을 검토한다. 기존 `SystemLocale`과 안전·동의 안내의 EN/KO 경계는 그대로 따른다.
