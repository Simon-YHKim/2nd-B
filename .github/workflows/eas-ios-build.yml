name: EAS iOS Build (IPA)

# iOS twin of eas-preview-build.yml (EAS Android Build). Produces the iOS
# release artifact with EAS-managed signing:
#   preview    -> internal-distribution IPA (ad hoc; installable only on
#                 devices registered with the Apple team)
#   production -> App Store IPA (TestFlight / App Store Connect via
#                 `eas submit -p ios` afterwards; submit.production.ios in
#                 eas.json already pins ascAppId 6792266942 / team 7CP84WS5C6)
#
# Unlike the Android twin there is NO google-services restore step here: the
# iOS Firebase config is injected by the EAS BUILDER from the file-type env
# var GOOGLE_SERVICE_INFO_PLIST (Secret, registered in development/preview/
# production), which app.config.js reads via
# process.env.GOOGLE_SERVICE_INFO_PLIST. The plist never touches this runner
# and stays out of git (.gitignore **/GoogleService-Info.plist).
#
# FIRST-BUILD PREREQUISITE (one-time, dispatch 260719): EAS holds no iOS
# distribution certificate / provisioning profile for this app yet (zero iOS
# build history), and this workflow runs --non-interactive, so the very first
# run FAILS at credential setup until iOS credentials exist on EAS servers.
# One-time setup (path (a), App Store Connect API key -- never commit the
# .p8):
#   1. ASC > Users and Access > Integrations > Keys -> generate an App
#      Manager key; download the .p8 once, note Key ID + Issuer ID.
#   2. Locally run `npx eas-cli credentials --platform ios` (not a build --
#      costs no build slot), pick the production profile, let EAS use the API
#      key to create + store the distribution certificate and provisioning
#      profile, and store the key for App Store submissions as well.
#   3. Re-run this workflow: it is fully non-interactive from then on.
#
# Build slots: every run consumes an EAS build slot -- trigger only after the
# owner confirms (workflow_dispatch keeps this human-gated by construction).

on:
  workflow_dispatch:
    inputs:
      profile:
        description: "EAS build profile (preview = internal ad-hoc IPA, production = App Store IPA)"
        required: true
        type: choice
        options:
          - preview
          - production
        default: production
      message:
        description: "Build message shown in EAS"
        required: false
        default: "Firebase-enabled iOS release"

permissions:
  contents: read

concurrency:
  group: eas-ios-build-${{ github.ref }}-${{ inputs.profile }}
  cancel-in-progress: false

jobs:
  submit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      build_id: ${{ steps.submit_build.outputs.build_id }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Verify OTA/native runtime policy
        run: npm run check:ota-runtime

      - name: Submit EAS iOS build
        id: submit_build
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          BUILD_MESSAGE: ${{ inputs.message }}
          BUILD_PROFILE: ${{ inputs.profile }}
        run: |
          set -euo pipefail
          if [ -z "${EXPO_TOKEN:-}" ]; then
            echo "::error::EXPO_TOKEN is required for EAS iOS builds."
            exit 1
          fi
          if [ "$BUILD_PROFILE" != "preview" ] && [ "$BUILD_PROFILE" != "production" ]; then
            echo "::error::Unsupported EAS build profile '$BUILD_PROFILE'."
            exit 1
          fi
          BUILD_JSON="$(npx eas-cli build \
            --platform ios \
            --profile "$BUILD_PROFILE" \
            --message "$BUILD_MESSAGE" \
            --non-interactive \
            --no-wait \
            --json)"
          printf '%s\n' "$BUILD_JSON"
          BUILD_ID="$(
            printf '%s' "$BUILD_JSON" | node -e '
              const fs = require("fs");
              const payload = JSON.parse(fs.readFileSync(0, "utf8"));
              const build = Array.isArray(payload) ? payload[0] : payload;
              if (!build?.id) process.exit(1);
              process.stdout.write(build.id);
            '
          )"
          echo "build_id=$BUILD_ID" >> "$GITHUB_OUTPUT"
          BUILD_URL="https://expo.dev/accounts/simon_k/projects/2nd-brain/builds/$BUILD_ID"
          echo "::notice title=EAS iOS build submitted::$BUILD_PROFILE build: $BUILD_URL"
          {
            echo "### EAS iOS build submitted"
            echo
            echo "- Profile: \`$BUILD_PROFILE\`"
            echo "- Artifact: IPA ($([ "$BUILD_PROFILE" = "production" ] && echo 'App Store distribution' || echo 'internal ad hoc'))"
            echo "- Build: $BUILD_URL"
          } >> "$GITHUB_STEP_SUMMARY"
